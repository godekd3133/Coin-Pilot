# Coin Pilot Scorecard

| Area | Definition of done | Current evidence | Status |
|---|---|---|---|
| Strategy contract | Completed-candle oversold reaction, delayed revalidation, bounded exits | Source + unit tests | PASS |
| Web UI | Desktop dashboard, responsive mobile layout, paper status controls | Browser/API smoke and PWA assets | PASS |
| Installable app | Manifest, icons, service worker, standalone metadata | Static manifest/service-worker checks | PASS |
| Historical validation | Fee/slippage-aware walk-forward holdout with minimum trades/PF/return/MDD | Fresh 7-day fixed-config top-20 run: 0/20 promoted; 17 training-gate failures, 3 holdout-gate failures | HOLD |
| Forward paper | Isolated multi-market ledger with telemetry, snapshots, durable strict trades, and continuity gate | v5-v39 preserved; v37 ended with 1 strict close and v33-v36/v38-v39 with 0 strict closes; fresh latest-code v40 is active in a separate 20-market ledger with per-market freshness, observed-rate, and insufficient-candle telemetry | IN PROGRESS |
| Live safety | Live scalping blocked until global validation promotion | `assertLiveValidationGate()` requires matching `fixed_config` report; current tuned/failed report blocked | PASS |
| Profitability | Forward thresholds met and independently reviewed | No qualifying evidence yet | NOT PROVEN |
| Continuity | Heartbeat gaps over the configured limit invalidate promotion | v4 preserved one interruption; v5 starts with zero interruptions; HTTP requests now have a finite timeout | IN PROGRESS |

## Latest evidence

- v4 shadow: 8 closed trades, 4 wins/4 losses, `+8.90 KRW`; relaxed shadow: 6 closed trades, 3 wins/3 losses, `-26.93 KRW`. Neither is promotion evidence.
- v5 is an isolated 20-market forward session using the timeout-hardened client. Strict, shadow, and loose cohorts remain separate; no live order path is enabled.
- v5 strict remains at 1 close, `KRW-WLD` 593 → 585, net `-289.53 KRW` / `-1.448%`. Its shadow has 5 closes, 2 wins/3 losses, net `+114.44 KRW`; loose shadow has 4 closes, 2 wins/2 losses, net `+135.86 KRW`. The paired observations include RAY `-310.53 KRW` in roughly two minutes and MIRA `-1.18 KRW` after max hold. The mixed small sample is not profitability evidence.
- A local no-response HTTP probe now fails in `117ms` with `ECONNABORTED` when configured for a `100ms` timeout. This closes the previously observed unbounded Upbit-socket wait class.
- Historical tuning now requires the selected training candidate to have at least 3 trades, non-negative return, and PF >= 1 before holdout promotion is even considered. Fresh 2-day BTC selected no-trade/zero-return behavior; fresh 7-day BTC had +0.0049% training return but only 1 trade; both were correctly marked `training_gate_failed`.
- A disabled-by-default `maxSignalRangePercent` volatility guard is now available as a separately testable candidate after v5 produced a fast RAY stop-loss. A 1-day RAY exploratory run produced no viable training candidate (`training_gate_failed`), so it remains disabled and needs a longer holdout before any decision.
- After a confirmed v5 ledger write failure at low disk headroom, the forward storage preflight/status margin is now unified at a configurable `SCALP_PAPER_MIN_STORAGE_MIB` (default 1024MiB). v5 remains preserved as orphan evidence; no unrelated cleanup was performed.
- Forward status now checks the saved owner PID as well as heartbeat age, so a dead runner is immediately shown as orphaned/STOPPED. Atomic JSON writes also remove their own failed temp file while preserving the original error.
- v6 starts with a complete 29-field `configSnapshot` and zero interruptions; v5 remains separate because its older session predates the snapshot contract.
- Latest v6 readback: session `paper-1788973156260`, 20 markets, 148 cycles, 0 strict closes, paired shadow/loose `KRW-SC` stop-loss `-344.77 KRW`, interruption 0, heartbeat current. This is an active observation window, not a profitability result.
- Latest default report `scalping_validation.json` (2026-09-09T17:15:02Z): XRP/IOST/KAT all failed the holdout gate; global `promoted=false`. Training-gate checks passed for the selected candidates, but holdout trade count/return/PF did not.
- Fixed current-config report `scalping_validation_fixed.json` (2026-09-09T17:41:35Z): BTC/XRP/IOST all failed the training gate; training returns were `-0.0471%`, `-0.0970%`, `-0.0175%`. This is direct evidence against the current runtime configuration, separate from tuned-candidate evidence.
- Fixed current-config FLOCK report `scalping_validation_fixed_flock.json` (2026-09-09T17:56:45Z): training `-0.0439%` / 9 trades / PF 0.69, while holdout was `+0.0525%` / 7 trades / PF 2.18; it remains `training_gate_failed`, demonstrating a regime-dependent result rather than a reliable edge.
- Fixed current-config top-20 report `scalping_validation_fixed_top20.json` (2026-09-09T18:09:50Z): 20 markets, 183 training trades and 74 holdout trades in total, but 0/20 promoted. Seven markets had positive holdout returns; none overcame the training/holdout gate as a reusable strategy.
- High-break A/B report `scalping_validation_fixed_nohigh_top20.json` (2026-09-09T18:35:20Z): removing `previous_high_break` increased holdout trades from 74 to 121 but worsened summed holdout return from `-0.1999%` to `-0.8461%` and reduced positive markets from 7 to 3. Keep the filter enabled; do not loosen it from starvation alone.
- Volume A/B report `scalping_validation_fixed_lowvol_top20.json` (2026-09-09T18:43:32Z): lowering `minVolumeRatio` from 1.0 to 0.8 increased holdout trades from 74 to 87 but worsened summed holdout return from `-0.1999%` to `-0.3453%`; keep the volume filter at 1.0.
- Rebound A/B report `scalping_validation_fixed_lowrebound_top20.json` (2026-09-09T18:47:09Z): lowering `minReboundPercent` from 0.15 to 0.10 increased holdout trades from 74 to 84 and slightly improved summed holdout return to `-0.1593%`, but worsened summed training return and still produced 0/20 promotion. Keep 0.15 pending stronger evidence.
- RSI recovery A/B report `scalping_validation_fixed_lowrsirecovery_top20.json` (2026-09-09T18:50:44Z): lowering `minRsiRecovery` from 2 to 1 kept holdout trades at 74 and slightly improved summed holdout return to `-0.1521%`, but training remained negative (`-0.8679%`) with 0/20 promotion. Keep 2.
- Close-strength A/B report `scalping_validation_fixed_lowclose_top20.json` (2026-09-09T18:56:47Z): lowering `minCloseStrength` from 0.65 to 0.55 kept the sample effectively unchanged (74 to 75 holdout trades) and only moved summed holdout return to `-0.1201%`; training remained negative and promotion stayed 0/20. Keep 0.65.
- Trend A/B report `scalping_validation_fixed_downtrend_top20.json` (2026-09-09T19:00:42Z): lowering `minTrendSlopePercent` from -0.2 to -0.5 increased holdout trades from 74 to 94 but worsened summed holdout return to `-0.2769%`; all 20 markets failed the training gate. Keep -0.2.
- v9 first shadow closures: NEAR max-hold `+161.64 KRW`, WAVES stop-loss `-295.06 KRW`, TRUMP max-hold `-73.45 KRW`; three closes total `-206.87 KRW`, 1 win/2 losses, PF `0.44`. All three carried `volume_confirmation_failed`; the result is small-sample diagnostic evidence only.
- v9 expanded shadow result: 7 closes, 1 win/6 losses, net `-1,312.84 KRW`, PF `0.11`; the outcome-aware recommendation now prioritizes keeping high-break and volume filters because their rejected cohorts are losing.
- v9 was stopped and preserved with `active=false`; it predates the shadow cooldown-parity change and is not mixed with the new observation window.
- v10 is a fresh isolated 20-market session with `configSnapshotComplete=true`, `riskInterval=1000ms`, `cooldownAfterLossMinutes=15`, and `maxConsecutiveLosses=3`. Its first two shadow/loose closes were `KRW-SC -530.94 KRW` and `KRW-IOST -306.09 KRW`, both stop-losses; strict remains at 0 closes. The ledger stores one-loss cooldown markers for both coins and has not re-entered `KRW-SC` during the observed window. This is diagnostic, small-sample evidence only.
- v10 was intentionally stopped and preserved before the new protective/momentum contract; v11 is the fresh latest-code observation window. At first persistence, v11 had `configSnapshotComplete=true`, `rsi_rebound`, 1-minute candles, strict protection triggers all disabled, risk interval `1000ms`, interruptions `0`, strict closes `0`, and one open shadow `KRW-KAT` candidate.
- v11 was stopped and preserved after 31 cycles with strict/shadow closes `0`; its logs showed intermittent 429 retries when a separate study ran concurrently. v12 is the fresh latest-code observation window after adding a process-wide request-slot gate shared by market and risk clients. At first persistence v12 had `rsi_rebound`, 1-minute candles, protection triggers `0`, risk interval `1000ms`, interruptions `0`, and strict/shadow closes `0`.
- v12 remained live through 8 persisted cycles after the shared gate change with no 429/network error in the runner output, heartbeat current, interruptions `0`, strict BUY/close `0`, and no shadow positions yet. This confirms request stability only; it is not a profitability result.
- v13 tested the best historical loss-reduction candidate (`minRebound=0.25`, `minVolume=1.5`, protection enabled) and exposed a protection-contract defect: the pre-fix `BREAK_EVEN_STOP` close was `-35.51 KRW` because the stop ignored round-trip fees and adverse slippage. v14 is the replacement session using the cost-adjusted break-even floor; at 15 cycles it had one open shadow `KRW-WLD`, no closes, and no interruptions, so the fix is not yet forward-profit evidence.
- v14 later opened strict `KRW-TRUMP` at `2904` with isolated portfolio KRW `980,000`; the new `strictEvaluation` status readback restored one active position with `configConsistent=true` and no drift from the saved v14 snapshot. At 64 cycles it remains open with strict/shadow closed trades `0`, no interruption, and no profitability conclusion is drawn.
- v14 was stopped and preserved at 82 cycles with strict `KRW-TRUMP` still open and no closed trade; v15 is the fresh latest-code candidate session after adding durable `strictOpenPositions` snapshots. At first persisted observation v15 had a complete snapshot, strict-open snapshot `[]`, strict/shadow closes `0`, and interruptions `0`.
- v15 was stopped and preserved after 20 cycles with strict/shadow closes `0`; v16 is the fresh latest-code candidate session after preserving zero-valued env settings and wiring documented paper thresholds. At first persisted observation v16 stored `minDays=7`, `minTrades=20`, `minReturnPercent=0.2`, `maxDrawdownPercent=15`, complete config snapshot, strict-open snapshot `[]`, and interruptions `0`.
- v16 latest readback after the concurrent study: 34 cycles, strict BUY/close `0`, shadow/loose each 2 open candidates and 0 closes, heartbeat current, interruptions `0`. It remains observation-only; its cycle did not become an eligibility result.
- v16 was stopped and preserved at 48 cycles with strict close `0`, shadow close `0`, and no interruption. v17 is the fresh candidate session after increasing the shared request slot from 100ms to 120ms; its first 12 cycles had no 429 in runner output, complete config snapshot, strict/shadow close `0`, and interruptions `0`.
- v17 was stopped and preserved at 24 cycles with strict/shadow closes `0`; v18 is a fresh 1-minute candidate session using `minReboundPercent=0.5`, while protection triggers remain disabled. At first persistence v18 had complete config/threshold snapshots, strict/shadow closes `0`, and interruptions `0`.
- v18 was stopped and preserved at 18 cycles with strict/shadow closes `0`; v19 was the long-observation candidate using `minReboundPercent=0.5` and `minVolumeRatio=1.5`, with protection disabled. At first persistence v19 had complete config/threshold snapshots, strict/shadow closes `0`, and interruptions `0`.
- v19 was stopped and preserved after 18 cycles with strict/shadow closes `0`; v20 is the replacement long-observation session using the same volume-1.5/rebound-0.5 candidate. At first persistence v20 had complete config/threshold snapshots, strict/shadow close `0`, and interruptions `0`; one shadow `KRW-CP` candidate was open at the first later poll.
- v20 latest readback: strict `KRW-CP` BUY at `20.7` remains open, strict closed `0`, strict snapshot persisted, assets approximately `999,990 KRW` at the last ledger read, and shadow `KRW-CP`/`KRW-WAVES` remain diagnostic open candidates with closed `0`. This is an unrealized, single-position observation and not a profitability result.
- v20 later closed the earlier rejected shadow `KRW-CP` candidate at `TAKE_PROFIT`: entry `20.5205`, exit `20.8791`, net `+329.16 KRW`, after roughly 10 minutes; its rejection reasons were `price_rebound_below_threshold` and `previous_high_break_failed`. At the latest ~106-cycle readback, the strict `KRW-CP` entry at `20.7` remained open while shadow CP was closed positive and WAVE/RAY/ETH were open; the favorable shadow result is a timing/selection diagnostic, not strict strategy profit evidence.
- v20 strict `KRW-CP` later exited at `MAX_HOLD_TIME` after 30 minutes with entry/exit `20.7` and net `-19.99 KRW` from round-trip fees; v20 ended cleanly at 182 cycles with no open positions. This is the first strict closed trade for the candidate and is negative evidence, not a promotion result.
- v21 is the fresh latest-code session after adding strict cooldown persistence and legacy-ledger migration. Its first persisted snapshot contains `strictRiskState` with empty cooldown maps, complete config/thresholds, and no interruptions; no live orders are enabled.
- v21 was stopped and preserved at 40 cycles with strict BUY/close `0`, shadow `3` closes totaling about `-946 KRW`, and VVV cooldown state persisted; v22 is the fresh range-floor candidate session. At first persisted observation v22 had `minRebound=0.5`, `minVolume=1.5`, `minSignalRange=0.4`, protection off, complete snapshot, strict/shadow close `0`, and interruptions `0`.
- v22 was stopped and preserved at 24 cycles with strict/shadow closes `0`; v23 is the replacement latest-code range-floor candidate. At first persisted observation v23 had `minRebound=0.5`, `minVolume=1.5`, `minSignalRange=0.4`, protection off, complete snapshot, empty strict risk state, and interruptions `0`.
- v23 later produced two strict BUY positions (`KRW-NEAR` and `KRW-PROS`), with persisted strict-open snapshots matching the isolated portfolio; at 20 persisted cycles both remained open, strict closed `0`, shadow closed `0`, heartbeat current, and interruptions `0`. The positions are unrealized observations only.
- v23 latest readback at 73 cycles has strict NEAR/PROS still open (NEAR high `3377`, PROS high `634`), strict closed `0`, and shadow KAT `STOP_LOSS -299.87 KRW` with TRUMP/ETH/WLD open; interruptions `0` and snapshot complete. Any positive evaluated assets remain unrealized and are not counted as profit.
- v23 later reached about 171 persisted cycles and closed strict NEAR/PROS at `MAX_HOLD_TIME` for `+15.85` and `+75.46 KRW` (2 trades, `+91.30 KRW` total); shadow ended at `-551.77 KRW`. A subsequent code pass added per-cycle batch ticker prefetch with individual fallback.
- v23 strict NEAR/PROS then both exited at `MAX_HOLD_TIME`: NEAR net `+15.85 KRW`, PROS `+75.46 KRW`, strict total `+91.30 KRW` across 2 trades. Shadow finished at `-551.77 KRW` after KAT stop and TRUMP/ETH max-holds. This is small, divergent evidence and not a promotion result.
- v24 is the first runtime session with batch prefetch and request telemetry: at 26 cycles it recorded ticker batch `26`, individual ticker `0`, candle requests `520` for 20 markets, and batch failures `0`. Its shadow IOST/SC stopped out for `-330.59/-376.42 KRW` (total `-707.02 KRW`), while strict remains at 0 closes; request-path success is separate from profitability.
- v24 was stopped cleanly and preserved at 199 cycles with strict closes `0`, shadow closes `4`, strict-open `[]`, and shadow WLD/ONDO positions left as diagnostic unrealized state. It predates the completed loss-circuit integration and is not mixed with v25.
- The opt-in global loss circuit breaker is now wired end-to-end with default count `0`: strict paper/live runtime entries, independent shadow books, and fee/slippage-aware backtests use the same sliding-window contract. A 51-test suite covers threshold trigger, expiry, legacy strict-loss migration, cross-market strict blocking, and independent shadow blocking; no default runtime behavior changed.
- v25 is the fresh latest-code 20-market forward session using explicit circuit count `0`. At 19 persisted cycles it has batch ticker `19`, individual ticker `0`, candle requests `380`, batch failures `0`, strict/shadow closes `0`, interruptions `0`, no open positions, and 1,000,000 KRW unchanged. It remains observation-only and must not be described as profitability evidence.
- The new shared-portfolio diagnostic lane was run on the same 30-day/1-minute/6-market cache. Current fixed config returned training `-1.3918%`/198 trades/PF `0.24` and holdout `-0.6060%`/97 trades/PF `0.36`; 3-fold baseline was `0/3` with aggregate holdout `-0.9839%`/161 trades. Per-market independent results must not be used as a substitute for this shared-balance check.
- Candidate experiments on that identical cache remained unpromotable: regime breadth holdout `-0.5106%`/73 trades/PF `0.36` and 3-fold `0/3`; overbought guard holdout `-0.6100%`/95/PF `0.33` and 3-fold `0/3`; next-candle bullish follow-through holdout `-0.1671%`/33/PF `0.39` and 3-fold `0/3`; wider SL/TP candidates also remained negative. Rebound thresholds `0.75/1.0/1.5%` improved isolated late holdouts but all multi-fold training gates failed, so none is enabled.
- The validator now supports fixed candle cache reuse and expanding multi-fold checks. The portfolio regime gate, rebound-overbought guard, and next-candle follow-through are explicit diagnostic candidates with UI/config visibility where applicable; runtime defaults remain unchanged.
- v26 is the fresh latest-code 20-market forward session. At first observation it opened strict `KRW-PROS` and shadow SOPH/CP/PROS; this is unrealized state, not profit evidence. Keep v26 isolated until positions close or the session is intentionally preserved.
- v26 was later stopped cleanly and preserved at 41 cycles: strict `KRW-PROS` closed by stop-loss at `-264.77 KRW` (`-1.3245%`), while shadow/loose `KRW-PROS` closed at `-304.58 KRW` (`-1.5229%`) and one shadow `KRW-CP` remained open. This is direct small-sample negative/cost-differential evidence, not a promotion result.
- v27 was a regime candidate stopped and preserved at 22 cycles with strict BUY/close `0`, regime mostly blocked (`breadth 0.50`, average `-0.78%` at final readback), and shadow positions opened before the later shadow-parity fix. It is not mixed with v28.
- Portfolio 3-fold regime sensitivity remained negative on the same 30-day cache: breadth `0.75` with threshold `-0.2%` gave `-0.8291%`, breadth `0.75` with threshold `0%` gave `-0.4343%`, breadth `0.5` with threshold `0%` gave `-0.4857%`, and lookback `15` gave `-0.4640%`; all were `0/3` folds. Keep regime disabled by default.
- A portfolio-only next-candle bullish follow-through contract also failed: single holdout `-0.1671%`/33 trades/PF `0.39`, 3-fold `0/3` with `-0.2741%`; it remains diagnostic-only and is not wired into the live 1–5 second contract.
- v28 is the latest regime-parity forward candidate, active in its own ledger. At latest readback it had 26 cycles, strict BUY/close `0`, shadow/loose one close each at `-327.40 KRW` with FLOCK still open, regime `breadth 0.65`/average `-0.1394%` currently confirmed, 28 shadow regime blocks each, batch ticker `26`, individual `0`, candle `520`, batch failures `0`, interruptions `0`. This is small-sample negative shadow evidence, not profitability evidence.
- v28 was later stopped cleanly and preserved after 130 cycles: strict BUY/close `0`, shadow/loose 6 closes, shadow `-1,564.76 KRW`, loose `-1,400.93 KRW`, with WAVES/VVV open. It is a strong diagnostic rejection of the regime candidate, not a promotion result.
- Loss-only timeout studies on the same 30-day cache also stayed negative: 5m `-0.5313%`/102 trades/PF `0.35`, 10m `-0.5172%`/100/PF `0.39`, 15m `-0.5657%`/99/PF `0.37`; max-hold combinations remained negative. The new `maxLosingHoldMinutes` option remains disabled by default.
- v29 is the fresh latest-code default strict forward session, explicitly using regime=false, overbought=false, loss-only timeout=0, and loss circuit count=0. At 18 cycles it has strict/shadow/loose BUY or closes `0`, no open positions, batch ticker `18`, individual `0`, candle `360`, batch failures `0`, interruptions `0`, and 1,000,000 KRW unchanged. It remains observation-only.
- v29 was stopped with strict `KRW-NEAR` and `KRW-WLD` still open after 67 cycles; strict/shadow closed `0` and no realized result was claimed. Its ledger and isolated portfolio remain preserved rather than mixed into v30.
- Strict entry attribution now persists signal key/time, reference price, rebound/RSI/recovery/volume/range features, delay milliseconds, and execution drift in open snapshots and close ledger records. A zero-drift serialization bug was found and fixed; the test now explicitly preserves `0` rather than converting it to null.
- v30 is the current default forward session after attribution wiring. At latest readback it has 79 cycles and strict ONDO/WLD/XRP still open with signal metadata (delays `3885/4902/1670ms`; stored drifts `null/-0.1821/+0.0533%` because this process predates the zero-preservation fix), strict closes `0`, shadow/loose one close each at `-286.79 KRW`, no interruptions, batch ticker `79`, individual `0`, candle `1580`, and batch failures `0`. These are unrealized/small-sample observations, not promotion evidence.
- The latest attribution test suite is `61/61 PASS`; the dashboard strict-position rendering now shows delay and execution drift beside each open position so the same evidence is visible on web, mobile, and the installable PWA surface.
- Paper session shutdown now persists `endedWithOpenPositions`, `strictOpenPositions`, and `stopReason`, and refuses same-ledger restart without explicit reset or `allowUnsettledResume=true`. This closes the session-boundary risk exposed by v29.
- v30 final readback: session `paper-1789004391817` ran from `2026-09-10T01:39:51Z` to `2026-09-10T02:12:02Z`, 239 cycles, and ended cleanly with no strict positions open and no continuity interruption. Strict closed 3 trades for `+23.59 KRW` total: XRP `-9.35` (`MAX_HOLD_TIME`), WLD `+52.93` (`MAX_HOLD_TIME`), and ONDO `-19.99` (`MAX_HOLD_TIME`). All three entries came from the same `2026-09-10T01:40:00` signal window; delays were `1670/4902/3885ms`, with recorded execution drift `+0.0533%/-0.1821%/0%`. Shadow and loose books each closed 5 trades for `-692.16 KRW`. This is a three-trade/small-sample observation, not profitability or promotion evidence.
- v30 request path remained stable through shutdown: batch ticker `239`, individual fallback `0`, candle requests `4780`, batch failures `0`; the isolated ledger did not touch the user's existing dry portfolio. v31 was then started separately with the zero-drift serialization fix.
- v31 final readback: session `paper-1789006444660` ran from `2026-09-10T02:14:04Z` to `2026-09-10T02:40:33Z`, 200 cycles, and stopped cleanly with 0 strict BUY/closed trades, 0 strict open positions, 0 interruptions, batch ticker `200`, individual fallback `0`, candle requests `4000`, and batch failures `0`. Shadow and loose each had 3 closed trades for `-268.60 KRW` and retained open diagnostic FLOCK/DOGE/IOST positions at shutdown; this is explicitly incomplete shadow evidence, not a settled profitability result. The three closed shadow trades were WAVES `-312.32` STOP_LOSS, VTHO `-294.50` STOP_LOSS, and PROS `+338.22` TAKE_PROFIT.
- Added the opt-in `maxEntriesPerSignalWindow` correlated-entry safeguard after v30 produced three strict entries on one signal key. Default remains `0`; it is wired through runtime/paper state, portfolio backtest, API range/investment readback, dashboard status, docs, and tests. Synthetic portfolio coverage proves cap=1 accepts one same-window candidate and blocks the second; no live/default setting was changed.
- Same-window cap diagnostic using one cached 2-day/1-minute window for KRW-BTC and KRW-ETH: cap `0`, `1`, and `2` all returned training `-0.0036%`/1 trade/PF `0`, holdout `-0.0121%`/1 trade/PF `0`, `signalWindowBlockedEntries=0`, and `promoted=false`. The comparison is under-sampled and does not justify enabling any cap.
- v32 final readback at `2026-09-10T04:09:14Z`: session `paper-1789008055439` ran from `02:40:55Z` to `04:08:59Z`, completed 652 cycles, and stopped cleanly with `endedWithOpenPositions=false` and zero interruptions. Strict closed 3 trades for `-44.9573 KRW` total (2 wins/1 loss): KAT `-154.2642 KRW` (`MAX_HOLD_TIME`), PROS `+78.1193 KRW` (`MAX_HOLD_TIME`), and TRUMP `+31.1877 KRW` (`MAX_HOLD_TIME`); all entries retained delay/drift attribution (`1134/1207/1338ms`, drift `0%`). Shadow/loose each closed 9 diagnostic trades for `+756.1470 KRW` (5 wins/4 losses) but retained an open SOPH position at shutdown. The strict result is a three-trade, negative, non-promotable sample; no filter is loosened.
- v33 initial readback at `2026-09-10T04:10:54Z`: session `paper-1789013409326` is active in `.paper-forward-v33` with 20 markets, a complete config snapshot, `maxEntriesPerSignalWindow=0`, zero interruptions, 1 cycle, 0 strict/shadow closes, and batch/candle request counts `1/20` with no batch failure. This is only a session-start integrity observation, not a profitability result.
- v33 final handling: the PTY ended the runner before its async shutdown handler persisted the stop; a ledger-only stop call then recorded `active=false`, `endedWithOpenPositions=false`, 117 cycles, 0 strict closes, and 0 interruptions. Shadow had 1 closed loss `-415.4996 KRW` and retained TRUMP/IOST open. This is preserved but not called a clean process shutdown or profitability evidence.
- v34 final readback at `2026-09-10T04:31:09Z`: session ran 31 cycles with 0 strict closes/open positions and stopped with `stopped_cleanly`; its 90-second candle freshness guard blocked 10 `stale_candle_snapshot` entries, while shadow retained one diagnostic KAT position. The guard was operational evidence only; no filter was loosened.
- Added `maxCandleAgeSeconds` freshness contract for v35: adaptive default is 90 seconds for 1-minute candles, missing/stale/future timestamps fail closed for strict and shadow entry, and telemetry/API/UI expose block count, reason, age, and configured budget. Unit/integration coverage is included.
- v35 latest readback at `2026-09-10T04:36:41Z`: session `paper-1789014685381` is active with 42 cycles, complete snapshot, `maxCandleAgeSeconds=90`, `maxEntriesPerSignalWindow=0`, 0 strict closes/open positions, 0 interruptions, and 4 `stale_candle_snapshot` blocks. The latest block measured candle timestamp `04:34:00Z` at `04:35:30.418Z` (`90.418s` old versus the `90s` limit); an earlier block measured `103.888s`. Shadow/loose each have 1 diagnostic IOST stop-loss (`-297.2971 KRW`) and retain KAT/BIRB open. This proves the data-quality brake is operating; it is not a profitability result.
- v35 final handling: owner PID disappeared while strict remained empty; ledger-only cleanup recorded `active=false`, 67 cycles, 0 strict closes, 6 stale blocks, and shadow/loose 2 stop-loss closes totaling `-583.0342 KRW` with KAT/SC diagnostic positions retained. The age-stat telemetry was preserved for v36; this is not profitability evidence.
- v36 final readback at `2026-09-10T04:47:58Z`: owner process was gone and ledger-only cleanup recorded `active=false`, 25 cycles, 0 strict closes/open positions, 11 stale blocks with candle age `94.511~164.745s` (average `127.675s`) against the 90-second limit, and shadow IOST/VTHO/UP2 diagnostic positions retained. This supports keeping the 90-second data-quality brake; no filter was loosened.
- Context-separated freshness telemetry now distinguishes `blockedSnapshots`, `blockedAnalyses`, and delayed `blockedEntries`, and persists `blockContexts` plus age statistics. The raw counter bug found during tests was fixed so partial/legacy telemetry initializes all counters safely.
- v37 latest readback at `2026-09-10T04:52:43Z`: session `paper-1789015691439` is active with 33 cycles, complete snapshot, `maxCandleAgeSeconds=90`, `maxEntriesPerSignalWindow=0`, 10 `analysis` freshness blocks and 0 delayed `entry_confirmation` blocks. Observed stale ages are `92.457~149.428s` (average `113.871s`) versus the 90-second limit; strict closes/open positions remain 0, interruptions 0, and shadow has 2 diagnostic stop-loss closes totaling `-584.7741 KRW` with FLOCK still open. This supports retaining the data-quality brake and is not a profitability result.
- v37 follow-up at `2026-09-10T04:55:03Z`: 49 cycles, strict closes/open positions 0, shadow 3 diagnostic closes with FLOCK/BIRB/SOPH open, freshness analysis blocks remain 10 with no delayed-entry blocks. A separate read-only KRW-BTC probe returned in 201ms with a 30.661s candle age, and five immediate probes measured `46.308~46.782s`; this indicates the 90s failures are intermittent stale snapshots in the multi-market stream rather than a permanently stale endpoint. Keep the fail-closed brake and do not loosen it from starvation.
- v37 live follow-up at `2026-09-10T05:03:29Z`: the same active session has strict `KRW-WLD` open at `563` (entry `04:57:21.286Z`, age 6.1 minutes, 3,274ms delay, drift 0%, rebound `0.1779%`, RSI `30.0074→42.8877`, volume `1.6409x`, trend `-0.0531%`). It has no realized strict result, so v37 must not be stopped or replaced yet. Freshness telemetry is 53 analysis blocks/0 delayed-entry blocks with max observed age `157.045s`; shadow has 4 closed losses totaling `-1,179.7002 KRW` and FLOCK/BIRB/WLD open. This is an open-position observation, not profitability evidence.
- v37 live follow-up at `2026-09-10T05:05:04Z`: WLD remains the only strict open position at `563` (age 7.7 minutes; no realized strict result), so the session remains in place. Freshness telemetry is 58 analysis blocks/0 delayed-entry blocks with max observed age `157.045s`; shadow has 5 closed trades totaling `-858.4428 KRW` (1 win/4 losses) with FLOCK/WLD open. The active v37 process predates the later `blockedByCoin` patch, so no market-level attribution is inferred from this ledger.
- v37 latest wait at `2026-09-10T05:13:11Z`: WLD is still the only strict open position at `563` (age 15.8 minutes, high `564`, delay `3274ms`, drift `0%`), with 0 strict closes and 0 realized strict P&L. Shadow has 5 closed trades totaling `-858.4428 KRW` and FLOCK/WLD/NEAR open; freshness analysis blocks are 64 with no delayed-entry blocks and max age `157.045s`. Keep the session boundary until WLD settles; do not claim profitability.
- v37 latest wait at `2026-09-10T05:21:05Z`: WLD remains the only strict open position (entry `563`, high `566`, age 23.7 minutes, delay `3274ms`, drift `0%`), with 0 strict closes and 0 strict realized P&L. Shadow has 6 closed trades totaling `-776.7715 KRW` (2 wins/4 losses) after FLOCK returned `+81.6713 KRW` at `MAX_HOLD_TIME`; shadow WLD/NEAR remain open. Freshness analysis blocks are 106 with no delayed-entry blocks, and max observed age has reached `239.224s` versus the 90-second limit. Keep the session boundary until WLD settles; do not claim profitability.
- v37 final readback at `2026-09-10T05:28:31Z`: session `paper-1789015691439` ran 302 cycles and stopped cleanly with `endedWithOpenPositions=false` and zero interruptions. Strict WLD entered at `563` after a `3274ms` delay with 0% drift and closed at `563` after the 30-minute max hold for `-19.9900 KRW` (`-0.1%`, fees only). Strict sample is 1 trade and not profitability evidence. Shadow/loose retained one diagnostic NEAR position; shadow closed 7 trades for `-836.6866 KRW`, loose 6 for `-533.7199 KRW`. Freshness old telemetry recorded 182 analysis blocks, max age `298.149s`; no delayed-entry blocks.
- v38 initial per-market readback at `2026-09-10T05:30:32Z`: session `paper-1789018149796` is active with 9 cycles, complete snapshot, `maxCandleAgeSeconds=90`, 0 strict closes/open positions, and 0 interruptions. Freshness blocked 8 analysis snapshots: KRW-WLD 6, KRW-WAVES 1, KRW-BIRB 1; age range `94.157~134.458s` (average `109.765s`). Shadow VVV/NEAR remain diagnostic open. This is data-quality attribution, not profitability evidence.
- v38 follow-up at `2026-09-10T05:31:54Z`: 17 cycles and 11 analysis freshness blocks, now KRW-WLD 9, KRW-WAVES 1, KRW-BIRB 1, with age range `94.157~157.874s` (average `120.781s`), 0 delayed-entry blocks, and 0 strict closes/open positions. The concentration in WLD is a candidate for further observation, not enough evidence to auto-exclude or loosen the 90-second brake.
- v38 follow-up at `2026-09-10T05:33:39Z`: 33 cycles and 16 analysis freshness blocks, attributed to KRW-WLD 10, KRW-NEAR 4, KRW-WAVES 1, and KRW-BIRB 1; age range `94.157~157.874s` (average `116.785s`), 0 delayed-entry blocks, and 0 strict closes/open positions. A separate probe at `05:33:57Z` fetched WLD/NEAR/WAVES/BIRB in `58~157ms` with fresh candle ages `56.862~57.164s`, so the stale concentration is not yet a stable market-specific feed defect. Do not auto-exclude markets or loosen the brake.
- v38 final readback at `2026-09-10T05:38:34Z`: session `paper-1789018149796` ran 69 cycles and stopped cleanly with 0 strict closes/open positions and 0 interruptions. Per-market stale telemetry recorded WLD 10, NEAR 9, WAVES 6, SC 6, SOL 4, and BIRB 1; shadow/loose had no closed trades and retained VVV/NEAR diagnostic positions. This is data-quality evidence only.
- v39 final readback at `2026-09-10T05:43:17Z`: session ran 25 cycles and was ledger-only cleaned after owner disappearance, with 0 strict closes/open positions and no interruptions recorded. It observed 3 NEAR stale snapshots and no strict/shadow trades. The ledger is preserved as orphan cleanup evidence, not a profitability result.
- Added insufficient-candle telemetry: markets returning fewer than the minimum analysis candle count are tracked separately with event count, minimum/recent received count, and required count; they are not mislabeled as stale or strategy rejection.
- v40 initial readback at `2026-09-10T05:44:02Z`: session `paper-1789019021617` is active with 1 cycle, complete snapshot, `maxCandleAgeSeconds=90`, 3 analysis stale blocks (FLOCK/SC/NEAR; ages `102.723~103.248s`), 0 strict closes/open positions, and 0 interruptions. All 20 markets were observed at startup and no insufficient-candle event was recorded. This is an integrity observation, not a profitability result.
- v40 follow-up at `2026-09-10T05:45:04Z`: 9 cycles, 17 analysis stale blocks, no delayed-entry blocks, 0 strict closes/open positions, and no interruptions. Stale rates are SC `7/9` (`77.8%`), NEAR `5/9` (`55.6%`), WLD `3/9` (`33.3%`), and FLOCK `2/9` (`22.2%`); the other observed markets are `0%`. No automatic exclusion or filter loosening is enabled.
- v40 latest readback at `2026-09-10T05:48:48Z`: 33 cycles, 36 analysis stale blocks, no delayed-entry blocks, 0 strict closes/open positions, and 0 interruptions. Per-market stale rates are WLD `16/33` (`48.5%`), SC `9/33` (`27.3%`), NEAR `5/33` (`15.2%`), BIRB `3/33` (`9.1%`), FLOCK `2/33` (`6.1%`), and SOL `1/33` (`3.0%`); other markets are `0%`. No insufficient-candle events occurred in this window. Keep the 90-second brake and do not auto-exclude from this short sample.
- v40 latest denominator readback at `2026-09-10T05:51:37Z`: 58 cycles, 47 analysis stale blocks, no delayed-entry blocks, 0 strict closes/open positions, 0 interruptions, and no insufficient-candle events. Per-market stale rates are WLD `16/58` (`27.6%`), SC `14/58` (`24.1%`), NEAR `7/58` (`12.1%`), BIRB `4/58` (`6.9%`), FLOCK `3/58` (`5.2%`), WAVES `2/58` (`3.4%`), and SOL `1/58` (`1.7%`); all other markets are `0%`. The rates changed materially as the window grew, so no market exclusion or threshold relaxation is justified yet.
- v40 latest readback at `2026-09-10T05:58:37Z`: 114 cycles, 97 analysis stale blocks, no delayed-entry blocks, 0 strict closes/open positions, 0 interruptions, and no insufficient-candle events. All markets were observed 114 times; stale counts were WLD 22, BIRB 20, SC 19, NEAR 11, DOGE 12, SOPH 7, FLOCK 3, WAVES 2, and SOL 1. Shadow/loose each retained one WAVES diagnostic position after one close. The new dashboard exposes the top stale-market summary and separate candle-deficiency status; no market exclusion or threshold relaxation is justified.
- v40 latest readback at `2026-09-10T06:02:12Z`: 146 cycles, 123 analysis stale blocks, no delayed-entry blocks, 0 strict closes/open positions, 0 interruptions, and no insufficient-candle events. Per-market stale rates are BIRB `28/146` (`19.2%`), WLD `26/146` (`17.8%`), SC `19/146` (`13.0%`), NEAR `17/146` (`11.6%`), DOGE `12/146` (`8.2%`), SOPH `7/146` (`4.8%`), FLOCK `3/146` (`2.1%`), WAVES `2/146` (`1.4%`), and SOL `1/146` (`0.7%`); other markets are `0%`. Shadow/loose each retain one BFC stop-loss and WAVES open. No market exclusion or threshold relaxation is justified from this operational sample.
- v40 latest readback at `2026-09-10T06:01:16Z`: 130 cycles, 113 analysis stale blocks, no delayed-entry blocks, 0 strict closes/open positions, 0 interruptions, and no insufficient-candle events. All markets were observed 130 times; stale rates are BIRB `28/130` (`21.5%`), WLD `26/130` (`20.0%`), SC `19/130` (`14.6%`), DOGE `12/130` (`9.2%`), NEAR `11/130` (`8.5%`), SOPH `7/130` (`5.4%`), FLOCK `3/130` (`2.3%`), WAVES/TRUMP/VVV `2/130` (`1.5%` each), and SOL `1/130` (`0.8%`); remaining markets are `0%`. Shadow has one BFC stop-loss and WAVES open. The rates are operational evidence only; no market exclusion or threshold relaxation is justified.
- v40 latest readback at `2026-09-10T06:04:08Z`: 162 cycles, 126 analysis stale blocks, no delayed-entry blocks, 0 strict closes/open positions, 0 interruptions, and no insufficient-candle events. Per-market stale rates are BIRB `28/162` (`17.3%`), WLD `26/162` (`16.0%`), SC `19/162` (`11.7%`), NEAR `17/162` (`10.5%`), DOGE `12/162` (`7.4%`), SOPH `7/162` (`4.3%`), FLOCK `3/162` (`1.9%`), WAVES `2/162` (`1.2%`), and SOL `1/162` (`0.6%`); other markets are `0%`. Shadow/loose retain one BFC stop-loss and WAVES open. No market exclusion or threshold relaxation is justified.
- The fixed-config validator now reports `validationMode=fixed_config`, and the UI labels fixed versus tuned reports so a tuned candidate cannot be mistaken for the live runtime settings.
- v8 adds an independent ticker-based position-risk monitor at `1000ms`; a deterministic test proves it can close an over-stop dry-run position without waiting for the full market-analysis cycle.
- v9 extends the same 1-second ticker monitor to shadow books, so diagnostic stop/take/max-hold results are not delayed by the 20-market scan. The shadow close retains the entry rejection reasons for filter attribution. Latest v9 has three open shadow positions and no close yet.
- Top-20 fixed-config report now includes historical rejection telemetry; the dominant training/holdout blockers are previous RSI not oversold, previous high-break failure, and rebound threshold failure.
- Live promotion now requires a `fixed_config` report whose comparable strategy/risk parameters match the current runtime; a promoted tuned report or config-drifted report is rejected.
- The package now exposes `npm run validate:scalping:fixed` as the explicit command for generating the fixed runtime report used by the live gate; `npm run validate:scalping` remains exploratory tuned validation.
- Same-window 1-minute A/B study (20 markets, generated `2026-09-09T19:46:46Z`) removed timestamp drift between candidates: baseline `74` holdout trades / `-0.1806%`, volume `1.5x` `55` / `-0.1322%`, rebound `0.25%` `46` / `-0.0791%`, break-even/trailing `74` / `-0.1239%`, and volume `1.5x` plus protection `55` / `-0.0888%`. Every variant remained `0/20` promoted; runtime defaults were not changed.
- Repeated same-window 1-minute study after the cost-adjusted protection fix (20 markets, generated `2026-09-09T20:36:17Z`) still failed promotion: baseline `75` / `-0.1875%`, volume `1.5x` `57` / `-0.1548%`, rebound `0.25%` `48` / `-0.0937%`, cost-adjusted protection `77` / `-0.1430%`, and volume `1.5x` plus protection `58` / `-0.0996%`. These values are diagnostic and no candidate was enabled.
- Same-window 1-minute rebound-width study found `minReboundPercent=0.5` as a low-sample aggregate positive: `14` holdout trades / `+0.0105%`, while volume `1.5x + rebound 0.5` had `12` / `+0.0065%`. Rebound-0.5 still had `19/20` training-gate failures and remains a forward candidate only; it does not authorize live use.
- Longer same-window 1-minute/30-day six-market study rejected the short-window optimism: baseline `129` / `-0.7842%`, rebound `0.5%` `21` / `-0.0571%`, and volume `1.5x + rebound 0.5%` `15` / `-0.0462%`. The combined candidate is the closest to flat but still negative and under-sampled; v19 is observational only.
- Same-window 1-minute momentum experiment (`momentum_breakout`) produced `530` holdout trades across 20 markets but `-3.7440%` summed return and `0/20` positive markets. It is explicitly diagnostic-only and must not be enabled as a fallback for filter starvation.
- Same-window 30-day/1-minute profile study across six markets found `bb_reclaim -0.5133%`/67 trades and `trend_rebound -0.0797%`/21 trades; neither passed promotion. The existing profile variants remain diagnostic.
- Same-window 5-minute baseline over 7 days was positive only in aggregate (`20` trades, `+0.0341%`) with `18/19` training-gate failures. A longer 30-day/5-minute six-market study was negative (`36` trades, `-0.0529%` baseline; `+0.0108%` rebound-25) and did not pass promotion. The timeframe switch is not justified yet.
- Same-window 15-minute/30-day study across 19 data-sufficient markets remained inconclusive/negative: baseline `9` trades / `-0.0153%`, rebound `0.25%` `6` / `+0.0135%`, protection `9` / `-0.0153%`; all variants were held. A timeframe change is not justified.
- Same-window 30-day exit study rejected lower targets: `SL0.8/TP1.0` `133` / `-0.8858%`, `SL0.8/TP1.2` `132` / `-0.8675%`, `SL1.2/TP1.2` `129` / `-0.8584%`; cost-adjusted protection `131` / `-0.7041%`. No exit candidate was promoted.
- Same-window 30-day hold-time study found `maxHold=5m` `137` / `-0.7542%`, `15m` `133` / `-0.6750%`, and `30m` `130` / `-0.8219%`; shorter hold reduced loss but remained negative and was not promoted.
- Per-cycle batch ticker prefetch is covered by a no-duplicate-data-access test; it reduces the 20-market analysis path's ticker requests from one per market to one batch request while retaining per-market fallback on batch failure. It changes request efficiency only, not the signal contract.
- Same-window 30-day range-floor study found `minSignalRange=0.2%` improved baseline `-0.8065%` to `-0.5743%`, and `0.4%` to `-0.1804%`, but still no promotion. Combining it with volume-1.5/rebound-0.5 changed `-0.0462%` (15 trades) to `-0.0450%` (13 trades) at floor 0.4%; this is a marginal, under-sampled diagnostic result only.
- Same-window 60-day/1-minute six-market study kept the range-floor combination near flat but not profitable: volume-1.5/rebound-0.5 `25` trades / `-0.0004%`, plus floor `0.2%` `24` / `-0.0229%`, plus floor `0.4%` `20` / `+0.0124%`. All six markets failed training gate, so the aggregate positive is not promotable.
- The reusable `npm run compare:scalping` study now fetches each market's candle window once and evaluates multiple fixed candidates on that identical data; its output is diagnostic and separated from `scalping_validation.json`.
- Upbit market and risk clients now share a process-wide request slot; a deterministic test verifies concurrent clients start requests at least 80ms apart, reducing self-inflicted rate-limit collisions without changing trading signals.
- Break-even protection now uses a shared cost-adjusted price helper (`protectionPrices.js`) and includes two fees plus adverse exit slippage; this prevents a label of "break-even" from hiding a predictable net loss. The protection candidate remains disabled in the default runtime until a holdout and forward gate pass.
- Environment parsing now preserves intentional zero values for valid scalping experiments (volume/close/trend/range/protection offsets), and `npm run paper:forward` now carries the documented paper minimum-days/trades/return/MDD thresholds into the ledger instead of silently falling back to code defaults.
- A 7-day SC range-guard study (`maxSignalRangePercent=1.0`) also failed training/holdout gates: training 2 trades/+0.0423%, holdout 2 trades/-0.0078%/PF 0. It remains diagnostic-only.
- A 2-day BTC exploratory run with explicit `SCALP_RSI_OVERSOLD=30` produced 0 training/holdout trades and `training_gate_failed`; the dedicated override is wired and snapshot-visible, but no RSI threshold is promoted from this sample.

- v40 latest readback at `2026-09-10T06:07:58Z`: 186 cycles, 134 analysis-only freshness blocks, no delayed-entry blocks, 0 strict closes/open positions, 0 interruptions, and no insufficient-candle events. All 20 markets were observed 186 times; stale counts were BIRB `30`, WLD `27`, SC `21`, DOGE `18`, NEAR `17`, SOPH `7`, TRUMP `6`, FLOCK `3`, WAVES/VVV `2` each, and SOL `1`. Batch ticker requests were `186` with `0` failures and candle requests were `3,720`. Shadow and loose each recorded two `STOP_LOSS` closes, respectively `-633.4542` and `-629.5806` KRW, with no open diagnostic positions; BFC's shadow signal carried volume/trend/high-break rejection reasons and WAVES carried previous-high-break failure. This is operational/small-sample evidence only; retain the current filters and do not auto-exclude markets.
- Verification after the current source snapshot: `npm test` passed `75/75`, and `git diff --check` passed. The test suite's transient network `EPIPE` retry did not fail a test; no live order path was enabled.
- UI/PWA smoke at `2026-09-10T06:13Z`: the deterministic dashboard on port `3144` loaded in the browser and visibly exposed the paper-mode banner, real-order lock, validation gate (`0/3`, hold), paper-session state, risk checks, portfolio/trading views, and settings. The root page, manifest, icon, service worker, responsive CSS, and redesign JS returned HTTP `200`; the manifest declares `display=standalone`, and the source contains viewport/apple standalone metadata, service-worker registration, and `760/980px` responsive breakpoints. This proves desktop browser delivery and installable/static PWA wiring; a native installed launch and a real device viewport remain separate evidence lanes.
- Research reproducibility improvement: `compareScalpingVariants.js` now accepts `SCALP_VARIANT_CANDLES_FILE` and records `fetched[].source`, so a saved `{ market: candles[] }` window can be reused without silently moving the comparison period. A cache-backed study on the 2,016-candle BTC/ETH window evaluated baseline plus eight candidates: baseline holdout `-0.0121%` with 1 trade, `max_hold_15m` `-0.0112%`, and `loss_timeout_5m` `-0.0088%`; rebound/range/protection candidates had 0 holdout trades. All candidates failed the training gate and remained `0/2` promoted. These are diagnostic results only; no runtime defaults changed.
- v40 follow-up at `2026-09-10T06:18:13Z`: owner PID `25274` remained active with 256 cycles, 0 strict closes/open positions, 3 shadow and 3 loose diagnostic positions open (`WLD`, `TRUMP`, `KAT`), and 3 closed diagnostic trades in each book. Shadow/loose realized profit was `-983.0018/-979.1282 KRW`; stale analysis blocks were `173`, delayed-entry blocks `0`, interruptions `0`, and request totals were `256` batch ticker / `5,120` candle requests with `0` failures. This is still a small diagnostic observation and not profitability evidence; do not replace the session or loosen filters while diagnostic positions remain open.
- Relaxed-shadow fixed-window study: added `SHADOW_VALIDATION_CANDLES_FILE` support to `validateShadowCandidate.js` and ran the BTC/ETH 2,016-candle cache. BTC training was `-0.0147%` / 5 trades / PF `0.08`, holdout `-0.0172%` / 2 trades / PF `0`; ETH training was `-0.0217%` / 5 trades / PF `0.05`, holdout `-0.0174%` / 4 trades / PF `0`. Both failed the training gate and remain diagnostic-only. The relaxed soft cohort is therefore negative on this same-window sample; do not loosen volume, previous-high-break, or other strict filters based on forward starvation.
- Alternative-profile cache study: on the same BTC/ETH 2,016-candle window, `bb_reclaim` produced 0 holdout trades, `trend_rebound` matched baseline at `-0.0121%` with 1 trade, and `momentum_breakout` worsened to `-0.0223%` with 2 trades; RSI 30/25 plus volume 1.5/rebound 0.5 produced 0 holdout trades. All six profiles/variants failed training gates and remained `0/2` promoted. No alternative profile replaces the current contract.
- v40 strict entry readback at `2026-09-10T06:23:25Z`: one strict `KRW-VVV` position was open from `2026-09-10T06:20:17Z` at `31,870 KRW`, after a `4,319ms` confirmation delay and `+0.1886%` execution drift. The signal passed the current contract with rebound `0.4738%`, RSI recovery `8.4898`, volume ratio `1.6861`, close strength `1`, and trend slope `-0.1633%`. Its risk boundary is stop `-1.2%`, take `+1.8%`, max hold `30m`, with loss-only timeout disabled. This is an open/unrealized observation, not profitability evidence; preserve v40 until the strict risk exit is recorded.
- v40 diagnostic follow-up at `2026-09-10T06:25:05Z`: strict VVV remained open. WLD closed in both shadow/loose books by `STOP_LOSS` at `554.445 KRW` from `562.562 KRW`, net `-308.2791 KRW`, with `volume_confirmation_failed` and `previous_high_break_failed`; each diagnostic book now has 4 closed and 3 open positions. This reinforces the existing soft-cohort loss signal but is not strict or profitability evidence.
- v40 cohort attribution at `2026-09-10T06:26:40Z`: RAY produced the first positive soft close, `TAKE_PROFIT` `+377.6073 KRW`, with volume/trend rejection but no previous-high-break rejection. Across 5 closed shadow trades, 4 losses (BFC/WAVES/UP2/WLD) all carried `previous_high_break_failed`; volume appeared on 2 losses and the RAY win, trend on 1 loss and the RAY win. Keep previous-high-break enabled; volume/trend remain unresolved due to the tiny 2-loss/1-win and 1-loss/1-win cohorts. This is diagnostic evidence only, not a promotion result.
- v40 cohort update at `2026-09-10T06:35:23Z`: KAT also closed `TAKE_PROFIT` for `+338.3177 KRW` while carrying previous-high-break, volume, and rebound-threshold rejection reasons. Across 6 closed shadow trades, the books are 4 losses/2 wins for `-575.3559 KRW`; previous-high-break appears in 4 losses/1 win, volume in 2 losses/2 wins, and trend in 1 loss/1 win. The sample remains too small for filter tuning; retain the historical high-break A/B decision and leave volume/trend defaults unchanged.
- v40 diagnostic follow-up at `2026-09-10T06:40:34Z`: TRUMP closed in both shadow/loose books by `MAX_HOLD_TIME` after 30 minutes, entry `2716.714` → exit `2701.296`, net `-133.3863 KRW`, with `price_rebound_below_threshold` and `volume_confirmation_failed`. Each diagnostic book now has 7 closed/1 open and realized `-708.7421/-704.8686 KRW`; strict VVV remains open and unrealized.
- v40 strict follow-up at `2026-09-10T06:41:23Z`: while the original strict VVV remained open, a second strict `KRW-TRUMP` entered at `2026-09-10T06:40:30Z` at `2,704 KRW`. It recorded `4,772ms` confirmation delay, `+0.0740%` execution drift, rebound `0.2226%`, RSI recovery `11.6104`, volume ratio `1.3671`, close strength `0.7143`, and trend slope `-0.0566%`. Strict closed trades remain `0`; this is an open/unrealized forward observation, not profitability evidence.
- Strict VVV result and ledger fix at `2026-09-10T06:49:34Z`: VVV exited by `TAKE_PROFIT` at `32,490 KRW`, net `+368.7017 KRW` / `+1.8444%`, after `4,319ms` confirmation delay and `+0.1886%` execution drift. The risk-monitor close path was found to save `strictTrades` before refreshing `strictOpenPositions`; `recordPaperStrictTrade()` now refreshes/persists the open snapshot immediately. A regression assertion covers both memory and disk, and the targeted test passed before the full `npm test` run (`75/75 PASS`). The running old v40 process was not restarted; its next cycle reconciled the snapshot and left only strict TRUMP open.
- v40 post-fix readback at `2026-09-10T06:53:37Z`: strict TRUMP is the only open strict position; VVV is absent from `strictOpenPositions` while its `CLOSE` remains in `strictTrades`. Shadow/loose have 9 closed/0 open each, shadow realized `-664.6758 KRW` with 3 wins/6 losses. The latest diagnostic pair was VVV `TAKE_PROFIT` `+347.1532 KRW` and VTHO `STOP_LOSS` `-303.0869 KRW`. The snapshot fix is confirmed in the running ledger; strict TRUMP remains unrealized.
- AI Desk UI smoke at `2026-09-10T06:31Z`: browser route rendered the advisory-only banner, `NO ORDER ROUTING` boundary, paper mode, real-order lock, provider cards, monitoring session form, event empty state, and consultation empty state. Read-only `/api/ai/monitoring` returned `200` with empty state; `/api/status` returned `200` in `DRY_RUN`. Provider status eventually rendered GPT/Codex `UNAVAILABLE` with configuration guidance and Claude `READY` with local subscription status; no API key was shown or stored, and no session/consultation was created. Direct timing isolated the initial delay to local CLI auth checks (GPT about `1.96s`, Claude about `5.56s`); server timeout is bounded at 8s per provider. Treat this as bounded first-load latency, not a trading-path failure.
- v40 wait readback at `2026-09-10T06:33:01Z`: strict VVV remained open about 13 minutes after entry; cycles `365`, strict closes `0`, shadow/loose each `5` closed and `3` open, interruptions `0`, analysis freshness blocks `223`, delayed-entry blocks `0`, and batch ticker/candle requests `365/7,300` with `0` failures. This remains an unrealized observation; do not replace the session before its risk exit.

## Stop rules

- v40 strict milestone at `2026-09-10T07:32:07Z`: KAT closed by `STOP_LOSS`, entry `7.09` → exit `6.98`, net `-330.0915 KRW` / `-1.6507%`. Three strict trades now total VVV `+368.7017`, TRUMP `+2.1773`, KAT `-330.0915`, combined `+40.7875 KRW`. Three trades are insufficient for profitability promotion; keep live orders blocked and continue collecting independent strict samples.

- v40 diagnostic follow-up at `2026-09-10T07:30:31Z`: KAT shadow closed by `STOP_LOSS`, entry `7.09709` → exit `7.00299`, net `-284.9090 KRW`, with no rejection reasons recorded. Shadow reached `14` closed trades, `5` wins/`9` losses, and `-819.1325 KRW`; strict KAT and loose KAT remain separate open observations.

- v40 diagnostic follow-up at `2026-09-10T07:28:30Z`: BIRB closed in both shadow/loose books by `TAKE_PROFIT`, entry `85.2852` → exit `86.913`, net `+361.3543 KRW`, carrying only `previous_high_break_failed`. Each diagnostic book now has `13` closed trades, `5` wins/`8` losses, and realized `-534.2235/-530.3500 KRW`; retain high-break and keep all tuning candidates diagnostic-only.

- v40 strict milestone at `2026-09-10T07:10:57Z`: strict ledger now has `strictOpenPositions=[]` after two completed trades. VVV `TAKE_PROFIT` net `+368.7017 KRW`; TRUMP `MAX_HOLD_TIME` after 30 minutes, entry `2704` → exit `2707`, net `+2.1773 KRW` after `20.0021 KRW` fees. Combined strict `+370.8789 KRW` / 2 trades is too small for profitability promotion. Shadow has 11 closed / `-1244.4069 KRW`; loose 11 closed / `-1240.5334 KRW`. The strict snapshot persistence fix is confirmed after both risk exits.

- v40 diagnostic follow-up at `2026-09-10T07:01:45Z`: IOST closed in both shadow/loose books by `STOP_LOSS`, entry `1.7017` → exit `1.67832`, net `-294.5043 KRW`, with `trend_filter_failed`. Each diagnostic book now has `10` closed/`0` open and realized `-959.1801/-955.3066 KRW`; strict TRUMP remains open/unrealized.
- v40 strict follow-up at `2026-09-10T07:11:55Z`: TRUMP closed by `MAX_HOLD_TIME` after 30 minutes, entry `2704` → exit `2707`, net `+2.1773 KRW`; strict ledger now contains VVV and TRUMP closes. A new strict KAT position entered at `7.09 KRW` at `07:11:15Z` after `4,719ms` confirmation delay, rebound `0.9957%`, RSI recovery `20.7090`, volume ratio `1.2176`, and close strength `1`. KAT is the next unrealized strict observation; do not promote from the two completed trades.
- v40 diagnostic follow-up at `2026-09-10T07:08:14Z`: VTHO closed in both diagnostic books by `STOP_LOSS`, entry `0.885885` → exit `0.874125`, net `-285.2268 KRW`, with trend/high-break rejection. Shadow reached `11` closed/`0` open at `-1244.4059 KRW`; loose reached `11` closed/`1` open at `-1240.5324 KRW` with a new BTC diagnostic position. Strict TRUMP remains open/unrealized.

- Do not set `DRY_RUN=false` while the global validation report is not promoted.
- Do not loosen filters from forward starvation without a fresh holdout result.
- Keep forward ledgers and smoke portfolios outside the user's existing dry portfolio.

- v40 current readback at `2026-09-10T07:38:50Z`: owner PID `25274` remains active, heartbeat current, cycles `859`, and interruptions `0`. Strict has no open position and 3 closed trades totaling `+40.7875 KRW` (`VVV +368.7017` TAKE_PROFIT, `TRUMP +2.1773` MAX_HOLD_TIME, `KAT -330.0915` STOP_LOSS). Shadow has 14 closed trades (`5W/9L`, `-819.1325 KRW`) with 2 open; loose has 14 closed and `-621.0737 KRW` with 2 open. No delayed-entry blocks occurred; analysis freshness blocks are `586`, and batch ticker/candle requests are `859/17,180` with zero batch failures. The strict three-trade result is still non-promotional. Shadow remains an immediate-entry relaxed diagnostic cohort, so its negative result is not directly interchangeable with strict delayed-entry evidence; do not change runtime filters from this sample.

- v40 final readback at `2026-09-10T07:46:03Z`: the owner was stopped only after strict positions were empty. Ledger `paper-1789019021617` is `active=false`, `stopReason=stopped_cleanly`, `endedWithOpenPositions=false`, with strict `3` closed trades and `+40.7875 KRW`; shadow/loose each retain `3` diagnostic positions after `15` closed trades, realized `-1,131.7584/-933.6995 KRW`. The session is preserved and not promoted.
- v41 initial integrity readback at `2026-09-10T07:46:23Z`: isolated ledger `.paper-forward-v41/paper_validation.json`, session `paper-1789026382048`, owner PID `35227`, target `10` markets, complete config snapshot, `maxCandleAgeSeconds=90`, delayed entry `1,000~5,000ms`, cycle `1`, stale blocks `0`, delayed-entry blocks `0`, batch ticker/candle requests `1/10`, batch failures `0`, strict/shadow closed trades `0`, interruptions `0`. This is a fresh candidate window, not profitability evidence; keep the top-10 universe as an experiment only.
- v41 follow-up at `2026-09-10T07:48:32Z`: 20 cycles with target 10, stale analysis blocks `0`, delayed-entry blocks `0`, batch ticker/candle requests `20/200`, batch failures `0`, interruptions `0`, and strict/shadow/loose trades `0`. The smaller universe currently improves data freshness and request load relative to v40, but has no trade or profitability evidence yet; leave the candidate running.
- Dashboard isolation fix: `src/scripts/runDashboard.js` now injects unique temp paths for both `virtualPortfolioFile` and `paperValidationFile` before constructing `MultiCoinTrader`. A port `3146` smoke after the fix logged no root portfolio load, returned index/manifest/service-worker `200`, and `/api/paper-validation` returned the deterministic `dashboard-mock-paper` session. `npm test` passed `75/75`, `node --check` and `git diff --check` passed. Native PWA installation/launch was not performed; no new installed-app evidence is claimed.
- Dashboard isolation regression test is now explicit in `test/dashboardIsolation.test.js`; current full suite is `76/76 PASS`. v41 follow-up at `2026-09-10T07:53:51Z`: owner PID `35227` active, cycle `69`, strict/shadow closed trades `0`, stale analysis blocks `8` (WAVES `7`, VVV `1`), delayed-entry blocks `0`, batch ticker/candle requests `69/690`, batch failures `0`, interruptions `0`. The top-10 freshness candidate is operationally healthier than v40 but remains unproven for profitability.
- v41 candidate observation at `2026-09-10T07:56:00Z`: 89 cycles, strict closed/open `0/0`, shadow/loose each have one open diagnostic `KRW-IOST` position and no closed trade. The rejected signal had rebound `0.6098%`, RSI `34.44→40.01`, volume `0.7416x`, trend `-0.2772%`, and rejection reasons `volume_confirmation_failed`, `trend_filter_failed`, `previous_high_break_failed`; strict correctly remained out. Stale blocks are `16` (WAVES `14`, VVV `2`), batch requests remain failure-free. Keep the position until its risk exit; do not infer filter performance before closure.
- v41 wait readback at `2026-09-10T08:02:31Z`: cycles `149`, strict `0` trades, IOST shadow/loose still open with no realized result, stale blocks `28` (WAVES `14`, VVV `2`, FLOCK `12`), batch ticker/candle requests `149/1,490`, failures `0`, interruptions `0`. IOST high reached `1.68 KRW` but no take-profit CLOSE was recorded; treat the move as unrealized only and keep the session boundary.
- v41 final readback at `2026-09-10T08:04:48Z`: stopped only after all strict/shadow/loose positions were empty; `active=false`, `stopReason=stopped_cleanly`, `endedWithOpenPositions=false`, `169` cycles, no interruptions. Strict had `0` trades. The one shadow/loose IOST diagnostic trade closed `STOP_LOSS` at `-301.6131 KRW` after `9m36s`, with volume `0.7416x`, trend `-0.2772%`, and previous-high-break rejection. This is direct negative evidence for the relaxed cohort and supports retaining the strict filters; it is not a strict profitability result.
- v42 initial integrity readback at `2026-09-10T08:05:10Z`: fresh isolated ledger `.paper-forward-v42/paper_validation.json`, session `paper-1789027509595`, owner PID `44562`, same top-10 market universe, complete config snapshot, and opt-in `maxLosingHoldMinutes=5`. Candle age `90s`, delayed entry `1,000~5,000ms`, cycle `1`, stale `0`, delayed-entry blocks `0`, batch ticker/candle requests `1/10`, failures `0`, strict/shadow trades `0`, interruptions `0`. This is a risk-control research candidate only; runtime defaults remain unchanged.
- v42 follow-up at `2026-09-10T08:07:21Z`: 20 cycles, `maxLosingHoldMinutes=5` preserved in the snapshot, strict/shadow/loose trades `0`, stale blocks `0`, delayed-entry blocks `0`, batch ticker/candle requests `20/200`, failures `0`, interruptions `0`. The candidate is operationally clean but has no performance evidence yet.
- v42 follow-up at `2026-09-10T08:09:29Z`: 40 cycles, strict/shadow/loose trades `0`, stale blocks `0`, delayed-entry blocks `0`, batch ticker/candle requests `40/400`, failures `0`, interruptions `0`. The five-minute loss-only timeout candidate remains an untested risk-control hypothesis; no runtime default changed.
- Read-only IOST timeout reconstruction: Upbit 1-minute candles around v41's `07:54:10Z` entry show the `07:59:00` close at `1.66 KRW`, above the shadow entry `1.65165 KRW`; the current loss-only timeout condition would therefore likely not fire at the five-minute mark. Later `08:03:00` candle closed `1.64` before the observed stop-loss path. OHLC cannot reconstruct exact intraminute ticker ordering, so this is diagnostic context only and does not claim that v42 improves the v41 loss.
- v42 first candidate readback at `2026-09-10T08:15:38Z`: VTHO shadow/loose candidate had only `previous_high_break_failed` as its rejection reason, with volume `1.7408x`, trend `-0.1715%`, close strength `0.7273`, and rebound `0.6803%`. It closed by `STOP_LOSS` after `3m30s` at `-283.7095 KRW`; the five-minute loss-only timeout did not fire. This is negative forward evidence for relaxing previous-high-break and an inconclusive observation for the timeout control. At `08:16:57Z`, v42 remained active at `107` cycles with no open positions, stale blocks `14`, batch requests `107/1,070`, and no failures.
- v42 later accumulated a second STOP_LOSS (IOST `-309.1662 KRW` after `3m26s`) and opened KAT/BFC diagnostic positions before its owner process disappeared at `08:21:17Z`. Ledger-only cleanup at `2026-09-10T09:49:13Z` set `active=false`, `stopReason=stopped_cleanly`, `endedWithOpenPositions=false`, preserving the two open shadow positions, 2 closed shadow losses totaling `-592.8757 KRW`, and strict `0` trades. Because the owner was gone and shadow positions were unsettled, v42 is incomplete diagnostic evidence and was not reused or promoted.
- v43 is the fresh continuation candidate using the same top-10 universe and `maxLosingHoldMinutes=5`, with its own isolated ledger; v42 remains immutable evidence.
- v43 initial integrity readback at `2026-09-10T09:49:48Z`: isolated ledger `.paper-forward-v43/paper_validation.json`, session `paper-1789033787184`, owner PID `48588`, complete snapshot, top-10 universe `BFC/BIRB/BTC/ETH/FLOCK/IOST/KAT/UP2/VTHO/XRP`, `maxLosingHoldMinutes=5`, candle age `90s`, cycle `1`, stale `0`, batch ticker/candle `1/10`, failures `0`, strict/shadow trades `0`, interruptions `0`. Market membership changed with current 24h volume ranking; v43 is independent research evidence.
- v43 follow-up at `2026-09-10T09:52:58Z`: 31 cycles, strict/shadow/loose trades `0`, stale blocks `0`, delayed-entry blocks `0`, batch ticker/candle requests `31/310`, failures `0`, interruptions `0`. No performance evidence yet; keep the isolated risk-control session running.
- v43 candidate readback at `2026-10-10T09:55:06Z`: 51 cycles, strict trades `0`, shadow/loose three open candidates and no closes. KAT entered at `09:53:04Z` with rebound `0.1449%` and previous-high-break rejection; UP2 and BIRB entered at the same `09:54` signal window with rejection sets including volume/close/trend/high-break. Stale blocks `0`, batch ticker/candle `51/510`, failures `0`, interruptions `0`. Keep candidates until risk exits; do not interpret unrealized state as performance.
- v43 first close at `2026-10-10T09:56:45Z`: UP2 closed by `STOP_LOSS` after `1m39s` for `-296.8270 KRW`; rejection reasons were volume, close strength, trend, and previous-high-break failures. The five-minute timeout did not fire. KAT/BIRB/FLOCK remain open; strict trades remain `0`, v43 stale blocks `0`, and batch ticker/candle requests at the latest readback are `91/910` with no failures.
- v43 risk-control close at `2026-10-10T10:00:16Z`: KAT closed by `MAX_LOSING_HOLD_TIME` after `7m11s` at `-59.9151 KRW` / `-0.2996%`; rejection reasons were rebound threshold and previous-high-break. This is the first direct forward evidence that `maxLosingHoldMinutes=5` can fire and cap a losing shadow position before the fixed stop-loss. It is a risk-control result, not proof of profitability. v43 cumulative shadow is `2` losses / `-356.7421 KRW`; BIRB and FLOCK remain open, strict trades `0`, stale blocks `0`, batch requests `111/1,110`, failures `0`.
- v43 BIRB readback at `2026-10-10T10:04:20Z`: BIRB closed by `TAKE_PROFIT` after about `9m14s` for `+332.3489 KRW` / `+1.6617%`, despite volume/trend rejection reasons. v43 shadow is now `1W/2L`, realized `-24.3932 KRW`; FLOCK remains open, strict trades `0`, stale blocks `0`, latest batch requests `141/1,410`, failures `0`.
- v43 follow-up at `2026-10-10T10:19:47Z`: a new shadow `KRW-IOST` position opened at `1.5015 KRW` after rebound `1.3514%`, with volume `0.7570x` and previous-high-break rejection; trend `-0.0221%` and close strength `1`. FLOCK and IOST are both unrealized; strict remains `0`, cycles `280`, stale blocks `22`, batch requests `280/2,800`, failures `0`.
- v43 IOST close at `2026-10-10T10:22:27Z`: IOST closed by `STOP_LOSS` after `3m12s` at `-325.7829 KRW` / `-1.6289%`, with volume and previous-high-break rejection; the five-minute timeout did not fire. Shadow is now `1W/3L`, realized `-350.1761 KRW`; FLOCK remains open, strict trades `0`, stale blocks `22`, batch requests `304/3,040`, failures `0`.
- v43 BFC close at `2026-10-10T10:23:54Z`: BFC closed by `STOP_LOSS` after `46s` at `-305.5835 KRW` / `-1.5279%`, with close-strength and previous-high-break rejection; the five-minute timeout did not fire. Shadow is now `1W/4L`, realized `-655.7596 KRW`; FLOCK remains open, strict trades `0`, stale blocks `22`, batch requests `320/3,200`, failures `0`.
- v43 current follow-up at `2026-10-10T10:25:12Z`: VTHO and BIRB entered the shadow/loose diagnostic books while FLOCK remains open. VTHO had only `trend_filter_failed`; BIRB had only `previous_high_break_failed`; strict remains `0`. Shadow is `5` closed (`1W/4L`, `-655.7596 KRW`) plus 3 open positions; stale blocks `22`, batch requests `328/3,280`, failures `0`.
- v43 final readback at `2026-09-10T16:10:05Z`: after the owner/network interruption, the signal handler persisted `active=false`, `stopReason=stopped_cleanly`, `endedWithOpenPositions=false`, strict `0` trades/open positions, and preserved two open shadow positions (IOST/VTHO). Shadow/loose closed `8` trades (`2W/6L`) for `-780.0203 KRW`. Telemetry recorded `387` cycles, `40` stale analyses, `70` individual ticker fallbacks, and `7` batch failures; BIRB exceeded the intended 30-minute max-hold during the DNS outage before a later `MAX_LOSING_HOLD_TIME` close. This is incomplete/invalid promotion evidence and is not reused.
- Risk data-gap hardening: added `src/risk/riskMonitor.js`, persisted last-success/failure/outage state, a default `30s` maximum open-position ticker gap, fail-closed paper/live loop stop after the gap, continuity invalidation, API/config exposure, dashboard display, and tests. This directly addresses the v43 outage evidence; it does not claim a trading edge.
- v44 initial integrity readback at `2026-09-10T16:24:53Z`: fresh isolated ledger `.paper-forward-v44/paper_validation.json`, session `paper-1789057492169`, owner PID `78191`, top-10 universe `B3/BFC/BIRB/BTC/ETH/IOST/KAT/UP2/VTHO/XRP`, `maxLosingHoldMinutes=5`, `maxRiskDataGapSeconds=30`, risk interval `1,000ms`, complete snapshot, strict/shadow trades `0`, interruptions `0`. First cycle recorded one stale candle block and no ticker batch failure; this is integrity evidence only.
- v44 follow-up at `2026-09-10T16:26:58Z`: owner PID `78191` remains active at `21` cycles, with strict/shadow trades `0`, shadow positions `0`, stale analysis blocks `21` (KAT `20`, BIRB `1`), batch ticker/candle requests `21/210`, failures `0`, and risk monitor outage state still clean (`totalFailures=0`, `continuityEligible=true`). No performance claim is made.
- v44 follow-up at `2026-09-10T16:28:01Z`: owner PID `78191` remains active at `31` cycles; strict/shadow trades and open positions are `0`, stale blocks `27` (KAT `20`, BIRB `2`, B3 `5`), batch ticker/candle requests `31/310`, failures `0`, and risk monitor state remains clean (`totalFailures=0`, `continuityEligible=true`). This verifies the new risk-gap state is present before any position is opened; profitability remains untested.
- v44 position-path readback at `2026-09-10T16:32:13Z`: with shadow BFC/IOST positions open, the new risk monitor persisted `lastSuccessAt`, `lastCheckedAt`, `consecutiveFailures=0`, `totalFailures=0`, `outageCount=0`, and `continuityEligible=true`; no batch failure occurred. At 71 cycles strict remained empty and shadow had BFC/IOST open, with stale blocks `49` (KAT `24`, BIRB `4`, B3 `21`). This validates risk-ticker success persistence, not profitability.
- v44 first timeout close at `2026-09-10T16:35:06Z`: IOST closed by `MAX_LOSING_HOLD_TIME` after about `5m00s` at `-59.9151 KRW` / `-0.2996%`, with trend/high-break rejection. The risk monitor persisted a successful ticker check at `16:35:25Z` with `totalFailures=0`, `outageCount=0`, and `continuityEligible=true`; BFC remained open. This validates the timeout path under healthy data, not strategy profitability.
- v44 first strict entry at `2026-09-10T16:37:11Z`: XRP entered the real strict paper portfolio after `2,762ms` delay with `+0.0543%` execution drift. Signal metadata: rebound `0.2175%`, RSI `33.2886→47.3545`, volume `1.3253x`, close strength `1`, trend slope `-0.0452%`; strict open snapshot persisted and strict closed trades remained `0`. At the same time the risk monitor had a successful ticker check at `16:38:54Z` (`0` failures/outages, continuity eligible), demonstrating that the new safety state is attached to a real strict position. Current assets remain unrealized; no profitability claim.
- v44 second timeout close at `2026-09-10T16:38:54Z`: BFC shadow/loose closed by `MAX_LOSING_HOLD_TIME` after about `6m47s` at `-59.9151 KRW` / `-0.2996%`, with volume rejection; risk monitor success remained current with `0` failures/outages and continuity eligible. Shadow now has `2` timeout losses totaling `-119.8302 KRW`, while strict XRP remains open and unrealized. This validates repeated risk-control execution under healthy ticker data, not profitability.
- v44 first strict close at `2026-09-10T16:42:12Z`: XRP entered after `2,762ms` delay with `+0.0543%` drift and closed by `손실 포지션 조기 청산 (5분 초과)` at `1,841 KRW` from `1,844 KRW`, net `-52.4954 KRW` / `-0.2626%`. Strict ledger now has `1` closed loss; strict open is empty. The paired shadow XRP closed `MAX_LOSING_HOLD_TIME` at `-81.5421 KRW`. Risk monitor success at `16:42:19Z` remained failure/outage-free and continuity eligible. This proves the new timeout and strict persistence path, not profitability.
- v44 follow-up at `2026-09-10T16:42:59Z`: strict and shadow positions are empty after the first strict loss; strict realized `-52.4954 KRW`, shadow `3` timeout losses / `-201.3723 KRW`. Risk monitor has `lastSuccessAt=16:42:59Z`, failure/outage `0`, continuity eligible; cycles `170`, stale analysis blocks `129` (KAT `46`, BIRB `23`, B3 `57`, ETH `3`), batch requests `170/1,700`, failures `0`. Keep v44 for additional strict samples; no promotion.
- v44 follow-up at `2026-09-10T16:44:03Z`: risk monitor remains healthy (`lastSuccessAt=16:44:03Z`, failures/outages `0`, continuity eligible). Strict remains `1` closed loss totaling `-52.4954 KRW`; shadow has `3` timeout losses totaling `-201.3723 KRW` with one open position, and loose has two open diagnostic positions. At `180` cycles, stale analysis blocks are `139` (KAT `47`, BIRB `29`, B3 `60`, ETH `3`), batch requests `180/1,800`, failures `0`. Continue collecting strict samples; do not promote the timeout candidate.
- v44 strict close follow-up at `2026-09-10T16:45:07Z`: strict remains `1` closed loss / `-52.4954 KRW` with no open position; shadow has `3` closed timeout losses / `-201.3723 KRW` and one open diagnostic position. Risk monitor last success `16:45:07Z`, failures/outages `0`, continuity eligible; cycles `190`, stale analysis blocks `142` (KAT `47`, BIRB `32`, B3 `60`, ETH `3`), batch requests `190/1,900`, failures `0`. Keep v44 for more strict observations; no promotion.
- v44 second strict entry at `2026-09-10T16:46:08Z`: KAT entered after `3,454ms` delay with `0%` execution drift. Signal metadata: rebound `0.5891%`, RSI `32.3423→46.1025`, volume `4.0869x`, close strength `1`, trend slope `-0.1262%`; strict open snapshot persisted. Risk monitor success at `16:47:20Z` remained failure/outage-free and continuity eligible. Strict still has only the earlier XRP loss (`-52.4954 KRW`) realized; KAT is unrealized.
- v44 B3 diagnostic close at `2026-09-10T16:51:32Z`: B3 shadow/loose closed by `MAX_LOSING_HOLD_TIME` after about `5m10s` at `-59.9151 KRW` / `-0.2996%`, with volume rejection. Strict KAT remained open from `16:46:08Z`; risk monitor was healthy with `0` failures/outages and continuity eligible. Shadow now has `4` timeout losses / `-261.2873 KRW`, cycles `250`, stale blocks `179`, batch requests `250/2,500`, failures `0`.
- v44 strict KAT close at `2026-09-10T16:55:07Z`: KAT closed by `손실 포지션 조기 청산 (5분 초과)` at its entry price after about `8m58s`, net `-19.9880 KRW` (round-trip fees). Strict now has `2` closed losses totaling `-72.4834 KRW`; shadow has `4` timeout losses totaling `-261.2873 KRW`. At `16:57:08Z`, risk monitor remained healthy (`lastSuccessAt` current, failures/outages `0`, continuity eligible), no positions were open, and the session continued for additional strict samples. This is risk-control evidence, not profitability evidence.
- v44 current readback at `2026-09-10T16:52:48Z`: strict KAT remains open from `16:46:08Z` with persisted delay `3,454ms`, drift `0%`, and no strict close beyond the earlier XRP loss (`-52.4954 KRW`). Shadow has `4` timeout losses / `-261.2873 KRW` and one open diagnostic position. Risk monitor last success `16:52:48Z`, failures/outages `0`, continuity eligible; cycles `260`, stale blocks `189`, batch requests `260/2,600`, failures `0`. Keep KAT until its risk exit; no promotion.
- v44 second strict close at `2026-09-10T16:55:07Z`: KAT closed by `손실 포지션 조기 청산 (5분 초과)` at its `6.83 KRW` entry price after about `8m58s`; realized net `-19.9880 KRW` consisting of round-trip fees. Strict now has `2` closed losses totaling `-72.4834 KRW`, no open strict position. Shadow remains `4` timeout losses / `-261.2873 KRW` with one open diagnostic position. Risk monitor success `16:55:07Z`, failures/outages `0`, continuity eligible; cycles `281`, stale blocks `197`, batch requests `281/2,810`, failures `0`.
- v44 diagnostic follow-up at `2026-09-10T16:58:49Z`: UP2 shadow/loose closed by `STOP_LOSS` after about `1m21s` at `-299.6320 KRW` / `-1.4982%`, with rebound, RSI-recovery, trend, and previous-high-break rejection. Strict remains `2` losses / `-72.4834 KRW` with no open position; shadow now `0W/5L`, `-560.9193 KRW`, with one open diagnostic position. Risk monitor last success `16:58:49Z`, failures/outages `0`, continuity eligible; cycles `315`, stale blocks `243`, batch requests `315/3,150`, failures `0`.
- v44 follow-up at `2026-09-10T17:01:25Z`: strict remains `2` closed losses totaling `-72.4834 KRW` with no open position; shadow remains `0W/5L`, `-560.9193 KRW`, with one open KAT diagnostic position. Risk monitor last success `17:01:25Z`, failures/outages `0`, continuity eligible; cycles `340`, stale blocks `251`, batch requests `340/3,400`, failures `0`. Keep the v44 evidence window active; no profitability promotion.
- v44 current readback at `2026-09-10T17:04:37Z`: strict remains `2` closed losses / `-72.4834 KRW`, shadow `5` losses / `-560.9193 KRW` with one open diagnostic position. Risk monitor last success `17:04:37Z`, failures/outages `0`, continuity eligible; cycles `370`, stale blocks `301`, batch requests `370/3,700`, failures `0`. No additional strict sample yet; keep the session running.
- v44 current readback at `2026-09-10T17:00:21Z`: risk monitor remains healthy (`lastSuccessAt` current, failures/outages `0`, continuity eligible). Strict has `2` closed losses totaling `-72.4834 KRW` and no open position; shadow has `5` closed losses totaling `-560.9193 KRW` with one open KAT diagnostic position. Cycles `330`, stale blocks `251`, batch requests `330/3,300`, failures `0`. No profitability or promotion claim.
- v44 final readback at `2026-09-10T17:11:31Z`: owner PID `78191` was stopped only after strict and diagnostic positions were empty. The isolated ledger is `active=false`, `stopReason=stopped_cleanly`, `endedWithOpenPositions=false`, `interruptions=0`, and risk continuity remains eligible with `0` failures/outages. Strict closed `2` trades, both loss-only timeout exits, totaling `-72.4834 KRW`; shadow closed `5` trades (`0W/5L`, `-560.9193 KRW`), and the immediate-entry loose book closed `6` (`1W/5L`, `-553.7318 KRW`). Final telemetry was `410` cycles, `314` analysis freshness blocks, `0` delayed-entry blocks, `105` shadow candidates, and batch ticker/candle requests `410/4,100` with `0` batch failures. Freshness block rates were KAT `81/418` (19.4%), B3 `85/417` (20.4%), and BIRB `126/417` (30.2%), while VTHO/XRP/BFC/BTC had no blocks. This is a preserved small-sample baseline and risk-control/data-quality evidence, not profitability or promotion evidence.
- v45 plan: run the unchanged strict contract and the same opt-in `maxLosingHoldMinutes=5` risk candidate on a manually isolated high-freshness cohort (`VTHO/XRP/BFC/BTC/ETH/IOST/UP2`) in `.paper-forward-v45`. This is a data-quality cohort comparison, not a filter relaxation; keep `DRY_RUN=true`, `maxRiskDataGapSeconds=30`, and the live validation gate unchanged.
- v45 initial integrity readback at `2026-09-10T17:12:40Z`: isolated ledger `.paper-forward-v45/paper_validation.json`, owner PID `55021`, session `paper-1789060360700`, target cohort `VTHO/XRP/BFC/BTC/ETH/IOST/UP2`, `maxLosingHoldMinutes=5`, `maxRiskDataGapSeconds=30`, and max candle age `90s`. At the first persisted cycle, strict/shadow/loose had no trades or positions, stale blocks were `0`, batch ticker/candle requests were `1/7`, and risk monitor had no failures. This is startup/integrity evidence only; no profitability inference.
- Freshness-cohort selector verification at `2026-09-10T17:19:23Z`: a 10-second isolated smoke using `PAPER_SMOKE_MARKETS=FRESH_FROM_LEDGER` and v44's ledger selected exactly `VTHO/XRP/BFC/IOST/BTC/ETH/UP2`, excluding KAT/B3/BIRB for block-rate over 5% with at least 100 observations. The smoke completed 2 cycles with no trade/position, no freshness or request failure, and wrote only under `/tmp/coinpilot-quality-smoke`. The new pure selector tests plus the existing suite are `83/83 PASS`; this utility remains research-only and does not change the default universe.
- v45 final readback at `2026-09-10T17:26:45Z`: owner PID `55021` was stopped cleanly with no strict or diagnostic positions open and no continuity interruption. The manually selected high-freshness cohort produced `0` strict trades, while shadow/loose each closed `3` losses totaling `-698.6921 KRW`: UP2 `STOP_LOSS -303.0869`, VTHO `MAX_LOSING_HOLD_TIME -108.6684`, and BFC `STOP_LOSS -286.9369`. Final telemetry was `142` cycles, `18` analysis freshness blocks, `0` delayed-entry blocks, and batch ticker/candle requests `142/994` with `0` failures. The v45 sample is too small for profitability, but its relaxed losses reinforce keeping trend/high-break filters; it is preserved as a non-promotional cohort observation.
- v46 plan: use `PAPER_SMOKE_MARKETS=FRESH_FROM_LEDGER` with v45 as the source, minimum `100` observations, and maximum freshness-block rate `5%`. This should deterministically remove UP2 after its v45 `11/142` block rate while retaining the same strategy/risk settings and isolated storage.
- v46 initial integrity readback at `2026-09-10T17:27:16Z`: the selector removed only `KRW-UP2` (`11/142` freshness blocks in v45) and selected `VTHO/XRP/BFC/BTC/ETH/IOST`. New isolated ledger `.paper-forward-v46/paper_validation.json` has owner PID `70111`, session `paper-1789061236308`, complete config snapshot, `maxLosingHoldMinutes=5`, and no trades/positions at cycle `1`. No live orders or default-config changes.
- v46 first candidate readback at `2026-09-10T17:31:27Z`: after `45` cycles, strict remained `0` trades/positions. The shadow book recorded one `KRW-BFC` `TAKE_PROFIT` at `+361.0848 KRW` after `2.16` minutes; the candidate was intentionally rejected by close-strength, trend, and previous-high-break filters, so this is one relaxed-cohort winner and not evidence to loosen strict filters. `KRW-IOST` showed `19/45` freshness blocks while the other five markets remained fresh; batch ticker/candle requests were `45/270` with `0` failures and risk continuity was healthy. Keep the cohort diagnostic-only and continue until more outcomes settle.
- v46 follow-up at `2026-09-10T17:34:29Z`: after `75` cycles, strict remained `0` trades/positions. Shadow/loose had `2` closed trades (`1W/1L`) totaling `-36.7978 KRW`: BFC `TAKE_PROFIT +361.0848` followed by BFC `STOP_LOSS -397.8826`; the second signal was rejected by volume/trend in strict. `KRW-IOST` accumulated `22/75` freshness blocks (29.3%) despite passing the prior v45 cohort filter; batch ticker/candle requests were `75/450` with `0` failures and risk continuity was healthy. This demonstrates that a historical freshness cohort can drift in live conditions; do not treat it as a permanent market-quality guarantee.
- v46 final readback at `2026-09-10T17:37:27Z`: owner PID `70111` was stopped cleanly with no strict or diagnostic positions open and no interruption. Strict closed `0`; shadow/loose each closed `2` trades (`1W/1L`) totaling `-36.7978 KRW`. Final telemetry was `106` cycles, `22` analysis freshness blocks all from `KRW-IOST` (`22/106`, 20.8%), `0` delayed-entry blocks, and batch ticker/candle requests `106/636` with `0` failures. This confirms that freshness quality can drift within a short session and is not a profitability result.
- v47 plan: use v46 as the freshness source with the same 100-observation/5% cap. Expected cohort is `VTHO/XRP/BFC/BTC/ETH`, excluding IOST after its v46 in-session block rate exceeded the cap. Keep all strategy and risk parameters unchanged.
- v47 initial integrity readback at `2026-09-10T17:38:01Z`: automatic freshness selection from v46 selected exactly `VTHO/XRP/BFC/BTC/ETH` and excluded `IOST` (`22/106`, 20.8%). New ledger `.paper-forward-v47/paper_validation.json` has owner PID `87164`, session `paper-1789061881879`, complete snapshot, and cycle `1` with no trades/positions, stale blocks, or request failures. No runtime default or live gate changed.
