# Project Context

## Domain

Upbit cryptocurrency automation and paper-trading research.

## Role

Safety-first implementation and evidence-driven strategy validation.

## Codebase

- `src/index.js` wires environment configuration, the trader, and the dashboard.
- `src/trader/multiCoinTrader.js` owns the trading cycle, virtual portfolio, paper ledger, and live-order gate.
- `src/analysis/technicalIndicators.js` owns the shared live indicator/rebound contract.
- `src/strategy/oversoldReactionStrategy.js` owns delayed-entry decisions and position risk exits.
- `src/backtest/scalpingBacktest.js` owns fee/slippage-aware simulation, tuning, and walk-forward validation.
- `simulateScalpingPortfolio()` in `src/backtest/scalpingBacktest.js` is the separate synchronized multi-market validation lane; it is diagnostic-only and does not replace the live gate.
- `src/risk/lossCircuitBreaker.js` owns the opt-in sliding-window loss circuit used by runtime, paper shadow books, and backtests.
- `public/index.html` is the responsive dashboard and PWA shell.

The default strategy is `oversold_reaction_scalping`: completed 1-minute candles, recent RSI-oversold reaction, 1–5 second revalidation delay, no averaging, bounded positions, and DRY_RUN by default. `scalping_validation.json` is a promotion gate; a failed holdout never authorizes live orders.

The process-wide loss circuit breaker is disabled by default (`count=0`). When explicitly enabled, strict runtime entries and each shadow book stop opening new positions after N losses in the configured time window; strict and shadow state is persisted separately.

The portfolio regime gate is also disabled by default; it is evaluated only in the synchronized multi-market diagnostic lane until a holdout supports enabling it.

`maxLosingHoldMinutes` is an opt-in loss-only exit candidate; zero is preserved as the runtime default.

The latest v30 forward session is preserved and stopped cleanly after all three strict positions settled. It produced 3 strict closes for `+23.59 KRW`, 5 shadow and 5 loose closes for `-692.16 KRW` each, 0 interruptions, and no open strict positions. This is small-sample evidence only. v31 was later stopped cleanly with no strict positions but three open diagnostic shadow positions preserved in its ledger. v32 in `.paper-forward-v32` then settled 3 strict closes for `-44.9573 KRW` with no strict positions or interruptions at shutdown; its shadow/loose books retain diagnostic state separately. v32 is still small-sample evidence and not profitability evidence. v33 and v34 were short latest-code observations with no strict closes; v34 demonstrated 10 stale-candle blocks in 31 cycles. v35 was preserved after 67 cycles with no strict closes, 6 stale blocks, and shadow loss evidence. v36 was preserved after 25 cycles with 11 stale blocks and age up to 164.745 seconds. v37 in `.paper-forward-v37` then settled one strict WLD trade for `-19.99 KRW` with no strict positions or interruptions at shutdown; its shadow/loose books retain diagnostic state separately. v38 and v39 were short latest-code observations with no strict closes; v38 ended with per-market stale attribution and v39 with its first complete per-market observation denominator. The current latest-code observation is isolated v40 in `.paper-forward-v40`, which includes per-market freshness and insufficient-candle telemetry, a complete snapshot, cap disabled, and zero interruptions at startup; its stale rates and data-quality outcomes are being measured without automatic market exclusion.

`maxEntriesPerSignalWindow` is an opt-in correlated-entry safeguard derived from v30's three same-signal-window entries. It defaults to `0`, is persisted in strict risk state, and is covered by portfolio backtest/API/UI tests; it has not been enabled or promoted.

## Tools

- Node.js ES modules and `node --test`.
- Public Upbit market/candle endpoints for read-only research.
- Dashboard API and PWA service worker.
- Isolated `npm run paper:forward` sessions under `.paper-forward*` so user `dry_portfolio.json` is not modified.

## Evidence boundary

Strict milestone update (`2026-09-10T07:32:07Z`): KAT closed by `STOP_LOSS`, entry `7.09` → exit `6.98`, net `-330.0915 KRW` / `-1.6507%`. The completed strict set is now 3 trades: VVV `+368.7017`, TRUMP `+2.1773`, KAT `-330.0915`, combined `+40.7875 KRW`. This remains a tiny/unrealized-history sample and does not authorize promotion or live orders; the ledger snapshot is expected to be empty after the close and must remain separate from diagnostic books.

Latest KAT diagnostic update (`2026-09-10T07:30:31Z`): shadow KAT closed by `STOP_LOSS`, entry `7.09709` → exit `7.00299`, net `-284.9090 KRW`, with no rejection reasons recorded. Shadow reached 14 closed trades, 5 wins/9 losses, and `-819.1325 KRW`; strict KAT and loose KAT remain separate open observations.

Latest v40 soft-cohort update (`2026-09-10T07:28:30Z`): BIRB closed by `TAKE_PROFIT`, entry `85.2852` → exit `86.913`, net `+361.3543 KRW`, carrying only `previous_high_break_failed`. Shadow/loose now have 13 closed trades, 5 wins/8 losses, and realized `-534.2235/-530.3500 KRW`; the cohort remains negative and diagnostic-only.

Strict milestone: by `2026-09-10T07:10:57Z`, v40 had two completed strict trades with `strictOpenPositions=[]`: VVV `TAKE_PROFIT` net `+368.7017 KRW` and TRUMP `MAX_HOLD_TIME` after 30 minutes, entry `2704` → exit `2707`, net `+2.1773 KRW` after `20.0021 KRW` fee. Combined strict realized profit is `+370.8789 KRW` across only 2 trades, so this is not profitability or promotion evidence. The strict snapshot fix is confirmed on disk after both closes. Shadow had 11 closed trades at `-1244.4069 KRW`; loose had 11 closed at `-1240.5334 KRW`, with diagnostic positions still open separately.

Latest v40 follow-up (`2026-09-10T07:01:45Z`): IOST closed in shadow/loose by `STOP_LOSS` at net `-294.5043 KRW` with `trend_filter_failed`; diagnostic books reached 10 closed/0 open and realized `-959.1801/-955.3066 KRW`. Strict TRUMP remains the only open strict position; VVV close and snapshot fix remain consistent.

At `2026-09-10T07:11:55Z`, strict TRUMP closed by `MAX_HOLD_TIME` after 30 minutes, entry `2704` → exit `2707`, net `+2.1773 KRW`; strict ledger now contains both VVV and TRUMP closes. A new strict KAT position entered at `7.09 KRW` at `2026-09-10T07:11:15Z` with `4,719ms` confirmation delay, rebound `0.9957%`, RSI recovery `20.7090`, volume ratio `1.2176`, and close strength `1`. Preserve KAT as the next unrealized strict observation.

At `2026-09-10T07:08:14Z`, VTHO closed in both diagnostic books by `STOP_LOSS`, entry `0.885885` → exit `0.874125`, net `-285.2268 KRW`, with `trend_filter_failed` and `previous_high_break_failed`. Shadow reached 11 closed/0 open at `-1244.4059 KRW`; loose reached 11 closed/1 open at `-1240.5324 KRW` with a new BTC diagnostic position. Strict TRUMP remains open/unrealized.

Backtests and paper runs are separate evidence lanes from real fills, wallet settlement, and profitability. Forward ledger records strict closes durably and marks long heartbeat gaps as continuity interruptions that invalidate promotion. Current historical and forward evidence has not passed the promotion gate; do not enable live orders based on static tests or training results.

At the latest v40 readback (`2026-09-10T06:07:58Z`), the owner process was alive at 186 cycles with a current heartbeat, 0 strict closes/open positions, 0 interruptions, 134 analysis-only stale blocks, 0 delayed-entry blocks, and 0 insufficient-candle events. All 20 markets were observed 186 times; batch ticker requests were 186 with 0 failures and candle requests were 3,720. Shadow and loose each had two stop-loss closes (`-633.4542` and `-629.5806` KRW) and no open diagnostic positions. This remains operational/small-sample evidence, not profitability evidence; do not loosen filters or enable live orders.

The variant research tool also accepts `SCALP_VARIANT_CANDLES_FILE` for fixed-window reuse. The first cache-backed BTC/ETH study kept all candidates diagnostic: every variant failed the training gate, with baseline holdout `-0.0121%`, loss-only 5-minute timeout `-0.0088%`, and no candidate promoted. Runtime defaults remain unchanged.

The v40 owner was still alive at `2026-09-10T06:18:13Z` with heartbeat `06:17:35Z`, 256 cycles, 0 strict closes/open positions, 3 shadow and 3 loose diagnostic positions open (`WLD`, `TRUMP`, `KAT`), 3 closed diagnostic trades in each book, and no interruptions. Shadow/loose realized profit was `-983.0018/-979.1282 KRW`; stale analysis blocks were 173 with no delayed-entry blocks, and request totals were 256 batch ticker / 5,120 candle requests with 0 batch failures. Keep this session isolated until those diagnostic positions settle or an explicit preserved-open boundary is chosen.

The relaxed-shadow validator now accepts `SHADOW_VALIDATION_CANDLES_FILE`. On the same 2,016-candle BTC/ETH cache, the relaxed cohort failed training and holdout gates in both markets (BTC holdout `-0.0172%`, ETH `-0.0174%`, PF `0`). This confirms that widening the shadow cohort is not a justified tuning direction from the current evidence.

The same cache also rejected alternative profiles: `bb_reclaim` had no holdout trades, `trend_rebound` matched the baseline loss, and `momentum_breakout` was worse; stricter RSI/volume/rebound combinations had no holdout trades. These remain diagnostic comparisons, not runtime changes.

At `2026-09-10T06:23:25Z`, v40 had one strict open position: `KRW-VVV`, entered at `2026-09-10T06:20:17Z` at `31,870 KRW`, with a 4,319ms confirmation delay and `+0.1886%` execution drift. The signal had rebound `0.4738%`, RSI recovery `8.4898`, volume ratio `1.6861`, close strength `1`, and trend slope `-0.1633%`; it passed the current strict contract. The saved risk boundary is stop `-1.2%`, take `+1.8%`, max hold `30m`, loss-only timeout disabled. Do not stop or replace v40 until this strict position reaches a recorded risk exit.

At `2026-09-10T06:25:05Z`, the same strict VVV position remained open. In the diagnostic books, WLD then closed by `STOP_LOSS` at `554.445 KRW` from `562.562 KRW`, net `-308.2791 KRW`, with `volume_confirmation_failed` and `previous_high_break_failed`; shadow/loose each had 4 closed trades and 3 open positions. This is additional soft-cohort loss evidence, not a strict or profitability result.

At `2026-09-10T06:26:40Z`, RAY added the first positive soft close in v40: `TAKE_PROFIT`, `+377.6073 KRW`, while carrying `volume_confirmation_failed` and `trend_filter_failed` but not previous-high-break failure. Across five closed shadow trades, the four losses (BFC/WAVES/UP2/WLD) all carried `previous_high_break_failed`, while volume was present on two losses and the RAY win, and trend was present on one loss and the RAY win. Keep previous-high-break enabled; treat volume/trend as unresolved cohorts requiring more data, not as immediate tuning targets.

At `2026-09-10T06:35:23Z`, KAT also closed `TAKE_PROFIT` for `+338.3177 KRW` while carrying `price_rebound_below_threshold`, `volume_confirmation_failed`, and `previous_high_break_failed`. The six closed shadow trades now total 4 losses/2 wins and `-575.3559 KRW`; previous-high-break appears in 4 losses/1 win, volume in 2 losses/2 wins, and trend in 1 loss/1 win. The cohorts remain too small for filter tuning; retain the historical A/B decision to keep high-break and leave volume/trend defaults unchanged.

At `2026-09-10T06:40:34Z`, TRUMP closed in shadow/loose by `MAX_HOLD_TIME` after 30 minutes: entry `2716.714`, exit `2701.296`, net `-133.3863 KRW`, with `price_rebound_below_threshold` and `volume_confirmation_failed`. The diagnostic books now have 7 closed/1 open each and realized `-708.7421/-704.8686 KRW`; strict VVV remains open and is tracked separately.

At `2026-09-10T06:41:23Z`, strict VVV was still open and a second strict position, `KRW-TRUMP`, entered at `2026-09-10T06:40:30Z` at `2,704 KRW`. The TRUMP strict entry had a `4,772ms` confirmation delay, `+0.0740%` execution drift, rebound `0.2226%`, RSI recovery `11.6104`, volume ratio `1.3671`, close strength `0.7143`, and trend slope `-0.0566%`. Strict trades remain unclosed; preserve both positions through their risk exits.

At `2026-09-10T06:49:34Z`, strict VVV exited by take-profit at `32,490 KRW`, net `+368.7017 KRW` / `+1.8444%`, after `4,319ms` entry confirmation delay and `+0.1886%` execution drift. A ledger integrity bug was found in the same path: the risk-monitor close wrote `strictTrades` before refreshing `strictOpenPositions`, leaving a short crash/restart window with a stale open snapshot. `recordPaperStrictTrade()` now refreshes and persists `strictOpenPositions` immediately after a strict close; the regression test asserts both in-memory and on-disk snapshots are empty after close. The old v40 process was not restarted; its next cycle removed VVV from the snapshot, leaving strict TRUMP as the only open position.

By `2026-09-10T06:53:37Z`, the corrected running ledger had strict TRUMP as the only open position, with VVV absent from `strictOpenPositions` and its `CLOSE` retained. Shadow/loose had no open positions and 9 closed trades each: shadow realized `-664.6758 KRW`, 3 wins/6 losses. The latest shadow pair included VVV `TAKE_PROFIT` `+347.1532 KRW` and VTHO `STOP_LOSS` `-303.0869 KRW`; strict TRUMP remains an open/unrealized observation.

The committed AI monitoring desk was verified in the deterministic dashboard: advisory-only/no-order-routing copy, paper/live boundary, provider cards, persistent-session form, event/consultation empty states, `/api/ai/monitoring` `200`, and `/api/status` `200` in DRY_RUN. Provider status latency came from the local `codex login status` / `claude auth status` subprocesses (about 1.96s/5.56s in this environment); each is bounded by the service's 8s status timeout, and the UI eventually rendered GPT unavailable guidance plus Claude ready. No AI session or consultation was created.

At `2026-09-10T06:33:01Z`, strict VVV was still open about 13 minutes after entry; v40 remained active with 365 cycles, 0 strict closes, 5 shadow/loose closes and 3 open positions in each diagnostic book, 0 interruptions, 223 analysis freshness blocks, and 7,300 candle requests with 0 batch failures. Preserve the session until the VVV risk exit is recorded.

At `2026-09-11T09:24:32Z`, the current isolated v54 session remains active at cycle `343` with a current heartbeat and `12/12` complete analyses. Strict has one closed XRP trade at `MAX_HOLD_TIME` for `-19.9900 KRW` / `-0.1000%` and no open positions. Shadow has 11 closed trades at `-1,369.6793 KRW` with open IOST/VTHO diagnostics; loose has 12 closed trades at `-1,459.7282 KRW` with open XRP/IOST/VTHO diagnostics. Stale analysis blocks total `27`, batch ticker/candle requests are `343/4,116` with zero batch failures, risk failures/outages are `0/0`, and continuity is eligible. Keep the v54 owner and diagnostic positions isolated until risk exits; do not mix diagnostic P&L into strict profitability or enable live orders.

At `2026-09-11T09:26:29Z`, v54 remained active at cycle `349`. The shadow and loose books both recorded the same new diagnostic outcomes: VTHO `TAKE_PROFIT` `+352.4711 KRW` despite `trend_filter_failed`, and IOST `STOP_LOSS` `-348.9018 KRW` with `volume_confirmation_failed` plus `previous_high_break_failed`. Shadow now has 13 closed trades at `-1,366.1100 KRW` with VTHO open; loose has 14 closed trades at `-1,456.1590 KRW` with XRP/VTHO open. Strict remains one XRP max-hold close at `-19.9900 KRW` and zero open positions. All 12 analyses completed, stale analysis blocks are 27, batch ticker/candle failures are `349/4,188/0`, risk failures/outages are `0/0`, and continuity is eligible. Treat the mixed trend result and negative volume/high-break result as diagnostic only; no filter changes or live promotion.

At `2026-09-11T09:28:06Z`, the loose XRP diagnostic closed at `MAX_HOLD_TIME` for `-114.0119 KRW` after carrying price-rebound and previous-high-break rejection. v54 was at cycle `355` with strict unchanged (`1` close, `-19.9900 KRW`, zero open), shadow `13` closes / `-1,366.1100 KRW` with VTHO open, and loose `15` closes / `-1,570.1709 KRW` with VTHO open. Only the final VTHO diagnostic remains in each book. Keep it isolated until risk exit; the result does not justify changing filters or the live gate.

At `2026-09-11T09:30:53Z`, the final v54 VTHO diagnostic remained open in both shadow and loose at about 5.8 minutes of age. The owner was alive at cycle `361`; strict remained one closed XRP max-hold trade (`-19.9900 KRW`) with no open positions, shadow remained 13 closes / `-1,366.1100 KRW`, and loose 15 closes / `-1,570.1709 KRW`. Analysis was complete `12/12`, stale analysis blocks `28`, requests were batch ticker `361` / candles `4,332` / batch failures `0`, risk failures/outages `0/0`, and continuity was eligible. Keep v54 isolated until VTHO exits; do not promote, loosen filters, or mix diagnostic P&L into strict results.

v54 final state at `2026-09-11T09:36:35Z`: VTHO exited at `09:35:35Z` in both diagnostic books via `STOP_LOSS`, entry `0.834834` → exit `0.823176`, net `-299.0048 KRW`, with `trend_filter_failed`. The runner ended at `09:36:19Z` after `379` cycles with `stopped_cleanly`, no strict or diagnostic open positions, no interruptions, and no temporary ledger. Strict closed one XRP max-hold trade for `-19.9900 KRW`; shadow closed 14 trades (`4W/10L`) for `-1,665.1148 KRW`; loose closed 16 trades (`4W/12L`) for `-1,869.1757 KRW`. This session is preserved as non-promotional evidence; no live orders were used.

v55 latest-code session started at `2026-09-11T09:37:19.280Z` in `.paper-forward-v55` with session `paper-1789119439280`. The startup readback at `09:37:52Z` shows cycle `1`, a complete `12/12` analysis, zero stale/freshness blocks, zero risk failures/outages, zero interruptions, and no strict or diagnostic trades/positions. It uses the same 12-market freshness cohort with a 15-second loop, 300ms process request interval, and 1-second risk checks. The read-only observer was switched to v55 on port `3152`; mutation APIs remain blocked. Keep this as a fresh continuity window and do not infer profitability from startup state.

At `2026-09-11T09:39:44Z`, v55 reached cycle `5` with strict/shadow/loose still empty. All `12/12` analyses were complete, stale blocks and risk failures remained zero, requests were batch ticker `5` / candles `60` / batch failures `0`, and continuity remained eligible. This confirms the fresh session is operational only; no profitability or promotion conclusion is available yet.

At `2026-09-11T09:44:58Z`, v55 cycle `24` had its first relaxed diagnostic entry: shadow/loose opened ETH at `3,362,359 KRW` from the `09:42:00Z` signal. It had strong RSI recovery (`19.0592 → 34.4705`) and volume `2.3944x`, but rebound `0.1192%` below the strict `0.15%` threshold, so strict confirmed candidates remained `0`. The session still had complete `12/12` analysis, stale/entry blocks `0/0`, request stats `24/288/0`, risk failures/outages `0/0`, and continuity eligible. Keep this as a diagnostic near-miss; do not loosen the runtime threshold from one observation.

At `2026-09-11T09:48:05Z`, v55 was active at cycle `33`; strict still had zero confirmed/closed/open positions. Shadow and loose each held ETH/XRP/IOST diagnostic positions with zero closes. Analysis was complete `12/12`; stale analysis/entry blocks were `1/0`, batch ticker/candle requests `33/396/0`, risk failures/outages `0/0`, interruptions empty, and continuity eligible. The isolated stale observation is retained as data-quality telemetry; the 90-second freshness guard remains unchanged.

At `2026-09-11T09:51:56Z`, v55 owner PID `98483` remained active at cycle `45`; strict still had zero confirmed/closed/open positions, and shadow/loose each held ETH/XRP/IOST diagnostics with zero closes. Analysis was complete `12/12`, stale analysis/entry blocks `8/0`, request stats `45/540/0`, risk failures/outages `0/0`, interruptions empty, and continuity eligible. Observer PID `1170` on port `3152` serves this v55 ledger read-only. Keep the six diagnostic positions separate from strict profitability evidence.

At `2026-09-11T09:59:29Z`, v55's relaxed IOST candidate had closed in both diagnostic books at `STOP_LOSS`, entry `1.34134` → exit `1.31868`, net `-357.5283 KRW`. The signal had rebound `0.7519%` and volume `1.3559x`, but trend slope `-0.6472%` and previous-high-break failed; strict did not enter. Shadow and loose each have one loss and `-357.5283 KRW`, with ETH/XRP still open. Keep trend/high-break filters unchanged and do not count this diagnostic result as strict profitability evidence.

At `2026-09-11T10:00:48Z`, v55 cycle `72` remained active with strict confirmed/closed/open `0/0/0`. Shadow and loose each had one closed IOST loss (`-357.5283 KRW`) and only ETH/XRP diagnostic positions open. Analysis was complete `12/12`, stale analysis/entry blocks `18/0`, requests `72/864/0`, risk failures/outages `0/0`, interruptions empty, and continuity eligible. Keep the remaining diagnostic books separate from strict profitability.

At `2026-09-11T10:02:12Z`, v55 was active at cycle `75`; strict still had no confirmed, closed, or open positions. Shadow and loose each retained the same two open diagnostics, ETH and XRP, after one IOST loss of `-357.5283 KRW`. Analysis remained complete `12/12`, stale analysis/entry blocks `18/0`, requests `75/900/0`, risk failures/outages `0/0`, interruptions empty, and continuity eligible. Keep observing without tuning from this small cohort.

At `2026-09-11T10:05:43Z`, v55 cycle `87` added a relaxed UP2 diagnostic at `87.8878 KRW`; the signal failed close strength, trend, and previous-high-break checks, so strict remained empty. Shadow and loose each had one closed IOST loss (`-357.5283 KRW`) and ETH/XRP/UP2 open. Analysis stayed complete `12/12`, stale analysis/entry blocks `19/0`, requests `87/1,044/0`, risk failures/outages `0/0`, interruptions empty, and continuity eligible. Keep diagnostics separate and do not loosen filters.

At `2026-09-11T10:13:20Z`, the relaxed ETH candidate closed at max hold, entry `3,362,359` → exit `3,361,635 KRW`, net `-24.2972 KRW` in both shadow and loose, with rebound threshold rejection. Strict remained empty. This is direct forward evidence against treating sub-threshold rebound candidates as a profitable relaxed cohort; keep the runtime threshold unchanged.

At `2026-09-11T10:15:20Z`, the relaxed XRP candidate closed at max hold, entry `1,829.828` → exit `1,828.170 KRW`, net `-38.0988 KRW` in both shadow and loose, with rebound and volume rejection. Shadow and loose each now have three closed losses totaling `-419.9243 KRW`; UP2 and VTHO remain diagnostic-open, while strict remains empty. Keep the runtime contract unchanged.

At `2026-09-11T10:17:17Z`, v55 cycle `120` remained active with strict confirmed/closed/open `0/0/0`. Shadow and loose each had three closed losses totaling `-419.9243 KRW`; only UP2 and VTHO diagnostics remained open. UP2 failed close-strength, trend, and previous-high-break; VTHO failed trend and previous-high-break. Analysis was complete `12/12`, stale analysis/entry blocks `27/0`, requests `120/1,440/0`, risk failures/outages `0/0`, interruptions empty, and continuity eligible. Keep the diagnostic books isolated and the strict contract unchanged.

At `2026-09-11T10:18:50Z`, v55 cycle `126` remained active with strict confirmed/closed/open `0/0/0`. Shadow and loose each retained the same three closed losses (`-419.9243 KRW`) and two open diagnostics, UP2 and VTHO. Analysis was complete `12/12`, stale analysis/entry blocks `28/0`, requests `126/1,512/0`, risk failures/outages `0/0`, interruptions empty, and continuity eligible. No runtime change is justified.

At `2026-09-11T10:20:10Z`, v55's VTHO relaxed candidate had stopped out at `0.796796 → 0.784215 KRW`, net `-335.4690 KRW`, after trend and previous-high-break failures. Shadow and loose each reached four closed losses totaling `-755.3933 KRW`; UP2 and KAT remained diagnostic-open, while strict remained empty. Keep the trend/high-break contract unchanged.

At `2026-09-11T10:21:25Z`, v55 cycle `132` remained active with strict confirmed/closed/open `0/0/0`. Shadow and loose each had four closed losses totaling `-755.3933 KRW`, with UP2 and KAT open diagnostics. Analysis was complete `12/12`, stale analysis/entry blocks `29/0`, requests `132/1,584/0`, risk failures/outages `0/0`, interruptions empty, and continuity eligible. Keep runtime settings unchanged and continue risk observation.

Research-only range-cap study generated at `2026-09-11T09:42:08Z` from the cached 7-day/1-minute BTC/ETH/XRP window. Caps of `0.5%`, `0.8%`, and `1.0%` produced the exact baseline result (`11` holdout trades, summed `-0.0683%`), indicating no effective filtering. The `0.25%` rebound plus `0.8%` cap left only one positive XRP trade (`+0.0026%`) with all training gates failed, while `0.5%` rebound/cap variants had no holdout trades. Do not change runtime settings from this study.

Research-only oversold-lookback study generated at `2026-09-11T09:50:33Z` from the same cached window. `oversoldLookback=3` produced 21 holdout trades and `-0.1451%`, worse than baseline 11 trades and `-0.0683%`; adding rebound `0.25%` produced 7 trades / `-0.0639%`, and adding volume `1.5x` produced 16 trades / `-0.1082%`. Every market failed the training gate. Keep runtime lookback at 1 and retain the study as rejected diagnostic evidence.

Research-only 14-day robustness study generated at `2026-09-11T09:54:54Z` from the cached 14-day/1-minute BTC/ETH/XRP window. Baseline was 23 holdout trades / `-0.1147%`; volume `1.5x` was less negative at `-0.0584%` but failed all training gates. Rebound `0.25%` and range floor `0.4%` were positive only on 2 and 1 trades respectively, with training failures; rebound `0.5%` combinations had zero trades. Max-hold, loss-timeout, fast-exit, and protective variants all stayed negative. Keep runtime settings unchanged and treat the report as diagnostic-only.

Narrow range-cap study generated from the same cache at the current continuation time. `maxSignalRangePercent=0.2%` produced 10 holdout trades / `-0.0682%` versus baseline 23 / `-0.1147%`, but all training gates failed and no market was positive. The `0.5%` cap had no effect and the rebound/cap candidate remained only 2 trades / `+0.0039%`. Do not promote or apply the narrower cap.

Cross-window 7-day check generated at `2026-09-11T10:04:08Z`: volume `1.5x` returned 8 trades / `-0.0435%` and range cap `0.2%` 4 trades / `-0.0264%` versus baseline 11 / `-0.0683%`. Both were negative, had zero positive markets, and failed all training gates. Rebound `0.25%` had one positive XRP trade only; lookback 3 worsened to 21 trades / `-0.1451%`. Keep these as diagnostic hypotheses, not runtime changes.

Rebound threshold sweep generated at `2026-09-11T10:07:29Z` (7-day) and `10:07:37Z` (14-day). Thresholds `0.25%` and `0.30%` were identical in each window: one positive XRP trade in 7 days and two positive XRP trades in 14 days, all under-sampled. Thresholds `0.35%` and `0.40%` produced no 7-day trades and one 14-day trade; `0.50%` produced none. Training gates failed across candidates, so runtime remains `0.15%`.

At `2026-09-11T10:22:39Z`, v55 cycle `135` remained active with strict confirmed/closed/open `0/0/0`. Shadow and loose each retained four closed losses totaling `-755.3933 KRW`, with UP2 and KAT diagnostics open. Analysis was complete `12/12`, stale analysis/entry blocks `29/0`, requests `135/1,620/0`, risk failures/outages `0/0`, interruptions empty, and continuity eligible. Preserve the diagnostics through risk exit and keep the runtime contract unchanged.

At `2026-09-11T10:24:35Z`, UP2 had closed in both relaxed books via `STOP_LOSS`, entry `87.8878` → exit `86.7132 KRW`, net `-287.0231 KRW`, after close-strength, trend, and previous-high-break failures. Shadow and loose each had five closed losses totaling `-1,042.4164 KRW`; only KAT remained open, while strict stayed empty. Keep the failed filters unchanged.

At `2026-09-11T10:25:52Z`, a new relaxed IOST candidate stopped out almost immediately (`1.3013 → 1.27872 KRW`, `-366.6856 KRW`) with volume, trend, and previous-high-break failures. Shadow and loose each reached six closed losses totaling `-1,409.1021 KRW`; only KAT remained open and strict remained empty. Keep the strict contract unchanged.

At `2026-09-11T10:28:48Z`, v55 cycle `153` had KAT and a newly opened RAY diagnostic position in shadow/loose. RAY failed volume confirmation; KAT failed previous-high-break. Shadow and loose each had six closed losses totaling `-1,409.1021 KRW`; strict remained empty. Analysis was complete `12/12`, stale analysis/entry blocks `30/0`, requests `153/1,836/0`, risk failures/outages `0/0`, interruptions empty, and continuity eligible. No runtime change.

At `2026-09-11T10:30:04Z`, KAT had stopped out in both relaxed books (`6.64664 → 6.55344 KRW`, `-300.1571 KRW`) after previous-high-break failure. Shadow and loose each had seven closed losses totaling `-1,709.2591 KRW`; RAY remained the only diagnostic-open position and strict remained empty. Keep the previous-high-break filter unchanged.

The observer suggested holding out `minReboundPercent=0.10%`; cache validation generated `2026-09-11T10:31:32Z` (7-day) and `10:31:35Z` (14-day) rejected it. The relaxed floor produced `25` trades / `-0.1450%` and `45` trades / `-0.2515%`, worse than baseline in both windows, with all training gates failed. Runtime remains `0.15%`.

At `2026-09-11T10:33:25Z`, v55 cycle `169` had RAY and a new BFC diagnostic open in shadow/loose after seven closed relaxed losses totaling `-1,709.2591 KRW`. RAY failed volume confirmation and BFC failed previous-high-break; strict remained empty. Analysis was complete `12/12`, stale analysis/entry blocks `30/0`, requests `169/2,028/0`, risk failures/outages `0/0`, interruptions empty, and continuity eligible. Keep the runtime contract unchanged.

On `2026-09-12T04:32:07Z`, the v55 owner PID `98483` was gone and its last heartbeat (`2026-09-11T10:38:20Z`) was about 17.9 hours old. The raw v55 ledger retained strict BFC and BFC/RAY/XRP diagnostic positions, so it was not reused or overwritten. The latest-source read-only status classified it as orphaned/stopped, continuity-ineligible, and unsettled on both strict and diagnostic position flags. This ledger is preserved as invalid/non-promotional evidence.

The orphan status bug was hardened in `getPaperValidationStatus()`: orphan sessions now fail closed for heartbeat continuity, and saved open strict/diagnostic snapshots set the unsettled flags even when the owner disappeared before normal shutdown. The orphan regression test and full `116/116` suite pass; follow-up paper runs must use a new ledger.

At `2026-09-11T10:36:02Z`, v55 opened its first strict BFC position at `49.9 KRW` after a `3,385ms` delay, from signal candle `10:33:00Z` and reference `50 KRW`. Signal rebound was `0.8065%`, RSI recovery `15.6426`, volume `1.3423x`, close strength `1.0`, trend slope `-0.0864%`, range `1.0081%`, and execution drift `-0.2000%`. Strict had one open and zero closes; stop/take/max-hold remained `1.2%/1.8%/30m`. Preserve BFC until the risk exit; do not count current value as profit.

Cooldown study generated at `2026-09-11T10:26:54Z` (7-day) and `10:27:04Z` (14-day). A 30-minute cooldown matched the 15-minute baseline; 60 and 120 minutes reduced trades but stayed negative and failed promotion. Seven-day returns were baseline/30m `-0.0683%`, 60m `-0.0688%`, 120m `-0.0649%`; 14-day returns were `-0.1147%`, `-0.1153%`, `-0.1149%`. Keep runtime cooldown at 15 minutes.

v56 started at `2026-09-12T04:38:59.159Z` in `.paper-forward-v56` with session `paper-1789187939159`, explicitly separate from orphan v55. At `04:39:35Z`, cycle `1` had complete `12/12` analysis, zero stale blocks, zero batch ticker failures, zero risk failures/outages, zero interruptions, and no strict or diagnostic trades/positions. The latest-source read-only observer on port `3153` reports `RUNNING`, `orphaned=false`, `readOnlyObserver=true`, and continuity eligible. Keep v56 as a fresh continuity window; do not combine it with v55.

At `2026-09-12T04:40:55Z`, v56 owner PID `55016` reached cycle `4` with strict/shadow/loose still empty. All `12/12` analyses completed; stale analysis/entry blocks were `1/0`, requests were `4/48/0`, risk failures/outages `0/0`, interruptions empty, and continuity eligible. Observer PID `55373` on port `3153` served the same v56 session read-only with no diagnostic opens. This remains operational evidence only.

At `2026-09-12T04:42:34Z`, v56 owner PID `55016` reached cycle `8` with strict/shadow/loose still empty. Analysis remained complete `12/12`; stale analysis/entry blocks were `7/0`, requests `8/96/0`, risk failures/outages `0/0`, interruptions empty, and continuity eligible. Observer PID `55373` on port `3153` continued to report v56 as non-orphaned and read-only. No profitability conclusion is available from startup cycles.

At `2026-09-12T04:46:46Z`, v56 owner PID `55016` reached cycle `23` with strict/shadow/loose still empty. Analysis remained complete `12/12`; stale analysis/entry blocks were `17/0`, requests `23/276/0`, risk failures/outages `0/0`, interruptions empty, and continuity eligible. Observer PID `55373` on port `3153` confirmed active, non-orphaned, read-only v56 with no open positions. This remains operational evidence only.

At `2026-09-12T04:58:21Z`, both dashboard surfaces include a responsive orphan alert showing owner loss, same-ledger reuse prohibition, strict/diagnostic unsettled counts, and last heartbeat. Full tests remain `117/117 PASS`; inline/manifest/static checks pass. CUA browser policy prevented a new screenshot, so the UI update is supported by source/HTTP checks without a visual capture claim.

At `2026-09-12T04:58:21Z`, v56 owner PID `55016` reached cycle `59` with strict/shadow/loose still empty. Analysis remained complete `12/12`; stale analysis/entry blocks were `43/0`, requests `59/708/0`, risk failures/outages `0/0`, interruptions empty, and continuity eligible. Observer PID `55373` on port `3153` confirmed active, non-orphaned, read-only v56 with no open positions.

At `2026-09-12T04:48:20Z`, v56 owner PID `55016` reached cycle `26` with strict/shadow/loose still empty. Analysis remained complete `12/12`; stale analysis/entry blocks were `17/0`, requests `26/312/0`, risk failures/outages `0/0`, interruptions empty, and continuity eligible. Observer PID `55373` on port `3153` confirmed active, non-orphaned, read-only v56 with no open positions. Keep the early window separate from v55 orphan evidence.

The orphan UI state was added to both dashboard surfaces: when `orphaned=true`, the page now shows a responsive red alert with owner loss, strict/diagnostic unsettled counts, last heartbeat, and same-ledger reuse prohibition. Shared CSS keeps the alert readable on narrow screens. Inline parse and full `117/117` tests pass. CUA browser policy could not load a browser surface for a new screenshot, so runtime HTTP/API and static evidence are used without claiming a visual capture.

At `2026-09-12T04:56:23Z`, v56 owner PID `55016` reached cycle `51` with strict/shadow/loose still empty. Analysis remained complete `12/12`; stale analysis/entry blocks were `30/0`, requests `51/612/0`, risk failures/outages `0/0`, interruptions empty, and continuity eligible. Observer PID `55373` on port `3153` confirmed active, non-orphaned, read-only v56 with no open positions.

At `2026-09-12T04:43:55Z`, v56 owner PID `55016` reached cycle `15` with strict/shadow/loose still empty. Analysis remained complete `12/12`; stale analysis/entry blocks were `16/0`, requests `15/180/0`, risk failures/outages `0/0`, interruptions empty, and continuity eligible. Observer PID `55373` on port `3153` confirmed active non-orphaned read-only v56. Keep this fresh window separate from v55.

At `2026-09-12T05:07:41Z`, the orphan status API also began exposing `orphanReason` (`owner_process_missing` or `heartbeat_stale`), and both dashboard alerts use the corresponding human-readable cause. v56 owner PID `55016` was active at cycle `82`; strict remained empty and shadow/loose each had one RAY diagnostic open. Analysis was complete `12/12`, stale analysis/entry blocks `56/0`, requests `82/984/0`, risk failures/outages `0/0`, interruptions empty, and continuity eligible.

At `2026-09-12T05:10:14Z`, v56 owner PID `55016` reached cycle `88`; strict remained at zero confirmed/closed/open, while shadow and loose each retained one RAY diagnostic position with no closed trades. Analysis was complete `12/12`, stale analysis/entry blocks `59/0`, requests `88/1,056/0`, risk failures/outages `0/0`, interruptions empty, and continuity eligible. Observer PID `55373` on port `3153` confirmed the active non-orphan read-only session.

At `2026-09-12T05:19:12Z`, v56 owner PID `55016` reached cycle `112`; strict remained at zero confirmed/closed/open. Shadow and loose each retained RAY plus a new STORJ diagnostic position, with no closed trades. STORJ failed close-strength, trend, and previous-high-break. Analysis was complete `12/12`, stale analysis/entry blocks `73/0`, requests `112/1,344/0`, risk failures/outages `0/0`, interruptions empty, and continuity eligible. Observer PID `55373` confirmed the active non-orphan read-only session.

At `2026-09-12T05:25:28Z`, v56 owner PID `55016` reached cycle `129`; strict remained at zero confirmed/closed/open. Shadow and loose each retained RAY and STORJ diagnostics with no closed trades. Analysis was complete `12/12`, stale analysis/entry blocks `74/0`, requests `129/1,548/0`, risk failures/outages `0/0`, interruptions empty, and continuity eligible. Observer PID `55373` confirmed the active non-orphan read-only session.

At `2026-09-12T05:21:49Z`, v56 owner PID `55016` reached cycle `121`; strict remained at zero confirmed/closed/open. Shadow and loose each retained RAY and STORJ diagnostics with no closed trades. Analysis was complete `12/12`, stale analysis/entry blocks `74/0`, requests `121/1,452/0`, risk failures/outages `0/0`, interruptions empty, and continuity eligible. Observer PID `55373` confirmed the active non-orphan read-only session; keep this under-sampled window unchanged.

At `2026-09-12T05:16:08Z`, v56 owner PID `55016` reached cycle `106`; strict remained at zero confirmed/closed/open, while shadow and loose each retained one RAY diagnostic position with no closed trades. Analysis was complete `12/12`, stale analysis/entry blocks `73/0`, requests `106/1,272/0`, risk failures/outages `0/0`, interruptions empty, and continuity eligible. Observer PID `55373` on port `3153` confirmed the active non-orphan read-only session.

At `2026-09-12T05:14:35Z`, v56 owner PID `55016` reached cycle `100`; strict remained at zero confirmed/closed/open, while shadow and loose each retained one RAY diagnostic position with no closed trades. Analysis was complete `12/12`, stale analysis/entry blocks `68/0`, requests `100/1,200/0`, risk failures/outages `0/0`, interruptions empty, and continuity eligible. Observer PID `55373` on port `3153` confirmed the active non-orphan read-only session.

At `2026-09-12T05:02:11Z`, v56 owner PID `55016` reached cycle `69` with strict/shadow/loose still empty. Analysis remained complete `12/12`; stale analysis/entry blocks were `49/0`, requests `69/828/0`, risk failures/outages `0/0`, interruptions empty, and continuity eligible. Observer PID `55373` on port `3153` confirmed active non-orphaned read-only v56 with no open positions. This remains operational evidence only.

v56 final state at `2026-09-12T05:30:31Z`: the runner ended at `05:29:10Z` with `risk_data_gap` after 9 consecutive `ENOTFOUND api.upbit.com` failures and a 31.47-second outage. Strict remained empty; STORJ diagnostic closed `TAKE_PROFIT` at `+323.1148 KRW`, while RAY remained open in shadow/loose. The ledger records one incomplete all-market cycle, risk continuity false, and diagnostic positions at stop; preserve it as invalid/non-promotional and do not resume it. A public API probe at `05:31:46Z` recovered with HTTP 200.

v57 started at `2026-09-12T05:32:58.783Z` in `.paper-forward-v57` with session `paper-1789191178783`, explicitly separate from v56. Its first read at `05:33:34Z` completed `12/12` analysis with stale/risk/request failures `0/0/0`, no trades or positions, and a complete config snapshot. Its read-only observer was started on port `3153` and confirmed v57 non-orphan, read-only, and continuity eligible.

At `2026-09-12T05:37:15Z`, v57 node PID `3898` reached cycle `7` with strict/shadow/loose still empty. Analysis was complete `12/12`, stale analysis/entry blocks `0/0`, requests `7/84/0`, risk failures/outages `0/0`, interruptions empty, and continuity eligible. Observer PID `5985` on port `3153` confirmed active, non-orphaned, read-only v57 with no open positions. Keep v56 risk-gap evidence separate.

v57 final state at `2026-09-12T05:42:25Z`: the runner ended at `05:41:45Z` with `risk_data_gap` after three consecutive `ENOTFOUND api.upbit.com` failures and a 31.62-second outage. Strict remained empty; SC relaxed diagnostic remained open in shadow/loose. The ledger records one incomplete all-market cycle, risk continuity false, and diagnostic positions at stop; preserve it as invalid/non-promotional and do not resume it.

After three read-only DNS/API probes succeeded with HTTP 200 at `05:43:07Z`, v58 started at `2026-09-12T05:45:18.633Z` in `.paper-forward-v58` with session `paper-1789191918633`, separate from v57. Its first read at `05:46:00Z` completed `12/12` analysis with no stale blocks, batch failures, risk failures/outages, interruptions, or trades/positions. The read-only observer was switched to v58 on port `3153` and confirmed active, non-orphan, read-only, and continuity eligible.

At `2026-09-12T05:57:37Z`, v58 owner PID `26309` reached cycle `19`; strict/shadow/loose remained empty. One earlier incomplete cycle recovered to complete `12/12` analysis (`totalIncompleteCycles=1`, `totalMissingMarkets=1`, max gap `112.55s`); current stale analysis/entry blocks were `7/0`, requests `19/227/0` with one batch failure and 12 individual ticker fallbacks, risk failures/outages `0/0`, interruptions empty, and continuity eligible. Observer PID `88199` on port `3153` confirmed active, non-orphan, read-only v58 with no open positions. Keep this operational event separate from v56/v57 risk-gap evidence.

At `2026-09-12T05:55:22Z`, v58 owner PID `26309` reached cycle `13`; strict/shadow/loose remained empty. One earlier incomplete cycle recovered to complete `12/12` analysis (`totalIncompleteCycles=1`, `totalMissingMarkets=1`, maximum gap `112.55s`); current stale analysis/entry blocks were `5/0`, requests `13/155` with one batch failure and 12 individual ticker fallbacks, risk failures/outages `0/0`, and continuity eligible. Observer PID `88199` on port `3153` confirmed active non-orphan read-only v58. Keep this operational data-quality event separate from profitability evidence.
v59 started at `2026-09-12T06:42:49.925Z` in `.paper-forward-v59` with session `paper-1789195369925`, after v56/v57 risk-gap and v58 incomplete-cycle evidence were preserved separately. At the `06:46:37Z` readback, owner PID `28063` and the read-only observer PID `29966` on port `3153` were active; the ledger was `RUNNING`, `orphaned=false`, `configConsistent=true`, and `configSnapshotComplete=true`. It had completed `13` cycles with `12/12` analyses, zero incomplete cycles/missing markets, risk failures/outages `0/0`, interruptions `0`, and no strict/shadow/loose trades or positions. Assets remained `1,000,000 KRW`; strict confirmed/closed/open was `0/0/0`; buy candidates were `0`. Six analysis snapshots were fail-closed for stale 1-minute candles (`maxAge=90s`, no entry-confirmation stale blocks), with per-market freshness observed and no insufficient-candle data. The observer APIs returned HTTP 200 for status, paper validation, and scalping validation; the PWA manifest and service worker also returned HTTP 200. This is early operational/data-quality evidence only, not profitability evidence; the historical validation report remains `promoted=false` and live orders remain prohibited.

After the shared Upbit request-slot hardening, the latest full test suite is `123/123 PASS`; all source JavaScript checks, four inline HTML scripts, manifest parsing, and `git diff --check` pass. The observed EPIPE/synthetic-timeout messages are intentional test fixtures with successful retry/fail-closed assertions, not suite failures.
Latest v59 readback at `2026-09-12T06:49:08Z`: owner PID `28063` and observer PID `29966` remain alive for the same session `paper-1789195369925`; the ledger has `21` cycles, `12/12` complete analyses, incomplete/missing `0/0`, risk failures/outages `0/0`, and continuity eligible. Strict/shadow/loose remain at zero trades and positions, assets remain `1,000,000 KRW`, and the observer reports `RUNNING`, `processAlive=true`, `orphaned=false`, `configConsistent=true`, and `eligible=false` only because the 7-day/20-trade promotion thresholds are not met.
Added statistical screening to the read-only validation lane. `calculateTradeReturnConfidence()` records a trade-level 95% one-sided t lower bound while preserving `null` for zero/one-trade samples; fixed validation requires training/holdout confidence metadata and the live gate rejects reports without it. The new latest fixed report was generated at `2026-09-12T07:03:16.376Z` for BTC/ETH/XRP: holdout `4/-0.0013%/PF 0.94`, `3/-0.0213%/PF 0`, and `7/-0.0249%/PF 0.34`, with lower bounds `-0.8161%`, `-0.6942%`, and `-0.6006%`; all `0/3` and `promoted=false`. It replaced the ignored dashboard report after the old file was backed up under `/tmp`.

v59 latest meaningful observation at `2026-09-12T07:03:45Z`: owner PID `28063` remained active at cycle `65`; strict opened `KRW-KAT` at `6.72 KRW` after `3,445ms` confirmation, with signal rebound `0.2985%`, RSI `32.8339 → 41.9083`, volume `2.0601x`, close strength `1.0`, trend slope `-0.0642%`, high-break true, and execution drift `0%`. Strict has no closed trade yet and one open position. Shadow/loose each separately recorded VTHO `STOP_LOSS` `-398.8965 KRW` carrying close-strength/trend/high-break rejection, while KAT and UP2 remain diagnostic opens. Analysis stayed complete `12/12`, incomplete/missing `0/0`, risk failure/outage `0/0`, and continuity eligible. This is an open strict observation plus diagnostic loss evidence; do not call it profitability or tune from one outcome.
After the confidence/UI changes, the full suite is `130/130 PASS`; all `src` JavaScript syntax checks, `public/pilot-redesign.js`, four inline HTML scripts, manifest parsing, and `git diff --check` pass. The current dashboard report is the `2026-09-12T07:03:16.376Z` fixed report with `statisticalConfidence.required=true`, `passed=false`, and `promoted=false`; no live order path is authorized.
PWA install fallback was hardened: non-standalone browsers now expose an install/action affordance even when `beforeinstallprompt` is unavailable; iOS Safari receives the manual `공유 → 홈 화면에 추가` instruction, Android/desktop receives the matching browser-menu instruction, and standalone metadata includes `apple-mobile-web-app-title`. This supports the web/mobile/install-app surface without changing API or trading semantics. After the change, four inline HTML scripts and the redesigned JS parse successfully and the full suite is `131/131 PASS`.

At `2026-09-12T07:07:55Z`, v59 owner PID `28063` was still active at cycle `77`; strict `KRW-KAT` remained open with zero closed trades, shadow/loose each had one diagnostic close (VTHO `STOP_LOSS`, `-398.8965 KRW`) and KAT/UP2 diagnostic positions open, analysis `12/12`, incomplete/missing `0/0`, risk `0/0`, and continuity eligible. Continue to keep strict and diagnostic P&L separate; KAT remains an unrealized observation.
Same-window A/B research at `2026-09-12T07:14:28Z` used fresh 7-day/1-minute BTC/ETH/XRP candles and wrote `/tmp/coinpilot-current-variant-20260912.json` without changing runtime. Baseline was `13` holdout trades / `-0.0386%`; volume `1.5x` was relatively less negative at `11` / `-0.0187%` but all training gates failed; next-candle follow-through `5` / `-0.0311%`, micro exit `13` / `-0.0346%`, and loss-only timeout `13` / `-0.0647%`. Rebound `0.25%` was `3` trades / `+0.0106%` across two positive markets but failed training and was rejected as under-sampled. No runtime parameter changed.

At `2026-09-12T07:14:09Z`, v59 owner PID `28063` remained active at cycle `95`; strict `KRW-KAT` was still open, highest observed price `6.78` from entry `6.72`, with strict closed/profit `0/0`. Shadow had one VTHO diagnostic stop-loss close for `-398.8965 KRW` and KAT/UP2 open; analysis/risk continuity remained healthy (`12/12`, incomplete/missing `0/0`, risk `0/0`). Preserve KAT until its independent risk exit.

v67 was cleanly stopped at `2026-09-13T00:14:41Z` after confirming no strict or diagnostic positions remained. Its final persisted read at `00:14:54.366Z` recorded cycle `131`, strict `0` trades/profit, shadow/loose each `4` relaxed closed losses totaling `-1,167.9505 KRW`, no interruptions, and a healthy analysis/risk path across all `12/12` markets. The TREE signal produced one strict BUY candidate but was cancelled during delayed revalidation (`반등 되밀림 0.84% > 허용 0.25%`); the legacy winner-shadow block remained `pending` despite no strict fill.

That read exposed an execution-lifecycle gap in the A/B telemetry: `winnerShadow.blockedEntries` was only resolved on a later strict close, so a cancellation with no fill could remain pending indefinitely. The latest source adds `resolveWinnerShadowBlockedEntryAsNotFilled()` to every delayed-entry cancellation path, records `not_filled` plus the cancellation reason/time, persists the update, and keeps it out of counterfactual profit/PF. Paper status and the PWA UI now separate blocked, pending, not-filled, and settled counts. The regression first failed with `pending` and then passed after the fix; full validation is `npm test 152/152 PASS`, with JS/HTML/manifest/service-worker/static diff checks clean. v67 itself is preserved as pre-fix evidence; no live order, user portfolio mutation, or promotion occurred.

v68 started at `2026-09-13T00:20:37.769Z` in `.paper-forward-v68-notfilled` with the latest lifecycle fix, the same 12-market freshness cohort, strict `maxReboundPercent=0`, and winner-shadow entry ceiling `0.4%`. The first persisted cycle at `00:20:39.258Z` completed analysis for `12/12` markets with no incomplete/missing data, no risk failure, no interruption, no strict or diagnostic positions, and a complete config snapshot. Its root paper lock is owned by the v68 process; it is the only new runner and remains DRY_RUN-only. The next meaningful check is a real delayed-confirmation cancellation, which should persist the matching blocked entry as `not_filled` rather than `pending`.

v68 subsequently lost its owner before its asynchronous shutdown completed. Because the process was absent, the ledger was not called clean: a ledger-only cleanup at `2026-09-13T00:28:55.767Z` recorded `stopReason=owner_process_missing`, cycle `61`, strict/shadow/loose zero trades and zero open positions, and preserved the original evidence. The stale root lock was left for the next paper owner to reclaim atomically; no user portfolio data was touched.

v69 started at `2026-09-13T00:29:20.851Z` in `.paper-forward-v69-notfilled-latest` with the latest source including all pre-order `not_filled` resolution call sites. It uses the same 12-market cohort, strict `maxReboundPercent=0`, and winner-shadow ceiling `0.4%`. The first persisted cycle completed `12/12` analyses with zero incomplete/missing data, zero risk failures, zero interruptions, empty strict/winner/shadow/loose books, and a complete config snapshot. Read-only observer port `3153` now reports the v69 session HTTP 200, process alive, and read-only; the session remains DRY_RUN-only.

At `2026-09-13T00:32:11.471Z`, v69 reached cycle `21` with `12/12` complete analyses, zero incomplete/missing data, zero risk failures/outages, zero interruptions, and strict/winner/shadow/loose still empty. Signal availability was oversold/rebound/confirmed BUY `0/0/0` over `36` unique windows. This is a quiet-market observation, not evidence that thresholds should be relaxed; keep the latest source and owner unchanged.

At `2026-09-13T00:36:13.132Z`, v69 owner PID `40512` and the root paper lock still matched; the ledger reached cycle `61` with `12/12` analyses complete, no incomplete/missing data, no risk failures/outages, no interruptions, and continuity eligible. Strict, winner-shadow, shadow, and loose books remained at zero trades and zero open positions. Availability remained oversold/rebound/confirmed BUY `0/0/0` across `84` unique windows. Preserve this as clean market-quiet evidence and do not force a signal or loosen the runtime contract.

At `2026-09-13T00:38:58.622Z`, v69 owner PID `40512` and lock still matched at cycle `81`. All `12/12` analyses completed, incomplete/missing and risk failure/outage counts remained `0/0` and `0/0`, continuity stayed true, and strict/winner/shadow/loose remained at zero trades and open positions. Signal availability was still oversold/rebound/confirmed BUY `0/0/0` over `108` unique windows. Treat this as an additional clean no-signal denominator; no threshold change or session replacement is justified.

At `2026-09-13T00:40:08.986Z`, v69 owner PID `40512` and the root lock still matched at cycle `101`. The ledger continued to receive complete `12/12` analyses with zero incomplete/missing data, zero risk failures/outages, zero interruptions, and continuity eligible. Strict, winner-shadow, shadow, and loose books remained empty; oversold/rebound/confirmed BUY stayed `0/0/0` across `141` unique windows. Keep the owner and thresholds unchanged while waiting for an actual signal.

At `2026-09-13T00:41:13.801Z`, v69 owner PID `40512` and the lock remained matched at cycle `111`. Analysis stayed complete for `12/12` markets, with incomplete/missing and risk failure/outage counts `0/0` and `0/0`, no interruptions, and continuity eligible. Strict, winner-shadow, shadow, and loose remained at zero trades and open positions; oversold/rebound/confirmed BUY remained `0/0/0` over `156` unique windows. This extends the clean market-quiet denominator without justifying forced entries or threshold relaxation.

The observer UI exposed a real synchronization boundary at approximately `2026-09-13T00:43Z`: the API already returned the active v69 session, but a long-lived Chrome tab still showed the prior v67 start time and TREE cancellation. Navigating with a cache-bust query refreshed the page to v69, so the mismatch was in browser refresh/cache lifecycle, not the paper ledger producer.

The redesign now refreshes paper/core state immediately on `visibilitychange` when a background tab returns to the foreground. The PWA shell also bumps the redesign asset query to `observer-readonly-5` and the service-worker cache to `coinpilot-shell-v9`, preventing an old cache-first shell from reusing the previous JS asset. `test/pilotRedesignRefresh.test.js` locks both contracts; each new test was observed failing before its corresponding fix and passing afterward. Latest source validation is `npm test 154/154 PASS`, all changed JS/inline HTML/manifest/service-worker checks pass, and `git diff --check` passes.

After reloading the latest shell at `localhost:3153/?v=16&cache=observer-readonly-5`, Chrome visibly showed v69 start `09:29`, read-only observer lock, validation `0/3`, and `winner shadow: ... 차단 0회 · 미체결 0회 · 대기 0회 · 정산 0회`. Foreground polling also advanced the displayed unique-window count from `228` to `240`, confirming the live API refresh path. No mutation controls or install action were used.

At `2026-09-13T00:50:02.854Z`, v69 owner PID `40512` and lock still matched at cycle `191`; strict/winner-shadow/shadow/loose remained at zero trades and open positions, oversold/rebound/confirmed BUY was `17/0/0`, unique windows `252`, analysis `12/12`, risk failures/outages `0/0`, and continuity remained true. This is still a no-confirmation forward sample; keep thresholds and the owner unchanged.
Freshness cohort read at `2026-09-12T07:19:44.667Z` from v59 (heartbeat `07:19:27.909Z`, cycle `110`) selected `KRW-BFC,KRW-VTHO,KRW-XRP,KRW-BTC,KRW-ETH,KRW-RAY,KRW-STORJ,KRW-SC` using at least 100 observations and max 5% analysis candle-block rate. It excluded UP2 `7/110=6.36%`, IOST `8/110=7.27%`, TFUEL `9/110=8.18%`, and KAT `8/111=7.21%`. This is a next-session research cohort only; KAT currently has an open strict position, so v59 was not stopped, reset, or automatically excluded.
Freshness-derived fixed validation at `2026-09-12T07:23:12.375Z` evaluated the eight-market candidate cohort in `/tmp/coinpilot-fresh-cohort-fixed-20260912.json`; all `0/8` were promoted. BFC was rate-limited and only had 2,938 candles (train/holdout one trade each), VTHO and SC had zero holdout trades, BTC had `+0.0077%` over 3 trades but a 95% lower bound of `-1.1426%`, XRP/RAY/STORJ remained negative or failed PF/trade gates. The extra read-only validation did not damage v59: at `07:23:35Z` v59 was active at cycle `119`, strict KAT open/closed `1/0`, analysis `12/12`, incomplete/missing `0/0`, risk `0/0`, continuity true.
v59 first strict settlement at `2026-09-12T07:28:34.171Z`: `KRW-KAT` exited by `스캘핑 최대 보유시간 초과 (30분)`, entry `6.72` → exit `6.75 KRW`, gross `+89.2411 KRW`, total fee `20.0346 KRW`, realized `profit=+69.2065 KRW` / `+0.3462%`. The owner remains active; at `07:31:19Z` cycle `144`, strict closed/open was `1/0`, observer realized profit was `+69.2065 KRW`, and the 95% confidence calculation correctly returned sample `1` with lower bound `null`, so the 20-trade gate remains false. The raw legacy ledger entry still has `type=BUY, action=CLOSE`; new code accepts action-based close and normalizes future saves without rewriting an active ledger.
Forward paper status now exposes `strictTradeConfidence` and `strictConfidenceGate` directly to the read-only API/UI. At v59 heartbeat `2026-09-12T07:31:19.138Z` / cycle `144`, the first strict KAT close is `+69.2065 KRW` (`+0.3462%`) with confidence sample `1`, lower bound `null`, and required-20 gate false. The latest-source observer on port `3153` was refreshed without touching the owner and returns the same confidence fields; manifest display is `standalone` and the service worker is served successfully.
At `2026-09-12T07:38:07Z`, the latest-source observer on port `3153` read v59 consistently: strict closed `1`, realized `+69.2065 KRW`, strict open `0`, confidence sample `1`, mean `+0.3462%`, lower bound `null`, and required-20 gate `false`; v59 owner PID `28063` remained active at cycle `164`. Shadow/loose each had three diagnostic closes totaling `-380.4128 KRW`: VTHO `-398.8965` stop-loss, KAT `+29.1031` max-hold, and UP2 `-10.6195` max-hold with volume/trend/high-break rejection. Analysis/risk continuity remained healthy. The observer refresh touched only the read-only process; no owner or active ledger rewrite occurred.
On `2026-09-12T18:20:26Z`, the active latest-code v60 session `paper-1789236633482` remained owned by PID `14080` and matched `.paper-session.lock`. Its read-only observer on port `3153` returned `RUNNING`, `processAlive=true`, `orphaned=false`, and `eligible=false`; cycle `33` had complete `12/12` analysis, incomplete/missing `0/0`, risk failures/outages `0/0`, strict/shadow/loose trades and positions `0`, and assets `1,000,000 KRW`. Analysis freshness had `115` fail-closed stale snapshots/analyses and no entry-confirmation stale blocks; buy/confirmed candidates were `0/0`. The high stale ratio is preserved as data-quality evidence and is not a reason to loosen the 90-second cap blindly. The v60 owner and lock are from a concurrent latest-code paper task; no second runner was started.

The current dirty worktree includes the concurrent winner-hold/concurrency changes plus the local strict-ledger/confidence changes; it was not reset, cleaned, committed, or pushed. After the test fixture was updated for winner-hold comparison keys, `npm test` passed `143/143` and source/HTML/manifest/diff checks passed while v60 remained alive.
Latest v60 readback at `2026-09-12T18:22:48Z`: owner PID `14080` remains alive for session `paper-1789236633482` and still matches `.paper-session.lock`; the read-only observer on port `3153` reports `RUNNING`, `processAlive=true`, `orphaned=false`, and `eligible=false`. Cycle `45` has complete `12/12` analysis, incomplete/missing `0/0`, risk failures/outages `0/0`, continuity true, strict/shadow/loose trades and positions `0`, buy/confirmed `0/0`, and `179` analysis-only stale blocks with `0` entry-confirmation blocks. Preserve this operational/data-quality sample and do not loosen freshness or start another owner while the lock is held.
Latest v60 readback at `2026-09-12T18:24:02Z`: owner PID `14080` and `.paper-session.lock` still match for session `paper-1789236633482`; cycle `49` had complete `12/12` analysis, incomplete/missing `0/0`, risk failure/outage `0/0`, continuity true, strict/shadow/loose trades and positions `0`, and assets `1,000,000 KRW`. Analysis-only stale blocks were `192/192`, entry-confirmation stale blocks `0`, and buy/confirmed candidates `0/0`. The read-only observer on port `3153` reports `RUNNING`, non-orphaned, and ineligible due to no promotion sample. Preserve this data-quality observation and do not loosen freshness or start another paper owner while the lock is held.

The latest dirty-worktree validation after aligning the winner-hold test fixture is `npm test 143/143 PASS`; source JavaScript, redesigned dashboard, inline HTML, manifest, and diff checks pass. No commit, reset, cleanup, or push was performed.
Latest v60 verification at `2026-09-12T18:25:15Z`: the owner PID `14080` and lock still match for session `paper-1789236633482`; cycle `53` completed `12/12` analysis with incomplete/missing `0/0`, risk failures/outages `0/0`, continuity true, strict/shadow/loose trades and positions `0`, and assets `1,000,000 KRW`. Analysis-only stale blocks were `212/212`, with entry-confirmation stale blocks `0` and buy/confirmed candidates `0/0`. The read-only observer on port `3153` remained `RUNNING`, non-orphaned, and ineligible due to insufficient sample. Keep v60 and the lock isolated; do not loosen freshness based on this alone.

At `2026-09-12T18:30:25Z`, v60 owner PID `14080` and `.paper-session.lock` still match for session `paper-1789236633482`; cycle `73` completed all `12/12` analyses with incomplete/missing `0/0`, risk failures/outages `0/0`, and continuity eligible. Strict recorded one confirmed/open `KRW-RAY` position (entry `2,046 KRW` at `18:30:08Z`, signal rebound `0.5900%`, RSI `17.84 → 39.13`, volume `6.03x`, high-break true) with zero closed trades; shadow/loose remained diagnostic-only with no closes. Analysis-only stale blocks reached `302/302`, while entry-confirmation stale blocks stayed `0`; no live order was enabled. The latest read-only observer returned HTTP `200`, `processAlive=true`, `orphaned=false`, but `configConsistent=false` solely because the pre-existing v60 snapshot predates the disabled-by-default `winnerExtendMinutes` and `winnerExtendMinProfitPercent` keys. This is preserved as conservative source/schema drift evidence; do not rewrite the ledger or auto-resume it under a newer source snapshot.

This continuation separated paper configuration diagnostics without weakening safety: `comparePaperValidationConfig()` now exposes `configSchemaDrift`, `configValueDrift`, and backward-compatible missing keys while keeping `configConsistent=false` and resume/live-promotion fail-closed. The redesigned dashboard and legacy surface explain schema-only differences as “actual value change not confirmed,” while real value mismatches remain red warnings. README and CLAUDE document the contract. A latest-source read-only observer was restarted on port `3153` without touching v60; API readback returns schema drift `[winnerExtendMinProfitPercent,winnerExtendMinutes]`, value drift `[]`, same session, `processAlive=true`, `orphaned=false`.

At `2026-09-12T18:43:54Z`, v60 owner/lock still match, cycle `121`, heartbeat current, strict `KRW-RAY` remains open (entry `2,046 KRW`, highest observed `2,057 KRW`), strict closed `0`, and no live order. Analysis remains complete `12/12` with incomplete/missing `0/0`, risk failures/outages `0/0`; analysis-only stale blocks `452`, entry stale `0`. The new regression coverage passes `npm test 144/144`. Source JS, redesigned JS, all 4 inline HTML scripts, standalone manifest, and `git diff --check` also pass.

UI verification used the latest-source observer: desktop AX/screenshot shows the schema-only warning in 검증 기록, and a `390px` CSS mobile viewport renders the same warning, strict confidence note, and fixed bottom navigation with `document.scrollWidth=380` versus `innerWidth=390` (no horizontal overflow). The temporary observer/tab and viewport were cleaned up; the persistent `3153` observer and v60 owner remain. No user portfolio, v60 ledger, or live trading state was changed.

Read-only fixed validation of the exact v60 12-market cohort completed at `2026-09-12T18:50:48.449Z` and was written only to `/tmp/coinpilot-v60-fixed-20260913.json`. With the current runtime contract (RSI 35, rebound 0.15%, volume 1.0x, close strength 0.65, trend slope -0.2%, previous-high break required, stop/take 1.2%/1.8%, 30-minute max hold), holdout totals were `34` trades, `-0.2318%` / `-2,317.74 KRW`, with only `3/12` markets positive. KAT was `-0.0820%` over 7 trades (PF 0.03), RAY `-0.0403%` over 9 trades (PF 0.65), IOST `-0.0559%` over 2 trades (PF 0), and all 12 markets failed training confidence; BFC/BTC/UP2 positive holdouts were only 1/3/3 trades and remained unpromotable. This is stronger evidence against an unqualified filter relaxation; no runtime parameter changed.

At `2026-09-12T18:52:07Z`, v60 owner/lock still match, cycle `149`, heartbeat current, strict `KRW-RAY` remains open (entry `2,046 KRW`, highest observed `2,073 KRW`), strict closed `0`, shadow/loose each have two diagnostic opens and no closes, analysis `12/12`, incomplete/missing `0/0`, risk `0/0`, continuity true, stale analysis blocks `523`, entry stale `0`. Keep the open position and source cohort isolated until its risk exit; do not infer a profitable strategy from the temporary +1.3% unrealized move.

The same-window trend-strength study completed at `2026-09-12T18:56:55.407Z` using the exact 12-market, 7-day/1-minute cohort and wrote only `/tmp/coinpilot-v60-trend-study-20260913.json`. Baseline reproduced `34` holdout trades, `3/12` positive, summed `-0.2269%`, training failures `12/12`. `minTrendSlopePercent=0` reduced the result to one positive UP2 trade (`+0.0319%` aggregate) with all 12 training gates failed; `0.1` and `0.2` produced zero holdout trades. Stronger trend gating is therefore not a valid improvement and remains research-only; runtime remains `-0.2%`.

After the study, v60 was still active at `2026-09-12T18:56:53Z`, cycle `165`, owner/lock matched, strict RAY open (`2,046` entry / `2,073` high), strict closed `0`, shadow/loose two open/no closes, analysis `12/12`, incomplete/missing `0/0`, risk `0/0`, continuity true, and stale analysis blocks `585` with entry stale `0`. The study process was read-only and did not affect v60 or user state.

At `2026-09-12T19:00:08.889Z`, v60 strict `KRW-RAY` settled by the fixed `30-minute max-hold` exit: entry `2,046` → exit `2,063 KRW`, gross profit `166.0948`, total fees `20.0730`, realized `+146.0218 KRW` / `+0.7305%`. The same signal in shadow/loose closed at `+115.5967 KRW` / `+0.5780%` after entry-price drift; both books remain diagnostic and had two closed trades totaling `-233.3052 KRW` with one open diagnostic position. v60 remains active at `19:04:42Z`, cycle `193`, strict open `0`, strict closed `1`, confidence sample `1/20` with no lower bound, analysis `12/12`, incomplete/missing `0/0`, risk `0/0`, continuity true, stale analysis blocks `672`, entry stale `0`. This is one positive realized sample, not strategy proof.

The paper status API now exposes `strictRecentTrades` and `strictEvaluation.recentTrades` with bounded, non-sensitive exit summaries. The latest-source observer/API and desktop 검증 기록 UI both show `KRW-RAY · 스캘핑 최대 보유시간 초과 · +146원 (+0.73%)`; strict confidence remains HOLD. No v60 ledger rewrite, user portfolio change, new runner, or live order occurred.

At `2026-09-12T19:05:49Z`, v60 remains active under owner/lock PID `14080` at cycle `197`; strict has no open position after the RAY close, one closed trade totaling `+146.0218 KRW`, and confidence sample `1/20` with no lower bound. Shadow/loose each retain one diagnostic open and two closes totaling `-233.3052 KRW`; analysis `12/12`, incomplete/missing `0/0`, risk `0/0`, continuity true, stale analysis `685`, entry stale `0`. The latest-source observer on `3153` returns the same strict recent trade and keeps `eligible=false`. Continue the active session; do not start a new owner while the lock is held.

Recent-trade observability verification completed: `strictRecentTrades` is bounded to five summaries, survives ledger reload in tests, and the desktop Forward paper card renders market, exit reason, net profit, and return. The implementation does not alter entry/exit decisions or promotion gates.

Post-exit read-only RAY candle check reached `2026-09-12T19:10:00Z`: after the actual `19:00:08Z` max-hold exit, price did not fall below the cost-adjusted break-even floor `2,053.17 KRW` and reached the fixed `1.8%` take target `2,082.828 KRW` (observed high `2,087`). Applying the backtest's `0.1%` adverse exit slippage estimates a winner-hold counterfactual of `+319.31 KRW / +1.5965%` versus actual `+146.02 KRW / +0.7305%`. This single case raises research priority for winner-hold but does not override the existing 14-day training-gate failure; the runtime flag remains disabled.

Winner-hold duration/threshold research used the same 14-day 12-market raw window saved at `/tmp/coinpilot-v60-winner-candles-14d-20260913.json`. The threshold sweep at `19:23:46Z` found 60-minute extension with no minimum profit was the least negative (`-0.3165%`), while 0.05/0.10/0.20/0.30% minimum profit thresholds were worse (`-0.3254%/-0.3291%/-0.3351%/-0.3352%`), all with 54 trades and 12/12 training failures. A duration sweep on the same cache found 15m `-0.3211%`, 30m `-0.3164%`, and 60m `-0.3165%`, all 54 trades and all training-failed. The relative improvement is consistent with the RAY post-exit case but absolute return remains negative; `winnerExtendMinutes=30` is only the next forward-shadow research priority, not a runtime change.

At the latest read `2026-09-12T19:12:46Z`, v60 owner/lock PID `14080` still match, cycle `222`, strict RAY closed1/+146.0218 with no strict open, shadow/loose each one diagnostic open and three closes totaling `-262.5903`, analysis `12/12`, incomplete/missing `0/0`, risk `0/0`, continuity true, stale analysis `773`, entry stale `0`. The research processes were read-only and did not alter v60.

The narrow `strong_rebound_no_highbreak` diagnostic was evaluated on the same 14-day 12-market cache at `2026-09-13`: requiring rebound `>=0.5%`, non-negative trend, and allowing no previous-high break produced only 2 holdout trades and `-0.0559%`; adding volume `>=1.5x` produced 1 trade and `-0.0279%`; adding RSI recovery `>=5` again produced 2 trades and `-0.0559%`. All 12 market training gates failed and no trade was positive. The small absolute loss is signal starvation, not evidence that high-break should be bypassed. Keep the default high-break contract unchanged.

At `2026-09-12T19:36:15.798Z`, v60 owner PID `14080` and `.paper-session.lock` still match, session `paper-1789236633482` active at cycle `301`; strict RAY remains the only strict close (`+146.0218 KRW`) with no open strict position. Shadow/loose each have four diagnostic closes totaling `+120.6076 KRW` and no open diagnostic positions. Analysis is complete `12/12`, incomplete/missing `0/0`, risk `0/0`, continuity true, stale analysis blocks `1,015`, entry stale `0`. Observer `3153` remains same-session/read-only/process-alive and ineligible due insufficient sample/config schema drift. Latest test after the research variants is `144/144 PASS`; no v60/user/live state changed.

At `2026-09-12T19:40:39.535Z`, v60 remains active under the same owner/lock at cycle `317`; strict still has only RAY close `+146.0218 KRW` and no open position. Shadow/loose each remain at four diagnostic closes totaling `+120.6076 KRW`, open `0`. Analysis `12/12`, incomplete/missing `0/0`, risk `0/0`, continuity true, stale analysis `1,080`, entry stale `0`. No new strict sample has appeared; preserve this baseline window while it continues.

At `2026-09-12T19:39:33.615Z`, v60 owner/lock still match, cycle `313`, session active, strict RAY remains the sole closed trade (`+146.0218 KRW`) with strict open `0`. Shadow/loose each have four diagnostic closes totaling `+120.6076 KRW`, open `0`; analysis `12/12`, incomplete/missing `0/0`, risk `0/0`, continuity true, stale analysis `1,061`, entry stale `0`. No new strict sample was generated after the RAY settlement. Keep the baseline session and research candidates separate; no runtime or live change.

At `2026-09-12T19:46:09.259Z`, direct PID `14080` verification confirms v60 remains alive and its lock matches. Cycle `337` has strict RAY closed `1`/`+146.0218 KRW`/open `0`, shadow/loose each 4 diagnostic closes `+120.6076 KRW`/open `0`, analysis `12/12`, incomplete/missing `0/0`, risk `0/0`, continuity true, stale analysis `1,167`, entry stale `0`. No new strict sample or terminal state occurred; keep the baseline session active.

At `2026-09-12T19:48:22.835Z`, direct owner PID `14080` and lock still match, cycle `345`; strict remains RAY close `1`/`+146.0218 KRW`/open `0`. The relaxed shadow book has four closed trades and no open position; the looser book has one new diagnostic `KRW-ETH` open from `19:46:09Z` (entry `3,425,422 KRW`, rebound `0.0585%`, volume `0.61x`, trend `-0.0165%`, previous-high-break failed). Analysis remains complete `12/12`, incomplete/missing `0/0`, risk `0/0`, continuity true, stale analysis `1,201`, entry stale `0`. No new strict sample; preserve strict/diagnostic separation and do not start another owner.

At `2026-09-12T19:52:49.684Z`, direct PID `14080` and `.paper-session.lock` still match, cycle `361`, and observer `3153` reports the same active non-orphan read-only session. Strict remains RAY close `1`/`+146.0218 KRW`/open `0`; loose ETH remains open from `19:46:09Z` and shadow has no open position. Analysis `12/12`, incomplete/missing `0/0`, risk `0/0`, continuity true, stale analysis `1,284`, entry stale `0`. No new strict sample or terminal state; do not start a replacement while the owner/lock and diagnostic position remain.

At `2026-09-12T19:55:03.165Z`, direct PID `14080` and lock remain alive/matched at cycle `369`; strict still has only RAY close `+146.0218 KRW` with open `0`. Shadow has 4 closes/open `0`; loose has 4 closes and the ETH diagnostic position from `19:46:09Z` remains open. Analysis `12/12`, incomplete/missing `0/0`, risk `0/0`, continuity true, stale analysis `1,320`, entry stale `0`. No new strict sample or terminal state; keep the baseline owner and diagnostic lane isolated.

At `2026-09-12T20:00:36.808Z`, direct PID `14080` and `.paper-session.lock` still match, cycle `389`; strict remains RAY close `1`/`+146.0218 KRW`/open `0`, shadow has 4 closes/open `0`, and loose ETH remains open from `19:46:09Z`. Analysis `12/12`, incomplete/missing `0/0`, risk `0/0`, continuity true, stale analysis `1,388`, entry stale `0`. No new strict sample or terminal state; keep baseline and diagnostic lanes separate.

Diagnostic recent-trade observability is now wired through `shadowEvaluation.recentTrades` and `looseShadowEvaluation.recentTrades`; the latest-source observer API returns STORJ `TAKE_PROFIT +383.20 KRW` with `previous_high_break_failed`, and the desktop Forward paper card renders it beside the strict RAY result. At `390px` CSS width, the added diagnostic card still has `scrollWidth=380` versus `innerWidth=390`; the temporary mobile tab/viewport was cleaned up. The current v60 owner remains untouched.

v60 reached a clean transition boundary at `2026-09-12T20:16:52.069Z`: it was stopped only after strict, shadow, and loose positions were all closed. Final v60 ledger state is `active=false`, `stopReason=stopped_cleanly`, strict 1 close `+146.0218 KRW`, shadow 4 closes `+120.6076 KRW`, loose 5 closes `+113.1357 KRW`, and no open positions or temporary ledger file. Lock was released and the v60 ledger remains preserved.

Started fresh v61 winner-shadow session at `2026-09-12T20:17:30.143Z` in `.paper-forward-v61-winner30`, session `paper-1789244250143`, owner PID `73683`, with the same 12 markets. Strict baseline winner-hold remains disabled (`winnerExtendMinutes=0`); `paperExperiments.winnerShadow` is enabled with entry contract `strict_confirmed_buy_signal`, extension `30` minutes, minimum profit `0`. At `20:18:36Z`/cycle `11`, strict and winnerShadow both had zero entries/closures, config snapshot was complete, interruptions empty, and the latest read-only observer on `3153` returned `paperExperimentConsistent=true`, `processAlive=true`, `orphaned=false`, `eligible=false` for insufficient sample. Keep v61 running; sidecar is research-only and excluded from strict promotion.

At `2026-09-12T20:22:55.847Z`, v61 owner PID `73683` and lock still match at cycle `51`; strict, winnerShadow, shadow, and loose all remain at zero trades and positions. There were `172` analysis-only stale blocks and `0` entry-confirmation blocks, while all 12 analyses completed with no incomplete/missing markets or risk failures. No strict confirmed BUY existed, so winnerShadow correctly recorded zero entries. Observer `3153` reports the same active session, `paperExperimentConsistent=true`, `processAlive=true`, `orphaned=false`, and `eligible=false` for insufficient sample.

At `2026-09-12T20:20:46.143Z`, v61 owner PID `73683` and lock still match at cycle `31`; all strict/winnerShadow/shadow/loose books remain at zero trades and positions, with analysis `12/12`, incomplete/missing `0/0`, risk `0/0`, and freshness analysis blocks `101`. The winnerShadow sidecar has not manufactured entries because no strict confirmed BUY signal exists. Observer `3153` returns the same session, `paperExperimentConsistent=true`, `processAlive=true`, `orphaned=false`, and `eligible=false` for insufficient sample.

The winnerShadow sidecar is implemented in `MultiCoinTrader`, `runPaperSmoke`, `runDashboard`, and the primary dashboard UI, with an env-controlled default-off flag (`SCALP_WINNER_SHADOW_EXTEND_MINUTES`). Its regression test covers strict-signal-only entry, 30-minute extension at max-hold, close persistence, and experiment drift blocking. Latest suite after sidecar implementation: `npm test 145/145 PASS`; source/HTML/manifest/diff checks pass. Desktop and 390 CSS px mobile AX confirm the `winner-hold shadow: 30분 연장 · strict 승격 제외` line without overflow.

At `2026-09-12T20:27:31Z`, direct read-only Upbit candle probes separated the v61 freshness issue from timestamp parsing: BFC/BTC/ETH/IOST/KAT/STORJ/VTHO/XRP returned a current `20:27` 1-minute candle (about 32-33 seconds old), while RAY/TFUEL/UP2 returned `20:21`/`20:22`/`20:23` candles (about 273-393 seconds old). The stale blocks are therefore caused by inactive-market candle gaps, not a UTC/KST parsing bug; the 90-second default remains unchanged.

At `2026-09-12T20:34:50.823Z`, v61 owner PID `73683` and root `.paper-session.lock` still match, session `paper-1789244250143` is active at cycle `160`, strict/winnerShadow/shadow/loose all have `0` trades and `0` open positions, and there are no live orders. All `12/12` market analyses complete with incomplete/missing `0/0`, risk failures/outages `0/0`, interruptions `0`, and `continuityEligible=true`; strict confirmed BUY/BUY/oversold observations remain `0/0/0`. Freshness analysis blocks are `679`; per-market telemetry identifies BTC/ETH/XRP as `0%` blocked, IOST `8.1%`, VTHO `16.9%`, STORJ `18.1%`, and BFC/KAT/RAY/SC/TFUEL/UP2 materially higher. A 100-observation, 5% ceiling diagnostic cohort currently selects `KRW-BTC/KRW-ETH/KRW-XRP` only; IOST is excluded after crossing the ceiling. This is data-quality evidence, not profitability evidence.

Added diagnostic-only `marketFreshnessCohort` to paper status/API and the redesigned dashboard. It exposes the observation threshold, selected/excluded rows, block rates, and next-paper candidate markets without changing the active session target list, strict metrics, execution, or live gate. `SCALP_MARKET_QUALITY_MIN_OBSERVATIONS` and `SCALP_MARKET_QUALITY_MAX_STALE_RATE` tune only this reporting threshold; runner cohort selection remains explicit through `PAPER_SMOKE_MARKETS=FRESH_FROM_LEDGER`. Latest read-only observer port `3153` returns HTTP `200`, same v61 session, `processAlive=true`, `orphaned=false`, and `marketFreshnessCohort.ready=true` with `BTC/ETH/XRP`. Desktop AX confirms the visible freshness cohort line and the research-only wording; existing 390px mobile no-overflow evidence remains valid for the additive text surface.

After this change, `npm test` is `146/146 PASS`; changed source/dashboard JavaScript syntax, inline HTML scripts, standalone manifest, and `git diff --check` all pass. The v61 owner, ledger, lock, user `dry_portfolio.json`, and live-order path were not modified.

Read-only fixed validation of the current freshness cohort completed at `2026-09-12T20:39:37.828Z` and wrote only `/tmp/coinpilot-v61-fresh-fixed-20260913.json`. Using the current strict runtime settings on 7-day/1-minute candles, BTC holdout was `+0.0077%` over 3 trades with PF `1.64` but a 95% lower bound of `-1.1426%`; ETH was `-0.0213%` over 3 trades/PF `0`; XRP was `-0.0274%` over 4 trades/PF `0.16`. All three failed the training confidence gate and the global result was `0/3`; freshness cohort selection improves data quality but does not establish profitability.

Immediately after the separate validation process, v61 remained healthy at `2026-09-12T20:40:08.929Z`: owner/lock PID `73683`, cycle `207`, heartbeat current, strict/winnerShadow/shadow/loose `0` trades/open, analysis `12/12`, incomplete/missing `0/0`, risk failures/outages `0/0`, interruptions `0`, and batch ticker/candle request stats `207/2,484/0`. The concurrent read-only holdout did not create a paper continuity or risk failure. Keep v61 active and do not promote the cohort or relax freshness.

At `2026-09-12T20:42:44.850Z`, v61 was clean-stopped at cycle `231` after all strict and diagnostic books were empty. Its preserved ledger `.paper-forward-v61-winner30/paper_validation.json` records `active=false`, `stopReason=stopped_cleanly`, strict/winnerShadow/shadow/loose `0` trades and `0` open positions, no interruptions, complete `12/12` analysis, and risk continuity eligible. This is a data-quality baseline, not a profitability sample.

At `2026-09-12T20:43:04.363Z`, v62 was started in isolated output `.paper-forward-v62-fresh` from the explicit `FRESH_FROM_LEDGER` cohort selected from v61. Its session is `paper-1789245784363`, owner PID `80206`, target list exactly `KRW-BTC/KRW-ETH/KRW-XRP`, and `winnerShadow` is enabled for a 30-minute research-only extension while strict winner-hold remains disabled. At `20:44:09.423Z`/cycle `13`, all `3/3` analyses were complete, stale/incomplete/missing/risk failures were `0/0/0/0`, interruptions were `0`, and strict/winnerShadow/shadow/loose had zero trades and positions. Read-only observer `3153` was switched to v62 and returns HTTP `200`, same session, `processAlive=true`, `orphaned=false`; the dashboard visibly shows the new three-market target and freshness observation progress. Keep v62 running as the clean forward cohort observation; it has not passed any profitability gate.

At `2026-09-12T20:47:29.346Z`, v62 remains active under owner PID `80206` and lock token `5d53a218-beb8-48c0-80d2-c32874d06c89` at cycle `49`. The three-market cohort has `49/49` fresh observations per market, `0` stale blocks, complete `3/3` analysis, incomplete/missing `0/0`, risk failures/outages `0/0`, interruptions `0`, and no strict/winnerShadow/shadow/loose trades or positions. Telemetry contains `147` HOLD decisions, all reasoned as `과매도 조건 없음 - 관망`; this is a market-quiet observation rather than filter starvation or evidence to lower RSI/rebound thresholds. Observer `3153` returns HTTP `200`, same-session/read-only/processAlive/non-orphan; keep v62 active and do not loosen the entry contract.

At `2026-09-12T20:48:38.416Z`, v62 remains active at cycle `61` with owner PID `80206` and the same lock. Each of BTC/ETH/XRP has `61/61` fresh observations and `0` stale blocks; analysis is complete `3/3`, incomplete/missing `0/0`, risk failures/outages `0/0`, interruptions `0`, and all four books remain at zero trades and positions. Across `183` analyses, `previous_rsi_not_oversold=183`, `oversoldObservations=0`, `strictReboundCandidates=0`, and `strictConfirmedCandidates=0`; classify this as market quiet/no setup, not filter starvation. Keep the current contract unchanged.

At `2026-09-12T20:49:44.464Z`, v62 remains active at cycle `73` with the same owner/lock. BTC/ETH/XRP each have `73/73` fresh observations and no freshness blocks; analysis is complete `3/3`, incomplete/missing `0/0`, risk failures/outages `0/0`, interruptions `0`, and all books remain empty. Telemetry now contains `219` market-quiet HOLD decisions, with `previous_rsi_not_oversold=219` and no oversold/rebound/confirmed BUY signal. This confirms a quiet market window rather than data starvation; do not relax the contract based on zero trades.

Research-only RSI threshold study completed at `2026-09-12T20:52:23.401Z` using the same 7-day/1-minute BTC/ETH/XRP candle window (`/tmp/coinpilot-v62-rsi-candles-20260913.json`, report `/tmp/coinpilot-v62-rsi-study-20260913.json`). Baseline runtime RSI 35 produced `10` holdout trades and summed `-0.0411%`; RSI 40 produced `17` trades and `-0.0905%`; RSI 45 produced `30` trades and `-0.0759%`. All three markets failed the training gate for every candidate; RSI 40/45 increased activity without improving return. The new `rsi_40`/`rsi_45` entries are research-tool candidates only; keep runtime RSI 35 unchanged.

At `2026-09-12T20:52:38.456Z`, v62 owner PID `80206` and lock still match at cycle `97`; strict/winnerShadow/shadow/loose remain at zero trades and positions, analysis is complete `3/3`, stale/incomplete/missing/risk failures are `0/0/0/0`, interruptions are `0`, and all `291` HOLD decisions report `과매도 조건 없음 - 관망`. The concurrent RSI study did not invalidate v62 continuity or risk state.

At `2026-09-12T20:54:24.203Z`, v62 remains active under owner PID `80206` and lock token `5d53a218-beb8-48c0-80d2-c32874d06c89` at cycle `121`. The selected BTC/ETH/XRP cohort is now ready under the 100-observation/5% ceiling (`121/121` fresh observations per market, stale `0`), with complete `3/3` analysis, incomplete/missing `0/0`, risk failures/outages `0/0`, interruptions `0`, and strict/winnerShadow/shadow/loose all at zero trades and positions. `363` HOLD decisions all report `과매도 조건 없음 - 관망`; `previous_rsi_not_oversold=363`, oversold/rebound/confirmed BUY remain `0/0/0`. The v62 fresh cohort is operationally clean but still has no profitability sample.

At `2026-09-12T21:00:38.815Z`, v62 remains active under owner PID `80206` and lock token `5d53a218-beb8-48c0-80d2-c32874d06c89` at cycle `193`. Strict/winnerShadow/shadow/loose remain at zero trades and positions; analysis is complete `3/3`, incomplete/missing `0/0`, risk failures/outages `0/0`, and interruptions `0`. Three ETH freshness blocks were recorded (`3/193`, `1.55%`, max age `104.409s`) while BTC/XRP remained `0/193`; this is below the 5% cohort ceiling and is retained as data-quality telemetry, not a reason to loosen the 90-second gate. The same session still reports `576` market-quiet HOLD decisions and no strict BUY.

Added source-side signal telemetry deduplication for future sessions: `uniqueSignalWindows`, `uniqueReasonCounts`, and `uniqueRejectionCounts` use `coin:rebound.signalKey` so repeated 5-second cycles do not inflate independent 1-minute evidence. Existing repeated-cycle counters remain for operational volume, while old v62 telemetry correctly reports `signalTelemetry.available=false` and `uniqueSignalWindows=null` in the latest-source observer; no historical unique-window estimate is fabricated. Regression coverage and docs were updated; v62 was not restarted for this observability-only change.

At `2026-09-12T21:03:35.852Z`, v62 was clean-stopped at cycle `229` with strict/winnerShadow/shadow/loose all empty, `stopped_cleanly`, no interruptions, complete `3/3` analysis, risk continuity eligible, and only `3` ETH stale snapshots (`1.31%`). Its ledger remains preserved as the pre-dedup fresh-cohort observation.

At `2026-09-12T21:03:52.521Z`, v63 started in isolated output `.paper-forward-v63-dedup` from v62's freshness ledger, with session `paper-1789247032521`, owner PID `85265`, target `KRW-BTC/KRW-ETH/KRW-XRP`, unchanged strict runtime settings, and winnerShadow 30-minute extension. At `2026-09-12T21:05:13.906Z`/cycle `13`, v63 had `6` unique signal windows (`2` per market) versus `39` repeated-cycle observations, unique reasons/rejections were populated, all three markets were fresh, analysis `3/3`, incomplete/missing/risk failures `0/0/0`, interruptions `0`, and all books had zero trades/positions. Read-only observer `3153` was refreshed to v63 and its desktop AX shows `고유 window 6 (중복 cycle 제외)`, with strict/winnerShadow status and order lock intact. This is observability/data-quality validation, not profitability evidence.

At `2026-09-12T21:06:43.835Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `25`. Each target market has `25/25` fresh observations and no stale blocks; analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain at zero trades and positions. The new deduplicated telemetry reports `12` unique signal windows (`4` per market) versus `75` repeated-cycle observations, with all `12` unique reasons currently `과매도 조건 없음 - 관망`; no strict confirmed BUY exists. Observer `3153` returns the same active read-only v63 and UI/API expose the deduped count.

At `2026-09-12T21:09:10.853Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `49`. BTC/ETH/XRP each have `49/49` fresh observations and stale `0`; analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and all four books remain at zero trades and positions. The deduplicated counters show `18` unique signal windows (`6` per market) versus `147` repeated-cycle observations; all `18` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. This strengthens the classification as a quiet market window rather than filter starvation; keep the strict contract unchanged.

At `2026-09-12T21:10:18.786Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `61`. BTC/ETH/XRP each have `61/61` fresh observations and stale `0`; analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and all books remain at zero trades and positions. The deduplicated counters show `21` unique signal windows (`7` per market) versus `183` repeated-cycle observations; all `21` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. The clean no-signal observation continues; do not treat repeated HOLD volume as independent profitability evidence.

At `2026-09-12T21:11:26.439Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `85`. BTC/ETH/XRP each have `85/85` fresh observations and stale `0`; analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and all books remain at zero trades and positions. The deduplicated counters show `27` unique signal windows (`9` per market) versus `255` repeated-cycle observations; all `27` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. This remains clean market-quiet evidence; keep the current threshold contract.

At `2026-09-12T21:12:30.536Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `97`. BTC/ETH/XRP each have `97/97` fresh observations and stale `0`; analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `30` unique signal windows (`10` per market) versus `291` repeated-cycle observations; all `30` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. Preserve this independent no-signal sample and keep RSI 35/runtime thresholds unchanged.

At `2026-09-12T21:13:35.573Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `109`. BTC/ETH/XRP each have `109/109` fresh observations and stale `0`; analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `33` unique signal windows (`11` per market) versus `327` repeated-cycle observations; all `33` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. This continues the clean no-signal observation; do not relax thresholds.

At `2026-09-12T21:14:41.725Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `121`. BTC/ETH/XRP each have `121/121` fresh observations and stale `0`; analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `36` unique signal windows (`12` per market) versus `363` repeated-cycle observations; all `36` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. Preserve this independent no-signal sample and keep the runtime contract unchanged.

At `2026-09-12T21:16:50.896Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `145`. BTC/ETH/XRP each have `145/145` fresh observations and stale `0`; analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `42` unique signal windows (`14` per market) versus `435` repeated-cycle observations; all `42` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. Keep v63 running and do not infer profitability or loosen thresholds from the absence of trades.

At `2026-09-12T21:18:02.400Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `157`. BTC/ETH/XRP each have `157/157` fresh observations and stale `0`; analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `45` unique signal windows (`15` per market) versus `471` repeated-cycle observations; all `45` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. This remains clean no-signal evidence; keep the current contract unchanged.

At `2026-09-12T21:20:14.002Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `181`. BTC/ETH/XRP each have `181/181` fresh observations and stale `0`; analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `51` unique signal windows (`17` per market) versus `543` repeated-cycle observations; all `51` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. Preserve the clean no-signal observation and keep runtime thresholds unchanged.

At `2026-09-12T21:23:03.416Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `205`. BTC/ETH/XRP each have `205/205` fresh observations and stale `0`; analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `60` unique signal windows (`20` per market) versus `615` repeated-cycle observations; all `60` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. This remains a clean no-signal forward sample; do not infer profitability or force entries.

At `2026-09-12T21:25:29.742Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `241`. BTC/ETH/XRP each have `241/241` fresh observations and stale `0`; analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `69` unique signal windows (`23` per market) versus `723` repeated-cycle observations; all `69` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. Keep the session running; the observed zero-trade window is not a profitability conclusion or a reason to force entries.

At `2026-09-12T21:27:46.406Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `265`. BTC/ETH/XRP each have `265/265` fresh observations and stale `0`; analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `75` unique signal windows (`25` per market) versus `795` repeated-cycle observations; all `75` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. Keep v63 clean and do not force entries or infer profitability from no-trade observations.

At `2026-09-12T21:31:07.946Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `301`. BTC/ETH/XRP each have `301/301` freshness observations; ETH has one stale analysis (`1/301`, max age not persisted in this read) while BTC/XRP remain unblocked. Analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `84` unique signal windows (`28` per market) versus `903` repeated-cycle observations; all `84` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. The isolated stale sample remains below the cohort limit and does not justify changing the 90-second gate.

At `2026-09-12T21:37:09.565Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `361`. BTC/ETH/XRP each have `361/361` freshness observations; ETH has one stale analysis (`1/361`) while BTC/XRP remain at `0/361`. Analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `102` unique signal windows (`34` per market) versus `1,083` repeated-cycle observations; all `102` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. This exceeds 100 independent no-signal windows while remaining operationally clean; classify the interval as market quiet and keep the runtime contract unchanged.

At `2026-09-12T21:36:01.023Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `349`. BTC/ETH/XRP each have `349/349` freshness observations; ETH has one stale analysis (`1/349`) while BTC/XRP remain at `0/349`. Analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `96` unique signal windows (`32` per market) versus `1,047` repeated-cycle observations; all `96` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. The single stale ETH observation remains below the cohort ceiling; retain the 90-second freshness gate and current runtime contract.

At `2026-09-12T21:39:28.882Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `397`. BTC/ETH/XRP each have `397/397` freshness observations; ETH has one stale analysis (`1/397`) while BTC/XRP remain at `0/397`. Analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `111` unique signal windows (`37` per market) versus `1,191` repeated-cycle observations; all `111` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. The single ETH stale sample remains below the quality ceiling; continue the clean forward observation without threshold relaxation.

At `2026-09-12T21:45:03.842Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `457`. BTC/ETH/XRP each have `457/457` freshness observations; ETH has two stale analyses (`2/457`) while BTC/XRP remain at `0/457`. Analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `126` unique signal windows (`42` per market) versus `1,371` repeated-cycle observations; all `126` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. ETH stale remains below the 5% quality ceiling; preserve the current strict contract and do not force entries.

At `2026-09-12T21:21:20.301Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `193`. BTC/ETH/XRP each have `193/193` fresh observations and stale `0`; analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `57` unique signal windows (`19` per market) versus `579` repeated-cycle observations; all `57` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. Keep the forward window clean and do not infer profitability from the absence of trades.

At `2026-09-12T21:24:22.405Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `229`. BTC/ETH/XRP each have `229/229` fresh observations and stale `0`; analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `66` unique signal windows (`22` per market) versus `687` repeated-cycle observations; all `66` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. Preserve this no-signal forward evidence; do not force trades or relax the runtime contract.

At `2026-09-12T21:26:39.441Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `253`. BTC/ETH/XRP each have `253/253` fresh observations and stale `0`; analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `72` unique signal windows (`24` per market) versus `759` repeated-cycle observations; all `72` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. This remains clean no-signal evidence and does not justify a runtime change.

At `2026-09-12T21:28:58.162Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `277`. BTC/ETH/XRP each have `277/277` fresh observations and stale `0`; analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `78` unique signal windows (`26` per market) versus `831` repeated-cycle observations; all `78` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. Preserve this clean no-signal sample and keep the current threshold contract.

At `2026-09-12T21:33:18.385Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `325`. BTC/ETH/XRP each have `325/325` freshness observations; ETH has one stale analysis (`1/325`) while BTC/XRP remain at `0/325`. Analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `90` unique signal windows (`30` per market) versus `975` repeated-cycle observations; all `90` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. The one ETH stale sample remains within the cohort tolerance; keep the 90-second freshness gate and runtime thresholds unchanged.

At `2026-09-12T21:32:12.744Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `313`. BTC/ETH/XRP each have `313/313` freshness observations; ETH has one stale analysis (`1/313`) while BTC/XRP remain at `0/313`. Analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `87` unique signal windows (`29` per market) versus `939` repeated-cycle observations; all `87` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. The one stale ETH sample remains below the 5% cohort limit; keep the 90-second gate and runtime thresholds unchanged.

At `2026-09-12T21:38:23.070Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `385`. BTC/ETH/XRP each have `385/385` freshness observations; ETH has one stale analysis (`1/385`) while BTC/XRP remain at `0/385`. Analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `108` unique signal windows (`36` per market) versus `1,155` repeated-cycle observations; all `108` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. The single stale ETH sample remains below the quality ceiling; retain the 90-second freshness gate and current runtime contract.

At `2026-09-12T21:46:11.426Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `469`. BTC/ETH/XRP each have `469/469` freshness observations; ETH has two stale analyses (`2/469`) while BTC/XRP remain at `0/469`. Analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `129` unique signal windows (`43` per market) versus `1,407` repeated-cycle observations; all `129` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. ETH stale remains below the 5% quality ceiling; keep the freshness and entry contracts unchanged.

At `2026-09-12T21:42:17.285Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `421`. BTC/ETH/XRP each have `421/421` freshness observations; ETH has two stale analyses (`2/421`) while BTC/XRP remain at `0/421`. Analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `117` unique signal windows (`39` per market) versus `1,263` repeated-cycle observations; all `117` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. ETH stale rate remains below the 5% quality ceiling; retain the 90-second gate and current runtime contract.

At `2026-09-12T21:30:01.530Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `289`. BTC/ETH/XRP each have `289/289` fresh observations and stale `0`; analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `81` unique signal windows (`27` per market) versus `867` repeated-cycle observations; all `81` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. Continue the clean no-signal observation and keep the runtime contract unchanged.

At `2026-09-12T21:34:23.278Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `337`. BTC/ETH/XRP each have `337/337` freshness observations; ETH has one stale analysis (`1/337`) while BTC/XRP remain at `0/337`. Analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `96` unique signal windows (`32` per market) versus `1,011` repeated-cycle observations; all `96` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. The single ETH stale sample remains below the 5% quality ceiling; keep the 90-second gate and current runtime contract.

At `2026-09-12T21:39:28.882Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `397`. BTC/ETH/XRP each have `397/397` freshness observations; ETH has one stale analysis (`1/397`) while BTC/XRP remain at `0/397`. Analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `111` unique signal windows (`37` per market) versus `1,191` repeated-cycle observations; all `111` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. The single ETH stale sample remains below the quality ceiling; continue the clean forward observation without threshold relaxation.

At `2026-09-12T21:19:05.595Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `169`. BTC/ETH/XRP each have `169/169` fresh observations and stale `0`; analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `48` unique signal windows (`16` per market) versus `507` repeated-cycle observations; all `48` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. The forward window remains clean but has no profitability sample; do not force entries or relax thresholds.

At `2026-09-12T21:15:47.208Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `133`. BTC/ETH/XRP each have `133/133` fresh observations and stale `0`; analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `39` unique signal windows (`13` per market) versus `399` repeated-cycle observations; all `39` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. Keep v63 as a clean no-signal forward observation and do not relax thresholds.

At `2026-09-12T21:43:23.991Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `433`. BTC/ETH/XRP each have `433/433` freshness observations; ETH has two stale analyses (`2/433`) while BTC/XRP remain at `0/433`. Analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `120` unique signal windows (`40` per market) versus `1,299` repeated-cycle observations; all `120` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. ETH stale remains below the 5% quality ceiling; keep the 90-second gate and current runtime contract.

At `2026-09-12T21:47:21.399Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `481`. BTC/ETH/XRP each have `481/481` freshness observations; ETH has two stale analyses (`2/481`) while BTC/XRP remain at `0/481`. Analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `132` unique signal windows (`44` per market) versus `1,443` repeated-cycle observations; all `132` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. ETH stale remains below the 5% quality ceiling; preserve the current contract and do not force entries.

At `2026-09-12T21:48:29.687Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `493`. BTC/ETH/XRP each have `493/493` freshness observations; ETH has two stale analyses (`2/493`) while BTC/XRP remain at `0/493`. Analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `138` unique signal windows (`46` per market) versus `1,479` repeated-cycle observations; all `138` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. ETH stale remains below the quality ceiling; keep freshness and entry contracts unchanged.

At `2026-09-12T21:53:05.611Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `541`. BTC/ETH/XRP each have `541/541` freshness observations; ETH has two stale analyses (`2/541`) while BTC/XRP remain at `0/541`. Analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `150` unique signal windows (`50` per market) versus `1,623` repeated-cycle observations; all `150` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. Keep v63 running; no forced entry or threshold change is justified.

At `2026-09-12T21:51:57.717Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `529`. BTC/ETH/XRP each have `529/529` freshness observations; ETH has two stale analyses (`2/529`) while BTC/XRP remain at `0/529`. Analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `147` unique signal windows (`49` per market) versus `1,587` repeated-cycle observations; all `147` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. ETH stale remains below the 5% quality ceiling; keep v63 running without forced entries or threshold changes.

At `2026-09-12T21:50:14.298Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `517`. BTC/ETH/XRP each have `517/517` freshness observations; ETH has two stale analyses (`2/517`) while BTC/XRP remain at `0/517`. Analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `144` unique signal windows (`48` per market) versus `1,551` repeated-cycle observations; all `144` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. ETH stale remains below the 5% quality ceiling; keep v63 running without forced entries or threshold changes.

At `2026-09-12T21:55:57.717Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `577`. BTC/ETH/XRP each have `577/577` freshness observations; ETH has three stale analyses (`3/577`) while BTC/XRP remain at `0/577`. Analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `159` unique signal windows (`53` per market) versus `1,731` repeated-cycle observations; all `159` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. ETH stale remains below the 5% quality ceiling; preserve the current freshness and entry contracts.

At `2026-09-12T22:00:37.130Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `625`. BTC/ETH/XRP each have `625/625` freshness observations; ETH has three stale analyses (`3/625`) while BTC/XRP remain at `0/625`. Analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `171` unique signal windows (`57` per market) versus `1,875` repeated-cycle observations; all `171` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. ETH stale remains below the 5% quality ceiling; keep v63 running and the current entry contract unchanged.

At `2026-09-12T21:54:13.821Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `553`. BTC/ETH/XRP each have `553/553` freshness observations; ETH has two stale analyses (`2/553`) while BTC/XRP remain at `0/553`. Analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `153` unique signal windows (`51` per market) versus `1,659` repeated-cycle observations; all `153` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. ETH stale remains below the 5% quality ceiling; preserve the current forward contract and do not force entries.

At `2026-09-12T21:57:06.805Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `589`. BTC/ETH/XRP each have `589/589` freshness observations; ETH has three stale analyses (`3/589`) while BTC/XRP remain at `0/589`. Analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `162` unique signal windows (`54` per market) versus `1,767` repeated-cycle observations; all `162` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. ETH stale remains below the 5% quality ceiling; keep the 90-second freshness gate and current entry contract.

At `2026-09-12T22:01:50.144Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `637`. BTC/ETH/XRP each have `637/637` freshness observations; ETH has three stale analyses (`3/637`) while BTC/XRP remain at `0/637`. Analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `174` unique signal windows (`58` per market) versus `1,911` repeated-cycle observations; all `174` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. ETH stale remains below the 5% quality ceiling; continue v63 without forced entries or threshold changes.

At `2026-09-12T22:06:16.016Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `685`. BTC/ETH/XRP each have `685/685` freshness observations; ETH has three stale analyses (`3/685`) while BTC/XRP remain at `0/685`. Analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `188` unique signal windows (`63/62/63` for BTC/ETH/XRP) versus `2,055` repeated-cycle observations; all `188` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. ETH stale remains below the quality ceiling; preserve the current freshness and entry contracts.

At `2026-09-12T21:58:53.242Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `613`. BTC/ETH/XRP each have `613/613` freshness observations; ETH has three stale analyses (`3/613`) while BTC/XRP remain at `0/613`. Analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `168` unique signal windows (`56` per market) versus `1,839` repeated-cycle observations; all `168` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. ETH stale remains below the 5% quality ceiling; retain the current gate and entry contract.

At `2026-09-12T22:04:44.738Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `673`. BTC/ETH/XRP each have `673/673` freshness observations; ETH has three stale analyses (`3/673`) while BTC/XRP remain at `0/673`. Analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `186` unique signal windows (`62` per market) versus `2,019` repeated-cycle observations; all `186` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. ETH stale remains below the quality ceiling; continue v63 without forced entries or threshold changes.

At `2026-09-12T22:07:25.792Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `697`. BTC/ETH/XRP each have `697/697` freshness observations; ETH has three stale analyses (`3/697`) while BTC/XRP remain at `0/697`. Analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `192` unique signal windows (`64` per market) versus `2,091` repeated-cycle observations; all `192` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. ETH stale remains below the quality ceiling; keep v63 and current thresholds unchanged.

At `2026-09-12T22:10:19.682Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `733`. BTC/ETH/XRP each have `733/733` freshness observations; ETH has three stale analyses (`3/733`) while BTC/XRP remain at `0/733`. Analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `201` unique signal windows (`67` per market) versus `2,199` repeated-cycle observations; all `201` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. ETH stale remains below the 5% quality ceiling; keep the current freshness and entry contracts unchanged.

At `2026-09-12T22:03:04.744Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `649`. BTC/ETH/XRP each have `649/649` freshness observations; ETH has three stale analyses (`3/649`) while BTC/XRP remain at `0/649`. Analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `180` unique signal windows (`60` per market) versus `1,947` repeated-cycle observations; all `180` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. Continue the clean forward observation with the current freshness and entry contracts.

At `2026-09-12T22:08:39.098Z`, v63 remains active under owner PID `85265` and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` at cycle `721`. BTC/ETH/XRP each have `721/721` freshness observations; ETH has three stale analyses (`3/721`) while BTC/XRP remain at `0/721`. Analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain empty. The deduplicated counters show `198` unique signal windows (`66` per market) versus `2,163` repeated-cycle observations; all `198` unique reasons are `과매도 조건 없음 - 관망`, with no oversold/rebound/confirmed BUY. Keep freshness and entry contracts unchanged.

At `2026-09-12T22:13:12.597Z`, v63 is still active under owner PID `85265` (parent `85239`), with cwd ownership confirmed at the repository and lock token `9dc97b7f-db36-42b4-ac9c-c4fbe2231a6f` unchanged. The ledger reached cycle `769` in session `paper-1789247032521`; BTC/ETH/XRP analysis is complete `3/3`, incomplete/missing/risk failures are `0/0/0`, interruptions are `0`, and strict/winnerShadow/shadow/loose remain at zero trades and zero open positions. The observer on port `3153` returns HTTP 200 for the same session, `processAlive=true`, `orphaned=false`, `configConsistent=true`, and `eligible=false`; deduplicated signal windows are `210`, with no BUY/oversold/rebound/confirmed events. Preserve v63 and its current entry/freshness contracts; no live order or user portfolio state changed.

At `2026-09-12T22:20:31.482Z`, v63 was stopped cleanly after cycle `854`: `active=false`, `stopReason=stopped_cleanly`, no temporary ledger remained, the root session lock was released, and strict/shadow/loose books had no open positions or trades. The no-signal data-quality session is preserved and is not mixed with the next candidate.

The optional `maxReboundPercent` exhaustion guard was then added consistently to completed-candle analysis, backtest, live strategy configuration, forward snapshots, validation scripts, API parameter ranges, and UI-driven updates. It defaults to `0` (disabled), applies only to oversold-rebound profiles, and emits `price_rebound_above_threshold` when a signal candle is already too extended. On the existing 14-day/12-market cache, fixed replay changed baseline `222 trades / -1.7039%` to max-rebound `0.4%` `140 trades / -0.7567%`; adding the diagnostic next-candle follow-through contract produced `43 trades / -0.2309%`. A proper 70/30 fixed holdout produced baseline `-0.3877% / 54 trades`, max-rebound `0.4%` `-0.0643% / 34 trades`, and max-rebound `0.4% + follow-through` `-0.0393% / 11 trades`; all `12/12` markets failed the training gate, so no candidate was promoted or made the runtime default. Test suite after the change is `149/149`.

Started isolated v64 candidate forward paper at `2026-09-12T22:26:27.803Z` using `.paper-forward-v64-maxrebound04`, target cohort BTC/ETH/XRP, `SCALP_MAX_REBOUND_PERCENT=0.4`, and winner-shadow extension 30 minutes. At `2026-09-12T22:28:37.511Z`, owner node PID `9210` (npm parent `9184`) was active at cycle `25`; config snapshot recorded max rebound `0.4`, winner-shadow enabled, all three analyses complete, incomplete/missing/risk failures `0/0/0`, strict/winner/shadow/loose all zero trades/open, and no live order. The read-only observer on port `3153` was safely switched from the stopped v63 ledger to v64.

After the source update, the dashboard rendered the current v64 read-only forward state: `0/3` validation gate, paper observation running, strict paper zero trades, and the new `최대 반등률 (%)` parameter visible with value `0.4` in the settings UI. Existing mobile/PWA verification remains additive and unchanged; no user portfolio or live state was touched.

At `2026-09-12T22:32:28.254Z`, the v64 owner PID `9210` remained alive under npm parent `9184`; its ledger was active in session `paper-1789251987803` at cycle `61`, with heartbeat age `36.8s`, analysis complete for all `3/3` markets, no incomplete/missing cycles, and zero risk failures/outages. Observer `3153` returned HTTP 200 for the same live session with `processAlive=true`, `orphaned=false`, `continuityEligible=true`, and `eligible=false`; max rebound remained `0.4`. No strict/winner/shadow/loose trades or open positions were present, so the candidate remains cleanly observable and non-promotional.

At `2026-09-12T22:36:45.769Z`, v64 remained active under owner PID `9210` at cycle `109`, with heartbeat age `35.8s`, analysis `3/3`, missing/incomplete/risk failures `0/0/0`, and no interruptions. The candidate produced `25` oversold observations and `2` strict rebound candidates but `0` confirmed BUYs; all books still had zero trades/open positions. Deduplicated telemetry had `33` unique windows (`11` per market); the current bottlenecks were minimum rebound, previous-high break, and volume confirmation, while `price_rebound_above_threshold` had not yet blocked a candidate. Filter starvation was therefore real, but historical relaxation studies remained negative, so no automatic parameter loosening was performed.

At `2026-09-12T22:38:46.792Z`, the v64 observer returned the same active session at cycle `133`, heartbeat age `27.5s`, continuity true, and eligibility false. Deduplicated telemetry reached `39` unique windows (`13` per market); oversold observations `49`, strict rebound candidates `11`, confirmed BUY `0`. Unique rejection counts remained concentrated in `price_rebound_below_threshold=39`, `previous_high_break_failed=35`, `previous_rsi_not_oversold=34`, `bullish_rebound_not_confirmed=34`, `rsi_recovery_below_threshold=30`, and `volume_confirmation_failed=24`; no max-rebound rejection occurred. Strict book remained `0` trades/open and `0` profit.

At `2026-09-12T22:41:38.055Z`, v64 was cleanly stopped at cycle `169` with no strict or diagnostic positions/trades, `stopped_cleanly`, no temporary ledger, and no lock remaining. The v64 candidate remains preserved as isolated max-rebound evidence.

The winner-shadow experiment was extended into a same-signal A/B entry filter. `SCALP_WINNER_SHADOW_MAX_REBOUND_PERCENT` is persisted in `paperExperiments` as `entryMaxReboundPercent`; with strict `maxReboundPercent=0`, the sidecar mirrors only confirmed strict BUYs at or below the ceiling, counts filtered entries, and remains excluded from strict assets/promotion. The API/UI now distinguish `rebound ceiling ... A/B` from winner-hold extension. Service-worker shell cache was bumped to `coinpilot-shell-v8` and the pilot asset query to `observer-readonly-4` so installable PWA clients receive the new UI.

Started v65 baseline/A-B forward paper at `2026-09-12T22:45:42.312Z` in `.paper-forward-v65-ab`, with strict max rebound `0`, winner-shadow ceiling `0.4`, winner extension `0`, and freshness cohort BTC/ETH/XRP. Owner node PID `14418` (npm `14391`) holds the lock. At `2026-09-12T22:49:08.268Z`, cycle `37` was active with config/experiment consistent, analysis `3/3`, no incomplete/missing/risk failures or interruptions, unique windows `12`, oversold `26`, strict rebound `11`, confirmed BUY `0`, and strict/winner/shadow/loose all zero trades/open. Observer `3153` reads the same session; CUA verification on the Validation ledger page visibly showed `rebound ceiling 0.40% A/B · 차단 0회 · 청산 0회 · strict 승격 제외` and read-only order controls. No user portfolio or live order changed.

After the service-worker cache bump and sidecar UI update, the latest source checks are `npm test 150/150 PASS`, all source JS syntax checks pass, `public/index.html` has two valid inline scripts, `pilot-redesign.js` parses, the manifest parses, and `git diff --check` passes. At `2026-09-12T22:50:15.733Z`, v65 owner `14418` remained active at cycle `49`, config/experiment consistent, heartbeat age `14.5s`, continuity true, unique windows `16`, oversold `38`, strict rebound `12`, confirmed BUY `0`, winner-shadow blocked `0`, and strict/winner/shadow/loose still zero trades/open. Observer `3153` returned HTTP 200 for the same session.

At `2026-09-12T22:53:32.853Z`, v65 was stopped cleanly at cycle `88` with no strict or diagnostic open positions/trades, no temporary ledger, and no lock. Its three-market A/B no-trade evidence is preserved.

Started v66 wide A/B forward paper at `2026-09-12T22:54:00.771Z` in `.paper-forward-v66-ab-wide`, selecting the top 20 non-stable KRW markets by current 24-hour volume. Strict remains baseline `maxReboundPercent=0`; winner-shadow remains same-signal ceiling `0.4` with no hold extension. At `2026-09-12T22:55:26.289Z`, cycle `9` had all `20/20` analyses complete, risk success, no interruptions, strict/winner/shadow/loose empty, and six relaxed candidates. At `2026-09-12T22:57:45.166Z`, cycle `25` had one `KRW-STORJ` position in both relaxed `shadow` and `looseShadow` only; its signal was rebound `0.6857%`, oversold RSI `32.97`, RSI recovery `7.48`, volume `4.64x`, close strength `1`, trend slope `-0.1117%`, while strict confirmed BUY remained `0`. At `2026-09-12T22:58:57.234Z`, the STORJ position was still open, strict/winner remained empty, analysis `20/20`, risk continuity true, and freshness blocks were concentrated in inactive markets (54 total; NEAR 23, CP 11, IQ 7, ETHFI 4, etc.). The position must remain unsettled and is being observed for its eventual exit; no forced stop/restart or parameter change was made.

Observer `3153` was switched to v66 and returns HTTP 200 for the same session. CUA browser verification on the Validation ledger page shows 20-market freshness observation, strict paper 0 trades, the A/B `rebound ceiling 0.40%` sidecar line, read-only order controls, and validation gate `0/3`.

At `2026-09-12T23:01:17.485Z`, v66 owner PID `17075` was still active at cycle `57` with heartbeat current, strict baseline empty, and one unsettled `KRW-STORJ` position in the relaxed shadow book only. The STORJ signal remained keyed to `2026-09-12T22:53:00` with rebound `0.6857%`, oversold RSI `32.97`, recovery `7.48`, volume `4.64x`, and trend `-0.1117%`; shadow closed trades remained `0` and realized profit `0`. The 20-market analysis and risk paths remained complete/healthy, and v66 was left running until the diagnostic position settles.

At `2026-09-12T23:03:12.334Z`, v66 remained active under the same owner/cwd at cycle `65`; the STORJ shadow/loose position was still open with no close or realized profit. The observer showed cycle `73`, `175` unique signal windows, strict confirmed BUY `0`, relaxed shadow entries `1`, and the same open position. Across all `20/20` configured markets, analysis remained complete and risk monitor continuity remained true with zero failures/outages/interruption. Freshness blocks reached `96` and were concentrated in inactive markets; the diagnostic freshness cohort was not yet ready because each market had only `73` observations versus the minimum `100`, so no automatic market change was made.

At `2026-09-12T23:05:00.405Z`, v66 owner PID `17075` remained alive at cycle `81`. Strict and winner-shadow had zero trades/open positions and zero confirmed BUY; relaxed shadow/loose had three entries/open positions at the runner snapshot, with `KRW-STORJ` still open and later `KRW-SOPH` observed at entry price `6.47647`, signal rebound `0.1550%`, oversold RSI `33.46`, RSI recovery `9.01`, volume ratio `0.282x`, and trend `-0.0771%`. The analysis path was complete for `20/20`, risk ticker success/outage remained `0/0`, continuity true, and freshness blocks were `107`; diagnostic open positions remain unsettled and must not be reclassified as a new baseline.

At `2026-09-12T23:06:40.512Z`, v66 cycle `97` produced the first settled relaxed diagnostic trade: `KRW-VTHO` closed `TAKE_PROFIT` at `0.914085` from entry `0.897897`, net `+340.2204 KRW` / `+1.7011%`. The signal had rebound `0.5605%`, oversold RSI `29.28`, RSI recovery `10.56`, volume `2.62x`, close strength `0.778`, signal range `1.009%`, and rejection reason `previous_high_break_failed`; strict baseline did not enter and winner-shadow remained empty. Shadow/loose each retained two open positions and one closed win, so this is a single relaxed winner, not profitability proof or permission to loosen the high-break filter. Observer/API and CUA Validation ledger UI both showed the recent VTHO diagnostic close.

Offline portfolio evidence on the same 14-day/12-market cache used the shared KRW balance and maximum three positions. Baseline 3-fold expanding validation aggregate holdout was `32 trades / -0.2288%`; max rebound `0.4` was `20 / -0.1130%`; max rebound `0.4` plus next-candle follow-through was only `6 / -0.0443%`; adding the existing regime gate at breadth `.67` did not change the six-trade result. Every fold failed the training gate. The safeguards reduce exposure and drawdown but do not establish positive expectancy, so no portfolio/runtime promotion was made.

At `2026-09-12T23:08:20.246Z`, v66 owner PID `17075` remained active at cycle `105`, with the same strict baseline/winner-shadow zero trades and two relaxed shadow/loose open positions after the VTHO close. Analysis remained complete `20/20`, risk ticker success/outage `0/0`, continuity true, interruptions `0`, and freshness blocks `143` concentrated in known inactive markets. Observer `3153` returned HTTP 200 for the same session and showed strict profit `0`, relaxed realized profit `+340.2204 KRW`, and the VTHO diagnostic close; no live or user portfolio state changed.

At `2026-09-12T23:10:36.250Z`, observer `3153` returned HTTP 200 for v66 session `paper-1789253640771`, active/non-orphaned/process-alive with cycle `129`, heartbeat age `8.0s`, continuity true, eligibility false, and `330` unique signal windows. Strict/winner remained `0` trades/open/profit; shadow/loose remained at one VTHO close `+340.2204 KRW` and two open diagnostic positions. No new confirmed BUY or winner-shadow entry appeared, so no parameter or live-gate change was justified.

At `2026-09-12T23:11:59.003Z`, v66 cycle `138` settled `KRW-STORJ` from the same `previous_high_break_failed` rejection cohort with `STOP_LOSS`, entry `88.1881`, exit `87.0129`, net `-286.2498 KRW` / `-1.4312%`. The relaxed shadow/loose books now had two closed trades (`1W/1L`, realized `+53.9706 KRW`) and two open positions; strict and winner-shadow remained empty. The subsequent UI readback showed the latest diagnostic STORJ stop and freshness cohort suggestion `13/20`; this two-trade rejection cohort is near-neutral and is not evidence to relax high-break.

At `2026-09-12T23:14:44.881Z`, v66 observer/API reported the same active, non-orphaned session at cycle `161`, heartbeat age `7.2s`, continuity true, and eligibility false. Strict and winner-shadow remained at zero trades/open/profit; shadow/loose retained two closed trades (`1W/1L`, realized `+53.9706 KRW`) and two open diagnostic positions. No new strict confirmed BUY or A/B sidecar entry appeared, so keep the runner and current contracts unchanged.

At `2026-09-12T23:16:11.021Z`, v66 owner PID `17075` remained active at cycle `169`, with strict/winner still empty and shadow/loose at two closed trades (`1W/1L`, `+53.9706 KRW`) plus two open diagnostics. Observer `3153` reported the same active/non-orphaned process at cycle `169`, unique windows `425`, heartbeat age `24.2s`, continuity true, and eligibility false. Analysis remained complete `20/20`, risk failures/outages `0/0`, and freshness blocks `206`; no new confirmed BUY or sidecar entry was observed.

At `2026-09-12T23:18:55.901Z`, v66 remained active at cycle `193`; telemetry showed `strictConfirmedCandidates=8`, `buyCandidates=1`, and `winnerShadowReboundBlockedEntries=1`, but strict/winner books still had no entries or positions. Relaxed shadow/loose remained at two closed trades (`1W/1L`, `+53.97 KRW`) and two open positions. Because v66 started before delayed-entry outcome telemetry was implemented, the single strict BUY candidate's eventual cancellation/success reason is not recoverable from the old ledger; the new `entryConfirmation*` telemetry will be used in the next clean session. Runner lock, analysis `20/20`, and risk continuity remained healthy.

Added delayed-entry outcome telemetry for subsequent sessions: attempts, successful revalidations, cancellations, bounded cancellation reasons, and last outcome. The UI signal summary now exposes the revalidation success/cancel ratio and top reason. This closes the evidence gap between a confirmed signal, delayed confirmation, and actual strict order without changing entry rules. Test suite after the change is `151/151`.

At `2026-09-12T23:26:10.875Z`, v66 remained active under owner PID `17075` at cycle `249`; strict baseline remained at zero entries/open/profit while telemetry retained `strictConfirmedCandidates=8`, `buyCandidates=1`, and one winner-shadow ceiling block. Relaxed shadow/loose remained at two closed trades (`1W/1L`, `+53.9706 KRW`) and two open diagnostic positions (`KRW-SOPH` and `KRW-IOST`). Observer `3153` was HTTP 200 for the same active/non-orphaned session with continuity true and eligibility false. The old v66 session cannot attribute the strict BUY cancellation because it predates the new entry-confirmation telemetry; leave it intact until diagnostic positions settle.

At `2026-09-12T23:28:14.608Z`, v66 strict baseline entered `KRW-ETHFI` at `963` from signal `2026-09-12T23:25:00` after a `4812ms` delay. Signal rebound was `0.4171%`, above the winner-shadow ceiling `0.4%`; winner-shadow blocked this same confirmed signal (`winnerShadowReboundBlockedEntries=2` cumulatively). Strict open count became `1`, with no exit result yet; shadow/loose remained at two closed trades (`1W/1L`, `+53.9706 KRW`) and two open. This is the first direct same-signal A/B entry divergence; do not judge the guard until ETHFI settles.

At `2026-09-12T23:30:21.668Z`, v66 remained active at cycle `280` with strict `KRW-ETHFI` still open (entry `963`, highest observed `965`, no realized exit) and winner-shadow still at two cumulative ceiling blocks with no entry. Relaxed shadow/loose remained at two closed trades (`1W/1L`, `+53.9706 KRW`) and two open diagnostics. Observer `3153` returned the same non-orphaned session, continuity true, eligibility false; analysis remained complete `20/20` and risk failure/outage `0/0`. The old session's confirmation telemetry is empty by schema, so do not infer its delayed outcome from candidate counts.

At `2026-09-12T23:34:35.542Z`, v66 cycle `312` remained active with strict `KRW-ETHFI` open at entry `963` and highest observed `968`, still without an exit. Winner-shadow had two cumulative ceiling blocks and no entry. Relaxed shadow/loose remained at three closed trades (`1W/2L`, `+24.8748 KRW`) and two open diagnostics. Observer was same session, active, non-orphaned, continuity true, eligibility false; analysis `20/20`, risk failure/outage `0/0`, interruptions `0`. The old session's `entryConfirmation*` telemetry remains unavailable; preserve all unsettled positions.

At `2026-09-12T23:36:42.833Z`, v66 cycle `328` still held strict `KRW-ETHFI` at entry `963`/high `968` with no exit; winner-shadow remained at two ceiling blocks and no entry. Relaxed shadow/loose remained at three closed trades (`1W/2L`, `+24.8748 KRW`) and two open diagnostics. Observer returned the same active/non-orphaned session with heartbeat age `2.3s`, continuity true, eligibility false, strict open `1`, analysis `20/20`, and risk failure/outage `0/0`; no new entry confirmation telemetry was available for this legacy session.

At `2026-09-12T23:37:46.276Z`, v66 cycle `336` still held strict `KRW-ETHFI` at entry `963`/high `968`; the winner-shadow had two cumulative ceiling blocks and no entry. Relaxed shadow/loose remained at three closed trades (`1W/2L`, `+24.8748 KRW`) and two open diagnostics. Observer `3153` returned the same active/non-orphaned session at cycle `336`, heartbeat age `52.3s`, continuity true, eligibility false, strict open `1`, analysis `20/20`, and risk failure/outage `0/0`. Preserve all unsettled positions; legacy entry-confirmation fields remain unavailable.

At `2026-09-12T23:33:32.097Z`, v66 strict `KRW-ETHFI` remained open at entry `963` with highest observed `968`, while winner-shadow continued to exclude the same `.4171%` rebound signal above its `.4%` ceiling. Relaxed shadow/loose settled `KRW-SOPH` at `MAX_HOLD_TIME`, net `-29.0958 KRW` / `-0.1455%`, with rejection reasons volume failure, close-strength failure, and high-break failure. Relaxed totals became `3 trades / 1W2L / +24.8748 KRW`, with two open positions; strict/winner remained no closed trades. The high-break rejection cohort remains two trades (`VTHO win`, `STORJ loss`) and is not actionable.

At `2026-09-12T23:39:53.136Z`, v66 cycle `352` remained active with strict `KRW-ETHFI` open at entry `963`/high `968` and no exit. Winner-shadow remained at two ceiling blocks/no entry; relaxed shadow/loose remained at three closed trades (`1W/2L`, `+24.8748 KRW`) and two open. Observer reported the same active/non-orphaned session, continuity true, eligibility false, analysis `20/20`, and risk failure/outage `0/0`; the legacy session still has no entry-confirmation telemetry.

At `2026-09-12T23:42:00.264Z`, v66 cycle `368` continued to hold strict `KRW-ETHFI` at entry `963` with no exit, while relaxed `KRW-IOST` settled at `MAX_HOLD_TIME`, entry `1.22122`, exit `1.22122` (cost-adjusted), net `-59.9151 KRW` / `-0.2996%`, with `volume_confirmation_failed` as its rejection reason. Relaxed shadow/loose totals became `4 trades / 1W3L / -35.0403 KRW` with one open; winner-shadow remained empty with two ceiling blocks. Analysis/risk continuity remained healthy across 20 markets. This adds a negative volume-rejection outcome; no filter was loosened.

Added blocked-signal counterfactual tracking for future same-signal A/B sessions. Unique winner-shadow ceiling blocks are stored in `winnerShadow.blockedEntries`; when strict closes the same coin/signal key, the sidecar outcome is settled with its own cost-adjusted entry, round-trip fee, and adverse slippage. Status/UI expose blocked count, settled count, counterfactual profit, PF, and recent blocked entries while keeping the result diagnostic-only. Tests now pass `151/151`.

At `2026-09-12T23:48:21.126Z`, v66 observer/API reported cycle `416`, strict `KRW-ETHFI` still open with no closed trade, winner-shadow two ceiling blocks/no entry, and relaxed shadow/loose four closed trades (`1W/3L`, `-35.0403 KRW`) with one open diagnostic position. The session remained active/non-orphaned, heartbeat age `17.2s`, continuity true, eligibility false. Since v66 predates blocked-entry persistence, ETHFI's counterfactual must be reconstructed after its strict exit rather than inferred from the old ledger.

At `2026-09-12T23:52:35.019Z`, v66 cycle `448` remained active; strict `KRW-ETHFI` was still open at entry `963` with no exit, winner-shadow had two ceiling blocks/no entry, and relaxed shadow/loose remained at four closed trades (`1W/3L`, `-35.0403 KRW`) with one open. The strict position was approaching its 30-minute max-hold boundary, but no exit event had occurred; preserve it for actual settlement and reconstruct its legacy counterfactual afterward.

At `2026-09-12T23:56:48.363Z`, v66 cycle `480` settled strict `KRW-ETHFI` at `MAX_HOLD_TIME`: entry `963`, exit market price `969`, strict net profit `+104.4960 KRW` / `+0.5227%`. The same signal's unfiltered relaxed shadow comparator settled at sidecar-cost-adjusted entry `963.963`, exit `968.031`, net `+64.3222 KRW` / `+0.3216%`; winner-shadow had blocked it because rebound `0.4171%` exceeded ceiling `0.4%`. This is a one-signal false negative for the ceiling, not a promotion or guard-rejection decision; the old v66 ledger did not persist blockedEntries, so the shadow close is the reconstructed counterfactual surrogate.

After ETHFI settlement, v66 had no strict or diagnostic open positions and no temporary ledger. It was stopped cleanly at `2026-09-12T23:59:00.946Z` and preserved before starting the next clean instrumented session. The final relaxed comparator totals were `5 closed trades`, `+29.2819 KRW` with no open positions; strict closed count was `1` at `+104.4960 KRW`, and winner-shadow remained `0` actual entries with `2` ceiling blocks.

The v66 strict ETHFI close and the relaxed shadow ETHFI close form a reconstructable same-signal counterfactual: strict used entry `963` and exit `969` for net `+104.4960 KRW` / `+0.5227%`; the relaxed shadow used sidecar-cost entry `963.963`, adverse-slippage exit `968.031`, and net `+64.3222 KRW` / `+0.3216%`. The winner-shadow ceiling would have skipped this profitable signal, so `maxRebound=0.4%` has one observed false negative. This single sample is insufficient to discard or promote the guard.

Started clean instrumented v67 at `2026-09-13T00:00:18.338Z` in `.paper-forward-v67-instrumented` using the prior v66 freshness cohort: 12 markets (`LSK,VTHO,FLOCK,XRP,ETH,SOPH,TREE,MTL,BTC,SC,SOL,STORJ`). Strict max rebound is `0`; winner-shadow ceiling is `.4`, winner extension `0`. At cycle `11` (`00:01:23.123Z`), config/experiment snapshot was complete and consistent, analysis `12/12`, no risk/analysis failures, no interruptions, no entries, and `entryConfirmationAttempts/Succeeded/Cancelled=0/0/0`. Observer `3153` was switched to v67; CUA Validation ledger UI visibly showed the new revalidation `0/0` summary and A/B `rebound ceiling 0.40% · 차단 0회 · 정산 0회 · 회피 손익 +0원` line. No live/user state changed.

At `2026-09-13T00:03:32.740Z`, v67 owner PID `33915` remained active at cycle `31`, with heartbeat age `28.0s`, analysis `12/12`, continuity true, strict/winner/shadow/loose all empty, and `entryConfirmationAttempts/Succeeded/Cancelled=0/0/0`. Observer `3153` returned the same active/non-orphaned session and eligibility false. Updated the UI A/B label to use unique blocked signal count (`blockedEntryCount`) rather than repeated-cycle block events; static HTML/JS/manifest/diff checks pass. v67 remains the sole runner and no live/user state changed.

At `2026-09-13T00:05:36.685Z`, v67 cycle `49` produced its first execution-quality sample: `KRW-TREE` generated a strict BUY candidate with rebound `0.8446%`; winner-shadow stored one unique blocked entry because it exceeded the `.4%` ceiling. The strict delayed confirmation attempted once and cancelled once with reason `반등 되밀림 0.84% > 허용 0.25%`; no strict fill occurred. One relaxed shadow position was open, while strict/winner closed counts remained zero. Analysis/risk continuity remained complete/healthy across 12 markets.

The subsequent CUA Validation ledger UI readback showed `지연 재검증 0/1 성공 · 취소 1 (반등 되밀림 0.84% > 허용 0.25%)` and `winner shadow: rebound ceiling 0.40% A/B · 차단 1회 · 정산 0회 · 회피 손익 +0원 · strict 승격 제외`, confirming the new telemetry is visible in the web/installable shell. No live or user portfolio state changed.

At `2026-09-13T00:07:36.871Z`, v67 cycle `67` remained active under owner PID `33915`; the TREE blocked entry was still pending with no strict fill or counterfactual settlement. Relaxed shadow/loose had three open entries and no closes. Entry confirmation telemetry remained `1 attempt / 0 success / 1 cancel` with the same retrace reason; winner-shadow had one unique blocked entry, zero settled, and zero counterfactual profit. Analysis/risk continuity remained complete/healthy across 12 markets, with no interruptions.

The live observer UI later reproduced a stale-screen boundary: API `3153` had already switched to v69 while a long-lived Chrome tab still displayed v67. Cache-bust navigation refreshed it to v69. The redesign had a 10-second foreground-only poll but no `visibilitychange` refresh, and the cache-first PWA shell reused the same JS asset URL. The source now refreshes on visibility return; index asset version is `observer-readonly-5` and service-worker cache is `coinpilot-shell-v9`. `test/pilotRedesignRefresh.test.js` covers both the lifecycle listener and index/service-worker version alignment; both tests failed before their fixes and pass afterward.

Browser verification at `localhost:3153/?v=16&cache=observer-readonly-5` visibly showed v69 start `09:29`, read-only observer banner, validation `0/3`, and `winner shadow: ... 차단 0회 · 미체결 0회 · 대기 0회 · 정산 0회`. Foreground polling advanced the displayed unique-window count `228→240`. No mutation or install action was performed. Latest full suite is `154/154 PASS`; JavaScript/inline HTML/manifest/service-worker/HTTP/diff checks pass.

v69 remains the active DRY_RUN-only paper owner: session `paper-1789259359366`, PID `40512`, lock matched. At `2026-09-13T00:51:25.067Z` it had cycle `205`, analysis `12/12`, incomplete/missing `0/0`, risk failures/outages `0/0`, continuity true, strict `0` trades/open, winner-shadow `0`, and one relaxed FLOCK shadow/loose `TAKE_PROFIT` close at `+338.8866 KRW` (`+1.6944%`). That candidate carried trend and previous-high-break rejection reasons, so it is diagnostic-only and does not justify filter relaxation.

At `2026-09-13T00:53:08.939Z`, v69 owner PID `40512` and lock remained matched at cycle `221`. Strict and winner-shadow remained at zero trades/open; shadow and loose each retained the single FLOCK `TAKE_PROFIT` close at `+338.8866 KRW` with no open diagnostic positions. Availability was oversold/rebound/confirmed BUY `37/9/0` across `299` unique windows, analysis stayed complete `12/12`, risk failures/outages stayed `0/0`, and continuity remained true. Keep the FLOCK result diagnostic-only and do not infer a strict edge from it.

At `2026-09-13T00:58:14.318Z`, v69 owner PID `40512` and lock remained matched at cycle `267`. Strict and winner-shadow still had zero trades/open; shadow and loose each retained the FLOCK `TAKE_PROFIT` close at `+338.8866 KRW` and two open diagnostics, `KRW-SOPH` and `KRW-FLOCK`. Availability was oversold/rebound/confirmed BUY `75/27/0` across `360` unique windows, analysis stayed complete `12/12`, risk failures/outages stayed `0/0`, and continuity remained true. Keep both relaxed positions through real risk exits and do not use the single relaxed win for strict promotion.

At `2026-09-13T00:57:41.589Z`, v69 owner PID `40512` and lock remained matched at cycle `258`. Strict and winner-shadow still had zero trades/open; shadow and loose each retained the FLOCK `TAKE_PROFIT` close at `+338.8866 KRW` and two open diagnostic positions, `KRW-SOPH` from signal `00:53` and `KRW-FLOCK` from signal `00:55`. Availability was oversold/rebound/confirmed BUY `75/27/0` across `347` unique windows, analysis stayed complete `12/12`, risk failures/outages stayed `0/0`, and continuity remained true. Keep both diagnostic positions through real risk exits and do not use the relaxed winner as strict promotion evidence.

At `2026-09-13T01:00:15.497Z`, v69's second relaxed `KRW-FLOCK` entry from signal `00:55` stopped out at net `-450.8971 KRW` (`-2.2545%`). The signal had rebound `1.0%`, volume `1.6571x`, close strength `0.5`, trend `-0.4200%`, and failed close-strength, trend, and previous-high checks. Shadow and loose each reached `2` closed trades (`1W/1L`, realized `-112.0105 KRW`) and retained open `KRW-SOPH`, `KRW-STORJ`, and `KRW-SC` diagnostics. Strict/winner remained empty; the mixed pair does not justify filter tuning.

At `2026-09-13T01:03:20.967Z`, `KRW-STORJ` stopped out in both relaxed books at net `-281.4716 KRW` (`-1.4074%`) after volume, trend, and previous-high rejection. Shadow and loose each reached `4` closed trades (`1W/3L`, realized `-764.4750 KRW`); `KRW-SOPH` then settled as a loss and by `01:04:36.865Z` only `KRW-SC` remained open. Strict/winner stayed at zero trades/open. The rejected cohort remains small but consistently argues for retaining the current high-break/trend/volume contract while SC completes its risk exit.

At `2026-09-13T01:05:14.907Z`, v69 owner PID `40512` and lock remained matched at cycle `330`. Strict and winner-shadow stayed at zero trades/open. Shadow and loose each had `4` closed trades (`1W/3L`, realized `-764.4750 KRW`) with only `KRW-SC` still open. Availability was oversold/rebound/confirmed BUY `159/66/0` across `444` unique windows; analysis `12/12`, risk failures/outages `0/0`, and continuity remained true. Preserve SC through its actual risk exit and keep the rejected-filter contract unchanged.

At `2026-09-13T01:06:15.002Z`, v69 owner PID `40512` and lock remained matched at cycle `339`. Strict and winner-shadow stayed at zero trades/open; shadow and loose each retained `4` closed trades (`1W/3L`, `-764.4750 KRW`) with only `KRW-SC` open. Availability was oversold/rebound/confirmed BUY `177/78/0` across `454` unique windows, analysis `12/12`, risk failures/outages `0/0`, and continuity remained true. Keep SC through its actual risk exit; no strict promotion or filter change is justified.

At `2026-09-13T01:07:15.074Z`, v69 owner PID `40512` and lock remained matched at cycle `348`. Strict and winner-shadow stayed at zero trades/open; shadow and loose each retained `4` closed trades (`1W/3L`, `-764.4750 KRW`) with only `KRW-SC` still open. Availability was oversold/rebound/confirmed BUY `195/94/0` across `466` unique windows, analysis `12/12`, risk failures/outages `0/0`, and continuity remained true. Preserve SC through its actual risk exit and do not promote or change filters.

At `2026-09-13T01:09:15.257Z`, v69 owner PID `40512` and lock remained matched at cycle `366`. Strict and winner-shadow stayed at zero trades/open; shadow and loose each retained `4` closed trades (`1W/3L`, `-764.4750 KRW`) with only `KRW-SC` open. Availability was oversold/rebound/confirmed BUY `219/109/0` across `491` unique windows, analysis `12/12`, risk failures/outages `0/0`, and continuity remained true. Keep SC through its actual risk exit; no filter change or promotion is justified.

v69 final state at `2026-09-13T01:11:11.658Z`: after controlled SIGTERM, the ledger recorded `endedAt=01:10:55.856Z`, `stopReason=stopped_cleanly`, cycle `381`, no strict or diagnostic open positions, no interruptions, and healthy analysis/risk continuity. Strict closed `0` trades; shadow and loose each closed `5` relaxed trades (`1W/4L`) with realized `-1,133.5387 KRW`. Preserve this as negative relaxed/no-strict baseline evidence and do not promote it.

v70 started at `2026-09-13T01:12:37.839Z` in `.paper-forward-v70-wide` as a diagnostic universe test, not a parameter relaxation. It resolves the current top `20` non-stable KRW markets, keeps strict `maxReboundPercent=0` and winner-shadow ceiling `0.4%`, and uses a `300ms` minimum Upbit request interval with a `15s` cycle interval. The first persisted cycle at `01:12:44.129Z` completed all `20/20` analyses with no incomplete/missing data, no risk failure, no interruption, empty strict/winner/shadow/loose books, and `oversold=3` but no rebound/confirmed BUY. Observer port `3153` now reads the v70 ledger read-only; no live/user state changed.

At `2026-09-13T01:15:26.871Z`, v70 reached cycle `7` with complete `20/20` analysis, risk failures/outages `0/0`, no interruptions, and continuity true. Strict and winner-shadow remained empty, while relaxed shadow/loose opened `KRW-CP` and `KRW-SC` with no closed diagnostics yet. Availability was oversold/rebound/confirmed BUY `24/4/0` over `60` unique windows. This is the intended wide-universe opportunity observation; preserve both diagnostic positions through risk exits and do not infer strict profitability.

At `2026-09-13T01:16:10.895Z`, v70 reached cycle `10` with complete analysis for all `20/20` markets and healthy risk continuity. `KRW-STORJ` stopped out in both relaxed books at net `-325.7829 KRW` (`-1.6289%`) after RSI recovery, volume, close-strength, trend, and previous-high rejection. CP, SC, and PUNDIX remained diagnostic-open; strict/winner stayed empty. Availability was oversold/rebound/confirmed BUY `39/9/0` across `95` unique windows. Preserve the wide diagnostic positions and do not infer strict profitability.

At `2026-09-13T01:18:07.479Z`, v70's CP and SC diagnostics also stopped out at net `-371.4789 KRW` and `-381.5294 KRW`. Shadow and loose each reached `3` closed trades (`0W/3L`, realized `-1,078.7912 KRW`) with only `KRW-PUNDIX` open. Strict/winner remained empty; all `20/20` analyses and risk continuity were healthy, with availability oversold/rebound/confirmed BUY `51/11/0` over `119` unique windows. The wide universe increases candidate opportunity but has not yielded strict confirmation; preserve PUNDIX and keep the current filters.

At `2026-09-13T01:18:32.091Z`, v70 owner PID `52171` and lock remained matched at cycle `16`. Strict and winner-shadow were still empty; shadow and loose each retained `3` closed trades (`0W/3L`, `-1,078.7912 KRW`) with only `KRW-PUNDIX` open. Availability was oversold/rebound/confirmed BUY `62/13/0` across `139` unique windows, analysis `20/20`, risk failures/outages `0/0`, and continuity true. Keep PUNDIX through its actual risk exit and do not alter the strict contract.

At `2026-09-13T01:19:42.623Z`, v70 owner PID `52171` and lock remained matched at cycle `19`. Strict and winner-shadow stayed at zero trades/open; shadow and loose each retained `3` closed trades (`0W/3L`, `-1,078.7912 KRW`) with open `KRW-PUNDIX` and `KRW-BFC` diagnostics. Availability was oversold/rebound/confirmed BUY `74/17/0` across `160` unique windows, analysis `20/20`, risk failures/outages `0/0`, and continuity remained true. Preserve both diagnostics through real exits; the wide universe has not yielded a strict confirmation.

At `2026-09-13T01:24:24.995Z`, v70 owner PID `52171` and lock remained matched at cycle `31`. PUNDIX had settled via `TAKE_PROFIT` at net `+505.7611 KRW`; shadow and loose each reached `4` closed trades (`1W/3L`, `-573.0301 KRW`) with only `KRW-BFC` open. The PUNDIX signal was rejected for volume and previous-high-break despite winning, so it does not support filter relaxation. Strict/winner remained empty; availability was oversold/rebound/confirmed BUY `116/32/0` across `256` unique windows, analysis `20/20`, risk failures/outages `0/0`, and continuity true. Preserve BFC through actual exit.

At `2026-09-13T01:26:46.183Z`, v70 owner PID `52171` and lock remained matched at cycle `37`. Strict and winner-shadow remained empty; shadow and loose each had `4` closed trades (`1W/3L`, `-573.0301 KRW`) with only `KRW-BFC` open. Availability was oversold/rebound/confirmed BUY `123/33/0` across `297` unique windows, analysis `20/20`, risk failures/outages `0/0`, and continuity remained true. Keep BFC through its real risk exit; no filter or live-gate change is justified.

At `2026-09-13T01:27:56.848Z`, v70 owner PID `52171` and lock remained matched at cycle `40`. PUNDIX had settled via `TAKE_PROFIT` at `+505.7611 KRW`; shadow and loose each retained `4` closed trades (`1W/3L`, `-573.0301 KRW`) with only `KRW-BFC` open. Availability was oversold/rebound/confirmed BUY `129/36/0` across `316` unique windows, analysis `20/20`, risk failures/outages `0/0`, and continuity remained true. Preserve BFC through actual exit; no filter or live-gate change is justified.

At `2026-09-13T01:29:07.380Z`, v70 owner PID `52171` and lock remained matched at cycle `43`. Strict and winner-shadow remained empty; shadow and loose each retained `4` closed trades (`1W/3L`, `-573.0301 KRW`) with only `KRW-BFC` open. Availability was oversold/rebound/confirmed BUY `135/36/0` across `344` unique windows, analysis `20/20`, risk failures/outages `0/0`, and continuity remained true. Preserve BFC through actual exit; no strict or filter promotion is justified.

At `2026-09-13T01:33:49.761Z`, the v70 wide diagnostic `KRW-FLOCK` entry from signal `01:30` stopped out at net `-447.1012 KRW` (`-2.2355%`) with volume confirmation failure. Shadow and loose each reached `5` closed trades (`1W/4L`, `-1,020.1313 KRW`) and retained open `KRW-BFC` and `KRW-SOPH`; strict and winner-shadow remained empty. Availability was oversold/rebound/confirmed BUY `152/43/0` across `434` unique windows, analysis `20/20`, risk failures/outages `0/0`, and continuity true. Keep volume and other filters unchanged while diagnostic positions settle.

At `2026-09-13T01:35:00.436Z`, v70 owner PID `52171` and lock remained matched at cycle `58`. Strict and winner-shadow stayed empty; shadow and loose each retained `5` closed trades (`1W/4L`, `-1,020.1313 KRW`) with open `KRW-BFC` and `KRW-SOPH`. Availability was oversold/rebound/confirmed BUY `158/43/0` across `454` unique windows, analysis `20/20`, risk failures/outages `0/0`, and continuity remained true. Continue risk observation without filter or strict-gate changes.

At `2026-09-13T01:37:21.620Z`, v70 owner PID `52171` and lock remained matched at cycle `64`. Strict and winner-shadow stayed empty; shadow and loose each retained `5` closed trades (`1W/4L`, `-1,020.1313 KRW`) with open `KRW-BFC`, `KRW-SOPH`, and `KRW-CP` diagnostics. Availability was oversold/rebound/confirmed BUY `169/47/0` across `512` unique windows, analysis `20/20`, risk failures/outages `0/0`, and continuity remained true. Preserve all three diagnostics through actual exits; no strict confirmation or filter promotion is justified.

At `2026-09-13T01:39:42.828Z`, v70 owner PID `52171` and lock remained matched at cycle `70`. Strict and winner-shadow stayed empty; shadow and loose each retained `5` closed trades (`1W/4L`, `-1,020.1313 KRW`) with open `KRW-BFC`, `KRW-SOPH`, and `KRW-CP` diagnostics. Availability was oversold/rebound/confirmed BUY `187/52/0` across `553` unique windows, analysis `20/20`, risk failures/outages `0/0`, and continuity remained true. Keep all diagnostics through their real exits; no strict or filter promotion is justified.

At `2026-09-13T01:42:04.018Z`, v70 owner PID `52171` and lock remained matched at cycle `76`. Strict and winner-shadow stayed empty; shadow and loose each retained `5` closed trades (`1W/4L`, `-1,020.1313 KRW`) with open `KRW-BFC`, `KRW-SOPH`, and `KRW-CP` diagnostics. Availability was oversold/rebound/confirmed BUY `203/57/0` across `595` unique windows, analysis `20/20`, risk failures/outages `0/0`, and continuity remained true. Preserve all diagnostic opens through actual risk exits; no strict or filter promotion is justified.

At `2026-09-13T01:30:18.039Z`, v70 owner PID `52171` and lock remained matched at cycle `46`. Strict and winner-shadow remained empty; shadow and loose each retained `4` closed trades (`1W/3L`, `-573.0301 KRW`) with open `KRW-BFC` and `KRW-SOPH` diagnostics. Availability was oversold/rebound/confirmed BUY `140/38/0` across `371` unique windows, analysis `20/20`, risk failures/outages `0/0`, and continuity remained true. Preserve both diagnostics through actual exit; no strict or filter promotion is justified.

v70 final state at `2026-09-13T01:45:14.654Z`: controlled SIGTERM preserved the wide-universe ledger at cycle `84`. Strict remained at zero trades/open positions; shadow and loose each had `5` closed trades (`1W/4L`) and realized `-1,020.1313 KRW`, while BFC/SOPH/CP diagnostic positions were still open at shutdown. The application ledger correctly recorded `endedWithDiagnosticOpenPositions=true` and `stopReason=stopped_with_unsettled_diagnostic_positions`; interruptions remained `0`, all `20/20` analyses completed, and risk/analysis continuity stayed healthy. The session is preserved and is not resumed or mixed into a new ledger.

The `requireReboundBelowOverbought` guard was added to the research variant catalog without changing runtime defaults. On the same fresh 7-day/1-minute BTC/ETH/XRP window at `2026-09-13T01:52:24.077Z`, baseline produced `9` holdout trades / `-0.0131%`, while the guard produced `8` / `-0.0044%` and improved BTC to `+0.0164%`/PF `6.08`. All three training gates still failed and the global result remained `0/3`; keep it diagnostic-only. The exact window and report are `/tmp/coinpilot-rebound-overbought-window-20260913.json` and `/tmp/coinpilot-rebound-overbought-7d-20260913.json`.

v71 default latest-code forward paper started at `2026-09-13T01:49:21.149Z` in `.paper-forward-v71-default` with top `20` non-stable KRW markets, a `300ms` Upbit request interval, and the unchanged strict runtime contract. At cycle `10`/`01:52:36.944Z`, all `20/20` analyses completed with no missing/incomplete markets, no risk failures/outages, continuity eligible, and strict/shadow/loose/winner books empty. This is operational startup evidence only; no live order or user portfolio mutation occurred.

Shared-portfolio 3-fold validation on the exact 7-day/1-minute BTC/ETH/XRP cache produced baseline holdout `16` trades / `-0.0642%` with `0/3` folds promoted. Enabling `requireReboundBelowOverbought` reduced the result to `15` trades / `-0.0555%`, but still produced `0/3` promoted folds because every fold failed the training confidence gate. Keep the guard research-only; reports are `/tmp/coinpilot-portfolio-baseline-7d-20260913.json` and `/tmp/coinpilot-portfolio-overbought-7d-20260913.json`.

v71 follow-up at `2026-09-13T01:59:08.585Z`: owner PID `61371` remained active at cycle `28`. Strict and winner-shadow had zero trades/open; shadow and loose each held one `KRW-RAY` diagnostic position with no close. Strict confirmed/BUY remained `0/0`, unique windows `207`, analysis `20/20`, risk/analysis continuity true, and interruptions `0`. Preserve RAY through its actual risk exit; no runtime parameter or live-gate change is justified.

v71 first relaxed settlement at `2026-09-13T02:01:36.009Z`: KRW-RAY stopped out in both shadow and loose at entry `2,073.071` → exit `2,043.954`, net `-300.6211 KRW` / `-1.5031%`. The signal had volume `1.1101x` and close strength `1`, but failed `trend_filter_failed` and `previous_high_break_failed`. At cycle `37`/`02:02:32.908Z`, strict remained at zero trades/open and both diagnostic books had one loss/no open, while all `20/20` analyses and risk/analysis continuity remained healthy. This is additional negative rejected-cohort evidence, not a reason to relax filters or promote a live configuration.
The latest exact 7-day/1-minute BTC/ETH/XRP cache was also run through fixed-config validation at `2026-09-13T02:05:05.016Z`. The generated report was saved as `scalping_validation.json` after backing up the prior ignored report to `/tmp/coinpilot-validation-before-20260913.json`. It remains `0/3` promoted with statistical confidence required but failed: BTC holdout `+0.0077%`/3 trades/PF `1.64`/lower `-1.1426%`, ETH `-0.0213%`/3/PF `0`/lower `-0.6942%`, XRP `+0.0005%`/3/PF `1.10`/lower `-0.4044%`; every training confidence gate failed. The observer UI now displays the refreshed report date and holdout sample `9`.
v71 follow-up at `2026-09-13T02:04:39.222Z`: owner PID `61371` and lock remained active at cycle `43`; strict/winner were zero trades/open, shadow/loose each held one settled KRW-RAY loss and no open position, strict confirmed/BUY `0/0`, unique windows `312`, analysis `20/20`, risk/analysis continuity true, and interruptions `0`. Keep v71 active for additional strict samples; no runtime or live-gate change is justified.
At `2026-09-13T02:10:58.176Z`, v71 recovered from a roughly one-minute network retry interval: the owner stayed alive, cycle advanced `58→61`, heartbeat resumed, and the completed analysis remained `20/20` with continuity eligible. Strict/winner stayed at zero trades/open; shadow/loose retained only the settled RAY loss (`-300.6211 KRW`) and no diagnostic open. The finite timeout/retry behavior prevented a restart or corrupted ledger; this is operational resilience evidence, not profitability evidence.
At `2026-09-13T02:13:34.010Z`, v71's relaxed shadow/loose books recorded an LSK `TAKE_PROFIT` of `+325.7732 KRW` followed by the next LSK entry's `STOP_LOSS` of `-286.9369 KRW`. The second signal had `+1.3462%` execution drift and failed trend/high-break, while the strict delayed entry would be subject to the `0.35%` chase limit. With the earlier RAY loss, both diagnostic books were at `3` closes (`1W/2L`) / `-261.7848 KRW`; B3 and TREE remained open. Strict stayed at zero trades/open and confirmed BUY `0`. Keep delayed revalidation/chase protection and preserve B3/TREE until risk exit.
At `2026-09-13T02:19:02.633Z`, v71 cycle `82` remained active with strict/winner at zero trades/open and shadow/loose at three closes (`1W/2L`, `-261.7848 KRW`) plus open B3/TREE diagnostics. B3 carries trend/high-break rejection and TREE carries volume rejection. Analysis `20/20`, risk/analysis continuity true, interruption `0`; keep both open until actual risk exit and do not use relaxed P&L as strict evidence.
The redesigned paper card now renders separate shadow and loose diagnostic aggregate cards (closed trades, realized profit, win rate, PF, active positions) under an explicit `strict 승격 제외` label. Redesign assets were versioned to JS `observer-readonly-6`, CSS `20260913-8`, and service-worker cache `coinpilot-shell-v10` so installed/returning PWA clients do not reuse the prior shell. Desktop CUA readback displayed the live v71 values; a 390px mobile render displayed the two cards as a single-column stack with document/body width `390px` and no horizontal overflow. The new UI contract test and static/HTTP checks pass; no trading logic, ledger, or live gate changed.
At `2026-09-13T02:24:55.688Z`, v71 cycle `100` remained active: strict/winner zero trades/open, shadow `4` closes / `+85.2406 KRW` with RAY/TREE open, loose `4` closes / `+85.2406 KRW` with ETH/RAY/TREE open, confirmed BUY `0`, analysis/risk continuity true, interruption `0`. The temporary positive diagnostic P&L is unsettled small-sample evidence and is not profitability proof.
At `2026-09-13T02:29:38.079Z`, TREE stopped out for `-298.9230 KRW`; shadow/loose therefore moved from temporary `+85.2406 KRW` to `-213.6824 KRW` across `5` closed trades (`1W/4L`). Shadow retains RAY and loose retains ETH/RAY. Strict remains at zero trades/open/confirmed BUY, cycle `109`, analysis/risk continuity true, interruption `0`. This validates the UI and evidence rule that open diagnostic P&L cannot be promoted as profitability.
At `2026-09-13T02:31:59.204Z`, v71 cycle `115` remained active with shadow/loose still `5` closed (`1W/4L`) / `-213.6824 KRW`; shadow held RAY/FLOCK and loose held ETH/RAY/FLOCK. Strict/winner remained zero trades/open/confirmed BUY, analysis `20/20`, risk/analysis continuity true, interruption `0`. The new UI diagnostic cards expose these active-position counts; preserve all relaxed opens before finalizing this evidence window.
At `2026-09-13T02:34:56.648Z`, the second KRW-RAY relaxed entry stopped out for `-286.7311 KRW` with volume/trend/previous-high rejection. Shadow/loose reached `6` closes (`2W/4L`) / `-500.4134 KRW`; rejection outcome samples now show trend/high-break `5` trades (`2W/3L`, `-201.4905 KRW`, PF `0.77`) and volume `2` losses (`-585.6540 KRW`). The read-only recommendation keeps both filters and prohibits loosening. FLOCK/SOL or ETH/FLOCK remain open and strict remains zero; do not promote this relaxed result.
At `2026-09-13T02:36:41.512Z`, v71 cycle `127` remained active: strict/winner zero trades/open/confirmed BUY, shadow/loose each `6` closes (`2W/4L`) / `-500.4134 KRW`, shadow FLOCK/SOL open and loose BTC/ETH/FLOCK open. Analysis `20/20`, risk/analysis continuity true, interruption `0`; keep these diagnostic positions through risk exits and do not change runtime filters.
At `2026-09-13T02:37:52.052Z`, v71 cycle `130` remained active with strict/winner zero trades/open/confirmed BUY. Shadow/loose stayed at `6` closes (`2W/4L`) / `-500.4134 KRW`; shadow held FLOCK/PROM/SOL and loose held BTC/ETH/FLOCK. Analysis `20/20`, risk/analysis continuity true, interruption `0`; preserve the unfinished diagnostic books and keep runtime filters unchanged.
At `2026-09-13T02:40:19.203Z`, v71 produced its first strict paper entry: KRW-SOPH opened at `6.15` after a `3,026ms` delayed revalidation. The signal passed with rebound `0.3263%`, RSI `31.5618→37.6416`, volume `2.7505x`, close strength `1`, trend slope `-0.1286%`, signal range `0.4894%`, and zero execution drift. The strict-open snapshot is persisted; no realized profit exists yet. At cycle `136`, highest price was `6.17` and break-even/trailing remained unarmed. Keep SOPH until its risk exit; this is unrealized evidence only.
At `2026-09-13T02:42:44.381Z`, the same strict SOPH position remained open at cycle `142` with high `6.19`. Entry confirmation telemetry is `1 attempt / 1 success / 0 cancelled`, execution drift `0`; break-even/trailing remain disabled by the default configuration and no strict P&L is realized. Shadow/loose stay at six closes / `-500.4134 KRW` with open relaxed positions. Preserve SOPH until a real risk exit.
At `2026-09-13T02:43:57.098Z`, SOPH remained the single open strict position at cycle `145`, high `6.19`, confirmation `1/1/0`, with no realized strict close. Shadow/loose remained at six closes / `-500.4134 KRW` with relaxed opens, while analysis/risk continuity stayed healthy. Preserve strict and diagnostic positions through actual risk exits.
At `2026-09-13T02:46:23.343Z`, cycle `151` kept SOPH as the sole strict open at entry `6.15`/high `6.19`; confirmation remained `1/1/0`, drift `0`, no close, and break-even/trailing unarmed. Shadow/loose remained six closes / `-500.4134 KRW` with relaxed opens, analysis/risk continuity true, interruption `0`. Preserve the first strict sample through its risk exit.
At `2026-09-13T02:48:48.843Z`, cycle `157` kept SOPH open roughly ten minutes after entry at `6.15`/high `6.19`, confirmation `1/1/0`, drift `0`, with no strict close and protections unarmed. Shadow/loose each reached `7` closes (`2W/5L`) / `-951.3106 KRW` with diagnostic opens; analysis/risk continuity true and interruption `0`. Preserve SOPH through risk exit and do not use open P&L as profit evidence.
At `2026-09-13T02:47:23.651Z`, relaxed FLOCK stopped out at `-450.8971 KRW` / `-2.2545%` with volume `0.9277x` and previous-high rejection. Shadow/loose reached `7` closes (`2W/5L`) / `-951.3106 KRW`, with two open diagnostics per book; strict SOPH remains the only strict open with no realized P&L. Keep volume/high-break filters and do not promote relaxed P&L.
At `2026-09-13T02:51:48.023Z`, the first strict trade KRW-SOPH closed by `STOP_LOSS` (`손절 실행 (-1.30% 하락)`), entry `6.15` → exit `6.07`, net `-279.8925 KRW` / `-1.4002%`, total fee `19.8600 KRW`, confirmation delay `3,026ms`, execution drift `0%`. Strict now has one closed loss, no open position, and confidence `1/20` with no lower bound; this is a first-sample loss, not a promotion decision. Shadow/loose remain seven closes (`2W/5L`) / `-951.3106 KRW` with diagnostic opens.
The post-close 390px validation-page render at approximately `2026-09-13T02:52Z` shows current assets `999,720 KRW`, one strict close `-280원`, strict active `0`, latest strict SOPH `-1.40%`, confidence `1/20 보류`, and separate shadow/loose `-951원`/PF `0.41` cards with no horizontal overflow. The strict/diagnostic UI contract is therefore verified after a realized strict loss.
After the strict SOPH loss, strict cooldown was persisted until `2026-09-13T03:06:48.023Z` (about 12.6 minutes remaining at `02:53:14`). Shadow/loose independently reopened SOPH at `6.06606` from signal `02:52`, with rebound `1.1686%`, volume `4.3179x`, trend `-0.2478%`, and trend/high-break rejection; strict stayed flat. This is live forward evidence that strict cooldown and diagnostic re-entry are separate state machines; the diagnostic re-entry is not strict performance.
At `2026-09-13T02:54:48.015Z`, strict remained at one closed loss (`-279.8925 KRW`) with no open position and cooldown active. Shadow reached `7` closes (`2W/5L`) / `-951.3106 KRW` with PROM/SOL/SOPH open; loose reached `8` closes (`2W/6L`) / `-1,028.6457 KRW` with BTC/SOPH open after ETH `MAX_HOLD_TIME` loss `-77.3351 KRW`. Raw strict and API aggregates agree; retain separate diagnostic accounting.
At `2026-09-13T02:57:09.223Z`, strict remained one closed loss / `-279.8925 KRW` with no open position and SOPH cooldown active. Shadow remained `7` closes (`2W/5L`) / `-951.3106 KRW` with PROM/SOL/SOPH open; loose remained `8` closes (`2W/6L`) / `-1,028.6457 KRW` with BTC/SOPH open. Analysis `20/20`, risk/analysis continuity true, interruption `0`; keep the session running without tuning or live promotion.
At `2026-09-13T02:59:30.432Z`, strict remained at one closed loss / `-279.8925 KRW`, no open position, and the SOPH cooldown was still active. Shadow/loose remained `7`/`8` closes with `-951.3106`/`-1,028.6457 KRW` and PROM/SOL/SOPH or BTC/SOPH diagnostic opens. Analysis `20/20`, risk/analysis continuity true, interruption `0`; wait for cooldown expiry and further strict samples without promotion.
At `2026-09-13T03:02:15.105Z`, SOL max-hold added `-45.5387 KRW` to shadow, which reached `8` closes (`2W/6L`) / `-996.8493 KRW` with PROM/SOPH open; loose remained `8` closes (`2W/6L`) / `-1,028.6457 KRW` with BTC/SOPH open. Strict stayed at one loss / `-279.8925 KRW`, no open, with about 4.2 minutes of SOPH cooldown remaining. Analysis/risk continuity true, interruption `0`; no promotion or filter relaxation.
At `2026-09-13T03:04:12.823Z`, strict remained one closed loss / `-279.8925 KRW`, no open, with about 2.5 minutes of SOPH cooldown remaining. Shadow/loose stayed at eight closes (`2W/6L`) / `-996.8493` and `-1,028.6457 KRW` with PROM/SOPH or BTC/SOPH diagnostics open. Analysis `20/20`, risk/analysis continuity true, interruption `0`; keep the isolated session and do not promote.
At `2026-09-13T03:06:57.481Z`, the SOPH cooldown expired without a strict re-entry. Shadow/loose each reached nine closes (`2W/7L`); shadow was `-1,109.2038 KRW` after PROM max-hold loss `-112.3545`, and loose was `-1,080.0261 KRW` after BTC max-hold loss `-51.3804`. Both retained ETHFI/SOPH diagnostics while strict stayed at one loss / `-279.8925 KRW` and flat. Keep strict revalidation/cooldown and do not promote relaxed results.
At `2026-09-13T03:10:05.744Z`, no strict re-entry occurred after cooldown expiry. Strict remained one closed loss / `-279.8925 KRW` and flat; shadow/loose remained nine closes (`2W/7L`) / `-1,109.2038` and `-1,080.0261 KRW` with ETHFI/SOPH diagnostics open. Analysis `20/20`, risk/analysis continuity true, interruption `0`; continue strict observation without promotion.
At `2026-09-13T03:23:14.656Z`, the SOPH diagnostic re-entry that occurred during strict cooldown closed by `MAX_HOLD_TIME` at `+38.7982 KRW` / `+0.1940%`. This was a different signal and separate relaxed book, so it does not offset the strict SOPH loss or justify removing cooldown. Shadow/loose each reached `10` closes (`3W/7L`) with realized `-1,070.4056`/`-1,041.2279 KRW`; B3/ETHFI remain open and strict remains one loss / `-279.8925 KRW` with no second sample.
At `2026-09-13T03:12:26.931Z`, strict remained one closed loss / `-279.8925 KRW`, flat after cooldown, with confirmation telemetry still `1/1/0`. Shadow/loose remained nine closes (`2W/7L`) / `-1,109.2038` and `-1,080.0261 KRW` with ETHFI/SOPH diagnostics open. Analysis/risk continuity true, interruption `0`; no tuning or live promotion.
At `2026-09-13T03:32:20.829Z`, relaxed B3 stopped out at `-301.4504 KRW` / `-1.5073%` despite volume `1.5585x`, with previous-high rejection. Shadow/loose reached `11` closes (`3W/8L`) / `-1,371.8560` and `-1,342.6783 KRW`; ETHFI/SOPH remained open. Rejection outcomes now show previous-high `9` trades / PF `0.41`, trend `6` / PF `0.81`, and volume `4` losses / PF `0`; retain all three filters. Strict remains one loss / `-279.8925 KRW` with no second sample.
At `2026-09-13T03:36:57.806Z`, relaxed ETHFI stopped at `MAX_HOLD_TIME`, entry `937.937` → exit `932.067`, net `-145.0382 KRW` / `-0.7252%`, with volume `0.1131x` and previous-high rejection. Shadow/loose reached `13` closes (`3W/10L`) / `-1,802.3758` and `-1,773.1981 KRW`; only SOPH diagnostic remained open. Outcome cohorts show previous-high `11` / PF `0.33`, trend `7` / PF `0.61`, and volume `5` losses / PF `0`; retain filters and keep strict evidence separate.
At `2026-09-13T03:40:41.286Z`, strict remained one closed loss / `-279.8925 KRW`, no open position and no second strict sample. Shadow/loose remained `13` closes (`3W/10L`) / `-1,802.3758` and `-1,773.1981 KRW` with only SOPH diagnostic open. Analysis/risk continuity true, interruption `0`; continue the isolated observation without tuning or live promotion.
At `2026-09-13T03:46:34.299Z`, the v71 owner stopped being observable at cycle `304` while its ledger still said `active=true`, `endedAt=null`, and had no terminal error. The read-only dashboard correctly fail-closed to `owner_process_missing`; the exact external termination cause is unknown because no process stderr/log was retained, so this is not classified as a strategy or network failure.
The same v71 ledger was resumed in place at `2026-09-13T03:54:36.442Z`, preserving a `482.139s` heartbeat interruption instead of creating a replacement evidence window. New owner PID `13975` is active with strict one loss (`-279.8925 KRW`) and shadow/loose each `13` closed trades plus three diagnostic open positions. No live orders or root portfolio mutation occurred.
Runner failure handling now records `terminalError`/`lastError` and attempts orderly paper stop and lock release for uncaught exceptions, unhandled rejections, and top-level runner failures; SIGKILL/host termination remains covered by orphan fail-closed detection. Status/API and both paper-card surfaces expose the recorded error when present. External redesign JS is included in syntax regression coverage; PWA asset versions are JS `observer-readonly-7` and service-worker cache `coinpilot-shell-v11`. Full suite is `156/156 PASS`, all JS/HTML/manifest/diff checks pass, and the restarted observer visibly reports v71 `RUNNING` with strict/relaxed separation.
MFE/MAE diagnostic hardening is now implemented without changing the default exit contract. Runtime strategy and shadow positions track observed highest/lowest prices; single-market and synchronized portfolio backtests include `maxFavorableExcursionPercent` and `maxAdverseExcursionPercent` on close records; status API and both paper views expose the values for exit tuning. The PWA shell was bumped to redesign JS `observer-readonly-8` and service-worker cache `coinpilot-shell-v12`; the full suite is now `159/159 PASS` with source/public JS, HTML, manifest/SW, and diff checks passing.
At `2026-09-13T04:13:07.782Z`, v71 remained active with strict SOPH loss `-279.8925 KRW` and open XRP; shadow/loose were each `14` closes (`4W/10L`) at `-1,662.5572`/`-1,633.3795 KRW`, with SOL/XRP/MTL diagnostics open. At `2026-09-13T04:13:58Z` the rejection cohorts were shadow previous-high `12` trades / PF `0.3924`, trend `7` / PF `0.6136`, volume `5` losses / PF `0`; retain filters and do not loosen. Continue until the strict XRP risk exit; no live orders.
At `2026-09-13T04:24:48.258Z`, strict XRP closed by `MAX_HOLD_TIME` after 30 minutes: entry `1,857` → exit `1,855 KRW`, net `-41.4961 KRW` / `-0.2076%`. Strict now has two losses totaling `-321.3886 KRW`; the 95% one-sided return lower bound is `-4.5687%` with sample `2/20`, so promotion remains blocked. This is direct evidence that a barely-above-floor `0.1617%` rebound did not recover round-trip costs within the fixed hold window; do not change the current runtime from this sample alone.
At `2026-09-13T04:26:48.482Z`, v71 remained active with strict flat after the two losses and one relaxed LSK position per shadow/loose. Each diagnostic book had `17` closed trades (`4W/13L`) with realized `-2,094.5455`/`-2,065.3678 KRW`; risk/analysis continuity stayed true. The active v71 owner predates the MFE/MAE implementation, so its old open/close records have null excursion fields; the next owner/session will record them.
v71 was then resumed once with the latest code to verify the direct termination path and received a second interruption (`85.433s` gap). Direct node `SIGTERM` at `2026-09-13T04:30:51.779Z` persisted `active=false`, removed the lock, and preserved the baseline with strict 2 losses (`-321.3886 KRW`) plus shadow/loose 18 closes (`4W/14L`) at `-2,388.2639`/`-2,359.0862 KRW`; one STEEM diagnostic position remained at stop, so `stopReason=stopped_with_unsettled_diagnostic_positions`. No positions or data were mixed into the next session.
v72 latest-code forward candidate started at `2026-09-13T04:31:29.716Z` in `.paper-forward-v72-rebound25`, using `minReboundPercent=0.25%`, the same RSI35/65, 90-second candle-age, and 1-second risk settings. At `2026-09-13T04:33:41.463Z` it had 17 cycles, strict 0 closes, shadow/loose each 3 stop-loss closes (`0W/3L`) / `-1,028.0007 KRW`, one ETHFI diagnostic open, and risk/analysis continuity true. MFE/MAE is now present: STEEM `-0.10%/-1.28%`, POWR `+0.84%/-1.98%`, LSK `+0.41%/-1.28%`. This is early diagnostic evidence only; no promotion or runtime default change.
The v72 read-only observer on port `3154` visibly renders MFE/MAE plus strict/relaxed separation, and a 390x844 mobile viewport has fixed bottom navigation and no horizontal overflow. The existing v71 observer remains available on port `3153`; both are read-only and do not own sessions.
At `2026-09-13T04:36:23.312Z`, v72 was active at cycle `33` with complete config, zero interruptions, strict `0` closes/open, and shadow/loose each `4` stop-loss closes (`0W/4L`) at `-1,375.7648 KRW`; ETHFI remained open. Its open position had MFE `+0.3293%` and MAE `-0.3145%` while carrying `volume_confirmation_failed`. At `04:34:17.879Z`, v72's prior losses had STEEM `-0.10%/+ -1.28%` MFE/MAE, POWR `+0.84%/-1.98%`, and LSK `+0.41%/-1.28%`; no strict profitability conclusion is available. Keep the 0.25% candidate diagnostic-only.
At `2026-09-13T04:37:54.987Z`, v72 reached cycle `49`: strict remained `0` closes/open, while shadow/loose had `4` losses / `-1,375.7648 KRW` and ETHFI open. The latest loss set's MFE/MAE was STEEM `-0.10%/-1.28%`, POWR `+0.84%/-1.98%`, LSK `+0.41%/-1.28%`, and STORJ `+0.56%/-1.54%`. This indicates relaxed candidates sometimes had a small favorable excursion but materially larger adverse excursion; it is evidence to keep strict filters and investigate protective exits, not to promote `minRebound=0.25%`.
At `2026-09-13T04:53:04.361Z`, v72 reached cycle `169` with `364` oversold observations, `124` strict rebound candidates, `0` strict confirmed candidates, `0` strict BUY, and `438` unique signal windows. Strict remained at zero trades/open; shadow/loose stayed at four losses / `-1,375.7648 KRW` with ETHFI open (MFE `+0.3293%`, MAE `-0.4218%`). Treat this as a market/contract selectivity observation, not evidence to relax filters or claim `0.25%` profitability.
At `2026-09-13T04:57:30.736Z`, v72 reached cycle `201` with strict still at `0` confirmed/BUY/closed, `397` oversold observations, `132` strict rebound candidates, `520` unique windows, and `156` stale analysis blocks. Shadow/loose reached `5` stop-loss losses / `-1,826.6620 KRW`; the latest POWR loss had MFE `-0.0999%` and MAE `-2.0587%`, while ETHFI remained open. Keep the candidate diagnostic-only and wait for ETHFI's real exit before preserving the session.
At `2026-09-13T04:58:56.576Z`, v72 reached cycle `209` with strict still empty and shadow/loose at `5` stop-loss losses / `-1,826.6620 KRW`; ETHFI remained open at age `26.9` minutes with MFE `+0.3293%` and MAE `-0.4218%`. The `0.25%` candidate remains an under-sampled, diagnostic-only forward window; preserve open diagnostics and do not promote.
At `2026-09-13T05:02:03.981Z`, v72 ETHFI closed by `MAX_HOLD_TIME` after 30 minutes: entry `931.931` → exit `934.065 KRW`, relaxed net `+25.7566 KRW` / `+0.1288%`, MFE `+0.4366%`, MAE `-0.4218%`, with volume rejection. Shadow/loose reached `6` closes (`1W/5L`) / `-1,800.9054 KRW`; strict remained `0` trades and STEEM remained as the only diagnostic open. The small positive diagnostic exit does not offset the relaxed cohort or prove strict profitability.
At `2026-09-13T05:05:28.055Z`, v72's final STEEM diagnostic position stopped out: entry `89.7897` → exit `88.6113 KRW`, net `-282.2126 KRW` / `-1.4111%`, MFE `+0.7911%`, MAE `-1.2136%`, with rebound/RSI/volume/trend/high-break rejection. v72 was then directly stopped at `2026-09-13T05:06:37.408Z` with `active=false`, `stopReason=stopped_cleanly`, strict `0`, shadow/loose `7` closes (`1W/6L`) / `-2,083.1179 KRW`, no open positions, no interruptions, and no terminal error.
v73 fresh latest-code baseline started at `2026-09-13T05:07:09.646Z` in `.paper-forward-v73-baseline-mfe`, using the original `minReboundPercent=0.15%` with RSI35/65, 90-second candle age, 1-second risk checks, and MFE/MAE telemetry. At `05:08:43.980Z` it was active at cycle `10`, strict/shadow/loose all had zero trades/positions, interruptions `0`, and risk/analysis continuity true. Its dedicated observer is on port `3155`; desktop and 390x844 mobile views show the read-only boundary and no horizontal overflow.
At `2026-09-13T05:12:25.910Z`, v73 reached cycle `42` with strict still `0` trades/open and three relaxed diagnostic positions. Their MFE/MAE were STEEM `+1.0302%/-0.7780%`, B3 `+0.5438%/-0.3574%`, and PUNDIX `+0.5130%/-0.0999%`; none was yet a realized result. Interruption remained `0` and risk/analysis continuity stayed true.
At `2026-09-13T05:16:14.873Z`, v73 owner PID `56670` remains active and matches the root `.paper-session.lock`; cycle `67`, analysis `20/20`, risk failures `0`, interruptions `0`, and both continuity lanes are eligible. Strict remains at zero trades/open. Shadow/loose each have two relaxed closes, B3 `TAKE_PROFIT +351.2207 KRW` and STEEM `STOP_LOSS -330.5950 KRW`, realized `+20.6257 KRW`; PUNDIX remains an open diagnostic with MFE `+1.1259%` and MAE `-0.0999%`. Keep this small cohort diagnostic-only and preserve PUNDIX until its real exit.
At `2026-09-13T05:22:32.967Z`, v73 owner PID `56670` is still active under the matching root lock at cycle `115`. Strict has `75` rebound candidates but `0` confirmed/BUY/closed; analysis remains `20/20`, risk failures `0`, interruptions `0`, and both continuity lanes are eligible. Shadow/loose each have `2` relaxed closes (`1W/1L`, `+20.6257 KRW`) and open PUNDIX (`MFE +1.7388%`, `MAE -0.0999%`) plus POWR (`MFE +0.4046%`, `MAE -0.9072%`). Preserve both diagnostic positions through actual exits and keep strict promotion/filter decisions unchanged.
At `2026-09-13T05:24:14.990Z`/`05:24:32.469Z`, v73 cycle `131` remains healthy under owner PID `56670` and the matching root lock: analysis `20/20`, risk failures `0`, interruption `0`, continuity eligible. PUNDIX closed `TAKE_PROFIT +429.4121 KRW`; POWR and STORJ closed `STOP_LOSS -281.4716`/`-295.1192 KRW`. Shadow/loose each have `5` closes (`2W/3L`, realized `-126.5530 KRW`) and no open positions. Strict remains empty despite `84` rebound candidates; confirmed/BUY are `0`, and unique rejection counts are led by previous-high `284`, volume `200`, and close-strength `180`. Keep the baseline strict contract and continue collecting strict samples; do not loosen from this small relaxed cohort.
At `2026-09-13T05:28:20.319Z`, v73 produced its first strict confirmed entry: `KRW-PROM` at `7,790 KRW` after a `1,122ms` delayed revalidation. The signal had rebound `0.2577%`, RSI `35.2196` after oversold RSI `26.8632`, recovery `8.3563`, volume `4.5594x`, close strength `1`, trend slope `-0.1949%`, and range `0.2577%`; execution drift was `+0.1285%`. The position was still open at high `7,805` with MFE `+0.1926%` and MAE `0%`; no strict close result exists yet. Treat this as an unsettled first strict sample and preserve it through its actual risk exit.
The 60-day/15-minute replay at `2026-09-13T05:39:50.204Z` used RSI `30/70` from `DEFAULT_CONFIG`, not v73's actual snapshot RSI `35/65`; retain it only as legacy coarse-timeframe research, not current-runtime validation. Its baseline and variants were negative and no gate passed. The shared-portfolio replay at `05:42:51.330Z` also used legacy RSI `30/70`, with baseline aggregate `33 trades / -0.3529%`; it must not be treated as exact v73 evidence.
v73's first strict position settled at `2026-09-13T05:57:04.595Z`: `KRW-PROM` hit the fixed 30-minute max-hold exit at `7,815 KRW` after entry `7,790`, with gross `+64.1528 KRW`, fees `20.0221 KRW`, net `+44.1307 KRW` / `+0.2208%`, MFE `+0.3209%`, and MAE `-0.9628%`. Its delayed entry revalidation succeeded after `1,122ms`; confidence is still only sample `1/20` with no lower bound, so the promotion gate remains false. Relaxed shadow/loose were at `9` closes (`2W/7L`, `-857.2016 KRW`) with two diagnostics open. Keep this as one positive strict observation and do not tune or promote from it.
At `2026-09-13T06:01:10.772Z`, v73 opened its second strict position, `KRW-POWR` at `100 KRW`, after a `4,204ms` delayed revalidation. The signal had rebound `1.1122%`, RSI `46.6260` after oversold RSI `33.8349`, recovery `12.7911`, volume `1.1584x`, close strength `1`, trend slope `+0.0365%`, and range `1.2133%`; execution drift was `0%`. At observation it had high `101`, MFE `+1.0%`, MAE `0%`, and no exit. Strict confirmation is `2/2` successful with no cancellation, but the second sample remains unsettled and must be closed before updating the confidence result.
At `2026-09-13T06:03:53.664Z`, the second strict `KRW-POWR` position settled at fixed take-profit: entry `100` → exit `102 KRW`, gross `+399.8000 KRW`, fees `20.1899 KRW`, net `+379.6101 KRW` / `+1.8990%`, MFE `+2.0%`, MAE `-0.8%`. Strict now has two wins, realized `+423.7408 KRW`, mean `+1.0599%`, but its 95% one-sided lower bound is `-4.2383%` at sample `2/20`, so the gate remains false. Relaxed shadow/loose have `12` closes (`3W/9L`, `-1,390.8660 KRW`) with one diagnostic open; no lane mixing or promotion.
At `2026-09-13T06:15:04.061Z`, relaxed `KRW-FLOCK` settled by `STOP_LOSS`: entry `106.106` → exit `103.896 KRW`, net `-436.1431 KRW` / `-2.1807%`, MFE `+0.8426%`, MAE `-1.9848%`. Its signal volume ratio was only `0.0899x`, trend slope `-0.3400%`, and previous-high break failed. Relaxed shadow/loose now have `15` closes (`3W/12L`, `-2,414.7785 KRW`) with no open positions. Keep this as rejected-cohort evidence for retaining volume/trend/high-break filters; strict remains two wins with a negative confidence lower bound.
At `2026-09-13T06:58:15.277Z`, relaxed `KRW-ETHFI` settled by `MAX_HOLD_TIME`: net `-59.9151 KRW` / `-0.2996%`, MFE `+0.3307%`, MAE `-0.2076%`. Shadow reached `17` closes (`4W/13L`, `-2,140.4061 KRW`) with no open position; loose retained `KRW-SOL` as its only diagnostic open. Strict remains two wins with lower confidence bound `-4.2383%`; no promotion or filter change.
The fixed-entry exit grid at `2026-09-13T05:58:59.051Z` also belongs to the legacy RSI `30/70` 15-minute study; it remains research-only and no exit combination passed its fold gate.
The snapshot-corrected combined report completed at `2026-09-13T06:41:16.019Z`. On 30-day/1-minute BTC/ETH/XRP data with exact v73 RSI `35/65`, shared-portfolio baseline was `161 trades / -1.0435%`, with three fold returns `-0.3109%`, `-0.4582%`, and `-0.2744%`; every tested variant was negative in every fold. Rebound `0.25%` was least negative at `48 / -0.2393%`, but still failed all folds. On the exact RSI `35/65` 60-day/15-minute replay across 19 sufficiently long markets, baseline was `73 / -0.5404%` with fold returns `-0.1948%`, `-0.0541%`, and `-0.2915%`; all variants were negative in every fold. Reports: `/tmp/coinpilot-snapshot-rsi35-65-3fold-20260913.json` and `/tmp/coinpilot-30d-1m-snapshot-study-20260913.json`.
The snapshot-corrected combined report completed at `2026-09-13T06:41:16.019Z`. On 30-day/1-minute BTC/ETH/XRP data with exact v73 RSI `35/65`, shared-portfolio baseline was `161 trades / -1.0435%`, with three fold returns `-0.3109%`, `-0.4582%`, and `-0.2744%`; every tested variant was negative in every fold. Rebound `0.25%` was least negative at `48 / -0.2393%`, but still failed all folds. On the exact RSI `35/65` 60-day/15-minute replay across 19 sufficiently long markets, baseline was `73 / -0.5404%` with fold returns `-0.1948%`, `-0.0541%`, and `-0.2915%`; all variants were negative in every fold. The exact v73 config came from the persisted session snapshot; `.env.example` still documents generic fallback RSI `30/70`, so future studies must explicitly load the active snapshot/environment. Reports: `/tmp/coinpilot-snapshot-rsi35-65-3fold-20260913.json` and `/tmp/coinpilot-30d-1m-snapshot-study-20260913.json`.
The higher-timeframe direction diagnostic at `2026-09-13T06:43:22.345Z` did not produce positive average returns for any 15/30/60-minute direction group on exact RSI `35/65` BTC/ETH/XRP candidates. The multi-candle diagnostic at `06:43:56.934Z` showed small positive ETH/XRP pockets only at `3` and `6` samples, while BTC's `11` two-follow-through samples remained negative; this is insufficient to create a live contract. Reports `/tmp/coinpilot-higher-timeframe-diagnostic-20260913.json` and `/tmp/coinpilot-multi-candle-diagnostic-20260913.json` remain research-only.
The official `validateScalping.js` fixed-config CLI was also run against the 30-day/1-minute cache with explicit RSI `35/65`: BTC holdout `-0.0697%` over 13 trades (PF `0.25`), ETH `-0.0435%` over 8 (PF `0.05`), XRP `-0.1402%` over 23 (PF `0.20`), with all training confidence gates failed and global `0/3` promotion. Report `/tmp/coinpilot-official-30d-1m-rsi35-65-20260913.json`. This confirms the exact project validation path agrees with the independent snapshot replay.
The corrected market-regime gate study completed at `2026-09-13T06:05:52.331Z` after fixing the one-off reader to use `metrics.marketRegimeBlockedEntries`. The strongest exposure-reduction setting, lookback `10`, breadth `0.5`, average return `>=0`, blocked `36` candidates and returned `4 trades / -0.0266%` across three folds, with no positive fold. A looser lookback `5` setting returned `11 / -0.0494%` with one positive fold. The gate is useful as a diagnostic risk throttle but not as a demonstrated alpha source; leave it disabled by default. Report: `/tmp/coinpilot-60d-15m-regime-grid-corrected-20260913.json`.
At `2026-09-13T06:59:41.934Z`, v73 cycle `860` remained healthy under the same owner/lock: analysis `20/20`, risk failures `0`, interruption `0`, continuity eligible. TREE's relaxed `TAKE_PROFIT +334.2876 KRW` was included before the ETHFI close; Shadow now has `17` closes (`4W/13L`, `-2,140.4061 KRW`) and no open position, while Loose has only `KRW-SOL` open. Strict remains at two wins / `+423.7408 KRW` with promotion false.
Validation reproducibility hardening is now in source: `src/research/scalpingValidationConfig.js` loads only `configSnapshotComplete=true` paper ledgers, copies an allowlisted strategy/risk contract, records snapshot source metadata in official reports, and fails closed on candle-unit mismatch. `SCALP_VALIDATION_CONFIG_SNAPSHOT_FILE` is documented in `.env.example`/README. The v73 exact snapshot replay passed through the official CLI with BTC `-0.0697%`/13, ETH `-0.0435%`/8, XRP `-0.1402%`/23, global `0/3`; four tests cover complete, incomplete, unit mismatch, and merge behavior. Full suite after this change is `163/163 PASS`.
At `2026-09-13T07:08:50.113Z`, v73 owner PID `56670` and the root lock remain active; Observer is HTTP `200`, `RUNNING`, non-orphaned, and process-alive. Cycle `932`, analysis `20/20`, risk failures `0`, interruptions `0`, continuity eligible. Strict remains `2` wins / `+423.7408 KRW` with lower confidence bound `-4.2383%` and promotion false. Shadow has `18` closes (`4W/14L`, `-2,420.5430 KRW`) with LSK/VTHO open diagnostics; Loose has `19` closes (`4W/15L`, `-2,480.4581 KRW`) with XRP/LSK/VTHO open diagnostics. Snapshot validation hardening did not disturb the paper owner or live-order boundary.
At `2026-09-13T07:07:22.493Z`, the v73 owner PID `56670` remained active with the root lock and Observer `RUNNING/orphaned=false/processAlive=true`. Cycle `924`, analysis `20/20`, risk failures `0`, interruptions `0`, continuity eligible. Strict remains `2` wins / `+423.7408 KRW` with lower bound `-4.2383%` and promotion false. Shadow has `18` closes (`4W/14L`, `-2,420.5430 KRW`) and no open position; Loose has `19` closes (`4W/15L`, `-2,480.4581 KRW`) with `KRW-XRP` open. Snapshot-authoritative validation code was added and did not disturb the active owner.
At `2026-09-13T07:10:26.043Z`, v73 cycle `948` remained healthy: owner PID `56670`, root lock, and Observer `3155` all matched/returned `RUNNING`, analysis `20/20`, risk failures `0`, interruption `0`, continuity eligible. Strict remains two closed wins / `+423.7408 KRW` with lower bound `-4.2383%` and promotion false. Shadow has `19` closes (`4W/15L`, `-2,707.4799 KRW`) with `KRW-VTHO` open; Loose has `20` closes (`4W/16L`, `-2,767.3950 KRW`) with `KRW-XRP` and `KRW-VTHO` open. Validation snapshot hardening remains isolated from the active paper owner and live boundary.
At `2026-09-13T07:12:31.061Z`, v73 opened its third strict position, `KRW-PUNDIX` at `168 KRW`, after a `2,378ms` delayed revalidation. The signal had rebound `0.5988%`, RSI `41.8982` after oversold RSI `34.9703`, recovery `6.9280`, volume `3.6633x`, close strength `1`, trend slope `-0.0785%`, and range `1.1976%`; execution drift was `0%`. It was still open at `169` with MFE `+0.5952%` and MAE `0%`; strict confirmation attempts are `3/3` with no cancellation. Preserve this unsettled third sample through its actual risk exit.
At `2026-09-13T07:12:17.802Z`, v73 cycle `956` remained healthy under owner PID `56670` and the root lock. Strict has two settled wins (`+423.7408 KRW`) plus open `KRW-PUNDIX`; Shadow/Loose have `19/20` relaxed closes (`4W/15L`, `-2,707.4799`/`-2,767.3950 KRW`) with VTHO/PUNDIX and XRP/VTHO/PUNDIX diagnostics open. Keep the third strict sample unsettled and do not promote or change runtime.
At `2026-09-13T07:13:36.578Z`, v73's third strict `KRW-PUNDIX` position settled at take-profit: entry `168` → exit `172 KRW`, gross `+475.2000 KRW`, fees `19.2933 KRW`, net `+455.9067 KRW` / `+2.2798%`, MFE `+2.3810%`, MAE `0%`, after `2,378ms` delayed revalidation. Strict now has `3/20` closed wins totaling `+879.6475 KRW`; mean `+1.4665%`, lower confidence bound `-0.3804%`, promotion false. Shadow has `21` closes (`6W/15L`, `-1,849.8549 KRW`) and no open position; Loose has `22` closes (`6W/16L`, `-1,909.7700 KRW`) with `KRW-XRP` open. Preserve the provisional strict sample and keep runtime unchanged.
At `2026-09-13T07:14:45.057Z`, v73 remained healthy under owner PID `56670` and the root lock (Observer HTTP `200`, `RUNNING`, non-orphaned, process-alive), cycle `980`, analysis `20/20`, risk failures `0`, interruption `0`, continuity eligible. Strict has three settled wins totaling `+879.6475 KRW`, lower bound `-0.3804%`, promotion false. Shadow/Loose have `21/22` relaxed closes (`6W/15L`, `-1,849.8549`/`-1,909.7700 KRW`); Loose retains `KRW-XRP` open. Runtime remains unchanged.
At `2026-09-13T07:22:15.941Z`, v73 opened its fourth strict position, `KRW-SOPH` at `6.06 KRW`, after a `4,026ms` delayed revalidation. Signal rebound was `0.3311%`, RSI `42.2642` after oversold RSI `30.6600`, recovery `11.6042`, volume `2.0141x`, close strength `1`, trend slope `-0.0657%`, and range `0.3311%`; execution drift `0%`. MFE/MAE were `0/0` at entry. Strict confirmation attempts are `4/4` with no cancellation; keep SOPH unsettled until its actual risk exit.
At `2026-09-13T07:33:18.534Z`, the fourth strict `KRW-SOPH` position remained open 11.8 minutes after entry `6.06 KRW`, with high `6.06`, MFE `0%`, MAE `-0.4950%`, and no exit. Strict remains three settled wins / `+879.6475 KRW` / lower bound `-0.3804%`; Shadow/Loose retain SOPH as the only unsettled diagnostic with `22`/`24` closes (`6W/16L`, `-2,155.4384`/`-2,275.2686 KRW`). Do not mix the open fourth strict sample into settled performance.
At `2026-09-13T07:40:45.286Z`, v73 entry-confirmation telemetry recorded `5` attempts, `4` successes, and `1` cancellation. The cancelled `KRW-TREE` signal failed delayed revalidation because retrace `0.48%` exceeded the configured `0.25%` limit, confirming stale/chased entries are blocked. The fourth strict `KRW-SOPH` remains unsettled at entry `6.06` with MFE `0%` and MAE `-0.495%`; settled strict remains three wins / `+879.6475 KRW` / lower bound `-0.3804%`.
At `2026-09-13T07:51:49.028Z`, the fourth strict `KRW-SOPH` position settled by the fixed 30-minute max-hold at the unchanged price `6.06` → `6.06 KRW`, gross `0`, fees `20.0070 KRW`, net `-20.0070 KRW` / `-0.1000%`, MFE `+0.1650%`, MAE `-0.4950%`, entry delay `4,026ms`. Strict now has `4/20` settled trades (`3W/1L`), realized `+859.6405 KRW`, mean `+1.0749%`, lower confidence bound `-0.3239%`, promotion false. This fee-only max-hold loss is preserved as one sample; no runtime change.
At `2026-09-13T07:52:08.439Z`, v73 remained active and healthy under owner PID `56670`/root lock/Observer: analysis `20/20`, risk failures `0`, interruption `0`, continuity eligible. Shadow/Loose have `25/27` relaxed closes (`7W/18L`, `-2,178.6262`/`-2,298.4564 KRW`) with one `KRW-SOL` diagnostic open in each; strict remains separate and non-promotional.
Historical evidence integrity hardening at `2026-09-13T08:41Z`: added `analyzeHistoricalCandleContinuity()` to the backtest layer. Default simulations, candidate collection, single-market walk-forward, and shared-portfolio walk-forward now fail closed on missing/non-increasing timestamps or gaps above 1.5 candle intervals; reports expose `dataQuality` and invalid markets. A regression fixture with a 3-minute gap and a mixed shared portfolio both fail closed. This exposed that the previous 30-day/1-minute BTC/ETH/XRP cache was not contiguous (BTC 13, ETH 105, XRP 4 gaps), and the 60-day/15-minute cache had gaps in 16/19 markets (up to 420 minutes). The old multi-market returns must therefore be treated as invalid research evidence, not current proof. On the four contiguous 15-minute markets BTC/ETH/XRP/SOL, exact RSI35/65 shared-portfolio 3-fold baseline remained negative: holdout aggregate `17 trades / -0.1098%`, fold returns `-0.0270%`, `-0.0386%`, `-0.0442%`, with all folds failing confidence. Trend-rebound long-hold variants were still under-sampled and not promoted. Full suite after the guard is `166/166 PASS` (including continuity and UI continuity-display tests), syntax and `git diff --check` pass; active v73 runner was not restarted or mutated.
At `2026-09-13T08:48:35.278Z`, v73 remained active under PID `56670`, matching root lock, and Observer `3155` (`RUNNING`, `orphaned=false`, `processAlive=true`). Cycle `1692`, analysis `20/20`, risk failures/outages `0/0`, interruption `0`, continuity true. Strict remains `4` closes (`3W/1L`), realized `+859.6405 KRW`, confidence mean `+1.0749%`, lower bound `-0.3239%`, eligible false/open0. Shadow/Loose increased to `30/32` closes (`8W/22L`, `-2,747.7631`; `8W/24L`, `-2,867.5933 KRW`) with 3 diagnostic opens each; recent shadow B3 stop-loss was `-292.3170 KRW`. No runtime/live change. Desktop observer UI readback shows strict/relaxed separation, read-only order lock, PWA shell, and updated telemetry; existing validation report remains the prior artifact until a new official contiguous-cache report is intentionally generated.
At `2026-09-13T09:07:59.847Z`, strict `KRW-B3` settled at fixed take-profit: entry `0.849` → exit `0.868 KRW`, net `+427.5324 KRW` / `+2.1368%`, MFE `+2.2379%`, MAE `-0.3534%`, after `2,043ms` delayed revalidation and `+0.2361%` execution drift. Strict reached `5` closes (`4W/1L`) and realized `+1,287.1728 KRW`; confidence lower bound `+0.2061%`, but only `5/20` samples, so promotion remains false.
At `2026-09-13T09:11:01.131Z`, strict `KRW-ETHFI` settled at fixed take-profit: entry `907` → exit `924 KRW`, net `+354.7987 KRW` / `+1.7734%`, MFE `+1.8743%`, MAE `-0.1103%`, after `2,894ms` delayed revalidation and zero drift. Strict now has `6` settled trades (`5W/1L`), realized `+1,641.9716 KRW`, mean `+1.3683%`, 95% one-sided lower bound `+0.5182%`, but remains ineligible until the 7-day/20-trade/other gates pass. At the read strict is flat; Shadow/Loose remain diagnostic-only and negative.
Segmented diagnostic lane was completed at `2026-09-13T09:24Z`: `splitHistoricalCandleSegments()`/`simulateScalpingSegmented()` split gaps without imputation, exclude segment-end `BACKTEST_END` positions as unknown, and hard-set promotion false. Cache-only `npm run validate:scalping:segments` reads all SCALP env fields and lets a complete paper snapshot override them. v73 snapshot 30d/1m report `/tmp/coinpilot-segmented-30d-1m-v73-v2-20260913.json`: 161 closes, 42W/119L, `-10,357.65 KRW`, unknown1, used36/excluded89. Explicit RSI35/65 60d/15m report `/tmp/coinpilot-segmented-60d-15m-rsi35-65-v2-20260913.json`: 51 closes, 4W/47L, `-3,764.02 KRW`, used34/excluded10,966, unknown0. Both are diagnostic-only and not promotion evidence. Full suite after this lane is `169/169 PASS`; syntax and `git diff --check` pass.
At `2026-09-13T09:29:36.507Z`, v73 remains active under PID `56670`/matching lock/Observer `3155`, cycle `2004`, `RUNNING`, non-orphaned/process-alive. Strict has `6` settled trades (`5W/1L`, `+1,641.9716 KRW`, lower bound `+0.5182%`, eligible false) with open `KRW-PROM` and `KRW-RAY`; Shadow/Loose have `33/35` closes (`8W/25L`, `-3,152.3993`/`-3,248.6583 KRW`) with 3 open each. Analysis `20/20`, risk failure `0`, interruptions `0`, continuity true. No runtime/live changes.
Research-only 5-minute timeframe check at `2026-09-13T09:39:33.490Z`: `8,640` KRW-BTC candles over 30 days were contiguous (`gapCount=0`), but the current v73 entry/risk values (RSI35/65, rebound0.15%, volume1.0x, previous-high break, SL1.2%, TP1.8%) produced training `-0.0875%` over 19 trades and holdout `-0.0369%` over 6 trades (`0W/6L`, PF0). Data quality passed, strategy/training/statistical gates failed, and no timeframe switch or live change was made; runtime remains 1-minute. Artifact: `/tmp/coinpilot-5m-v73-btc-20260913.json`.
The same 5-minute diagnostic was extended to ETH/XRP at `2026-09-13T09:45:53.948Z`: both had `8,640` contiguous candles (`gapCount=0`). ETH was training `-0.0931%` (`3W/13L`, PF0.12) and holdout `-0.0270%` (`1W/5L`, PF0.26); XRP was training `-0.0246%` (`5W/7L`, PF0.63) and holdout `-0.1011%` (`1W/9L`, PF0.003). All three contiguous 5-minute markets failed promotion; runtime remains 1-minute. Artifact: `/tmp/coinpilot-5m-v73-eth-xrp-20260913.json`.
v73 then recorded strict max-hold settlements for PROM (`+70.7059 KRW`) and RAY (`2,041` → `2,054 KRW`, net `+107.4482 KRW`, `+0.5366%`, MFE `+0.6369%`, MAE `-1.0779%`, confirmation `3,521ms`). Strict is now 8 closed (`7W/1L`, `+1,820.1256 KRW`, confidence lower bound `+0.4851%`) with only XRP open; the minimum `20` trade gate remains false. This is small-sample forward evidence and does not justify exit or live-contract changes.
The v73 strict exit-type read at `2026-09-13T09:52Z` is descriptive only: 4 take-profits were all wins (`+1,617.8479 KRW`, mean `+2.0222%`), while 4 max-holds were 3W/1L (`+202.2777 KRW`, mean `+0.2526%`). Across 8 closes, mean return is `+1.1374%`, mean MFE `+1.2911%`, mean MAE `-0.4749%`, and mean confirmation delay `2,904.5ms`; no fixed exit or delayed-entry parameter was changed.
v73 XRP then settled at `2026-09-13T10:06:04.477Z`: entry `1,830` → exit `1,831 KRW` by the fixed 30-minute max-hold, net `+12.7858 KRW` / `+0.0639%`, MFE `+0.2186%`, MAE `-0.0546%`, delayed confirmation `2,409ms`, execution drift `+0.0547%`. Strict is now 9 closed (`8W/1L`, `+1,832.9114 KRW`, lower bound `+0.4115%`) with no open strict positions; minimum 20 trades remains unmet. Shadow/Loose retain TREE/STORJ diagnostics only.
The observed heartbeat/risk gap led to a safety fix for future sessions: stale active risk observations now fail closed from the last known good timestamp even when no request-failure callback has fired. `monitoringActive`, attempt persistence, `RISK_CHECK_STALE`, and a watchdog were added; idle no-position periods remain eligible. The first latest-code v74 run still showed a 61-second in-flight risk request whose late success did not stop the process, so v74 is preserved as invalid diagnostic evidence (`29` cycles, no strict trades, TREE open). `recordRiskMonitorSuccess()` now receives the configured gap and retroactively invalidates late success callbacks; the trader wrapper stops on that state. Delayed-success module and paper-session tests cover it, and the full suite at that stage was `178/178 PASS`. v73/v74 were not reused or mixed.
Analysis-cycle freshness was then hardened after v76 showed a real 51-second idle analysis heartbeat gap: `analysisActive`, attempt/start timestamps, an independent analysis watchdog, stale gap persistence, and idle clearing now cover full-market analysis. A cycle exceeding `SCALP_MAX_ANALYSIS_DATA_GAP_SECONDS` stops with `analysis_data_gap` and records missing markets, independently of risk protection; module/trader/timer tests cover it. The current full suite after the UI status addition is `182/182 PASS`. v76 remains pre-analysis-watchdog evidence.
v76 latest-code forward observation ended at `2026-09-13T10:49:16.693Z` after `77` cycles: one strict BLAST take-profit `+352.0771 KRW`, no strict open positions, Shadow/Loose each one win `+385.6734 KRW` with diagnostic opens. Its read-only dashboard/API returned `200` for paper/status/manifest/service-worker and rejected start/stop mutations with `403`; the session is non-promotional because diagnostics were open at stop.
v77 latest-code session started at `2026-09-13T10:51:38.554Z`, owner PID `20853`, matching lock. At `10:54:17Z` it was cycle `21` with `analysisActive=true` and `analysisStartedAt` persisted before network work; strict/shadow/loose empty, risk idle, analysis `20/20`, interruptions `0`. This is operational startup evidence only, not profitability or promotion evidence.
At `2026-09-13T10:56:39.181Z`, v77 was cycle `40` with owner/lock/Observer healthy. Strict remained at `0` trades; Shadow/Loose each had one XRP diagnostic position. Analysis completed `20/20` and closed its in-flight marker; risk was active with attempt and success both at `10:56:37.082Z`, failures `0`, interruptions `0`, continuity true. The port3155 read-only dashboard returned `200` for read surfaces and `403` for session mutations. Do not loosen filters from this undersampled zero-strict observation.
At `2026-09-13T10:58:38.360Z`, v77 reached cycle `55` with owner/lock/Observer healthy. Strict remained at `0` trades; Shadow/Loose each carried XRP and ETHFI diagnostics. Analysis was complete for the prior cycle and a new `analysisActive` attempt was persisted; risk was active with attempt/success `10:58:37.270Z`, failures/outages `0/0`, interruptions `0`, continuity true. This is operational evidence only; keep filters unchanged.
At `2026-09-13T11:01:29.118Z`, v77 reached cycle `77` with strict still empty and no confirmed strict candidate. Shadow/Loose each had one diagnostic loss (`-429.1759 KRW`); Shadow kept XRP/ETHFI open and Loose kept XRP/ETHFI/ETH open. Analysis `20/20`, risk current/failure-free, interruptions `0`, continuity true. The first diagnostic loss is too small for filter tuning; preserve the strict contract.
The paper UI now renders `analysisActive` as an explicit elapsed in-flight cycle status in both redesign and legacy cards, with the unfinished state excluded from profitability samples. PWA asset version is `observer-readonly-10` and service-worker cache is `coinpilot-shell-v14`; UI regression and static checks pass. This was a presentation-only change with no trading contract change.
Historical no-trade semantics were verified against the Upbit minute-candle contract and a direct BTC request: the missing `2026-08-14T18:25:00` candle is omitted because the API returns only intervals with executions. Added research-only `fillNoTradeCandleGaps()` and cache-only `npm run validate:scalping:no-trade-fill`; each missing interval becomes previous-close flat OHLC with volume `0`, and the report records `syntheticNoTradeCount`, `validForReplay`, and `promoted=false` unconditionally. Replayed the v77 snapshot against the raw 30d/1m BTC/ETH/XRP cache at `2026-09-13T11:43:06.845Z`: synthetic counts `13/108/4`, holdout returns `-0.0697%/-0.0435%/-0.1402%`, trades `13/8/23`, PF `0.25/0.05/0.20`; all training/confidence gates failed and no runtime parameter changed. Artifact: `/tmp/coinpilot-no-trade-fill-v73-30d1m-20260913.json`. Full suite after filler/CLI is `185/185 PASS`; source/public JS, HTML inline scripts, manifest/SW, and `git diff --check` pass. Current v77 remains active and isolated; no live orders.
At `2026-09-13T11:36:30.118Z`, v77 owner PID `20853` and read-only Observer `3155` were healthy (`RUNNING`, non-orphaned/process-alive), cycle `347`, analysis `20/20`, risk failures `0`, interruptions `0`, storage healthy. Strict remains `0` trades/open and ineligible; Shadow/Loose are `4` closes (`1W/3L`) at `-350.2997/-339.6529 KRW`, no open diagnostics. Keep v77 running; do not loosen filters or enable live.
The same no-trade flat-fill diagnostic on the raw 60d/15m 20-market cache with explicit RSI35/65 current-style settings generated `/tmp/coinpilot-no-trade-fill-v73-60d15m-20260913.json` at `2026-09-13T11:48:31.296Z`. All fillable markets became continuity-valid, but most holdouts were under-sampled: aggregate holdout was `25` trades, only B3 (`+0.0319%`, 1 trade) and RAY (`+0.0098%`, 2 trades) were positive, and every training gate failed. CP remained insufficient at `481` filled candles. This is diagnostic-only and did not change runtime or live eligibility. Historical timestamp parsing now treats zone-less `candle_date_time_utc` as UTC consistently under KST host timezone tests.
v77 ended at `2026-09-13T11:40:13.807Z` with `stopReason=risk_data_gap`, cycle `377`, strict `0` trades, and one open diagnostic position in each relaxed book. Its risk state recorded a `299.319s` gap from `11:35:10.094Z`, then a late callback marked `RISK_CHECK_STALE`; the fail-closed behavior is correct, but the exact contribution of queue starvation versus a paused/blocked event loop is not proven from the old process. A deterministic FIFO backlog test did reproduce a design defect: queued analysis requests could consume all future rate-limit slots before risk. Replaced the shared FIFO promise chain with a priority queue, marked the real risk ticker call as `priority=risk`, and added a caller seam test. v78 is the first latest-code observation of this fix.
v78 started at `2026-09-13T11:58:14.080Z` in `.paper-forward-v78-risk-priority`, owner PID `35595`, with read-only Observer `3156`. At `2026-09-13T11:59:21.315Z` it was active cycle `9`: analysis `20/20`, risk attempt/success current, failures/outages `0/0`, continuity true, interruptions `0`, strict `0` trades/open, Shadow/Loose each `2` open diagnostic positions. Observer paper/index/manifest/SW reads returned `200`; paper start mutation returned `403`. Keep v78 running to test whether risk gaps recur; no live orders.
v78 follow-up at `2026-09-13T12:03:46.209Z`: owner PID `35595`/npm parent `35569` and cwd were verified; cycle `43`, heartbeat `12:03:41.901Z`, strict `0` closes/open, Shadow/Loose `3` closes each (`0W/3L`) at `-899.2102 KRW`, with 2 diagnostic opens each. Risk last success `12:03:43.642Z`, failures/outages `0/0`, max gap `0`, continuity true; analysis `20/20`, interruptions `0`, stale blocks `0`. The priority-queue fix has not yet seen a repeat of v77's 299-second gap, but this remains operational evidence only.
Research on continuity-preserving filled windows did not produce a runtime candidate. Capped 30d/1m v78-snapshot tuning over 512 of 20,736 candidates failed BTC/ETH/XRP holdout gates (training-positive choices had 0/1/3 holdout trades). Shared portfolio 3-fold on the same filled 30d/1m window returned `74` holdout trades / `-0.4776%`, all folds failed. Selected 60d/15m 20-market variants remained negative: baseline `-0.1859%`/25 trades, rebound25 `-0.1625%`/20, rebound50 `-0.0948%`/14, tight-exit `-0.1484%`/25, momentum_breakout `-4.7987%`/670; no variant passed promotion and no runtime parameter changed. Artifacts: `/tmp/coinpilot-tuned-filled-v78-30d1m-20260913.json`, `/tmp/coinpilot-portfolio-filled-v78-30d1m-3fold.json`, `/tmp/coinpilot-variants-filled-v78-60d15m.json`.
v78 read at `2026-09-13T12:12:07.169Z`: active cycle `107`, owner/lock/Observer healthy, strict `0` trades/open, Shadow/Loose each `6` closed (`0W/6L`) at `-1,908.7613 KRW` with two diagnostic opens. Risk attempt/success `12:12:05.500Z`, failures/outages `0/0`, max gap `0`, continuity true; analysis `20/20`, interruption `0`, stale `0`. The priority queue remains operationally healthy through this observation; no profitability conclusion is available.
Additional same-window 30d/1m v78 filled-cache combination study (`/tmp/coinpilot-variants-filled-v78-30d1m-combos.json`) rejected volume/rebound/trend/range/protection combinations: baseline `-0.2534%`/44 trades, rebound25 `-0.0200%`/9, volume15 `-0.1525%`/33, range20+volume15 `-0.0824%`/20, and all strong-rebound/no-highbreak, trend-nonnegative, protective, and volume15+protective combinations were non-positive or zero-trade. No runtime candidate was applied.
v78 read at `2026-09-13T12:14:47.871Z`: active cycle `128`, strict `0` trades/open, Shadow/Loose `7` closes each (`0W/7L`) at `-2,303.8039 KRW` with two diagnostic opens. Risk attempt/success `12:14:47.788Z`, failures/outages `0/0`, max gap `0`, continuity true; analysis `20/20`, stale blocks `1`, interruptions `0`. Continue isolated forward observation; no live orders.
v78 latest read at `2026-09-13T12:16:31.133Z`: owner PID `35595`/npm `35569`/root lock and Observer `3156` remain healthy; cycle `141`, strict `0` closes/open, Shadow/Loose `7` closes (`0W/7L`) at `-2,303.8039 KRW` with 3 diagnostic opens each. Risk attempt/success are current, failures/outages `0/0`, max gap `0`, continuity true; analysis expected/analyzed `20/20`, interruption `0`, stale `0`. Keep v78 active; no runtime/live change.
v78 latest read at `2026-09-13T12:19:14.115Z`: owner PID `35595`, npm `35569`, root lock, and Observer `3156` healthy; cycle `162`, strict `0` trades/open, Shadow/Loose `7` closes (`0W/7L`) at `-2,303.8039 KRW` with 3 diagnostic opens each. Risk success `12:19:14.115Z`, failures/outages `0/0`, max gap `0`, continuity true; analysis expected/analyzed `20/20`, interruptions `0`, stale `1`. No runtime tuning or live action.
v78 read at `2026-09-13T12:20:45.296Z`: cycle `174`, owner/lock/Observer healthy, strict `0` trades/open. Shadow/Loose remain `7` closes (`0W/7L`) at `-2,303.8039 KRW` with 3 diagnostic opens each. Risk attempt/success `12:20:45.296Z`, failures/outages `0/0`, max gap `0`, continuity true; analysis `20/20`, interruptions `0`, stale `1`. Recent relaxed losses repeatedly carry `trend_filter_failed` and/or `previous_high_break_failed`; keep the existing strict filters and do not loosen from this small diagnostic sample.
v78 read at `2026-09-13T12:21:50.518Z`: cycle `182`, strict remains `0` trades/open, Shadow/Loose each `8` closes (`1W/7L`) at `-1,966.5061 KRW`, 2 diagnostic opens. The new relaxed winner `KRW-ARK` hit take-profit for `+337.2978 KRW` after entry rebound `2.0325%`, but carried `volume_confirmation_failed`, `trend_filter_failed`, and `previous_high_break_failed`; this is a single counterexample to filter rejection, not a reason to loosen. Risk failures/outages `0/0`, max gap `0`, continuity true; analysis `20/20`, interruptions `0`, stale `1`.
v78 latest read at `2026-09-13T12:23:16.676Z`: owner/lock/Observer healthy, cycle `193`, strict `0` trades/open, Shadow/Loose `8` closes (`1W/7L`) at `-1,966.5061 KRW` with 2 diagnostic opens. Risk attempt/success `12:23:16.676Z`, failures/outages `0/0`, max gap `0`, continuity true; analysis expected/analyzed `20/20`, interruptions `0`, stale `1`. Continue v78; no runtime/live change.
Targeted filled-window soft hypothesis study at `2026-09-13T12:25:07.185Z`: large_rebound_soft holdout `1` trade / `-0.0043%`, volume15_soft `41` / `-0.1474%`, rebound25_soft `13` / `-0.0178%`, rebound25_volume15 `9` / `-0.0095%`; all failed training/confidence gates. No runtime candidate applied.
v78 read at `2026-09-13T12:27:40.910Z`: cycle `227`, strict `0` trades/open, Shadow/Loose `9` closes (`2W/7L`) at `-1,554.4665 KRW` with 3 diagnostic opens. Risk attempt/success current, failures/outages `0/0`, max gap `0`, continuity true; analysis `20/20`, interruptions `0`, stale `2`. Continue v78; no runtime/live change.
v78 read at `2026-09-13T12:28:56.820Z`: owner PID `35595`/npm `35569`/lock/Observer healthy, cycle `237`, strict `0` trades/open, Shadow/Loose `9` closes (`2W/7L`) at `-1,554.4665 KRW` with 2 diagnostic opens. Risk attempt/success `12:28:56.820Z`, failures/outages `0/0`, max gap `0`, continuity true; analysis expected/analyzed `20/20`, interruptions `0`, stale `2`. No runtime/live change.
Targeted filled-window soft hypothesis study at `2026-09-13T12:25:07.185Z` tested large rebound and relaxed volume/trend/high-break combinations: large_rebound_soft `1` holdout trade / `-0.0043%`, volume15_soft `41` / `-0.1474%`, rebound25_soft `13` / `-0.0178%`, rebound25_volume15 `9` / `-0.0095%`; all failed training/confidence gates. No runtime candidate applied.
v78 latest read at `2026-09-13T12:29:58.874Z`: active cycle `245`, strict `0` trades/open, Shadow/Loose `9` closes (`2W/7L`) at `-1,554.4665 KRW` with 2 diagnostic opens. Risk attempt/success current, failures/outages `0/0`, max gap `0`, continuity true; analysis expected/analyzed `20/20`, interruptions `0`, stale `2`. Keep v78 active; no runtime/live change.
v78 latest read at `2026-09-13T12:31:17.110Z`: owner/lock/Observer healthy, cycle `255`, strict `0` trades/open, Shadow/Loose `10` closes (`2W/8L`) at `-1,614.3816 KRW` with 2 diagnostic opens. New SOPH relaxed max-hold loss was `-59.9151 KRW`; risk attempt/success current, failures/outages `0/0`, max gap `0`, continuity true; analysis `20/20`, interruptions `0`, stale `2`. No runtime/live change.
v78 read at `2026-09-13T12:32:13.360Z`: cycle `262`, strict `0` trades/open, Shadow/Loose `10` closes (`2W/8L`) at `-1,614.3816 KRW` with 3 diagnostic opens. Risk attempt/success current, failures/outages `0/0`, max gap `0`, continuity true; analysis `20/20`, interruptions `0`, stale `2`. No new strict sample or runtime/live change.
v78 latest read at `2026-09-13T12:34:34.578Z`: owner/lock/Observer healthy, cycle `281`, strict `0` trades/open, Shadow/Loose `13` closes (`3W/10L`) at `-1,820.7557 KRW` with 1 diagnostic open. New relaxed outcomes: POWR stop-loss `-292.0219`, UP2 take-profit `+421.6237` followed by stop-loss `-335.9759`; rejection cohorts remain trend `10`/3W7L/`-1,176.7203`, previous-high `9`/2W7L/`-1,211.7034`, volume `8`/3W5L/`-550.8769`, close-strength `2`/1W1L/`+117.7859`. Risk failures/outages `0/0`, max gap `0`, continuity true; analysis `20/20`, interruptions `0`, stale `4`. No runtime/live change.
v78 latest read at `2026-09-13T12:35:34.576Z`: owner/lock/Observer healthy, cycle `288`, strict `0` trades/open, Shadow/Loose `13` closes (`3W/10L`) at `-1,820.7557 KRW` with 1 diagnostic open. Risk attempt/success current, failures/outages `0/0`, max gap `0`, continuity true; analysis expected/analyzed `20/20`, interruptions `0`, stale `4`. No new strict sample or runtime/live change.
Added future-session shadow execution realism: relaxed `shadow`/`looseShadow` now reject a diagnostic entry whose observed price is beyond the strict `maxEntryRetracePercent` or `maxEntryChasePercent` from the rebound reference, while preserving legacy fixtures without a reference as non-enforceable. New `shadowEntryExecutionBlockedEntries`/reason telemetry distinguishes rejected impossible fills. Regression covers chase, retrace, and in-bound entry; full suite after the change is `188/188 PASS`, syntax/HTML/PWA/diff checks pass. Current v78 predates this change and therefore has no execution-block telemetry.
v78 pre-guard read at `2026-09-13T12:43:53.598Z`: owner/lock/Observer healthy, cycle `352`, strict `0` trades/open, Shadow/Loose `14` closes (`3W/11L`) at `-2,101.9814 KRW` with 3 diagnostic opens. Risk attempt/success `12:43:50.387Z`, failures/outages `0/0`, max gap `0`, continuity true; analysis `20/20`, interruptions `0`, stale `6`. Preserve v78 opens through risk exit before starting the next code session.
Future-session shadow execution boundary is implemented in `MultiCoinTrader`: shadow/loose now block a diagnostic fill when the observed price exceeds strict retrace/chase limits relative to a valid rebound reference, with per-book block counters and reasons; legacy diagnostics without a reference remain non-enforceable rather than fabricated. Tests cover chase, retrace, and in-bound cases; full suite is `188/188 PASS`, syntax/HTML/PWA/diff/debug checks pass. Current v78 predates this code and remains a preserved pre-guard sample.
v78 read at `2026-09-13T12:45:27.727Z`: owner/lock/Observer healthy, cycle `364`, strict `0` trades/open, Shadow/Loose `16` closes (`3W/13L`) at `-2,711.9613 KRW` with 2 diagnostic opens. New MTL stop-loss `-293.9536 KRW`; risk attempt/success current, failures/outages `0/0`, max gap `0`, continuity true; analysis `20/20`, interruptions `0`, stale `6`. Keep v78 until its open diagnostics settle before starting the next-code session.
Added paper UI display for future shadow execution-boundary telemetry and bumped PWA assets to `observer-readonly-11` / `coinpilot-shell-v15`; regression covers asset/cache coupling and paper diagnostics. Full suite after UI/guard changes is `188/188 PASS`; JS/HTML/manifest/SW/diff/debug checks pass.
v78 pre-guard read at `2026-09-13T12:49:09.918Z`: owner/lock/Observer healthy, cycle `393`, strict `0` trades/open, Shadow/Loose `17` closes (`3W/14L`) at `-3,028.6157 KRW` with 3 diagnostic opens (`BLAST`, `FLOCK`, `STEEM`). Risk failures/outages `0/0`, max gap `0`, continuity true; analysis `20/20`, interruptions `0`, stale `7`. Preserve all v78 diagnostic opens through risk exit before launching next-code v79.
v78 latest pre-guard read at `2026-09-13T12:50:15.007Z`: owner/lock/Observer healthy, cycle `401`, strict `0` trades/open, Shadow/Loose `17` closes (`3W/14L`) at `-3,028.6157 KRW` with 3 opens (`BLAST`, `FLOCK`, `STEEM`). Risk attempt/success current, failures/outages `0/0`, max gap `0`, continuity true; analysis `20/20`, interruptions `0`, stale `7`. ARK had since stopped out at `-316.6544 KRW`; no strict sample. Preserve v78 until diagnostics settle before next-code session.
UI now presents future `shadowEntryExecutionBlockedEntries`/chase/retrace counts in both paper surfaces; PWA versions are `observer-readonly-11` and `coinpilot-shell-v15`. The new guard/UI regression and full source/static validation are green at `189/189 PASS`; current v78 predates the guard.
v78 latest pre-guard read at `2026-09-13T12:52:29.324Z`: owner/lock/Observer healthy, cycle `418`, strict `0` trades/open, Shadow/Loose `19` closes (`3W/16L`) at `-3,726.6794 KRW` with `STEEM` and `UP2` diagnostic opens. Risk attempt/success `12:52:29.324Z`, failures/outages `0/0`, max gap `0`, continuity true; analysis expected/analyzed `20/20`, interruptions `0`, stale `7`. Preserve v78 open diagnostics before next-code v79.
v78 latest pre-guard read at `2026-09-13T12:55:27.674Z`: owner/lock/Observer healthy, cycle `441`, strict `0` trades/open, Shadow/Loose `19` closes (`3W/16L`) at `-3,726.6794 KRW` with `STEEM`, `UP2`, and `B3` diagnostic opens. Risk attempt/success current, failures/outages `0/0`, max gap `0`, continuity true; analysis expected/analyzed `20/20`, interruptions `0`, stale `7`. Preserve v78 open diagnostics before next-code v79.
v78 stopped cleanly at `2026-09-13T12:56:44.246Z` after `451` cycles with `strictTrades=0`, Shadow/Loose `20` closes (`3W/17L`) at `-4,016.0546 KRW`, and open diagnostics `STEEM/UP2/POWR`. `stopReason=stopped_with_unsettled_diagnostic_positions`, continuity remained true, and the owner disappeared. The stale root lock was moved recoverably to `/tmp/coinpilot-v78-stale-lock-20260913.json`; v78 remains preserved and non-promotional.
Latest-code v79 started at `2026-09-13T12:57:26.436Z` in `.paper-forward-v79-shadow-boundary`, owner PID `79442`, root lock token `e6734b12...`, and read-only Observer `3157`. At `2026-09-13T12:59:02.673Z` cycle `12` was active with analysis `20/20`, risk failures/outages `0/0`, continuity true, strict `0`, Shadow/Loose each one stop-loss (`-419.1959 KRW`), and execution-boundary blocks `0`. Observer paper/index/manifest/SW reads returned `200`; session start mutation returned `403`. v79 is the first session measuring the new shadow retrace/chase guard.
v79 read at `2026-09-13T13:03:14.871Z`: cycle `45`, strict `0` trades/open, Shadow/Loose each `1` stop-loss (`-419.1959 KRW`) with 3 diagnostic opens. New execution-boundary guard blocked 3 entries per book: chase `1`, retrace `2`; no blocked entry was counted as a fill. Risk failures/outages `0/0`, max gap `0`, continuity true; analysis `20/20`, interruptions `0`, stale `0`. This confirms the guard is active; it is not a profitability conclusion.
v79 read at `2026-09-13T13:04:25.959Z`: cycle `54`, strict `0` trades/open, Shadow/Loose each `1` stop-loss (`-419.1959 KRW`) with 3 diagnostic opens. Execution-boundary guard blocks are shadow `3` (chase1/retrace2) and loose `4` (chase1/retrace3), none counted as fills. Risk failures/outages `0/0`, max gap `0`, continuity true; analysis `20/20`, interruptions `0`, stale `0`. Continue forward observation; no profitability claim.
v79 read at `2026-09-13T13:01:58.775Z`: cycle `35`, strict `0` trades/open, Shadow/Loose each `1` stop-loss (`-419.1959 KRW`) with 3 diagnostic opens. Execution-boundary blocks remain `0` because current entries were within reference limits. Risk success current, failures/outages `0/0`, max gap `0`, continuity true; analysis `20/20`, interruptions `0`, stale `0`. Keep v79 active.
v79 read at `2026-09-13T13:05:50.743Z`: cycle `64`, strict `0` trades/open, Shadow/Loose each `1` stop-loss (`-419.1959 KRW`) with 3 diagnostic opens. Execution-boundary guard blocked shadow `6` entries (chase2/retrace4) and loose `7` (chase2/retrace5); each book has 4 attempted entries including the one closed loss, so blocked candidates were not counted as fills. Risk attempt/success current, failures/outages `0/0`, max gap `0`, continuity true; analysis `20/20`, interruptions `0`, stale `0`. Guard effect is confirmed as execution-quality evidence only.
v79 latest read at `2026-09-13T13:08:32.230Z`: owner/lock/Observer healthy, cycle `84`, strict `0` trades/open, Shadow/Loose each `1` stop-loss (`-419.1959 KRW`) with 3 diagnostic opens. Execution-boundary blocks remain shadow `6` (chase2/retrace4) and loose `7` (chase2/retrace5); risk failures/outages `0/0`, max gap `0`, continuity true; analysis `20/20`, interruptions `0`, stale `0`. Guard activation remains stable; no profitability conclusion.
v79 latest read at `2026-09-13T13:07:20.344Z`: owner/lock/Observer healthy, cycle `74`, strict `0` trades/open, Shadow/Loose each `1` stop-loss (`-419.1959 KRW`) with 3 diagnostic opens. Execution-boundary blocks remain shadow `6` (chase2/retrace4) and loose `7` (chase2/retrace5); risk failures/outages `0/0`, max gap `0`, continuity true; analysis `20/20`, interruptions `0`, stale `0`. No profitability claim; continue v79.
v78 read at `2026-09-13T12:33:14.321Z`: cycle `270`, strict `0` trades/open, Shadow/Loose `13` closes (`3W/10L`) at `-1,820.7557 KRW` with 1 diagnostic open. New relaxed outcomes were POWR stop-loss `-292.0219 KRW` and UP2 take-profit `+421.6237 KRW` followed by UP2 stop-loss `-335.9759 KRW`. Rejection cohorts: trend 10 trades / 3W7L / `-1,176.7203 KRW`; previous-high 9 / 2W7L / `-1,211.7034`; volume 8 / 3W5L / `-550.8769`; close-strength 2 / 1W1L / `+117.7859`. Risk failures/outages `0/0`, max gap `0`, continuity true; analysis `20/20`, interruptions `0`, stale `3`. Keep strict trend/high-break/volume filters; close-strength positive result is under-sampled.
Segmented diagnostic lane added at `2026-09-13T09:24Z`: exported `splitHistoricalCandleSegments()` and `simulateScalpingSegmented()`; added cache-only `npm run validate:scalping:segments` and `test/validateScalpingSegments.test.js`. It splits at historical gaps without flat-filling, excludes segment-end `BACKTEST_END` positions from realized metrics as `unknownBoundaryPositions`, records excluded short segments, and hard-sets `promoted=false`. v73 snapshot-configured 30d/1m report `/tmp/coinpilot-segmented-30d-1m-v73-v2-20260913.json`: 161 closed, 42W/119L, net `-10,357.65 KRW`, unknown1, used36/excluded89 segments. Explicit RSI35/65 60d/15m report `/tmp/coinpilot-segmented-60d-15m-rsi35-65-v2-20260913.json`: 51 closed, 4W/47L, net `-3,764.02 KRW`, used34/excluded10,966, unknown0. CLI config was corrected to read all SCALP env fields; snapshot remains authoritative when supplied. Full suite after lane is `169/169 PASS`, syntax/diff checks pass; no runtime/live change.
Higher-timeframe momentum research lane added at `2026-09-13T13:23:05Z`: `src/research/higherTimeframeMomentum.js`, `src/scripts/validateHigherTimeframeMomentum.js`, `npm run research:htf-momentum`, and five tests. It uses contiguous raw 15-minute candles, aggregates completed 1-hour/4-hour bars, enters on the next base candle, and includes fees, slippage, conservative stop-first OHLC exits, max hold, and expanding walk-forward folds. On `/tmp/coinpilot-60d-15m-20mk-20260913.json` using contiguous `KRW-BTC/ETH/XRP/SOL`, full-window results were positive but small (`1h RSI65/trend7d +0.1988%/55 trades/PF1.62`, `1h RSI65/trend14d +0.2272%/45/PF2.00`, `4h RSI65/trend14d +0.2165%/38/PF1.92`); every variant failed at least one market/fold validation, usually later-regime negative/low-sample confidence, so no forward shadow or runtime change is justified. Report `/tmp/coinpilot-htf-momentum-60d-20260913.json` is hard `promoted=false`; full suite after this lane is `194/194 PASS`, all source/test JS, inline HTML, PWA manifest/assets, and `git diff --check` pass.
The earlier ad hoc OOS file `/tmp/coinpilot-oos-60d-15m-4mk.json` was reviewed and rejected: all four markets share a raw no-trade gap around `2026-07-05T17:00Z→21:45Z`, and direct official API behavior confirms missing candles are omitted when no trades occur. Its apparent +48h momentum result is not valid contiguous OOS evidence. Re-running the new lane against it produced `/tmp/coinpilot-htf-momentum-oos-20260913.json` with zero trades and all variants/folds failed closed; do not cite the old positive result as validation.
v79 latest-code read at `2026-09-13T13:29:05.800Z`: owner/lock/Observer healthy, cycle `242`, strict `0` closes/open/profit, Shadow/Loose each `7` entries and `5` closes (`2W/3L`) at `-117.9075 KRW`, open `KRW-MTL/KRW-LSK`. Execution-boundary blocks are shadow `12` (chase4/retrace8) and loose `13` (chase4/retrace9); risk failures/outages `0/0`, analysis `20/20`, interruptions `0`. This remains execution-quality and small diagnostic evidence, not a promotion result; no runtime/live change.
After adding the higher-timeframe lane, the concurrent draft `validateRegimeMomentum.js` was audited and made fail-closed at the evidence seams: it now uses full canonical groups (no partial 1h bars), adds `CLOSE` type to confidence samples, requires the confidence gate explicitly, and preserves gap segments as unknown. Re-run on current contiguous BTC/ETH/XRP/SOL: BTC/ETH/XRP holdout returns `-12.9765%/-12.0552%/-8.9309%` with only 8/8/5 confidence samples; SOL `+9.3864%` has 4 samples and one unknown boundary. The gapped OOS file has one gap per market and all confidence gates false. It remains draft research-only and is not wired to runtime.
v79 read at `2026-09-13T13:40:14.602Z`: active owner/lock/Observer healthy, cycle `328`, strict `0` closes/open/profit, shadow `7` entries/`7` closes (`3W/4L`) `-66.8227 KRW` flat, loose `8` entries/`7` closes `-66.8227 KRW` with `KRW-ETH` open. Execution-boundary blocks unchanged shadow12/loose13; risk failures0, analysis gaps0, interruptions0. Keep v79 isolated; no runtime/live change.
The corrected draft regime CLI reports current-window portfolio `+4.9989%` with MDD `7.1637%` but mixed holdouts and confidence gates false; gapped-window portfolio `+0.7662%` has 4 unknown boundaries. Full current test suite including draft regime tests is `202/202 PASS`; no runtime/live wiring.
Canonical higher-timeframe portfolio replay was added and executed at `2026-09-13T13:46:12Z`: on contiguous July-September BTC/ETH/XRP/SOL, 1h RSI65/trend7d returned `+5.5494%` (55 closes, PF1.4433, MDD6.3818%, unknown0), 1h RSI65/trend14d `+7.6535%` (45, PF1.9147, MDD5.8572%, unknown0), 1h RSI70/trend7d `+3.5809%` (46, PF1.3068), cross-up `+5.0408%` (36, PF1.6401), and 4h RSI65/trend14d `+6.5798%` (38, PF1.7605, unknown1). Every candidate still fails market walk-forward folds, so `eligibleForFurtherShadow=false`; gapped OOS portfolio is `DATA_QUALITY_FAIL`. Portfolio replay uses shared balance, same timestamp exit-before-entry ordering, maxPositions, and rejects mismatched grids.
After adding portfolio replay, current full source suite is `204/204 PASS`; all JS syntax, inline HTML/PWA, and diff checks pass. No runtime/live wiring or parameter promotion.
Read-only strategy research UI was added at `2026-09-13`: `/api/strategy-research` reads an explicitly configured higher-timeframe report and hard-forces `researchOnly=true`/`promoted=false`; the validation-history page renders shared-portfolio variants and fold/boundary HOLD status. Browser mock verification on port `3143` showed the card and API `200`; a 390px viewport showed the card/nav and after removing the legacy `body min-width:320px` constraint, `document.scrollWidth=clientWidth` with no horizontal overflow. Assets were bumped to redesign JS `observer-readonly-15`, CSS `20260913-10`, service-worker cache `coinpilot-shell-v20`; the counterfactual UI, reason-cohort, and loss/opportunity summary additions are included in this cache generation. Route/UI coupling tests passed; research card remains display-only.
After UI wiring and the mobile overflow fix, the latest full suite is `207/207 PASS`; no order/live semantics changed.
v79 read at `2026-09-13T13:59:16.624Z`: owner/lock/Observer active, cycle `475`, strict `0` trades/open/profit; shadow `12` entries/`10` closes (`4W/6L`) `-394.8795 KRW` with POWR/VTHO open; loose `12` entries/`9` closes (`3W/6L`) `-784.0654 KRW` with ETH/POWR/VTHO open. Execution blocks remain shadow13/loose14, risk failures0, analysis20/20, interruption0. Strict telemetry has oversold991/rebound425/confirmed0 and stale snapshot blocks73; no filter relaxation or live.
v79 was stopped at `2026-09-13T14:08:30.877Z` with `active=false`, strict0, shadow/loose11 closes and diagnostic open `POWR/MTL/LSK`, execution blocks15/16, no temporary ledger file, and `stopReason=stopped_with_unsettled_diagnostic_positions`; this ledger is preserved and not mixed.
Latest-code v80 started at `2026-09-13T14:08:50.343Z` in `.paper-forward-v80-counterfactual` with owner PID `22673`, complete 20-market snapshot, risk interval1s, and no user-state writes. At `2026-09-13T14:11:47.512Z` cycle23: strict0, shadow/loose each 3 open (`LSK/FLOCK/MTL`), counterfactual blocked/pending/settled `0/0/0`, analysis20/20, risk failures0, interruptions0. Keep v80 running to observe real boundary blocks and subsequent diagnostic-only counterfactual settlement.
Execution-boundary counterfactual telemetry was added to future paper sessions: blocked shadow/loose signals are recorded separately with signal key, blocked reason, virtual entry, MFE/MAE, and later fresh-price stop/take/max-hold settlement; session-stop pending entries become `unresolved` and never enter realized P&L. Status/UI expose blocked, pending, settled, unresolved, and counterfactual net profit without changing strict/shadow/loose fills. Regression coverage includes positive settlement, unresolved stop, status projection, and UI copy. Full suite after this change is `209/209 PASS`; v80 is latest-code and has not yet generated a blocked event at cycle33.
Authoritative v80 read at `2026-09-13T14:17:04.731Z`: owner/lock healthy, cycle `63`, strict `0` closes/open/profit; shadow and loose each `4` entries/`2` closes (`1W/1L`) at `-8.1182 KRW`, open `KRW-FLOCK/KRW-MTL`. Execution-boundary blocks and counterfactuals remain `0`; analysis `20/20`, risk failures `0`, interruptions `0`.
v80 first counterfactual settlement at `2026-09-13T14:23:46.886Z`: cycle `115`, shadow/loose each `6` entries/`3` real closes (`2W/1L`) realized `+378.1304 KRW`, open `FLOCK/MTL/BLAST`. Three boundary records per book: STORJ `entry_chase_exceeded` settled `STOP_LOSS` `-470.3457 KRW`; LSK `entry_retrace_exceeded` settled `STOP_LOSS` `-386.8017 KRW`; STEEM `entry_retrace_exceeded` remained pending. Counterfactual total `-857.1474 KRW` is separate from actual shadow/loose P&L; telemetry blocks `3/3`, risk failures0, analysis20/20, interruptions0. This is execution-quality evidence, not a profitability claim.
v80 counterfactual cohort completed at `2026-09-13T14:25:25.001Z`: cycle `127`, shadow/loose each `4` actual closes with realized `+45.5970 KRW` and open `FLOCK/MTL`; STORJ chase, LSK retrace, and STEEM retrace counterfactuals all settled `STOP_LOSS` at `-470.3457/-386.8017/-306.0890 KRW`, total `-1,163.2364 KRW`. This is three per-signal loss-avoidance observations, not a portfolio-profit or generalized guard-efficacy proof; no runtime filter change.
Read-only v80 Observer after settlement returned `HTTP 200`: actual shadow/loose realized `+45.5970 KRW` and counterfactual `-1,163.2364 KRW` with `3 settled/1 pending`; strict remained `0`. index/manifest/SW were `200`, Observer start mutation `403`; Observer was stopped and v80 runner preserved.
Latest v80 read at `2026-09-13T14:27:00.067Z`: cycle `140`, active, strict0; shadow `6` entries/`4` actual closes `+45.5970 KRW`, open `FLOCK/MTL`, boundary `4` records (`3 settled`, `1 pending`) and counterfactual `-1,163.2364 KRW`; risk/analysis/interruption healthy. No general guard promotion.
Authoritative v80 read at `2026-09-13T14:28:59.097Z`: cycle `154`, active owner/lock, strict0; shadow/loose each `8` entries/`6` actual closes (`2W/4L`) realized `-556.5753 KRW`, open `FLOCK/MTL`; boundary records `6` per book (`4 settled STOP_LOSS`, `2 pending`) with counterfactual `-1,513.7173 KRW`. New pending entries: POWR chase (MFE+.9456/MAE-.6807) and POLYX retrace (MFE+.4372/MAE-1.1741). Risk failures0, analysis20/20, interruptions0. Guard is promising for observed blocked losses but strategy remains negative/small-sample.
v80 reason cohort read at `2026-09-13T14:31:25.864Z`: cycle `170`, shadow/loose each `9` entries/`8` actual closes (`3W/5L`) realized `-371.3210 KRW`, open `MTL`; boundary records `9` (`6 settled`, `3 pending`) with counterfactual `-1,462.9486 KRW`. Reason outcomes: chase `3` blocked / `2` settled / `-762.1222 KRW`; retrace `6` / `4` / `-700.8264 KRW`. This remains per-signal guard evidence only; actual relaxed P&L is negative, strict0, no tuning/promotion.
OOS3 audit: `/tmp/coinpilot-oos3-60d-15m-4mk.json` has a common raw no-trade gap `2026-04-12T17:00Z→22:30Z` (21 missing 15m intervals) across BTC/ETH/XRP/SOL; canonical higher-timeframe replay fails closed. Segmented draft diagnostic is `-1.0845%`/36 events/MDD3.1419/unknown5, so it is not contiguous promotion evidence.
OOS4 audit: `/tmp/coinpilot-oos4-60d-15m-4mk.json` is raw-contiguous, but canonical higher-timeframe candidates all fail market walk-forward; 1h RSI65/trend7d portfolio `+0.3656%`, RSI65/trend14d `-0.3247%`, cross-up `+1.7514%`, all eligible false. Segmented draft portfolio `+0.7139%`/30 events/MDD8.1691/unknown4 has all four market holdouts negative and confidence false; positive total is train-driven/under-sampled. Do not promote or wire runtime.
Latest v80 read at `2026-09-13T14:35:32.407Z`: active cycle `203`, strict0, shadow/loose actual `8` closes `-371.3210 KRW` with open `MTL/VTHO/ARK`; execution-boundary records `10` per book (`8 settled`, `2 pending`) and counterfactual realized `-2,234.5312 KRW`; risk0, analysis20/20, interruptions0. Chases/retraces remain diagnostic-only; actual strategy P&L is negative.
v80 latest cohort read at `2026-09-13T14:38:16.110Z`: active cycle `223`, strict0; shadow/loose each `13` entries/`10` actual closes (`3W/7L`) realized `-1,023.2889 KRW`, open `VTHO/LSK/UP2`. Boundary records `13` per book (`11 settled`, `2 pending`) with counterfactual `-3,174.7509 KRW`; chase `4/4` settled losses `-1,500.2316`, retrace `7` settled (`1W/6L`) `-1,674.5192`. Risk0, analysis20/20, interruptions0; retain guard, do not tune from this single small cohort.
v80 latest cohort read at `2026-09-13T14:38:16.110Z`: active cycle `223`, strict0; shadow/loose each `13` entries/`10` actual closes (`3W/7L`) realized `-1,023.2889 KRW`, open `VTHO/LSK/UP2`. Boundary records `13` per book (`11 settled`, `2 pending`) with counterfactual `-3,174.7509 KRW`; chase `4/4` settled losses `-1,500.2316`, retrace `7` settled (`1W/6L`) `-1,674.5192`. Risk0, analysis20/20, interruptions0; retain guard, do not tune from this single small cohort.
Counterfactual summary now also exposes loss-direction count/amount versus missed-profit-direction count/amount; both are diagnostic and never enter realized or promotion metrics. v80 currently has 10 settled boundary candidates: chase 4/4 losses, retrace 7 settled with 1 win/6 losses, plus 2 pending records in the latest read.
An auxiliary daily regime-momentum shadow process (`src/scripts/runRegimeMomentumShadow.js`, PID43740, `.paper-momentum-shadow-v1/`) is alive with 3 cycles, zero positions/trades, breadth0, and all four 7-day trends negative. Its ledger records a config drift from trend>0/breadth>=1 to trend>2/breadth>=3, so it is explicitly non-promotional and must not be combined with v80; owner is alive despite a ~3.4-minute daily polling heartbeat age.
The auxiliary daily shadow was restarted/reconfigured at `2026-09-13T14:46:01Z` (owner PID48062, 12 markets, breadthMin1) and now has 5 cycles with DOT/ATOM open, breadth2, no closes, and the prior four-market config drift retained. It is still non-promotional and separate from v80; its daily heartbeat was ~71s old at the 14:47 read while owner remained alive.
Authoritative v80 read at `2026-09-13T14:47:17.218Z`: active cycle `292`, strict0; shadow/loose each `16` entries/`13` closes (`4W/9L`) realized `-1,300.5264 KRW`, open `VTHO/B3/POWR`; boundary `16` records (`13 settled`, `3 pending`) counterfactual `-3,046.1084 KRW`; risk0, analysis20/20, interruption0. Keep v80 isolated; no runtime/live.
Latest authoritative v80 read at `2026-09-13T14:53:37.040Z`: owner PID22673 and root lock still match, active cycle `341`, strict `0` trades/open, shadow/loose each `14` actual closes (`5W/9L`) realized `-963.7554 KRW` with `VTHO/POWR/B3` open. Execution-boundary records are `17` per book (`13 settled`, `4 pending`), with settled counterfactual net `-3,046.1084 KRW`; pending records are HIVE/POLYX/UP2/B3. Analysis completed `20/20`, risk last success current with failures/outages `0/0`, interruptions `0`. The four pending counterfactuals and open diagnostic positions require v80 to remain active; no runtime/live/promotion change.
v80 settlement read at `2026-09-13T14:59:27.698Z`: POWR relaxed diagnostic position closed by `TAKE_PROFIT`, entry `84.2842` → exit `85.914`, net `+366.3575 KRW` / `+1.8318%`, MFE `+2.0357%`, MAE `-0.0999%`, while carrying price/recovery/volume/trend/previous-high rejection reasons. Shadow/loose now each have `15` closes (`6W/9L`) at `-597.3979 KRW`, with VTHO/B3/ARK open. Boundary records are `17` each, `15` settled and `2` pending (POLYX/UP2), settled counterfactual net `-3,028.5848 KRW`. Strict remains `0`; analysis `20/20`, risk failures/outages `0/0`, interruptions `0`. This is relaxed diagnostic evidence only; keep v80 active and do not promote.
Latest v80 settlement read at `2026-09-13T15:00:29.787Z`: VTHO relaxed diagnostic closed by `TAKE_PROFIT`, entry `1.08108` → exit `1.10889`, net `+493.9762 KRW` / `+2.4699%`, MFE `+2.6751%`, MAE `-1.0249%`, while carrying close-strength/trend/previous-high rejection reasons. B3 separately closed by `STOP_LOSS`, entry `0.769769` → exit `0.75924`, net `-293.2841 KRW` / `-1.4664%`, MFE `+0.8095%`, MAE `-1.2691%`, with volume/trend rejection. Shadow/loose now each have `17` closes (`7W/10L`) realized `-396.7059 KRW`, open ARK/HIVE/CVC. Boundary records are `18` each, `15` settled and `3` pending (POLYX/UP2/ARDR), settled counterfactual net `-3,028.5848 KRW`. Strict remains `0`; analysis `20/20`, risk failures/outages `0/0`, interruptions `0`. Keep this as relaxed diagnostic evidence only and leave v80 active for open positions/pending records.
Latest v80 revalidation at `2026-09-13T15:02:48.175Z`: CVC relaxed diagnostic closed by `STOP_LOSS`, entry `52.7527` → exit `52.0479`, net `-286.9369 KRW` / `-1.4347%`, with MFE `+1.4166%`, MAE `-1.4347%`, and volume/close/trend rejection context. Shadow/loose each now have `18` closes (`7W/11L`) realized `-683.6428 KRW`, open ARK/HIVE/ARDR. Boundary records remain `18` each (`15 settled`, `3` pending: POLYX/UP2/ARDR) with settled counterfactual net `-3,028.5848 KRW`. Strict remains `0`; analysis `20/20`, risk failures/outages `0/0`, interruptions `0`. v80 remains active because diagnostic positions and pending records are unsettled; no tuning or live promotion.
v80 selectivity read at `2026-09-13T15:04:06.913Z`: cycle `422`, strict remains `0` trades/open, while unique telemetry has `1,730` oversold observations and `581` strict rebound candidates but `0` confirmed candidates/entry attempts. This identifies the current bottleneck as the strict confirmation contract, not an incomplete analysis cycle: all `20/20` markets analyzed, incomplete `0`, risk failures/outages `0/0`, interruptions `0`. Shadow/loose remain `18` closes (`7W/11L`) at `-683.6428 KRW`, open ARK/HIVE/ARDR; boundary `15` settled/`3` pending with counterfactual net `-3,028.5848 KRW`. Do not loosen filters from this live selectivity count; keep v80 isolated.
v80 latest settlement read at `2026-09-13T15:05:52.334Z`: ARDR relaxed diagnostic closed `STOP_LOSS`, entry `39.4394` → exit `38.7612`, net `-363.5712 KRW` / `-1.8179%`, MFE `+0.6608%`, MAE `-1.6212%`, with RSI recovery/volume/trend/previous-high rejection reasons. ARK closed `TAKE_PROFIT`, entry `205.205` → exit `208.791`, net `+329.1597 KRW` / `+1.6458%`, MFE `+1.8494%`, MAE `-0.5872%`, with trend/previous-high rejection reasons. Shadow/loose now each have `20` closes (`8W/12L`) realized `-718.0543 KRW`, with HIVE open. Boundary records are `18` each, `16` settled and `2` pending (POLYX/UP2), settled counterfactual net `-3,342.1905 KRW`. Strict remains `0`; analysis `20/20`, risk failures/outages `0/0`, interruptions `0`. Relaxed outcomes remain non-promotional; keep HIVE/pending records under observation.
v80 boundary completion read at `2026-09-13T15:09:28.408Z`: the final pending `KRW-UP2:2026-09-13T14:44:00` retrace record settled as counterfactual `STOP_LOSS`, net `-293.9536 KRW` / `-1.4698%`, MFE `+0.9554%`, MAE `-1.2724%`. All `18` boundary records are now settled with net `-3,917.6158 KRW`: chase `5/5` losses (`-1,779.7512 KRW`) and retrace `13` (`3W/10L`, `-2,137.8646 KRW`). This is separate from actual shadow/loose P&L. The same v80 read has shadow `20` closes (`8W/12L`) at `-718.0543 KRW`, open HIVE/POLYX/XRP; loose open HIVE/ETH/POLYX; strict `0`. Analysis `20/20`, risk failures/outages `0/0`, interruptions `0`. Boundary evidence is complete for this cohort but remains diagnostic-only; keep open positions under risk observation.
v80 strict confirmation read at `2026-09-13T15:12:33.805Z`: one confirmed `KRW-POLYX` delayed-entry attempt was cancelled before a strict fill because the price retraced `0.56%`, exceeding the configured `0.25%` allowance. Confirmation telemetry is `7` confirmed candidates, `1` attempt, `0` successes, `1` cancellation; strict ledger remains `0` trades/open. Boundary records grew to `21` with `19` settled and `2` pending (POLYX/CVC), counterfactual settled net `-4,229.9370 KRW`. Shadow open HIVE/POLYX/XRP; loose open HIVE/ETH/POLYX. Analysis `20/20`, risk failures/outages `0/0`, interruptions `0`. This directly validates delayed retrace protection but is not profitability evidence.
v80 current read at `2026-09-13T15:16:34.725Z`: strict remains `0` trades/open with confirmation telemetry `7` candidates, `1` attempt, `0` successes, `1` cancellation. Shadow/loose actual books remain `20` closes (`8W/12L`) at `-718.0543 KRW`; shadow open HIVE/POLYX/XRP and loose open HIVE/ETH/POLYX. Boundary records are `23` each (`21` settled, `2` pending: POLYX/ARDR), settled counterfactual net `-4,268.0354 KRW`. Analysis remains `20/20` with incomplete `0`, risk failures/outages `0/0`, interruptions `0`; v80 stays active for unsettled diagnostic positions and pending records.
Latest single-source v80 read at `2026-09-13T15:19:34.875Z`: owner PID22673/root lock match, active cycle `540`, strict `0` trades/open/profit; shadow/loose each `20` actual closes (`8W/12L`) realized `-718.0543 KRW`, shadow open HIVE/POLYX/XRP and loose open HIVE/ETH/POLYX. Boundary records `23` each (`21` settled, `2` pending), settled counterfactual net `-4,268.0354 KRW`; confirmation telemetry `7` confirmed/`1` attempt/`0` success/`1` cancel. Analysis `20/20`, risk failures/outages `0/0`, interruptions `0`. Keep v80 active; no tuning or live promotion.
Research-only high-break study at `2026-09-13`: cache `/tmp/coinpilot-filled-v78-30d1m-20260913.json`, current-style RSI35/65, rebound0.15, volume1, closeStrength0.65, trend-0.2, 30-day 1-minute BTC/ETH/XRP. Baseline holdout was `44` trades / `-0.2534%`; `requirePreviousHighBreak=false` was `56` trades / `-0.2522%`. Both variants had `0/3` positive markets and all three training gates failed; the tiny aggregate improvement is not a robust candidate. Report `/tmp/coinpilot-v80-highbreak-cache-study-20260914.json`; no runtime change or promotion.
Latest v80 read at `2026-09-13T15:27:11.879Z`: owner PID22673/root lock match, active cycle `598`, strict `0` trades/open/profit. Shadow/loose each have `21` actual closes (`8W/13L`) realized `-1,006.5090 KRW`; shadow open POLYX/XRP/UP2 and loose open ETH/POLYX/UP2. All `23` boundary records are settled with counterfactual net `-4,870.8736 KRW`; confirmation telemetry remains `7` confirmed/`1` attempt/`0` success/`1` cancellation. Analysis `20/20`, risk failures/outages `0/0`, interruptions `0`. Keep v80 isolated; relaxed and counterfactual results remain non-promotional.
v80 read-only UI/API check at `2026-09-13T15:14:25Z`: a temporary dashboard observer on port `3158` read the v80 ledger with HTTP `200`, `readOnlyObserver=true`, `processAlive=true`, `orphaned=false`, strict confidence `0/20` with gate false, analysis `20/20`, risk failures/outages `0/0`; `/api/scalping-validation`, `/api/strategy-research`, root, manifest, service worker, and icon all returned `200`. Manifest reported `display=standalone`, and `POST /api/paper-validation/start` returned `403`; the observer was then stopped without touching v80 owner/lock. UI/API/PWA readback is verified, but native installation remains unrun.
v80 latest forward read at `2026-09-13T15:29:40.192Z`: owner PID22673/root lock match, active cycle `617`, strict `0` trades/open/profit; shadow `23` closes (`9W/14L`) realized `-873.7699 KRW`, open XRP/FLOCK/STEEM, and loose `23` closes (`9W/14L`) `-873.7699 KRW`, open ETH/FLOCK/STEEM. All `23` boundary records remain settled at counterfactual `-4,870.8736 KRW`; confirmation telemetry `7` confirmed/`1` attempt/`0` success/`1` cancel. Analysis `20/20`, risk failures/outages `0/0`, interruptions `0`. The relaxed high-break-failed cohort is now `17` trades (`9W/8L`, `+940.7426 KRW`) but remains a mixed small-sample diagnostic; current cached holdout still rejects high-break relaxation.
v80 latest risk read at `2026-09-13T15:31:41.607Z`: FLOCK/CVC/ARDR/STEEM relaxed diagnostic exits added four losses, leaving shadow/loose each `27` actual closes (`9W/18L`) realized `-2,398.7133 KRW`; shadow open XRP and loose open ETH. The active rejection cohorts now show trend failure `26` trades (`9W/17L`, `-2,114.3320 KRW`) and previous-high failure `21` (`9W/12L`, `-584.2008 KRW`), so the earlier temporary positive high-break cohort did not persist. One boundary record is pending (HIVE retrace, 23 settled), settled counterfactual net remains `-4,870.8736 KRW`. Strict remains `0`; analysis `20/20`, risk failures/outages `0/0`, interruptions `0`. Keep all runtime filters unchanged and do not promote high-break relaxation.
v80 latest diagnostic read at `2026-09-13T15:34:58.579Z`: PUNDIX relaxed diagnostic closed `STOP_LOSS`, entry `81.1811` → exit `80.1198`, net `-281.1984 KRW` / `-1.4060%`, MFE `-0.0999%`, MAE `-1.2085%`, with price/recovery/trend/previous-high rejection reasons. Shadow/loose each now have `29` closes (`9W/20L`) realized `-2,998.7890 KRW`; shadow open XRP/LSK and loose open ETH/LSK. Rejection cohorts are trend failure `28` (`9W/19L`, `-2,714.4076 KRW`) and previous-high failure `23` (`9W/14L`, `-1,184.2765 KRW`). Boundary has `26` records (`23` settled, `3` pending: HIVE/STORJ/PUNDIX), settled counterfactual remains `-4,870.8736 KRW`; strict remains `0`, analysis `20/20`, risk failures/outages `0/0`, interruptions `0`. This strengthens the decision to keep trend/high-break filters and not promote relaxed logic.
v80 latest settlement read at `2026-09-13T15:37:26.334Z`: LSK first closed `TAKE_PROFIT` `+338.0023 KRW`, but a subsequent LSK signal with only `trend_filter_failed` closed `STOP_LOSS`, `458.458` → `450.549`, net `-364.6762 KRW`, MFE `+0.7726%`, MAE `-1.6268%`. Shadow/loose now each have `31` closes (`10W/21L`) realized `-3,025.4628 KRW`, shadow open XRP/MTL and loose open ETH/MTL. Rejection cohorts are trend failure `30` (`10W/20L`, `-2,741.0815 KRW`) and previous-high failure `24` (`10W/14L`, `-846.2741 KRW`). Boundary records are `29` each (`25` settled, `4` pending: HIVE/MTL/PUNDIX/ARK), settled counterfactual remains `-5,543.5209 KRW`; strict remains `0`, analysis `20/20`, risk failures/outages `0/0`, interruptions `0`. The paired LSK outcomes show a one-signal winner followed by a loss, not a tuning basis.
v80 max-hold read at `2026-09-13T15:39:33.946Z`: Shadow `KRW-XRP` closed `MAX_HOLD_TIME`, entry `1836.835` → exit `1831.167`, net `-81.6482 KRW` / `-0.4082%`; Loose `KRW-ETH` closed `MAX_HOLD_TIME`, entry `3,386,383` → exit `3,383,613`, net `-36.3383 KRW` / `-0.1817%`. Shadow now has `33` closes (`10W/23L`) realized `-3,429.3956 KRW`, open ARK; Loose `33` closes (`10W/23L`) realized `-3,384.0858 KRW`, open ARK. Boundary records are `30` each (`26` settled, `4` pending), settled counterfactual net `-5,919.1102 KRW`; strict remains `0`, confirmation telemetry `7` confirmed/`1` attempt/`0` success/`1` cancel. Analysis `20/20`, risk failures/outages `0/0`, interruptions `0`. The max-hold losses add no basis for relaxing filters or changing exits.
v80 actual-book boundary read at `2026-09-13T15:42:24.293Z`: ARK relaxed diagnostic closed `STOP_LOSS`, entry `202.202` → exit `198.801`, net `-356.0550 KRW` / `-1.7803%`, MFE `-0.0999%`, MAE `-1.6845%`. Shadow/loose actual books are now empty at `34` closes (`10W/24L`), realized `-3,785.4506`/`-3,740.1407 KRW`; the earlier `23` boundary records remain settled, while newer boundary records total `30` (`26` settled, `4` pending: HIVE/MTL/PUNDIX/ARK). Settled counterfactual net is `-5,919.1102 KRW`; strict remains `0`, analysis `20/20`, risk failures/outages `0/0`, interruptions `0`. Actual relaxed books are empty, but pending counterfactuals remain under observation; no runtime change.
v80 current boundary wait at `2026-09-13T15:44:38.352Z`: owner PID22673/root lock match, active cycle `732`, strict `0` trades/open/profit. Shadow/loose actual books remain empty at `34` closes (`10W/24L`) with realized `-3,785.4506`/`-3,740.1407 KRW`; boundary records `31` each (`28` settled, `3` pending: HIVE/HIVE/ARDR) and settled counterfactual net `-6,143.8882 KRW`. Analysis `20/20`, incomplete `0`, risk failures/outages `0/0`, interruptions `0`; keep v80 active to settle pending counterfactuals, no runtime/live change.
v80 terminal read at `2026-09-13T15:47:45.875Z`: owner PID22673 is absent and root lock is released; ledger is `active=false`, ended `2026-09-13T15:47:21.241Z` after `754` cycles with `stopReason=risk_data_gap`, `lastFailureCode=RISK_CHECK_STALE`, and max risk gap `299.536s`. Strict remained `0` trades/open; Shadow/Loose each had `34` closes (`10W/24L`) at `-3,785.4506`/`-3,740.1407 KRW`, with one open diagnostic `KRW-SOPH`; boundary records were `32` each (`28` settled, `4` pending), settled counterfactual `-6,143.8882 KRW`. Risk continuity is false, so v80 is permanently non-promotional and must not be resumed or combined with a new cohort. A current read-only Upbit probe at `2026-09-13T15:48:26.687Z` returned HTTP `200` three times in `102/16/25ms`; this shows endpoint recovery now but does not establish the historical gap's root cause.
New v81 post-gap session started at `2026-09-13T15:49:43.999Z` in `.paper-forward-v81-post-gap` with session `paper-1789314583999`, owner PID `72406`, npm parent `72380`, and a new root lock. At `2026-09-13T15:50:52.474Z`/cycle9 it had a complete configuration snapshot, 20/20 analysis, risk failures/outages `0/0`, interruptions `0`, strict `0` trades, and zero realized relaxed trades; FLOCK/MTL were only initial diagnostic open positions. The v81 session is separate from v80's risk-invalid ledger and uses the unchanged strict runtime contract.
The v81 read-only dashboard observer on port `3158` then returned HTTP `200` for paper, validation, research, root, manifest, service worker, and icon; paper state was `readOnlyObserver=true`, `processAlive=true`, `orphaned=false`, strict confidence `0/20` with gate false, analysis `20/20`, risk failures/outages `0/0`, and manifest `display=standalone`. The paper start mutation returned `403`; observer was stopped without touching v81 owner/lock. Native PWA installation remains unrun.
v81 health follow-up at `2026-09-13T15:54:16.168Z`: owner PID72406/root lock match, active cycle `34`, strict `0` trades/open/profit, shadow/loose `0` closed with FLOCK/MTL diagnostic positions, analysis `20/20`, incomplete `0`, risk failures/outages `0/0`, interruptions `0`. The v80 risk gap has not reproduced in this first v81 interval; continue v81 without changing the runtime contract.
v81 continued health read at `2026-09-13T15:59:35.670Z`: owner PID72406/root lock match, active cycle `76`, strict/Shadow/Loose realized trades remain `0`, FLOCK/MTL diagnostic positions remain open, analysis `20/20`, incomplete `0`, risk failures/outages `0/0`, interruptions `0`. The first approximately 10-minute v81 interval remains continuity-eligible; no runtime or parameter change.
v81 latest health read at `2026-09-13T16:02:07.292Z`: owner PID72406/root lock match, active cycle `95`, strict/Shadow/Loose realized trades remain `0`, FLOCK/MTL diagnostic positions remain open, analysis `20/20`, incomplete `0`, risk failures/outages `0/0`, interruptions `0`. Risk continuity remains true and no v80 stale gap has reappeared; keep v81 running unchanged.
v81 latest health read at `2026-09-13T16:09:02.398Z`: owner PID72406/root lock match, active cycle `148`, strict/Shadow/Loose realized trades remain `0`, FLOCK/MTL diagnostic positions remain open, analysis `20/20`, incomplete `0`, risk failures/outages `0/0`, interruptions `0`. The post-v80 fresh session remains continuity-eligible; keep v81 active with the unchanged runtime contract.
v81 continued health read at `2026-09-13T16:06:38.229Z`: owner PID72406/root lock match, active cycle `130`, strict/Shadow/Loose realized trades remain `0`, FLOCK/MTL diagnostic positions remain open, analysis `20/20`, incomplete `0`, risk failures/outages `0/0`, interruptions `0`. The post-v80 fresh session remains continuity-eligible; no runtime or parameter change.
v81 first diagnostic settlement read at `2026-09-13T16:11:40.527Z`: STORJ relaxed diagnostic closed `STOP_LOSS`, entry `53.053` → exit `52.1478`, net `-360.8975 KRW` / `-1.8045%`, MFE `-0.0999%`, MAE `-1.6078%`, with volume and trend rejection reasons. Shadow/loose each have `1` close (`0W/1L`) at `-360.8975 KRW`, while FLOCK/MTL remain open. Strict remains `0`; analysis `20/20`, risk failures/outages `0/0`, interruptions `0`. This is negative diagnostic evidence consistent with retaining volume/trend filters, not strict profitability evidence.
v81 current read at `2026-09-13T16:12:52.629Z`: owner PID72406/root lock match, active cycle `177`, strict `0` trades/open, shadow/loose each `1` close (`0W/1L`) at `-360.8975 KRW`, FLOCK/MTL open, analysis `20/20`, risk failures/outages `0/0`, interruptions `0`. No v80 stale gap has reappeared; keep v81 active for FLOCK/MTL risk exits.
v81 final clean-stop read at `2026-09-13T16:20:35.832Z`: after both actual relaxed books reached zero open positions and no boundary pending, owner PID72406 released the root lock with `stopReason=stopped_cleanly` after `238` cycles. Strict remained `0`; Shadow/Loose each closed `3` diagnostics (`1W/2L`) at `-243.4532 KRW`; risk failures/outages `0/0`, risk/analysis continuity true, interruptions `0`. The v81 ledger is preserved as a short, non-promotional cohort.
v82 trend-floor research session started at `2026-09-13T16:21:06.383Z` in `.paper-forward-v82-trend0`, session `paper-1789316466383`, owner PID `78157`, with a new root lock and `configSnapshot.minTrendSlopePercent=0` while other current contract values remain unchanged. At `2026-09-13T16:24:38.237Z`/cycle28, strict had `0` trades/open, oversold `36`, rebound `16`, confirmed `0`; Shadow/Loose had no closed trades, FLOCK/STORJ diagnostic positions, one boundary pending, analysis `20/20`, risk failures/outages `0/0`, interruptions `0`. This is research-only and does not alter the default `-0.2%` runtime.
v82 selectivity follow-up at `2026-09-13T16:27:40.992Z`: owner PID78157/root lock match, active cycle `51`, trend floor `0%`, strict `0` trades/open; oversold `55`, rebound `32`, confirmed/BUY `0/0`. Shadow/loose remain without closed trades and hold FLOCK/STORJ diagnostics with one boundary pending. Analysis `20/20`, risk failures/outages `0/0`, interruptions `0`; the stronger trend floor is still unproven and remains research-only.
v82 final clean-stop read at `2026-09-13T16:32:15.224Z`: owner PID78157 released the root lock after `86` cycles with `stopReason=stopped_cleanly`; strict remained `0` trades/open, Shadow/Loose each closed `3` diagnostics (`1W/2L`) at `-432.5884 KRW`, boundary `1` settled at counterfactual `+385.8998 KRW`, and open/pending were `0/0`. Risk and analysis continuity were eligible with failures/outages `0/0`, interruptions `0`. The trend-floor `0%` candidate is preserved as research-only; default runtime remains `-0.2%`.
v83 rebound-floor research session started at `2026-09-13T16:32:45.145Z` in `.paper-forward-v83-rebound25`, session `paper-1789317165145`, owner PID `89484` (npm parent `89458`), with `minReboundPercent=0.25%` and current trend floor `-0.2%`. At `2026-09-13T16:35:04.726Z`/cycle18, oversold/rebound/confirmed/BUY were `0/0/0/0`, strict/relaxed books had no trades or positions, analysis `20/20`, and risk failures/outages `0/0`; this is an early no-signal observation, not a profitability result.
v83 latest research read at `2026-09-13T16:41:48.211Z`: owner PID89484/root lock match, active cycle `71`, `minReboundPercent=0.25%`, strict `0` trades/open, oversold `28`, rebound `5`, confirmed/BUY `0/0`; Shadow/Loose have no closed trades and one `KRW-POWR` diagnostic open, analysis `20/20`, risk failures/outages `0/0`, interruptions `0`. The candidate remains unproven and research-only.
v83 counterfactual follow-up at `2026-09-13T16:44:33.713Z`: one blocked `KRW-STORJ` retrace signal settled counterfactually at `TAKE_PROFIT`, virtual entry `49.3493` → market exit `50.2497`, net `+442.7761 KRW` / `+2.2139%`, MFE `+2.4186%`, MAE `-0.0999%`; no actual relaxed close and no strict fill occurred. The actual Shadow/Loose books still hold POWR/STEEM, strict remains `0`, analysis `20/20`, risk failures/outages `0/0`, interruptions `0`. This single counterfactual winner is diagnostic-only and does not justify changing the rebound floor.
v83 strict confirmation read at `2026-09-13T16:47:26.706Z`: one confirmed `KRW-MTL` candidate reached delayed revalidation but was cancelled because retrace was `0.27%` versus the allowed `0.25%`. Telemetry is `6` confirmed candidates, `1` attempt, `0` successes, `1` cancellation; strict remains `0` trades/open. Shadow/Loose actual books hold POWR/STEEM/LSK with no closes, boundary has `2` records (`1` settled, `1` pending MTL), and settled counterfactual net is `+442.7761 KRW`. Analysis `20/20`, risk failures/outages `0/0`, interruptions `0`; this validates the protection path but not rebound-floor profitability.
v83 latest read at `2026-09-13T16:48:51.717Z`: owner PID89484/root lock match, active cycle `124`, rebound floor `0.25%`, strict `0` trades/open, shadow/loose no actual closes with POWR/STEEM/LSK open, boundary `2` (`1` settled/`1` pending), analysis `20/20`, risk failures/outages `0/0`, interruptions `0`. Keep v83 research-only.
v83 diagnostic read at `2026-09-13T16:51:46.369Z`: LSK relaxed diagnostic closed `STOP_LOSS`, entry `466.466` → exit `459.54`, net `-316.6544 KRW` / `-1.5833%`, MFE `+0.5432%`, MAE `-1.3862%`, with volume/trend/previous-high rejection reasons; POWR also closed `STOP_LOSS -280.1123 KRW` in the same period. Shadow/loose now each have `2` closes (`0W/2L`) at `-596.7667 KRW`, with STEEM open. Boundary `2` records (`1` settled, `1` pending MTL), settled counterfactual `+442.7761 KRW`; strict remains `0`, analysis `20/20`, risk failures/outages `0/0`, interruptions `0`. The rebound-floor candidate remains negative in actual relaxed observations and unpromoted.
v83 latest diagnostic read at `2026-09-13T16:55:00.218Z`: STORJ's subsequent relaxed signal closed `STOP_LOSS`, entry `48.1481` → exit `47.0529`, net `-474.4699 KRW` / `-2.3723%`, MFE `+1.3539%`, MAE `-2.1768%`, with trend/previous-high rejection; POWR also closed `STOP_LOSS -280.1123 KRW`. Shadow/loose now each have `3` closes (`0W/3L`) realized `-1,071.2366 KRW`, with STEEM open. Boundary `2` records (`1` settled, `1` pending MTL), settled counterfactual `+442.7761 KRW`; strict remains `0`, confirmation `7/1/0/1` (confirmed/attempt/success/cancel), analysis `20/20`, risk failures/outages `0/0`, interruptions `0`. The rebound-floor `0.25%` candidate is negative in actual relaxed observations and remains unpromoted.
v83 latest diagnostic read at `2026-09-13T17:01:50.875Z`: CVC relaxed diagnostic closed `STOP_LOSS`, entry `45.6456` → exit `44.8551`, net `-366.0129 KRW` / `-1.8301%`, MFE `+0.9955%`, MAE `-1.6335%`, with volume/close-strength/trend/previous-high rejection reasons. Shadow/loose each have `4` closes (`0W/4L`) realized `-1,437.2495 KRW`, with STEEM open. Boundary `3` records (`1` settled, `2` pending), settled counterfactual `+442.7761 KRW`; strict remains `0`, confirmation `7/1/0/1`, analysis `20/20`, risk failures/outages `0/0`, interruptions `0`. The rebound-floor candidate is negative in actual relaxed observations and remains research-only.
v83 latest settled-book read at `2026-09-13T17:03:45.101Z`: STEEM and CVC completed the current relaxed sequence; Shadow/Loose each have `5` closes (`0W/5L`) realized `-1,739.7447 KRW`, with actual positions empty. The latest STEEM loss was `-302.4952 KRW` / `-1.5125%` (MFE `+1.4800%`, MAE `-1.3152%`, price/volume/trend/previous-high rejection). Boundary records are `3` (`2` settled, `1` pending MTL), settled counterfactual net `+806.2174 KRW`; strict remains `0`, confirmation `7/1/0/1`, analysis `20/20`, risk failures/outages `0/0`, interruptions `0`. The `0.25%` rebound-floor candidate is negative in five actual relaxed closes and remains unpromoted; keep the MTL pending record until risk settlement.

v83 terminal read at `2026-09-13T17:05:44.213Z`: owner PID89484 is absent and root lock is released; ledger ended `2026-09-13T17:05:09.899Z` after `251` cycles with `stopReason=risk_data_gap`, `lastFailureCode=RISK_CHECK_STALE`, max risk gap `173.245s`, and risk continuity `false`. Strict remained `0`; Shadow/Loose each had `5` relaxed closes (`0W/5L`) at `-1,739.7447 KRW`, with `KRW-PUNDIX` diagnostic open; boundary records `4` each (`2` settled, `2` pending), settled counterfactual `+806.2174 KRW`. Current Upbit read-only probe at `2026-09-13T17:06:08.156Z` returned HTTP `200` three times in `237/15/16ms`; current recovery does not establish the historical gap's root cause. v83 is permanently non-promotional and must not be resumed or mixed.

v84 volume-floor research session started at `2026-09-13T17:08:27.752Z` in `.paper-forward-v84-volume15`, session `paper-1789319307752`, owner PID `22444` (npm parent `22418`), with `minVolumeRatio=1.5x` and current rebound/trend/high-break values unchanged. At `2026-09-13T17:12:02.767Z`/cycle28, strict had `0` trades/open, oversold `72`, rebound `25`, confirmed/BUY `0/0`; Shadow/Loose had LSK/POWR diagnostic positions, one boundary pending, analysis `20/20`, risk failures/outages `0/0`, interruptions `0`. This is research-only; default volume floor `1.0x` and live gates remain unchanged.
v84 continued health read at `2026-09-13T17:16:40.689Z`: owner PID22444/root lock match, active cycle `63`, volume floor `1.5x`, strict `0` trades/open/profit, Shadow/Loose no closed trades with LSK/POWR diagnostic positions, one boundary pending, analysis `20/20`, incomplete `0`, risk failures/outages `0/0`, interruptions `0`. The volume-floor candidate remains unproven and research-only.

v84 first boundary settlement read at `2026-09-13T17:24:00.281Z`: one blocked `KRW-STEEM` chase signal settled counterfactually as `STOP_LOSS`, net `-303.0869 KRW` / `-1.5154%`; actual Shadow/Loose still hold LSK/POWR with no closes, strict remains `0`. Volume floor `1.5x` session analysis is `20/20`, risk failures/outages `0/0`, interruptions `0`; keep counterfactual separate and do not promote the candidate.
v84 current read at `2026-09-13T17:29:29.046Z`: owner PID22444/root lock match, active cycle `161`, volume floor `1.5x`, strict `0` trades/open/profit, Shadow/Loose no actual closes with LSK/POWR open, boundary `1` settled/`0` pending at counterfactual `-303.0869 KRW`. Analysis `20/20`, risk failures/outages `0/0`, interruptions `0`; actual exit evidence is still unavailable, so keep the candidate research-only.

v84 latest verified read at `2026-09-13T17:33:48.830Z`: owner PID22444/root lock/cwd remained matched and active at cycle `194`; volume floor `1.5x`, strict `0` trades/open/profit, Shadow/Loose each `1` actual close (`1W/0L`) at `+374.5094 KRW`, with `KRW-POWR` still open. Boundary records remain `1` settled/`0` pending with counterfactual `-303.0869 KRW`; telemetry oversold/rebound/confirmed/BUY `125/26/0/0`, analysis `20/20`, risk failures/outages `0/0`, interruptions `0`. The candidate remains research-only; the default `1.0x` volume floor and live gate are unchanged.
v84 health read at `2026-09-13T17:35:55.397Z`: owner PID22444/root lock/cwd remained matched and active at cycle `210`, heartbeat current. Volume floor `1.5x`; strict remains `0` trades/open/profit`; Shadow/Loose remain at `1` actual close (`1W/0L`) `+374.5094 KRW` with `KRW-POWR` open; boundary `1` settled/`0` pending, counterfactual `-303.0869 KRW`. Telemetry oversold/rebound/confirmed/BUY `139/26/0/0`; analysis `20/20`, risk failures/outages `0/0`, interruptions `0`. Full `npm test -- --test-reporter=dot` exited `0`; no source/runtime parameter change.
v84 latest runtime read at `2026-09-13T17:39:58.902Z`: owner PID22444 remained active with current heartbeat and cycle `242`; strict remains `0` trades/open/profit. Shadow has `1` actual close (`1W/0L`) `+374.5094 KRW` with `KRW-POWR` open; Loose has the same realized result with `KRW-POWR/KRW-ETH` open. Boundary remains `1` settled/`0` pending at counterfactual `-303.0869 KRW`; analysis `20/20`, incomplete `0`, risk failures/outages `0/0`, continuity true, interruptions `0`. No source/runtime/live change.

Read-only Observer UI smoke against the v84 ledger passed in the same run: desktop dashboard rendered paper/strict-lock state; at `390x844`, `clientWidth=scrollWidth=375` and `bodyScrollWidth=375` (no horizontal overflow), manifest `/manifest.webmanifest` and standalone meta were present. `/`, paper, validation, research, manifest, service worker, and icon endpoints returned HTTP `200`; paper-session start returned HTTP `403`. Observer reported `readOnlyObserver=true`, `processAlive=true`, `orphaned=false`, `continuityEligible=true`, and `configSnapshotComplete=true`. The temporary Observer was stopped after verification; v84 runner remained alive.
v84 settlement read at `2026-09-13T17:41:23.410Z`: owner PID22444 remained active at cycle `252` with current heartbeat; `KRW-POWR` closed by `MAX_HOLD_TIME` at entry `80.6806` → exit `80.919`, net `+39.0431 KRW`. Shadow now has `2` actual closes (`2W/0L`) at `+413.5526 KRW` with no open positions; Loose has the same two closes and `KRW-ETH` open. Boundary remains `1` settled/`0` pending at counterfactual `-303.0869 KRW`; strict remains `0`. Analysis `20/20`, risk failures/outages `0/0`, continuity true, interruptions `0`. Two relaxed wins are insufficient for promotion; default volume `1.0x` and live gate remain unchanged.
v84 continuation read at `2026-09-13T17:43:28.943Z`: owner PID22444 remained active at cycle `268` with heartbeat current; the brief prior `analysisActive=false` state recovered to `analysisActive=true` with `20/20` analyzed and `incomplete=0`. Strict remains `0`; Shadow remains `2W/0L` at `+413.5526 KRW` with no open positions; Loose remains `2W/0L` with `KRW-ETH` open. Boundary remains `1` settled/`0` pending at counterfactual `-303.0869 KRW`; risk failures/outages `0/0`, continuity true, interruptions `0`. This is normal cycle-boundary recovery, not a data-gap event; no source/runtime or promotion change.
v84 verified wait at `2026-09-13T17:46:12.029Z`: owner PID22444 remained active at cycle `289` with current heartbeat; strict remains `0` trades/open/profit, Shadow remains `2W/0L` at `+413.5526 KRW` with no open positions, and Loose remains `2W/0L` with `KRW-ETH` open. Boundary remains `1` settled/`0` pending at counterfactual `-303.0869 KRW`; analysis `20/20` active with `incomplete=0`, risk failures/outages `0/0`, continuity true, interruptions `0`. No new settlement or source/runtime change; keep the same ledger until ETH risk exit.
v84 latest verified wait at `2026-09-13T17:47:16.955Z`: owner PID22444 remained active at cycle `298` with heartbeat current; strict remains `0` trades/open/profit, Shadow remains `2W/0L` at `+413.5526 KRW` with no open positions, and Loose remains `2W/0L` with `KRW-ETH` open. Boundary remains `1` settled/`0` pending at counterfactual `-303.0869 KRW`; analysis `20/20` active with `incomplete=0`, risk failures/outages `0/0`, continuity true, interruptions `0`. No new settlement or source/runtime change; continue the same ledger until ETH risk exit.
v84 health read at `2026-09-13T17:49:31.760Z`: owner PID22444/root lock remained active at cycle `315` with current heartbeat; strict remains `0` trades/open/profit, Shadow remains `2W/0L` at `+413.5526 KRW` with no open positions, and Loose remains `2W/0L` with `KRW-ETH` open. Boundary remains `1` settled/`0` pending at counterfactual `-303.0869 KRW`; telemetry oversold/rebound/confirmed/BUY `159/34/0/0`, analysis `20/20` active with `incomplete=0`, risk failures/outages `0/0`, continuity true, interruptions `0`. Current full `npm test -- --test-reporter=dot` exited `0`; no source/runtime or promotion change.
v84 latest health read at `2026-09-13T17:50:51.734Z`: owner PID22444/root lock remained active at cycle `325` with heartbeat current; strict remains `0` trades/open/profit, Shadow remains `2W/0L` at `+413.5526 KRW` with no open positions, and Loose remains `2W/0L` with `KRW-ETH` open. Boundary remains `1` settled/`0` pending at counterfactual `-303.0869 KRW`; telemetry oversold/rebound/confirmed/BUY `169/34/0/0`, analysis `20/20` active with `incomplete=0`, risk failures/outages `0/0`, continuity true, interruptions `0`. No new settlement, source/runtime, or promotion change.
v84 latest cycle read at `2026-09-13T17:52:48.664Z`: owner PID22444 remained active at cycle `340` with heartbeat and `lastComplete` current; strict remains `0` trades/open/profit. Shadow remains `2W/0L` at `+413.5526 KRW` and now has `KRW-FLOCK` open; Loose remains `2W/0L` with `KRW-ETH/KRW-FLOCK` open. Boundary remains `1` settled/`0` pending at counterfactual `-303.0869 KRW`; telemetry oversold/rebound/confirmed/BUY `177/37/0/0`, analysis `20/20` with `incomplete=0`, risk failures/outages `0/0`, continuity true, interruptions `0`. The persisted `analysisActive=false` snapshot coincided with a current `lastComplete` and next-cycle progress, so it is treated as a cycle-state observation rather than a data-gap event; no source/runtime or promotion change.
v84 latest verified read at `2026-09-13T17:54:00.943Z`: owner PID22444/root lock remained active at cycle `349` with current heartbeat and `analysisActive=true`; strict remains `0` trades/open/profit. Shadow remains `2W/0L` at `+413.5526 KRW` with `KRW-FLOCK` open; Loose remains `2W/0L` with `KRW-ETH/KRW-FLOCK` open. Boundary remains `1` settled/`0` pending at counterfactual `-303.0869 KRW`; telemetry oversold/rebound/confirmed/BUY `179/39/0/0`, analysis `20/20`, incomplete `0`, risk failures/outages `0/0`, continuity true, interruptions `0`. No new settlement, source/runtime, or promotion change.
v84 health read at `2026-09-13T17:56:16.503Z`: owner PID22444 remained active at cycle `366` with heartbeat and `lastComplete` current; strict remains `0` trades/open/profit. Shadow remains `2W/0L` at `+413.5526 KRW` with `KRW-FLOCK` open; Loose remains `2W/0L` with `KRW-ETH/KRW-FLOCK` open. Boundary remains `1` settled/`0` pending at counterfactual `-303.0869 KRW`; telemetry oversold/rebound/confirmed/BUY `179/39/0/0`, analysis `20/20`, `incomplete=0`, risk failures/outages `0/0`, continuity true, interruptions `0`. The research-only momentum comments were aligned with the current 400-day negative audit; `RegimeMomentumStrategy` behavior tests `7/7` and full `npm test -- --test-reporter=dot` exited `0`; no runtime logic or promotion change.
v84 latest verified read at `2026-09-13T17:57:36.331Z`: owner PID22444/root lock remained active at cycle `377` with heartbeat and `lastComplete` current; strict remains `0` trades/open/profit. Shadow remains `2W/0L` at `+413.5526 KRW` with `KRW-FLOCK` open; Loose remains `2W/0L` with `KRW-ETH/KRW-FLOCK` open. Boundary remains `1` settled/`0` pending at counterfactual `-303.0869 KRW`; telemetry oversold/rebound/confirmed/BUY `179/39/0/0`, analysis `20/20` with `incomplete=0`, risk failures/outages `0/0`, continuity true, interruptions `0`. The current strict confirmation starvation remains an observation rather than a reason to relax filters; no new settlement or runtime/promotion change.
v84 latest health read at `2026-09-13T17:58:49.455Z`: owner PID22444/root lock remained active at cycle `386`; heartbeat and `lastComplete` are current, strict remains `0` trades/open/profit, Shadow remains `2W/0L` at `+413.5526 KRW` with `KRW-FLOCK` open, and Loose remains `2W/0L` with `KRW-ETH/KRW-FLOCK` open. Boundary remains `1` settled/`0` pending at counterfactual `-303.0869 KRW`; telemetry oversold/rebound/confirmed/BUY `179/39/0/0`, analysis `20/20` with `incomplete=0`, risk failures/outages `0/0`, continuity true, interruptions `0`. The persisted `analysisActive=false` coincided with a current `lastComplete` and risk success, so it is not classified as a data gap; no settlement or runtime/promotion change.
v84 latest verified read at `2026-09-13T17:59:59.783Z`: owner PID22444/root lock remained active at cycle `395`; strict remains `0` trades/open/profit, Shadow remains `2W/0L` at `+413.5526 KRW` with `KRW-FLOCK` open, and Loose remains `2W/0L` with `KRW-ETH/KRW-FLOCK` open. Boundary remains `1` settled/`0` pending at counterfactual `-303.0869 KRW`; telemetry oversold/rebound/confirmed/BUY `179/39/0/0`, analysis `20/20`, `lastComplete` current, `incomplete=0`, risk failures/outages `0/0`, continuity true, interruptions `0`. Source inspection confirms `analysisActive=false` is the intentional post-success state from `recordAnalysisDataSuccess()`, not a health failure; no new settlement or runtime/promotion change.
v84 latest runtime read at `2026-09-13T18:01:43.602Z`: owner PID22444/root lock remained active at cycle `408`; the current analysis cycle is in flight with `analysisActive=true`, while `lastCompleteAt` is the immediately preceding cycle, `currentGapStartedAt=null`, `incomplete=0`, expected/analyzed `20/20`, and risk success is current with failures/outages `0/0`. Strict remains `0` trades/open/profit; Shadow remains `2W/0L` at `+413.5526 KRW` with `KRW-FLOCK` open; Loose remains `2W/0L` with `KRW-ETH/KRW-FLOCK` open. Boundary remains `1` settled/`0` pending at counterfactual `-303.0869 KRW`; interruptions `0`. This read confirms the health state transitions between in-flight and post-success are functioning; no new settlement or runtime/promotion change.
v84 loss read at `2026-09-13T18:02:54.105Z`: `KRW-FLOCK` closed by `STOP_LOSS`, entry `102.102` → exit `99.9`, net `-450.8971 KRW` / `-2.2545%`; MFE `-0.0999%`, MAE `-2.0587%`. The signal carried `volume_confirmation_failed`, `trend_filter_failed`, and `previous_high_break_failed`, and its per-coin cooldown/consecutive-loss state was persisted. Shadow/Loose are now each `3` closes (`2W/1L`) at `-37.3446 KRW`, PF `0.9172`; Shadow has no open position and Loose retains `KRW-ETH`. Boundary remains `1` settled/`0` pending at counterfactual `-303.0869 KRW`; strict remains `0`, telemetry oversold/rebound/confirmed/BUY `207/39/0/0`, analysis `20/20`, risk failures/outages `0/0`, continuity true, interruptions `0`. This is a negative three-trade research sample; no filter relaxation or promotion.
v84 ETH settlement read at `2026-09-13T18:06:08.422Z`: `KRW-ETH` closed by `MAX_HOLD_TIME`, entry `3,409,406` → exit `3,401,595`, net `-65.7695 KRW` / `-0.3288%`; MFE `-0.0412%`, MAE `-0.2172%`, with `price_rebound_below_threshold` and `volume_confirmation_failed`. Shadow remains `3` closes (`2W/1L`) at `-37.3446 KRW` with no actual open positions; Loose reaches `4` closes (`2W/2L`) at `-103.1141 KRW` with no actual open positions. Boundary records are now `2` settled/`1` pending per book: the new `KRW-FLOCK` chase blocks are still pending and have MFE `+1.4841%` and `+0.1100%`, so keep the runner alive until their counterfactual exits are observed. Strict remains `0`; telemetry oversold/rebound/confirmed/BUY `242/59/0/0`, analysis `20/20` with `incomplete=0`, risk last success current with failures/outages `0/0`, continuity true, interruptions `0`. This reinforces that relaxed actual P&L is not promotion evidence; no default or live change.
v84 boundary follow-up at `2026-09-13T18:08:57.520Z`: owner PID22444 remained active at cycle `464`, with actual Shadow/Loose positions empty and the same realized results (Shadow `3` closes, `2W/1L`, `-37.3446 KRW`; Loose `4` closes, `2W/2L`, `-103.1141 KRW`). Two boundary records are settled (`STEEM -303.0869 KRW`, `FLOCK 18:04 +340.5729 KRW`) for counterfactual net `+37.4860 KRW`; the `FLOCK 18:05` record remains pending at MFE `+1.2643%`/MAE `-0.3098%`. Analysis `20/20`, `lastComplete` current, `incomplete=0`, risk continuity true with failures/outages `0/0`, interruptions `0`. The guard has mixed loss-avoidance and missed-profit directions, so no efficacy or promotion conclusion is drawn.
v84 pending-boundary wait at `2026-09-13T18:11:04.126Z`: owner PID22444 remained active at cycle `481`; actual Shadow/Loose positions remain empty with realized results unchanged (Shadow `3` closes, `2W/1L`, `-37.3446 KRW`; Loose `4` closes, `2W/2L`, `-103.1141 KRW`). Boundary remains `2` settled/`1` pending with counterfactual net `+37.4860 KRW`; the pending `KRW-FLOCK:2026-09-13T18:05:00` record is still open at MFE `+1.2643%`/MAE `-0.4147%`. Strict remains `0`; analysis `20/20`, `lastComplete` current, `incomplete=0`, continuity true, risk failures/outages `0/0`, interruptions `0`. Do not mark the pending record unresolved by timeout; no runtime or promotion change.
v84 latest pending-boundary read at `2026-09-13T18:12:20.012Z`: owner PID22444/root lock remained active at cycle `491` with heartbeat and `lastComplete` current; strict remains `0` trades/open/profit. Shadow remains `3` closes (`2W/1L`) at `-37.3446 KRW` with no open positions, and Loose remains `4` closes (`2W/2L`) at `-103.1141 KRW` with no open positions. Boundary remains `2` settled/`1` pending at counterfactual net `+37.4860 KRW`; no new actual settlement. Analysis `20/20`, `incomplete=0`, continuity true, interruptions `0`; pending FLOCK is retained for its actual counterfactual exit rather than timeout resolution. No runtime or promotion change.
v84 latest pending-boundary wait at `2026-09-13T18:13:34.209Z`: owner PID22444/root lock remained active at cycle `501` with current heartbeat and `lastComplete`; strict remains `0` trades/open/profit. Shadow remains `3` closes (`2W/1L`) at `-37.3446 KRW`, Loose remains `4` closes (`2W/2L`) at `-103.1141 KRW`, and both actual books remain empty. Boundary remains `2` settled/`1` pending at counterfactual net `+37.4860 KRW`; no new settlement. Analysis `20/20`, `incomplete=0`, continuity true, interruptions `0`; retain the pending FLOCK record for a real counterfactual exit and do not resolve by timeout.
v84 latest pending-boundary read at `2026-09-13T18:14:47.793Z`: owner PID22444/root lock remained active at cycle `509` with current heartbeat and `lastComplete`; strict remains `0` trades/open/profit. Shadow remains `3` closes (`2W/1L`) at `-37.3446 KRW`, Loose remains `4` closes (`2W/2L`) at `-103.1141 KRW`, and both actual books remain empty. Boundary remains `2` settled/`1` pending at counterfactual net `+37.4860 KRW`; no new settlement. Analysis `20/20`, `incomplete=0`, continuity true, interruptions `0`; retain pending FLOCK for a real counterfactual exit rather than timeout resolution. No runtime or promotion change.
v84 latest health read at `2026-09-13T18:04:48.023Z`: owner PID22444/root lock remained active at cycle `432` with heartbeat current; strict remains `0` trades/open/profit. Shadow remains `3` closes (`2W/1L`) at `-37.3446 KRW` with no open positions; Loose remains `3` closes (`2W/1L`) at `-37.3446 KRW` with `KRW-ETH` open. Boundary remains `1` settled/`0` pending at counterfactual `-303.0869 KRW`; telemetry oversold/rebound/confirmed/BUY `226/45/0/0`, analysis `20/20` with `incomplete=0`, risk failures/outages `0/0`, continuity true, interruptions `0`. No new settlement or runtime/promotion change.

v84 latest pending-boundary read at `2026-09-13T18:15:59.043Z`: owner PID22444/root lock remained active at cycle `518` with current heartbeat and `lastComplete`; strict remains `0` trades/open/profit. Shadow remains `3` closes (`2W/1L`) at `-37.3446 KRW`, Loose remains `4` closes (`2W/2L`) at `-103.1141 KRW`, and both actual books remain empty. Boundary remains `2` settled/`1` pending at counterfactual net `+37.4860 KRW`; no new settlement. Analysis `20/20`, `incomplete=0`, continuity true, interruptions `0`; pending FLOCK has not reached its counterfactual exit and remains preserved. No runtime or promotion change.

v84 terminal read at `2026-09-13T18:18:16.085Z`: owner PID22444 and root lock were gone; ledger ended at `2026-09-13T18:17:56.483Z` with `stopReason=risk_data_gap`, `riskMonitor.lastFailureCode=RISK_CHECK_STALE`, `currentOutageStartedAt=2026-09-13T18:06:04.836Z`, and `maxObservedGapSeconds=707.02`; continuity is `false`. Analysis completed `20/20` with `totalIncompleteCycles=0`, and the ledger recorded `534` batch ticker requests with `0` batch failures, so this was not an analysis-cycle or batch-request failure. Shadow/Loose ended with `3`/`4` actual closes (`2W/1L`, `-37.3446 KRW`; `2W/2L`, `-103.1141 KRW`) and an open diagnostic `KRW-FLOCK`; the pending FLOCK counterfactual was marked unresolved at session stop. Root cause was confirmed by the minimal repro and source call chain: after an idle period with no protected positions, the next risk attempt reused the pre-idle `lastSuccessAt` and falsely measured the idle interval as stale. `recordRiskMonitorAttempt()` now starts a fresh epoch when no existing outage is present while preserving explicit outage state; regression and integration tests pass. v84 is permanently non-promotional and must not be resumed or mixed.

v85 latest-code risk-idle-fix session started at `2026-09-13T18:22:05.193Z` in `.paper-forward-v85-risk-idle-fix`, session `paper-1789323725193`, owner PID `83399` (npm `83372`), with default `minVolumeRatio=1.0`, `positionRiskCheckIntervalMs=1000`, and `maxRiskDataGapSeconds=30`. At `2026-09-13T18:24:04.952Z`/cycle `15`, config snapshot was complete; strict had `0` trades/open, Shadow/Loose each had `0` closes with diagnostic `KRW-B3/KRW-ARK` positions, analysis `20/20`, risk failures/outages `0/0`, continuity true, interruptions `0`. This is a fresh cohort validating the idle-restart fix; it does not resume or mix v84.
v85 strict-entry health read at `2026-09-13T18:26:38.978Z`: owner PID83399/root lock remained active at cycle `33`; `KRW-CVC` completed the delayed strict confirmation with `entryDelayMs=1397`, execution drift `0%`, signal rebound `1.2766%`, RSI `34.4679` → `44.9375`, volume ratio `1.4493`, close strength `1`, and trend slope `-0.1034%`. Strict now has `1` open position and `0` closed trades; telemetry is oversold/rebound/confirmed `59/28/3`, confirmation attempts/success/cancel `1/1/0`. Shadow/Loose each recorded one B3 `STOP_LOSS` close at `-360.5194 KRW` and hold B3/FLOCK/CVC diagnostics. Analysis `20/20`, `incomplete=0`, risk failures/outages `0/0`, continuity true, interruptions `0`. This is the first live forward confirmation that the idle-restart fix permits a new strict epoch to reach a real delayed entry; no promotion or live-order change.
v85 latest verified health read at `2026-09-13T18:31:15.896Z`: owner PID83399/root lock/cwd remained matched at cycle `66`; strict `KRW-CVC` remains open with `0` closed strict trades, while Shadow/Loose each have `1` diagnostic close at `-360.5194 KRW`. Analysis is `20/20` with `incomplete=0`, last complete current; risk last success is current with failures/outages `0/0`, continuity true, interruptions `0`. CVC has not reached its risk exit yet; preserve v85 as a fresh post-fix cohort and do not promote or mix it with v84.
v85 latest health read at `2026-09-13T18:40:46.054Z`: owner PID83399/root lock/cwd remained matched at cycle `136`; strict `KRW-CVC` remains open with `0` closed strict trades, MFE `+1.0504%`, MAE `-0.8403%`. Shadow/Loose remain at `3` diagnostic closes (`0W/3L`) and `-972.7353 KRW`, with only `KRW-CVC` still open; one boundary record remains pending per book. Telemetry oversold/rebound/confirmed `111/66/6`, confirmation attempt/success/cancel `1/1/0`; analysis `20/20`, `incomplete=0`, risk last success current with failures/outages `0/0`, continuity true, interruptions `0`. The idle-restart fix remains healthy; no strict exit, runtime change, or promotion.
v85 latest post-fix read at `2026-09-13T18:35:13.356Z`: owner PID83399/root lock/cwd remained matched at cycle `95`; strict `KRW-CVC` remains open with `0` closed strict trades, MFE `+0.6303%` and MAE `-0.8403%`. Shadow/Loose each have `2` diagnostic closes (`0W/2L`) at `-651.0769 KRW`, with `KRW-B3/KRW-CVC` actual diagnostic positions open; the latest FLOCK close was the second loss at `-290.5575 KRW`. Boundary has one pending record per diagnostic book. Telemetry oversold/rebound/confirmed `97/52/6`, confirmation attempt/success/cancel `1/1/0`; analysis `20/20`, `incomplete=0`, risk success current with failures/outages `0/0`, continuity true, interruptions `0`. The idle-restart fix remains healthy through this observation; relaxed losses are not promotion evidence.
v85 latest diagnostic read at `2026-09-13T18:37:05.192Z`: owner PID83399/root lock/cwd remained matched at cycle `110`; strict `KRW-CVC` remains open with `0` closed strict trades, MFE `+0.6303%`, MAE `-0.8403%`. Shadow/Loose each have `3` diagnostic closes (`0W/3L`) at `-972.7353 KRW`, with `KRW-CVC` still open in both diagnostic books. The three relaxed losses were ARK `-360.5194`, FLOCK `-290.5575`, and B3 `-321.6584` KRW; their signal volume ratios were `0.2209`, `0.3076`, and `0.9374`, with FLOCK/B3 also carrying trend and previous-high rejection. Telemetry oversold/rebound/confirmed `111/66/6`, confirmation attempt/success/cancel `1/1/0`; analysis `20/20`, `incomplete=0`, risk last success current with failures/outages `0/0`, continuity true, interruptions `0`. The post-fix risk monitor remains healthy; the relaxed low-volume cohort is negative and does not justify changing the default filter.
v85 latest verified read at `2026-09-13T18:39:18.757Z`: owner PID83399/root lock/cwd remained matched at cycle `126`; strict `KRW-CVC` remains open with `0` closed strict trades, MFE `+1.0504%`, MAE `-0.8403%`. Shadow/Loose remain at `3` diagnostic closes (`0W/3L`) and `-972.7353 KRW`; both now have only `KRW-CVC` open after ARK/FLOCK/B3 losses, and one boundary record remains pending per book. Telemetry oversold/rebound/confirmed `111/66/6`, confirmation attempt/success/cancel `1/1/0`; analysis `20/20`, `incomplete=0`, risk last success current with failures/outages `0/0`, continuity true, interruptions `0`. No new strict close; v85 remains an unpromoted post-fix cohort.
v85 latest diagnostic read at `2026-09-13T18:33:31.158Z`: owner PID83399/root lock/cwd remained matched at cycle `82`; strict `KRW-CVC` remains open and has `0` closed strict trades. Shadow/Loose each have `2` diagnostic closes (`0W/2L`) at `-651.0769 KRW`, with `KRW-B3/KRW-CVC` still open; the latest `KRW-FLOCK` stop-loss was `95.1951` → `93.906`, net `-290.5575 KRW`, MFE `+0.2152%`, MAE `-1.2554%`, and rejection reasons included price rebound, RSI recovery, volume, trend, and previous-high failures. FLOCK cooldown/consecutive-loss state is persisted. Strict telemetry oversold/rebound/confirmed `84/44/6`, confirmation attempt/success/cancel `1/1/0`; analysis `20/20`, `incomplete=0`, risk last success current with failures/outages `0/0`, continuity true, interruptions `0`. The post-fix risk monitor remains healthy; the relaxed cohort is negative and non-promotional.

v85 latest health read at `2026-09-13T18:29:48.496Z`: owner PID83399/root lock/cwd remained matched at cycle `55`; default config snapshot has `minVolumeRatio=1.0`, risk interval `1000ms`, max risk gap `30s`. Strict has one open `KRW-CVC` position created after delayed confirmation (`entryDelayMs=1397`, signal rebound `1.2766%`, MFE `+0.6303%`, MAE `-0.6303%`) and no closed strict trades. Shadow/Loose each have one `KRW-ARK` `STOP_LOSS` close at `-360.5194 KRW` and open `KRW-B3/KRW-FLOCK/KRW-CVC` diagnostics. Telemetry oversold/rebound/confirmed `81/44/6`, confirmation attempt/success/cancel `1/1/0`; analysis `20/20`, `incomplete=0`, risk failures/outages `0/0`, continuity true, interruptions `0`. The post-fix session remains healthy; no promotion or live-order change.

v85 latest verified health read at `2026-09-13T18:42:11.140Z`: owner PID83399/root lock/cwd remained matched at cycle `147`; strict `KRW-CVC` remains open with `0` closed strict trades, MFE `+1.0504%`, MAE `-0.8403%`. Shadow/Loose remain at `3` diagnostic closes (`0W/3L`) and `-972.7353 KRW`, with only `KRW-CVC` open and one boundary record pending per book. Telemetry oversold/rebound/confirmed `111/66/6`, confirmation attempt/success/cancel `1/1/0`; analysis `20/20`, `lastComplete` current, `incomplete=0`, risk last success current with failures/outages `0/0`, continuity true, interruptions `0`. No strict exit, new settlement, runtime parameter, or promotion change.
v85 latest verified health read at `2026-09-13T18:45:38.766Z`: owner PID83399/root lock/cwd remained matched at cycle `172`; strict `KRW-CVC` remains open with `0` closed strict trades, MFE `+1.0504%`, MAE `-0.8403%`. Shadow/Loose remain at `3` diagnostic closes (`0W/3L`) and `-972.7353 KRW`, with only CVC open and one boundary record pending per book. Telemetry oversold/rebound/confirmed `111/66/6`, confirmation attempt/success/cancel `1/1/0`; analysis `20/20`, `incomplete=0`, risk last success current with failures/outages `0/0`, continuity true, interruptions `0`. No strict exit, new settlement, runtime parameter, or promotion change.
v85 strict CVC settlement read at `2026-09-13T18:48:15.411Z`: `KRW-CVC` closed by stop-loss at entry `47.6` → exit `47.0`, gross `-251.9748 KRW`, total fee `19.8640 KRW`, net `-271.8388 KRW` / `-1.3599%`; MFE `+1.0504%`, MAE `-1.2605%`. The strict signal had rebound `1.2766%`, RSI `34.4679` → `44.9375`, volume ratio `1.4493`, close strength `1`, and trend slope `-0.1034%`; strict cooldown `15` minutes and consecutive loss `1` were persisted. Shadow/Loose each now have `4` diagnostic closes (`0W/4L`) at `-1,283.9960 KRW`, with actual positions empty; one boundary record remains pending per book. Analysis `20/20`, `incomplete=0`, risk was healthy through the close with failures/outages `0/0`, continuity true, interruptions `0`. This is the first strict profitability sample of v85 and is negative; do not tune or promote from one trade, but keep the post-fix session for new-epoch health and further independent exits.
v85 idle-health read at `2026-09-13T18:49:52.743Z`: owner PID83399/root lock/cwd remained active at cycle `204`; strict has `1` closed trade (`-271.8388 KRW`) and no open position, Shadow/Loose each have `4` diagnostic closes (`0W/4L`) at `-1,283.9960 KRW` with no open positions. Risk is intentionally idle (`monitoringActive=false`) with failures/outages `0/0`, continuity true; analysis remains `20/20` with `incomplete=0` and current `lastCompleteAt`. No new position or settlement occurred after the CVC close; wait for a new signal to validate the fresh risk epoch in the live runner, with no parameter or promotion change.
v85 latest idle read at `2026-09-13T18:51:41.359Z`: owner PID83399/root lock/cwd remained active at cycle `218`; strict remains at `1` closed trade (`-271.8388 KRW`) with no open position, Shadow/Loose remain at `4` diagnostic closes (`0W/4L`) and `-1,283.9960 KRW` with no open positions. Risk is intentionally idle with last success before the idle transition, failures/outages `0/0`, continuity true; analysis is `20/20` with current `lastCompleteAt` and `incomplete=0`. No new signal or settlement occurred; preserve the session to observe the next fresh risk epoch, with no parameter or promotion change.
v85 latest idle-health read at `2026-09-13T18:53:23.799Z`: owner PID83399/root lock/cwd remained active at cycle `231`; strict remains at `1` closed trade (`-271.8388 KRW`) with no open position, Shadow/Loose remain at `4` diagnostic closes (`0W/4L`) and `-1,283.9960 KRW` with no open positions. No new signal or settlement occurred after the CVC close; risk remains intentionally idle with failures/outages `0/0`, continuity true, and analysis `20/20`/`incomplete=0`. The live fresh-epoch path has not yet been exercised again because no new position exists; keep v85 running without parameter or promotion change.
v85 terminal read at `2026-09-13T18:55:58.898Z`: owner PID83399 and root lock were gone after a clean stop at `2026-09-13T18:55:41.123Z`; `stopReason=stopped_cleanly`, `cycles=251`, strict `1` close (`-271.8388 KRW`), Shadow/Loose each `4` diagnostic closes (`0W/4L`) at `-1,283.9960 KRW`, actual open/pending positions `0/0`, and continuity eligible. The final settled boundary record was `KRW-ARK` retrace-blocked with counterfactual `MAX_HOLD_TIME -59.9151 KRW`; no unresolved boundary remained. v85 is preserved as a non-promotional cohort and is not resumed or mixed.

v86 loss-only-hold research session started at `2026-09-13T18:56:13.383Z` in `.paper-forward-v86-loss-hold-5m`, session `paper-1789325773383`, owner PID `15925`, with default volume `1.0x`, `maxLosingHoldMinutes=5`, max hold `30` minutes, risk interval `1000ms`, and max risk gap `30` seconds. At `2026-09-13T18:56:41.183Z`/cycle `3`, config snapshot was complete; strict/Shadow/Loose had no trades or positions, analysis `20/20`, risk idle with failures/outages `0/0`, continuity true, interruptions `0`. This is a separate research-only cohort measuring the loss-only exit candidate; v85 remains closed.
v86 latest no-signal read at `2026-09-13T18:59:09.536Z`: owner PID15925/root lock/cwd remained active at cycle `23`; strict/Shadow/Loose still have `0` trades and `0` positions, so the `maxLosingHoldMinutes=5` exit has no sample yet. Analysis `20/20`, `incomplete=0`, risk idle with failures/outages `0/0`, continuity true, interruptions `0`. Keep v86 isolated until a natural position/exit is observed; no parameter or promotion change.
v86 latest no-signal read at `2026-09-13T19:03:21.861Z`: owner PID15925/root lock/cwd remained active at cycle `56`; strict/Shadow/Loose remain at `0` trades and `0` positions, so the loss-only `5` minute exit still has no forward sample. Analysis `20/20`, `incomplete=0`, risk idle with failures/outages `0/0`, continuity true, interruptions `0`. Continue the separate v86 observation without changing runtime defaults or promotion state.
v86 latest no-signal read at `2026-09-13T19:04:31.973Z`: owner PID15925/root lock/cwd remained active at cycle `66`; strict/Shadow/Loose remain at `0` trades and `0` positions, so `maxLosingHoldMinutes=5` still has no forward sample. Analysis `20/20`, `incomplete=0`, risk idle with failures/outages `0/0`, continuity true, interruptions `0`. Keep the separate candidate running without changing runtime defaults or promotion state.
v86 latest no-signal read at `2026-09-13T19:06:03.709Z`: owner PID15925/root lock/cwd remained active at cycle `78`; strict/Shadow/Loose remain at `0` trades and `0` positions, with oversold observations `7` but rebound candidates `0`. The `maxLosingHoldMinutes=5` candidate has no forward exit sample yet. Analysis `20/20`, `incomplete=0`, risk idle with failures/outages `0/0`, continuity true, interruptions `0`; keep the separate observation running without changing runtime defaults or promotion state.
v86 latest no-signal read at `2026-09-13T19:07:16.948Z`: owner PID15925/root lock/cwd remained active at cycle `88`; strict/Shadow/Loose remain at `0` trades and `0` positions, oversold observations `17`, rebound candidates `0`. The loss-only `5` minute exit still has no forward sample. Analysis `20/20`, `incomplete=0`, risk idle with failures/outages `0/0`, continuity true, interruptions `0`; continue the separate observation without changing runtime defaults or promotion state.
v86 fresh-epoch runtime read at `2026-09-13T19:09:35.131Z`: owner PID15925/root lock/cwd remained active at cycle `106`; after the idle period, `KRW-POWR` opened in Shadow/Loose at `2026-09-13T19:07:24.732Z` and risk monitoring resumed with a current success, failures/outages `0/0`, continuity true. The relaxed position signal had rebound `0.4779%`, RSI recovery `10.1006`, volume ratio `0.0162`, and only `volume_confirmation_failed`; it has not reached the 5-minute loss-only exit yet. Strict remains `0` trades/open, Shadow/Loose remain `0` closes with POWR open, analysis `20/20`, `incomplete=0`, interruptions `0`. This is direct runtime evidence that idle→new-position risk epoch reactivation no longer reuses a stale success baseline; no default or promotion change.
v86 first loss-only exit read at `2026-09-13T19:13:13.910Z`: `KRW-POWR` closed in Shadow/Loose via `MAX_LOSING_HOLD_TIME` exactly about five minutes after entry `84.1841`, using exit price `83.8161`; net `-107.3350 KRW` / `-0.5367%`, MFE `-0.0999%`, MAE `-0.6938%`. The signal had volume ratio `0.0162` and `volume_confirmation_failed`; per-coin cooldown `15` minutes and consecutive loss `1` were persisted. Strict remains `0` trades/open; Shadow/Loose each have `1` diagnostic close and no open position, analysis `20/20`, `incomplete=0`, risk idle with failures/outages `0/0`, continuity true, interruptions `0`. This confirms the candidate exit mechanism in forward runtime, not comparative profitability; keep `maxLosingHoldMinutes=0` as the default.
v86 second-entry read at `2026-09-13T19:16:09.427Z`: after the POWR loss and idle transition, Shadow/Loose opened `KRW-LSK`, `KRW-ARK`, and `KRW-PUNDIX` diagnostic positions from the `19:14` signal window; risk monitoring resumed with current success, failures/outages `0/0`, and continuity true. LSK carried volume/trend/previous-high rejection (`0.6542` volume, trend `-0.3265`); ARK carried previous-high rejection only (`1.6424` volume, trend `-0.0840`); PUNDIX carried volume/previous-high rejection (`0.1876` volume, trend `-0.0878`). Strict remains `0` trades/open, Shadow/Loose remain `1` close (`0W/1L`, `-107.3350 KRW`) with these three positions, analysis `20/20`, `incomplete=0`, interruptions `0`. This is direct idle→new-position epoch evidence and a useful filter-outcome cohort; no default or promotion change.
v86 latest post-exit read at `2026-09-13T19:14:45.006Z`: owner PID15925/root lock/cwd remained active at cycle `146`; Shadow/Loose each have `1` diagnostic close (`0W/1L`) at `-107.3350 KRW` with no actual open positions, and one boundary record remains pending per book. Strict remains `0` trades/open; analysis `20/20`, `incomplete=0`, risk is intentionally idle with failures/outages `0/0`, continuity true, interruptions `0`. The 5-minute loss-only exit has no comparative baseline in this forward cohort; preserve v86 until its pending counterfactual settles and do not change the runtime default.
Read-only loss-only A/B on the same `.cap-study-candles.json` window at `2026-09-13T19:02:04.145Z`: BTC baseline (2 trades) returned `-0.0121%` with PF `0` and MDD `0.0121%`; the identical entry contract with `maxLosingHoldMinutes=5` returned `-0.0088%`, PF `0`, and MDD `0.0088%`. ETH had zero trades under both settings. The 5-minute loss-only exit reduces this tiny cache-window loss but does not create positive return or usable confidence; v86 remains the separate forward test and the runtime default stays `0`.
v85 latest health read at `2026-09-13T18:44:14.557Z`: owner PID83399/root lock/cwd remained matched at cycle `161`; strict `KRW-CVC` remains open with `0` closed strict trades, MFE `+1.0504%`, MAE `-0.8403%`. Shadow/Loose remain at `3` diagnostic closes (`0W/3L`) and `-972.7353 KRW`, with only `KRW-CVC` open and one boundary record pending per book. Analysis `20/20`, `incomplete=0`, risk last success current with failures/outages `0/0`, continuity true, interruptions `0`. No strict exit, new settlement, runtime parameter, or promotion change.

## Strategy search verdict (2026-09-14)

Exhaustive audit of price-signal families across ~400 days x 12 liquid KRW markets (segmented replay, cost 0.2%, shared-balance portfolio): oversold scalping, 60m gated momentum, BTC lead-lag, and daily trend-following all fail to produce positive absolute returns in a bear-dominated window. The trend-switch family is the only defense-positive contract (−10.8% vs buy&hold −56.7%, halved drawdown, ~57% exposure) — its honest product form is a regime defense overlay, not alpha. Forward A/B evidence accumulates in `.paper-momentum-shadow-v1` (72h fixed hold) and `.paper-momentum-shadow-regime` (hold-while-trend); `node src/scripts/momentumShadowStatus.js` prints both. All momentum artifacts remain diagnostic-only; the default scalping contract and live gates are unchanged.

v86 PUNDIX loss-only exit at `2026-09-13T19:22:31.151Z`: the Shadow/Loose diagnostic position entered at `151.151 KRW` from the `19:14` signal window and exited at `150.849 KRW` via `MAX_LOSING_HOLD_TIME` after the opt-in 5-minute loss-only boundary. Net profit was `-59.9151 KRW` / `-0.2996%`, with MFE `+0.5617%` and MAE `-0.7615%`; signal volume ratio was `0.1876` and rejection reasons were `volume_confirmation_failed`/`previous_high_break_failed`. Both diagnostic books now have `4` losses and `-412.2273 KRW` realized; Shadow retains `KRW-FLOCK`, Loose retains `KRW-FLOCK` and `KRW-XRP`. The VTHO and PUNDIX execution-boundary counterfactuals remain pending. Risk/analysis continuity is healthy (`0` failures/outages, `20/20` complete); this is mechanism evidence only and does not change the runtime default (`maxLosingHoldMinutes=0`) or promotion state.
v86 actual relaxed exits at `2026-09-13T19:29:08.019Z`: Shadow/Loose FLOCK and Loose XRP both reached the opt-in 5-minute loss-only boundary and closed without winners. Shadow ended at `5/5` losses and `-619.2241 KRW`; Loose ended at `6/6` losses and `-689.9762 KRW`; actual positions are empty in both books. The two execution-boundary counterfactuals (`KRW-VTHO:2026-09-13T19:12:00`, `KRW-PUNDIX:2026-09-13T19:14:00`) remain pending and must settle from later prices before a clean session stop. Risk is idle after all actual positions close with `0` failures/outages and continuity true; no runtime default or promotion change.
v86 Loose ETH loss-only exit at `2026-09-13T19:40:15.156Z` (ledger close observed by `2026-09-13T19:40:21.598Z`): the 5-minute candidate produced its seventh relaxed loss, leaving Shadow at `5` closes / `-619.2241 KRW` and Loose at `7` closes / `-755.7526 KRW`, with no actual open positions at that boundary. The exact ETH close record remains in the ledger; this observation did not change defaults or promotion.
v86 VTHO execution-boundary counterfactual settled at `2026-09-13T19:43:11.690Z`: the `19:12` signal blocked by `entry_retrace_exceeded` reached `MAX_HOLD_TIME` after 30 minutes at exit market price `1.09`, net `+124.7153 KRW`, MFE `+0.825%`, MAE `-1.025%`. It is recorded only in the boundary counterfactual lane, not Shadow/Loose realized P&L. PUNDIX remains pending because the runner requires a later fresh analysis price for settlement; both books have no actual positions at the latest boundary checks, while new diagnostic positions can still arrive. No runtime or promotion change.
v86 PUNDIX execution-boundary counterfactual settled at `2026-09-13T19:47:52.401Z`: the `19:14` signal blocked by `entry_retrace_exceeded` reached `MAX_HOLD_TIME` at exit market price `151`, net `+73.0188 KRW`, MFE `+1.232%`, MAE `-0.1%`. It remains outside actual Shadow/Loose realized P&L. A new diagnostic B3/POWR cohort then opened in both books, with a POWR boundary record pending; preserve those positions and do not stop or mix the session.
v86 relaxed follow-up: B3 closed in both diagnostic books at `2026-09-13T19:52:56.411Z` by `TAKE_PROFIT`, entry `0.832832` → exit `0.848151`, net `+347.5145 KRW` / `+1.7376%`, MFE `+1.9413%`, MAE `-0.3400%`, despite `trend_filter_failed` and `previous_high_break_failed`. Loose BTC separately closed at `2026-09-13T19:51:03.206Z` via `MAX_LOSING_HOLD_TIME` for `-60.1051 KRW`; Shadow/Loose then stood at `1W/5L` (`-271.7096 KRW`) and `1W/8L` (`-468.3431 KRW`) before further open POWR/STEEM/BTC observations. This remains a small relaxed mechanism cohort, not a default or promotion result.
v86 STEEM execution-boundary counterfactual settled at `2026-09-13T19:59:02.317Z`: the `19:48` signal blocked by `entry_retrace_exceeded` hit `STOP_LOSS` at market price `83.6`, net `-295.6135 KRW`, MFE `-0.0999%`, MAE `-1.2808%`. It is a loss-avoidance counterfactual and remains outside actual Shadow/Loose P&L. At the same read, Shadow/Loose had MTL and ARK actual diagnostic positions open, so v86 remained active despite all older boundary records being settled.
v86 strict ARK sample settled at `2026-09-13T20:01:37.343Z`: delayed confirmation `2356ms` admitted `KRW-ARK` at `194 KRW`, then the opt-in 5-minute loss-only exit closed at `193 KRW` for net `-122.9797 KRW` / `-0.6152%`, MFE `0%`, MAE `-0.5155%`. Strict now has one small negative close and no open strict position; this is research-cohort evidence and does not alter the default `maxLosingHoldMinutes=0` or live gate.
v86 STEEM relaxed sample settled at `2026-09-13T20:11:55.445Z`: Shadow/Loose closed the shared diagnostic position by `MAX_LOSING_HOLD_TIME`, entry `83.8838` → exit `83.6163`, net `-83.7099 KRW` / `-0.4185%`, MFE `+0.9730%`, MAE `-0.2191%`, with volume/close-strength/trend/previous-high rejection reasons. At the latest boundary read the only actual position in both books is FIL; STEEM's same-signal boundary counterfactual remains pending.
v86 STEEM boundary from the later `19:59` signal settled at `2026-09-13T20:27:16.666Z`: the `entry_retrace_exceeded` counterfactual reached `TAKE_PROFIT` at market price `85.4`, net `+393.8114 KRW`, MFE `+2.1733%`, MAE `-0.0999%`. This missed-profit counterfactual remains outside actual Shadow/Loose P&L; the actual books have no pending boundary records at this read but still hold FIL/CVC positions.
v86 CVC relaxed sample settled at `2026-09-13T20:28:01.312Z`: Shadow/Loose closed by `TAKE_PROFIT`, entry `48.3483` → exit `49.2507`, net `+352.9231 KRW` / `+1.7646%`, MFE `+1.9684%`, MAE `-0.3067%`, despite volume/trend/previous-high rejection reasons. A new ARDR actual diagnostic then opened in both books; pending boundaries were empty at the latest read, but FIL/ARDR remained open.
v86 FIL relaxed sample settled at `2026-09-13T20:33:21.893Z` by `MAX_HOLD_TIME`, entry `1330.329` → exit `1338.66`, net `+105.1270 KRW` / `+0.5256%`, MFE `+1.6290%`, MAE `-0.0999%`, carrying `previous_high_break_failed`. The same 5-minute loss-only cohort has both early loss exits and this positive max-hold outcome; it is not enough to promote or tune the default.
v86 ARDR relaxed sample settled at `2026-09-13T20:34:32.801Z` by `MAX_LOSING_HOLD_TIME`, entry `36.6366` → exit `36.5634`, net `-59.9151 KRW` / `-0.2996%`, MFE `+0.9919%`, MAE `-0.0999%`, carrying volume/close-strength rejection. The actual books are empty again; the same-signal ARDR execution-boundary counterfactual remains pending and is not combined with actual P&L.
v86 ARDR execution-boundary counterfactual settled at `2026-09-13T20:59:15.995Z` by `MAX_HOLD_TIME`, with blocked entry `36.8368` and exit market price `37.2`; net `+156.8250 KRW`, MFE `+1.257%`, MAE `-0.643%`. It remains a missed-profit counterfactual outside Shadow/Loose actual P&L. A new STEEM/POWR/ARK actual cohort and ARK/STEEM pending boundaries were present afterward.
v86 later relaxed read: POWR actual closed in both books at `2026-09-13T21:00:44.581Z` via `MAX_LOSING_HOLD_TIME`, entry `83.8838` → exit `83.4165`, net `-131.2996 KRW` / `-0.6565%`, MFE `+0.2577%`, MAE `-0.4575%`, carrying volume/previous-high rejection. By `2026-09-13T21:00:44.918Z`, Shadow/Loose held STEEM and ARK actuals with ARK/STEEM boundary records pending; no promotion or default change.
v86 ARK relaxed sample settled at `2026-09-13T21:01:14.633Z` via `MAX_LOSING_HOLD_TIME`, entry `196.196` → exit `193.806`, net `-263.3853 KRW` / `-1.3169%`, MFE `+0.4098%`, MAE `-1.1193%`, carrying trend/previous-high rejection. STEEM remained the only actual open position in both books while ARK/STEEM boundary records remained pending.
v86 continued actual read: the STEEM position entered from the `20:53` signal window exited at `2026-09-13T21:12:10.927Z` via `MAX_LOSING_HOLD_TIME`, entry `83.6836` → exit `83.5164`, net `-59.9151 KRW` / `-0.2996%`, MFE `+1.0951%`, MAE `-0.4584%`. By `2026-09-13T21:12:24.163Z`, Shadow/Loose had `16`/`19` actual closes (`4W/12L`, `-259.9942` / `4W/15L`, `-456.6278 KRW`), no actual open positions, and only the later STEEM boundary pending.
v86 ARK execution-boundary counterfactual settled at `2026-09-13T21:11:16.690Z` by `STOP_LOSS`, blocked entry `194.194` → exit market price `192`, net `-366.6856 KRW`, MFE `+0.925%`, MAE `-1.637%`; it remains outside actual P&L.
v86 STORJ relaxed sample settled at `2026-09-13T21:22:50.991Z` by `TAKE_PROFIT`, entry `48.4484` → exit `49.3506`, net `+352.0701 KRW` / `+1.7604%`, MFE `+1.9642%`, MAE `-0.7191%`, carrying trend/previous-high rejection. By `2026-09-13T21:23:04.999Z`, actual positions were empty; Shadow/Loose had `17`/`20` actual closes (`5W/12L`, `+92.0759` / `5W/15L`, `-104.5576 KRW`), while STEEM/STORJ execution-boundary counterfactuals remained pending.
v86 STORJ execution-boundary counterfactual settled at `2026-09-13T21:22:58.710Z` by `TAKE_PROFIT`, blocked entry `48.4` → exit market price `49.6`, net `+434.4672 KRW`, MFE `+2.377%`, MAE `-1.132%`. It remains outside actual realized P&L; the only older pending boundary afterward was STEEM (`20:56` signal).
v86 STEEM execution-boundary counterfactual (`20:56` signal) settled at `2026-09-13T21:28:19.679Z` by `MAX_HOLD_TIME` after `entry_chase_exceeded`, blocked entry `84.084` → exit market price `83.7`, net `-131.1297 KRW`, MFE `+0.4947%`, MAE `-0.6945%`. It is a loss-avoidance counterfactual outside actual Shadow/Loose P&L. A new MTL boundary then remained pending; actual/strict open was 0.
v86 B3 relaxed sample settled at `2026-09-13T21:39:10.597Z` by `TAKE_PROFIT`, entry `0.88088` → exit `0.903096`, net `+483.9054 KRW` / `+2.4195%`, MFE `+2.6246%`, MAE `-0.0999%`, despite volume/trend rejection. By `2026-09-13T21:39:32.673Z`, actual Shadow/Loose were `18`/`21` closes (`6W/12L`, `+575.9813` / `6W/15L`, `+379.3478 KRW`); only the MTL execution-boundary counterfactual remained pending.
v86 HIVE relaxed sample settled at `2026-09-13T21:45:15.160Z` by `MAX_LOSING_HOLD_TIME`, entry `70.4704` → exit `70.1298`, net `-116.5631 KRW` / `-0.5828%`, MFE `+0.0420%`, MAE `-0.3837%`, carrying previous-high rejection. By `2026-09-13T21:45:37.106Z`, actual Shadow/Loose were `19`/`22` closes (`6W/13L`, `+459.4182` / `6W/16L`, `+262.7847 KRW`), actual open was 0, and only MTL boundary remained pending.
v86 MTL execution-boundary counterfactual settled at `2026-09-13T21:54:30.523Z` by `MAX_HOLD_TIME`, blocked entry `378.378` → exit market price `378`, net `+46.1492 KRW`, MFE `+0.963%`, MAE `-0.366%`. It remains outside actual P&L. At the latest read only CVC actual/boundary observations remained (actual open in both books, one same-signal boundary per book).
v86 CVC actual loss and FLOCK follow-up at `2026-09-13T21:58:35.916Z`: CVC closed in both relaxed books at `2026-09-13T21:57:13.490Z` by `MAX_LOSING_HOLD_TIME`, net `-59.9151 KRW`, while a new FLOCK actual entered at `21:57:13.377Z`; the CVC boundary remains pending. Actual totals reached Shadow `20` closes (`6W/14L`, `+399.5032 KRW`) and Loose `23` closes (`6W/17L`, `+202.8696 KRW`). These remain diagnostic-only; strict has one ARK loss and no open position.
v86 FLOCK actual closed at `2026-09-13T22:01:50.903Z` in both relaxed books by `STOP_LOSS`, entry `98.1981` → exit `96.903`, net `-283.5042 KRW` / `-1.4175%`, MFE `+0.0019%`, MAE `-1.2201%`, carrying close-strength rejection. At `22:02:13.150Z`, Shadow/Loose reached `21`/`24` actual closes (`6W/15L`, `+115.9989` / `6W/18L`, `-80.6346 KRW`), actual open was 0, and only the CVC boundary remained pending.
v86 strict HIVE opened at `2026-09-13T22:05:09.988Z` after delayed confirmation `1727ms`, entry `69.9`, rebound `0.2869%`, RSI recovery `11.8105`, volume ratio `1.9415`, close strength `1`, trend `-0.1991%`; it remains an open strict observation under the opt-in 5-minute loss-only candidate. At the same read Shadow/Loose held HIVE diagnostics and the CVC boundary was pending; no default/live change.
v86 HIVE risk exits at `2026-09-13T22:06:18.366Z`: the strict HIVE position hit the normal `-1.2%` stop-loss first (entry `69.9` → exit `69`, net `-277.2017 KRW` / `-1.3869%`, MFE `0%`, MAE `-1.2876%`, after `1727ms` confirmation); Shadow/Loose both recorded `STOP_LOSS` net `-316.6544 KRW`. This is separate from the 5-minute loss-only boundary, and the CVC execution-boundary counterfactual remained pending.
v86 CVC execution-boundary counterfactual settled at `2026-09-13T22:11:12.983Z` by `STOP_LOSS`, blocked entry `49.3493` → exit market price `48.8`, net `-302.1024 KRW`, MFE `+0.911%`, MAE `-1.313%`; it remains outside actual P&L. At `22:11:46.326Z`, actual Shadow/Loose held FIL/POLYX/POWR positions (`22`/`25` closes, `6W/16L`/`6W/19L`, `-200.6554`/`-397.2890 KRW`) and no pending boundaries.
v86 POLYX actual exit at `2026-09-13T22:13:22.129Z`: Shadow/Loose both used `MAX_LOSING_HOLD_TIME`, entry `53.1531` → exit `52.947`, net `-97.4670 KRW` / `-0.4873%`, MFE `+0.0882%`, MAE `-0.4762%`, with volume/previous-high rejection. By `22:13:39.589Z`, Shadow/Loose were `23`/`27` actual closes (`6W/17L`, `-298.1225` / `6W/21L`, `-572.3172 KRW`); FIL/POWR actuals and a FIL boundary remained.

PWA redesign installation fix at `2026-09-14`: the visible redesign shell now calls its own `initProgressiveInstall()`, registers `/sw.js`, handles `beforeinstallprompt`/`appinstalled`, and exposes `pilot-pwa-state` plus `pilot-pwa-install` inside the visible mode banner. The shell versions are `pilot-redesign.js?v=observer-readonly-16`, `pilot-redesign.css?v=20260914-02`, and `coinpilot-shell-v22`; the banner span selector was narrowed so the install state remains a status pill. Browser evidence on read-only port `3158` showed `설치 가능` and the install button; root/manifest/service-worker/icon endpoints returned HTTP `200`. OS-level installed launch/relaunch remains unverified because the available browser automation cannot access the Chrome system install surface; do not claim it as completed.
v86 bounded terminal at `2026-09-13T23:06:04.866Z`: after Shadow/Loose exceeded the research target and the forward stream continued to create new actual/boundary records, the exact owner PID `15925` was stopped at an explicit bounded research boundary. The ledger is `active=false`, `stopReason=stopped_with_unsettled_diagnostic_positions`, strict has `3` closes (`0W/3L`, `-495.5247 KRW`) with no open strict position, Shadow has `39` closes (`7W/32L`, `-1661.0397 KRW`) and diagnostic `KRW-FIL/KRW-CVC` positions preserved at stop, and Loose has `44` closes (`7W/37L`, `-2184.7099 KRW`) with the same diagnostic positions preserved. Each diagnostic book has `4` unresolved execution-boundary counterfactuals; no data was deleted or mixed into strict promotion. Risk had `0` failures/outages with continuity true, and analysis ended at cycle `1924` with `20/20` complete and continuity true. This maxLosingHoldMinutes=`5` cohort is decisively negative diagnostic evidence; runtime default remains `0`, live remains blocked, and the cohort is not resumed or mixed.
Stop-safety hardening added after the bounded-stop decision: `stopPaperValidationSession()` snapshots pending boundary count before resolving it, exposes `pendingCounterfactualCountAtStop`, and uses `stopped_with_unsettled_boundary_counterfactuals` when no open positions remain but pending boundaries existed, preventing false `stopped_cleanly` status in future sessions. Regression coverage passes in the final `218/218` full test run; the already-running v86 used the pre-fix code but still honestly reported unsettled diagnostic positions in its stop reason.
Momentum shadow recovery at `2026-09-13T23:10Z`: ownership checks found existing fixed PID `54814` and regime PID `54815` processes rather than starting duplicates. Their exact research directories had been removed while the processes were alive, causing repeated `ENOENT` on `ledger.json.tmp` in `/private/tmp/momo-fixed.log` and `/private/tmp/momo-regime.log`; the in-memory books were still alive with ETH/NEAR open and no trades. Recreated only `.paper-momentum-shadow-v1`/`.paper-momentum-shadow-regime` and their scoped PID locks, after which the processes successfully rewrote ledgers at cycles `105`/`74`, with no trades and breadth `4`. Fixed already carries a recorded schema drift (old config lacked `mode`); both remain diagnostic-only and no live path is involved. `saveLedger()` now recreates its exact research directory before each atomic write to prevent this failure from recurring.
Momentum shadow post-recovery check at `2026-09-13T23:15:54.749Z`: fixed advanced to cycle `106` and regime to `75`; both owners remained alive with ETH/NEAR open, zero closed trades, breadth `4`, and no new `cycle error`/ENOENT log tail. This confirms the scoped directory/lock recovery is effective for the next polling cycle; it is operational evidence only, not profitability evidence.
Momentum shadow second post-recovery check at `2026-09-13T23:20:54.785Z`: fixed advanced to cycle `107` and regime to `76`; both owners remained alive with ETH/NEAR open, zero closed trades, breadth `4`, and no new `cycle error`/ENOENT log tail. The repaired directories continue to persist the existing in-memory books; no candidate is promotion-eligible.
Momentum shadow third post-recovery check at `2026-09-13T23:25:54.824Z`: fixed advanced to cycle `108` and regime to `77`; both owners remained alive with ETH/NEAR open, zero closed trades, breadth `4`, and no new `cycle error`/ENOENT log tail. The recovered A/B lane remains operational-only and unpromoted.
Momentum shadow latest handoff check at `2026-09-13T23:30:57.681Z`: fixed advanced to cycle `109` at `23:30:52.558Z` and regime to cycle `78`; both owners remained alive with ETH/NEAR open, zero closed trades, breadth `4`, and no new `cycle error`/ENOENT log tail. Leave both diagnostic owners running and recheck owner/lock/ledger before any next action.
Momentum shadow current continuation check at `2026-09-13T23:36:00.532Z`: fixed advanced to cycle `110` at `23:35:55.353Z` and regime to cycle `79`; both owners remained alive with ETH/NEAR open, zero closed trades, breadth `4`, and no new `cycle error`/ENOENT log tail. This remains operational wait evidence; no profitability or promotion conclusion is available.
Momentum shadow latest daily polling check at `2026-09-13T23:41:25.3Z`: fixed advanced to cycle `111` at `23:40:58.158Z` and regime to cycle `80` at `23:41:03.345Z`; both owners remained alive with ETH/NEAR open, zero closed trades, breadth `4`, and no new `cycle error`/ENOENT log tail. The A/B forward lane remains operational-only and outcome-unknown until a completed daily exit is recorded.
Review-fix pass at `2026-09-14T06:2xZ`: the change-set review ended `REJECT` and all findings were implemented. `historicalTimestampForCandle` now accepts epoch-ms numbers and digit strings (floor `>=1e11` keeps bare years on the date-parser path) and is the single shared parser for `scalpingBacktest.js`, `historicalCandleSeries.js`, and `higherTimeframeMomentum.js`. `monitorOpenPositions()` now passes the `winnerShadow` exit override (`winnerExtendMinutes`/`winnerExtendMinProfitPercent`) so the 1s risk path no longer closes the sidecar at strict max-hold; a regression test through the production risk path verifies a same-aged `shadow` book still closes `MAX_HOLD_TIME` while `winnerShadow` extends. `validateScalpingNoTradeFill.js` `baseConfig` reached env parity with the sibling lanes (tradingFee/oversoldLookback/maxReboundPercent/volumeLookback/trend*/signal-range/rebound-below-overbought/next-candle-bullish/marketRegime*/lossCircuitBreaker*/maxPositions/portfolioAllocation); snapshot merge stays authoritative and `promoted=false` hard-sets are unchanged. Unused `recordAnalysisDataIdle` import removed, telemetry indentation fixed, scratch `collect-*.mjs`/`.paper-momentum-shadow-*` artifacts removed. Suite `226/226` green, `git diff --check` clean; no live-order or promotion boundary changed.
Momentum shadow owner loss at `2026-09-14T03:11Z`: both A/B owners (fixed pid54814, regime pid54815) died while the machine slept; heartbeats went stale with ledgers preserved (fixed held ETH/NEAR, regime had 2 closes). `momentumShadowStatus.js` now reports `STOPPED:owner_process_missing`/`heartbeat_stale` using the same orphan convention as the forward-paper status instead of printing a healthy-looking stale heartbeat.
Momentum shadow relaunch collision at `2026-09-14T06:23Z`: a concurrent Codex (ChatGPT app-server, ppid 5631) session detected the same dead lane and relaunched both books with explicit env (`MODE`, `POLL_MS=300000`, 12 markets) while a relaunch from this session briefly ran a default 4-market regime config. The ledgers now carry honest transient drift records (`previous` = 4-market config, `changedAt` 06:21:54/06:23:09) and are back on the 12-market contract under pids 47508/47509 with current heartbeats; fixed still holds ETH/NEAR, regime holds none. The runner's dead-pid lock takeover and ledger resume worked as designed; process management for this lane is left to the Codex session to avoid repeated ownership races.
Regime book first exits: ETH `REGIME_OFF` `3,436,000 -> 3,372,000` `-2.0626%` and NEAR `REGIME_OFF` `3,228 -> 3,126` `-3.3599%`, realized `-1,145,630 KRW` on the 2026-09-13 daily close while the fixed book still holds the same ETH/NEAR entries (72h max-hold). This is the lane's first completed-exit evidence and it is negative for regime mode so far; both books remain diagnostic-only and cannot promote.
2026-09-14 daily-momentum continuation: added mark-to-market equity and runner lifecycle state to the two daily shadow ledgers. Stale-lock recovery is now recorded as `abrupt_or_external_termination`, graceful SIGINT/SIGTERM and fatal boundaries persist a stop reason, and the read-only `/api/momentum-shadow` projection plus redesigned history panel show cash, marked equity, realized/unrealized P&L, open marks, heartbeat state, and both contracts without exposing filesystem paths. Browser smoke confirmed the panel on desktop and `390x844` with document/body width `375px`; PWA install affordance remains visible, while native installed launch/relaunch is still unverified.
The fixed/regime ledgers were resumed only after explicitly restoring the original 12-market contract. A brief 4-market launch was stopped immediately and remains visible as config drift; it is not a clean comparison interval. Current owners are fixed PID `47508` and regime PID `47509`, with cycles `151`/`120`, marked equity `98,724,007`/`98,854,370 KRW`, and fixed open ETH/NEAR versus regime no open positions. Both remain research-only and promotion-blocked.
The new repeatable daily study (`src/research/dailyMomentumStudy.js`, `fetchDailyMomentumCandles.js`, `validateDailyMomentumVariants.js`) fetched 400 daily candles for the same 12 markets. After correcting realized-P&L accounting to sum closed trades rather than treating open capital as a loss, every tested variant stayed negative: best full return was regime trend `>1%` with breadth `>=2` at `-13.702%` (MDD `28.01%`); adding two-up-bar confirmation plus a 3-day loss cooldown produced `-17.094%` in the comparable regime candidate, and trend `>2%`/breadth `>=3`/two-up-bar reached `-12.754%` but still had three negative segments. All candidates are `HOLD`; no runtime parameter or live gate changed.
Daily study correction at `2026-09-14T06:38Z`: UTC parsing now treats suffix-less `candle_date_time_utc` as UTC and excludes the in-progress current UTC day, preventing lookahead. A fresh 400-day cache/sweep produced the authoritative figures: baseline fixed trend0/breadth1 `-18.834%` (314 trades, PF `0.85`, MDD `32.96%`); best full return regime trend1/breadth2 `-13.896%` (165 trades, PF `0.74`, MDD `28.01%`); two-up-bar regime trend2/breadth3 `-12.952%` but `3/4` segments negative. No candidate is promotion-eligible.
The redesigned history screen was rechecked on a cache-free origin at `390x844`: fetched assets were `pilot-redesign.css?v=20260914-03` and `pilot-redesign.js?v=observer-readonly-17`, API `/api/momentum-shadow` returned `200`, the A/B panel displayed current equity and open marks, and document/body scroll width stayed `375px`. Service-worker cache advanced to `coinpilot-shell-v23`; an old origin still served v22 until its worker updated, so update/activation is an explicit release checkpoint.
The A/B projection now also surfaces a Korean configuration-history warning when a ledger has drift, so a live-looking heartbeat or positive mark cannot hide an invalid comparison cohort; the current 4-market transient relaunch is visible as promotion-blocking evidence.
Benchmark-gate research continuation: on the latest 400-day window, regime `trend>2% + breadth>=3 + BTC 7-day trend>=2%` with benchmark-off exits returned `+13.268%`, PF `1.38`, MDD `12.90%`, and four nonnegative 100-day segments; the same candidate on 800 days returned `+50.693%`, MDD `32.82%`, but one of eight segments was `-6.302%`, so it remains a forward-shadow candidate rather than a promotion result. Added benchmark gate/exit semantics to daily study and runner with missing data fail-closed.
The separate `.paper-momentum-shadow-btc-gate-v1` owner PID `57105` is active at cycle `2`, 12 markets, benchmark BTC trend `-4.46%`, gate closed, no entries, and marked equity `100,000,000 KRW`. It is displayed as a third research card in the web/mobile/PWA history view and is isolated from fixed/regime ledgers.
Cost-stress segment correction: the top3/bottom3 7-day synthetic short candidate has no positive segment even at fee `0.1%`/borrow `0`; borrow `0.03%/day` lowers the 400-day marked result to `-0.162%` and the 800-day marked result to `-2.802%`. No synthetic short or benchmark candidate is connected to the live/order path.
Benchmark runner, third-card UI, market-neutral cost sensitivity, and isolated route tests bring the current full suite to `245/245` green; changed-file syntax checks and `git diff --check` also pass.
Synthetic market-neutral continuation at `2026-09-14T06:47Z`: added `dailyMarketNeutralStudy.js` and `validateDailyMarketNeutral.js` using the same UTC-normalized 400-day/12-market completed-daily cache. The short leg is explicitly synthetic inverse-return research and is not connected to Upbit spot execution. The best candidate, top3/bottom3 with 7-day rebalance, ended at `-0.038%` marked return, `+1.842%` realized return, PF `1.02`, MDD `14.03%`, but all four independent segments were negative (`-1.113/-6.183/-4.563/-2.216%`) and six boundary positions were unresolved; it is `HOLD`, not a promotion or runtime candidate. Ten/fourteen-day rebalance and bear-short variants also failed segment consistency.
Daily study code and UI/API safety additions are covered by full Node test `236/236` pass, changed-file syntax checks, redesigned bundle syntax, and `git diff --check`. Current fixed/regime owners remain PID `47508`/`47509`, cycles `154`/`123`, 12 markets, with fixed ETH/NEAR marked at `98,724,007 KRW` and regime two-close equity at `98,854,370 KRW`; both remain diagnostic-only.
Synthetic market-neutral continuation at `2026-09-14T06:47Z`: `dailyMarketNeutralStudy.js` and `validateDailyMarketNeutral.js` test cross-sectional top/bottom trend with a synthetic short leg. The best 7-day rebalance result was marked `-0.038%`, realized `+1.842%`, PF `1.02`, MDD `14.03%`, but all four independent segments were negative and the short leg is not executable through Upbit spot; keep `HOLD` and do not connect it to runtime/live.
Runner contract safety follow-up: `momentumShadowRunnerConfig.js` now inherits persisted ledger mode/markets when optional env overrides are absent, preventing a restart from silently switching a 12-market regime book to the four-market fixed default. Explicit env values remain allowed and are still recorded as drift. Full suite after this addition is `236/236` green; current owners are PID `47508`/`47509`, cycles `154`/`123`.
Market-neutral cost stress at `2026-09-14T06:55Z`: official report v5 evaluates transaction fee `0.1/0.2/0.3%` × synthetic short borrow `0/0.01/0.03% per day` with four segment results per row. The apparent full-window gain at fee `0.1%`/borrow `0` (`+2.718%`) still has four negative segments (`-0.398/-5.576/-3.793/-1.508%`); at fee `0.2%`/borrow `0.03%` the marked result is `-2.843%`. Synthetic short remains a cost-sensitive, non-executable defense experiment and `HOLD`.
The same resolver, cost stress, daily study, UI projection, and runner lifecycle changes pass the latest full Node suite `240/240`; the 12-market fixed/regime owners remain alive at cycles `155`/`124` with no runtime/live promotion change.
Benchmark candidate expansion at `2026-09-14T07:38Z`: the daily study now includes the BTC benchmark gate (`0/1/2%`) and optional benchmark-off exit. The `trend>2%/breadth>=3/BTC>=2%/exit` candidate is `+13.268%` on the recent 400-day window with MDD `12.90%` and four nonnegative segments, but `+50.693%` over 800 days still contains a `-6.302%` segment; it remains a forward research candidate, not promotion proof. Its separate runner PID `57105` is alive at cycle `2`, BTC trend `-4.46%`, gate closed, entries `0`, equity `100,000,000 KRW`.
Benchmark gate/exit is visible in the third responsive card (`BTC gate 방어 후보`) with `BTC gate closed · 추세 -4.46%`; mobile `390x844` remains `375px` document/body width with three cards. Full current suite after the third-card and benchmark-detail UI changes is `245/245` green; no runtime/live connection was added.
Per-ledger polling safety at `2026-09-14`: the benchmark candidate's `15분` interval is now persisted as `config.pollMs` and used by API/CLI heartbeat freshness; the old missing-field ledger also receives a benchmark-specific fallback, preventing a live 15-minute owner from being falsely marked stale at the five-minute default.
Drawdown circuit-breaker research at `2026-09-14T07:45Z`: added `maxPortfolioDrawdownPercent` to the daily simulator as an opt-in cash/risk safeguard. The recent 400-day benchmark candidate did not trigger it and kept `+13.268%`/MDD `12.90%`; the 800-day variant with DD15 stopped exposure at MDD `15.96%` but still had negative segments and remained `HOLD`. Default is `0` and runtime/live is unchanged. Current full suite is `246/246` green; fixed/regime/benchmark owners remain alive.
Dynamic benchmark exposure scaling at `2026-09-14T07:52Z`: added linear position-size scaling between configured benchmark trend bounds. Recent 400-day candidates remained negative (`-0.935%/-2.734%` for trend1/trend2 variants); 800-day aggregate positives still had negative recent segments. No new owner started; scaling remains research-only and default-disabled. Latest full suite is `247/247` green.
現物 top-K relative-strength one-off study at `2026-09-14`: on the recent 400-day cache every top1/top2/top3 × 3/7/14-day rotation was negative (best approximately `-31.729%` for top3/7-day), so no new rotation owner was started. The 800-day positive top1/top2/top3 results were concentrated in older rising segments and recent 400-day segments stayed negative; keep benchmark gate and existing owners instead.
Shadow exit-contract centralization at `2026-09-14T08:18Z`: `updatePaperShadowPosition` now resolves book-specific exits via `resolveShadowExitConfig(stateKey, configOverride)` — `winnerShadow` automatically applies `winnerShadowExtendMinutes`/`winnerShadowExtendMinProfitPercent`, `shadow`/`looseShadow` keep strict config, and an explicit override still wins for deliberate simulations. Analysis and risk-monitor call sites no longer carry duplicated override objects, so a future call site cannot silently drop the winner-hold contract (the defect class fixed earlier today). A new unit test pins merge precedence (derived < explicit override; strict books untouched).
Validation fee env parity: `SCALP_VALIDATION_FEE` (introduced with the new diagnostic lanes) is now honored by every `SCALP_VALIDATION_*` family lane — `validateScalping`, `validatePortfolio`, `compareScalpingVariants`, and `validateShadowCandidate` previously hardcoded `tradingFee: 0.0005` while segments/no-trade-fill read the env. Same-window cross-lane comparisons can no longer diverge silently on fee assumptions. The live gate parity-checks `tradingFee` against the runtime snapshot (no runtime env knob, always `0.0005`), so an env-fee report fails closed and can never contaminate promotion. Legacy `runBacktest` (no SCALP env family) and the self-namespaced `SCALP_HTF_*` momentum lane are intentionally unchanged.
Full suite `248/248` green; `git diff --check` clean. No momentum-shadow lane files were modified beyond the earlier read-only status-printer orphan detection; three Codex-owned shadow owners (fixed/regime/benchmark-gate) remain alive.
Validation/live config-contract closure at `2026-09-14T13:30Z`: `VALIDATION_SNAPSHOT_KEYS` declared six keys that `getPaperValidationConfigSnapshot` never emitted (`bbPeriod`, `bbStdDev`, `emaPeriod`, `maxPositions` was emitted but gate-ignored, `portfolioAllocation`, `requireNextCandleBullish`), so `copyValidationFields` silently dropped them and `comparableKeys` could not compare them — a live runtime at `SCALP_PORTFOLIO_ALLOCATION=0.5` or `BB_PERIOD=50` would have passed parity against evidence produced under different values. The runtime snapshot now resolves each key exactly as consumed (`config.emaLong`→`emaPeriod`, `this.portfolioAllocation`, strategy `||` defaults), and `comparableKeys` compares all six; a stale or mismatched report fails closed on `missingKeys`/drift. `runPaperSmoke` now maps `BB_PERIOD`/`BB_STD_DEV`/`EMA_LONG` like `index.js` (production `emaPeriod` resolves to 60, not the sim-default 20), and every `SCALP_VALIDATION_*` family lane records the same env-mirrored values so cross-lane reports stay comparable. Gate test fixture extended; a `portfolioAllocation` drift assertion proves the new coverage. All 100+ forward-paper ledgers are dead, so no live session flips to schema drift; older-ledger fixed validations resolve via env/default fallbacks. MACD params remain deliberately excluded from both contracts (not signal-gating); `validateShadowCandidate` keeps its pinned candidate contract. Full suite `257/257` green; `git diff --check` clean.
Gate-contract single-sourcing at `2026-09-14T13:30Z`: `comparableKeys` had drifted into a third hand-maintained list inside `validatePromotionReport` — `entryDelayMinMs`/`entryDelayMaxMs` were gate-compared but never recordable (`validateScalping` baseConfig lacked them and they are deliberately outside `VALIDATION_SNAPSHOT_KEYS`), so every `fixed_config` report would have thrown `missingKeys` and the live-gate path was unpassable. The keys are now env-recorded in the authoritative lane, and the gate list moved to `LIVE_GATE_COMPARABLE_KEYS = VALIDATION_SNAPSHOT_KEYS ∪ RUNTIME_BEHAVIOR_CONTRACT_KEYS` in `scalpingValidationConfig.js` — one module owns the whole validation-contract surface. New invariant tests pin both directions: every declared snapshot key is emitted by `getPaperValidationConfigSnapshot`, and every gate-compared key is snapshot-emitted and recordable in `validateScalping.js` (source scan; verified by mutation). Full suite `259/259` green; `git diff --check` clean.
Live/paper env-parity guard at `2026-09-14T14:20Z`: `index.js` and `runPaperSmoke.js` resolve scalping config through two independent env→config mappings — the same duplication class that let the paper lane silently resolve `emaPeriod=20` while production resolved `60`. Verified the full `SCALP_*` env sets now agree (excluding documented live-only `SCALP_REQUIRE_VALIDATION_PASS`/`SCALP_ALLOW_AVERAGING` and paper-only `SCALP_PAPER_*`), and added a source-scan regression test in `test/scalpingValidationConfig.test.js` that fails when either entry path reads a `SCALP_*` env the other cannot reach. Full suite `262/262` green.
Legacy entry fail-closed guard at `2026-09-14T14:25Z`: `src/multiCoinIndex.js` (initial-commit legacy entry, only referenced in README_ADVANCED.md) never set `strategyMode`, so the trader's `oversold_reaction_scalping` default silently applied a legacy-shaped config — `maxPositions:1000`, `portfolioAllocation:0.3`, `stopLoss 5%`, 60s check interval, no SCALP_* mapping. The entry now resolves `TRADING_STRATEGY` and refuses scalping resolution with a pointer to `npm start` (exit 1, before any network/portfolio touch); explicit non-scalping `TRADING_STRATEGY` preserves the legacy path and forwards the mode into config. A spawn-based regression test asserts exit 1 + the npm-start message. Live-gate wiring re-verified: `assertLiveValidationGate()` is the first statement of `MultiCoinTrader.start()` and short-circuits only for dryRun/non-scalping/explicit opt-out. Full suite `263/263` green.
