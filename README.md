# 🤖 다중 코인 자동매매 시스템

업비트 거래소를 통한 고급 자동매매 시스템입니다. 다중 코인 거래, 실시간 파라미터 최적화, 백테스팅 검증이 통합된 완전 자동화 솔루션입니다.

## ✨ 주요 기능

### 1. **다중 코인 동시 거래**
   - 여러 코인을 동시에 분석 및 거래
   - 포트폴리오 관리 (최대 포지션 수, 할당 비율)
   - 각 코인별 독립적인 전략 적용

### 2. **기술적 분석**
   - RSI (Relative Strength Index)
   - MACD (Moving Average Convergence Divergence)
   - 볼린저 밴드 (Bollinger Bands)
   - 이동평균선 (Moving Averages)
   - 거래량 분석

### 3. **뉴스 감성 분석**
   - 실시간 암호화폐 뉴스 모니터링
   - AI 기반 감성 분석
   - 긴급 뉴스 자동 감지

### 4. **자동 손절/익절**
   - 설정 가능한 손절률, 익절률
   - 실시간 포지션 모니터링
   - 리스크 관리 시스템

### 5. **🆕 지속적 파라미터 최적화**
   - 유전 알고리즘 기반 자동 최적화
   - 백그라운드에서 지속적으로 실행
   - 런타임에 파라미터 실시간 업데이트
   - 드라이 모드: 6시간마다, 실전 모드: 24시간마다

### 6. **백테스팅 시스템 (드라이 모드)**
   - 과거 데이터로 전략 검증
   - 주기적인 백테스팅으로 파라미터 검증
   - 수익률, 승률, 최대 낙폭, 샤프 비율 등 상세 통계

### 7. **웹 대시보드**
   - 실시간 포트폴리오 모니터링
   - 거래 통계 및 이력 조회
   - 최적화 파라미터 확인
   - 백테스팅 결과 확인

## 설치

```bash
npm install
```

## 설정

1. `.env.example` 파일을 `.env`로 복사
2. 업비트 API 키 설정
3. 거래 설정 조정

```bash
cp .env.example .env
```

## 사용법

### 통합 시스템 실행 (권장)

```bash
# 다중 코인 자동매매 + 지속적 최적화 + 대시보드 (모두 포함)
npm start
```

이 명령어 하나로 다음이 모두 실행됩니다:
- 다중 코인 자동매매
- 백그라운드 파라미터 최적화 (드라이: 6시간마다, 실전: 24시간마다)
- 정기 백테스팅 (드라이 모드에서 1시간마다)
- 웹 대시보드 (http://localhost:3000)

### 개별 도구 실행

```bash
# 백테스팅만 실행
npm run backtest

# 최적화만 실행 (연속 모드)
npm run optimize

# 대시보드만 실행
npm run dashboard
```

## 📊 모니터링

### 웹 대시보드
- 브라우저에서 `http://localhost:3000` 접속
- 포트폴리오 현황, 거래 통계, 최적화 결과 실시간 확인

### 로그 파일
- `logs/trading-YYYY-MM-DD.log`: 일반 로그
- `logs/error-YYYY-MM-DD.log`: 에러 로그
- `logs/trades.log`: 거래 이력

### 결과 파일
- `optimal_config.json`: 최신 최적 파라미터
- `optimization_history.json`: 최적화 이력
- `backtest_results_*.json`: 각 코인별 백테스팅 결과

## ⚙️ 환경 변수 설정

### 필수 설정
```env
# API 키 (실전투자 시 필수)
UPBIT_ACCESS_KEY=your_access_key
UPBIT_SECRET_KEY=your_secret_key

# 거래 모드
DRY_RUN=true                  # true: 모의투자, false: 실전투자

# 다중 코인 설정
TARGET_COINS=KRW-BTC,KRW-ETH,KRW-XRP
MAX_POSITIONS=3               # 최대 동시 포지션 수
PORTFOLIO_ALLOCATION=0.3      # 각 코인에 최대 30% 할당
```

### 드라이 모드 설정
```env
DRY_RUN_SEED_MONEY=10000000   # 시작 시드 머니 (1000만원)
CHECK_INTERVAL_DRY=30000      # 체크 간격: 30초
BACKTEST_INTERVAL=3600000     # 백테스팅 간격: 1시간
```

### 최적화 설정
```env
OPTIMIZATION_DAYS=60          # 최적화 학습 기간: 60일
OPTIMIZATION_INTERVAL=86400000           # 실전 최적화 간격: 24시간
OPTIMIZATION_INTERVAL_DRY=21600000      # 드라이 최적화 간격: 6시간
POPULATION_SIZE=20            # 개체군 크기
GENERATIONS=10                # 세대 수
```

## ⚠️ 주의사항

### 실전 투자 전 필수 확인사항

1. **충분한 모의투자 기간**
   - 최소 1주일 이상 `DRY_RUN=true`로 테스트
   - 다양한 시장 상황에서 검증

2. **백테스팅 검증**
   - 승률 60% 이상 권장
   - 최대 낙폭 20% 이하 권장

3. **최적화 검증**
   - 과최적화(Overfitting) 주의
   - 여러 기간으로 검증

4. **리스크 관리**
   - 손절/익절 반드시 설정
   - 투자 금액 제한
   - 여유 자금으로만 투자

5. **지속적 모니터링**
   - 웹 대시보드 정기 확인
   - 로그 파일 검토
   - 이상 거래 발견 시 즉시 중지
